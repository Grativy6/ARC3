# Optional online development credential. Leave unset for local/offline runs.
ARC_API_KEY=

# First-party runtime mode: synthetic, local, online, or competition.
ARC3_MODE=local

# Competition mode is offline by default and ignores remote credentials.
ARC3_NETWORK_ENABLED=false
