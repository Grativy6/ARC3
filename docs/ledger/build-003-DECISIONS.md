# ARC3 Build 003 decision ledger

This ledger is append-oriented. Supersede decisions with new entries; do not erase prior authority or failure evidence.

## D-003-0001 - Select `r11l-495a7899` as the external development target

- **Status:** ADOPTED
- **Stage:** 00
- **Date:** 2026-08-24
- **Commit:** `23b3b8b04eb00a8635e0c3efcfb9103c7dc1120a`
- **Decision:** Predeclare the exact cached `r11l-495a7899` development identity for real offline play. Keep the ID only in evaluation configuration and evidence, never in production policy logic.
- **Alternatives:** `ls20-9607627b`; an arbitrary development game; the sealed public holdout.
- **Evidence:** The frozen partition classifies `r11l` as development; its asset matches `sha256:483e583c88e91c2ae58ad1fa7b274d97813993796ce798551a563e1a9a78a7ff`; it is the only prior development identity with a completed level; it has six declared levels.
- **Why:** This is the strongest repository-grounded progress signal while preserving the project holdout boundary. It is a target selection, not a game-specific rule.
- **Consequences:** Final real-play evidence is labeled `local-public-source-preview-exposed`; static scans must prove the ID is absent from production policy.
- **Reopening condition:** Static asset identity or OFFLINE compatibility fails before play, or the owner specifies a different authorized target.

## D-003-0002 - Implement only bounded BLA and CLEF mechanisms

- **Status:** ADOPTED
- **Stage:** 00
- **Date:** 2026-08-24
- **Commit:** `8c31b734401d6d5c6360facf95389ac18c13804e`
- **Decision:** Map BLA v0.9.1 to structured residuals, provisional/versioned mechanic ledgers, earned support, local-first repair, and saturation. Map CLEF v1.0 to explicit layer declarations, independent evidence families, validity gates, residual promotion, readability walls, and scale-relevance stopping.
- **Alternatives:** Terminology-only comments; physical/thermodynamic game laws; CORAL scoring; wholesale replacement of existing hypotheses/world models.
- **Evidence:** Zenodo records 20807530 and 21193511 and their sole PDF artifacts; attached Build 003 workflow.
- **Why:** These are the operationally justified source mechanisms that can be tested at the existing observation/action boundary.
- **Consequences:** No physical surface-tension, curvature, free-energy, thermodynamic, or material-cost analogy becomes a game rule. PAL v2.2 terminology controls conflicts; CORAL remains proposed and non-operational.
- **Reopening condition:** Measured integration evidence shows a narrower mechanism is necessary while preserving the claim and trace boundaries.

## D-003-0003 - Treat official `WIN` as the sole completion authority

- **Status:** ADOPTED
- **Stage:** 00
- **Date:** 2026-08-24
- **Commit:** pending
- **Decision:** Continue while the official state is `NOT_FINISHED`. Treat `GAME_OVER` as failure evidence, preserve the trace, issue only `RESET` when permitted, revise implicated hypotheses, and continue within declared budgets. Stop as complete only on an actually returned `GameState.WIN`.
- **Alternatives:** Stop after a level transition, score increase, synthetic success, confidence threshold, or complete-looking mechanic map.
- **Evidence:** Current official actions/full-play documentation and the owner instruction.
- **Why:** Environment state is the authoritative external completion boundary.
- **Consequences:** Receipts report total submissions, non-reset scored actions, and resets separately because upstream counters differ.
- **Reopening condition:** Only a later official interface revision verified from primary sources.

## D-003-0004 - Reuse Build 002 lifecycle and governor boundaries

- **Status:** ADOPTED
- **Stage:** 01
- **Date:** 2026-08-24
- **Commit:** pending
- **Decision:** Extend existing perception, world-model, exploration, trace, memory, governor, adapter, and packaging surfaces. Do not create a second budget authority or rewrite the competition lifecycle.
- **Alternatives:** Parallel new controller stack; game-specific solver; per-action durable checkpointing; allocator tracing in competition mode.
- **Evidence:** Build 002 freeze and architecture audit.
- **Why:** Existing contracts already provide deterministic receipts, legal-action checks, replay, bounded modes, and packaging; Build 003 gaps are mechanic learning and cross-level reuse.
- **Consequences:** New source should live in already packaged top-level modules unless the package allowlist is explicitly extended and tested.
- **Reopening condition:** A measured integration need cannot be expressed safely through existing contracts.

## D-003-0005 - Scope cross-group occlusion guards to observed composite glyphs

- **Status:** ADOPTED
- **Stage:** 10
- **Date:** 2026-08-25
- **Commit:** pending
- **Decision:** Apply whole-box target preservation and predicted cross-mediator separation only when the active affine group was assembled from an observed multicolor composite glyph. Preserve the earlier sparse-cell and endpoint-clearance rules for ordinary single-color affine groups.
- **Alternatives:** Apply the new guards to every affine group; remove the guards entirely; encode a target-level exception.
- **Evidence:** Campaign 13 reached level 3 and showed that composite target and mediator occlusion destroys the remaining group identity. Campaign 14 then failed at level 2 after the global inter-mediator guard removed a valid ordinary affine route. The first divergence was between ordinary rotations `(34,9)` and `(23,21)`; disabling the target-box gate alone did not repair it. With composite-only scope, a clean policy matches all 29 pre-transition Campaign 13 actions and the returned observation is level 3 / `NOT_FINISHED`; 40 visual-policy tests, Ruff, and strict mypy pass.
- **Why:** The safety rule is earned by a composite-glyph failure mode and must not backflow into mechanics whose readable identity is a single observed component.
- **Consequences:** Production logic remains observation-derived and contains no game ID, fixed board coordinate, color exception, or walkthrough. Composite preservation remains covered by the multicolor fixture; ordinary target-box and inter-mediator proximity semantics have regression assertions.
- **Reopening condition:** A later official residual demonstrates the same destructive cross-group occlusion for a non-composite group and a generic observable discriminator can be established.

## D-003-0006 - Preserve observable affine evidence with perception-isomorphic guards

- **Status:** ADOPTED
- **Stage:** 10
- **Date:** 2026-08-25
- **Commit:** pending
- **Decision:** Before accepting a compound affine relocation, protect every currently observable target region and the prospective connector/tether raster, and reject endpoint or mediator-sector placements that the production parser's own component-merging relation would make unreadable. Use the same observed geometry and merge thresholds in planning that perception uses after the action.
- **Alternatives:** Protect only occupied target cells; use independent hand-tuned planning distances; enumerate target-specific safe coordinates; accept evidence loss and reconstruct the group from historical confidence.
- **Evidence:** Campaign 15 officially reached level 3, then actions whose endpoint, connector, tether, or translated compound sector crossed a visible target region or merged with same-color scene structure erased mechanics that had been readable immediately before the action. The campaign ended `NOT_FINISHED` after 112 submissions, including two `RESET` actions, with the score boundary reporting `GAME_OVER`; recording SHA-256 `2a2b3da7fad9e5bb8d4fea3b2f33133bdf2e9076ac66ea49f02dc9c6744b2a14`.
- **Why:** Planning safety must preserve the evidence surfaces on which the generic learner depends. A guard expressed in the parser's observable topology is falsifiable and game-agnostic; a separate approximation can authorize exactly the collision that perception later interprets as disappearance.
- **Consequences:** Production policy protects compound identity without a game ID, fixed coordinate, color exception, or walkthrough. A predicted exact local collapse remains bounded local progress only; it never substitutes for an official level transition or `GameState.WIN`.
- **Reopening condition:** A fresh official residual shows that the shared perception/planning relation rejects a necessary action or still permits observable compound evidence to be erased.

## D-003-0007 - Rank certified two-step staging routes with bounded follow-up potential

- **Status:** SUPERSEDED BY D-003-0008
- **Stage:** 10
- **Date:** 2026-08-25
- **Commit:** pending
- **Decision:** When the current same-group choices offer only shallow immediate improvement, allow an observation-certified two-step staging route to compete with immediate role switching by ranking the route's best legal follow-up potential. Execute only the first environment action, observe its consequence, and replan; do not bind or simulate an unobserved second action.
- **Alternatives:** Continue greedy immediate-potential ranking; enumerate arbitrary multi-step action sequences; encode the Campaign 16 coordinates or target identity; globally prefer staging over role switching.
- **Evidence:** Campaign 16, frozen at `0b87a923500041757213fc421e5e165530b0c11c`, completed two levels but entered a shallow-improvement cycle and exhausted bounded same-group actions after 33 official submissions. The raw and official final states remained `NOT_FINISHED`; recording SHA-256 `9531e3137b8b08d7f178c6df0d33f248f6b87740bbb58471b131b992778891a0`.
- **Why:** The smallest discriminating repair compares a mechanically certified stage-plus-follow-up route against the already available role-switch alternative without opening an unbounded search surface. Re-observation after the first action preserves trace honesty and permits residuals to reopen the route.
- **Intended consequences (not earned):** Lookahead would have remained deterministic, local to the currently observed mechanic, and bounded to one certified follow-up. Replay regression prevented these intended consequences from becoming accepted policy.
- **Reopening condition:** Local tests cannot certify the follow-up relation, the bounded ranking regresses an earlier official route, or a fresh official residual shows that deeper planning is necessary.
- **Supersession evidence:** Implementing the broad follow-up ranking changed a previously preserved official replay route. The replay regression rejected the mechanism before fresh official play. A narrower observation-level diagnosis found that the implicated candidate coordinate was inside the current active glyph and that an endpoint center had contaminated a sparse target under the parser's own merge relation.
- **Superseded by:** D-003-0008. Preserve D-003-0007 as failed-mechanism evidence; do not treat its unexecuted second-action estimate as accepted policy.

## D-003-0008 - Reject hitbox-contained relocations and separate certified sparse-target contaminants

- **Status:** ADOPTED
- **Stage:** 10
- **Date:** 2026-08-25
- **Commit:** `dc38e7d4c154a8cde602eb8ebc117bb223b8ac2d`
- **Decision:** Reject any proposed relocation coordinate inside the active endpoint's currently observed bounding box because it provides no evidence of a new center. When exactly one endpoint center is joined to a sparse target and removing that center reconstructs the parser's complete symmetric target ring, prioritize the smallest readable separation action. Accept a planned marker consequence only when the predicted center movement or outer-color role transfer is observed. Retain the target-identity constraint whenever any relocation restores the exact ring, and require a dedicated separation plan to do so. Certify contamination and prospective separation with the parser's exact small-component merge relation, then reobserve before further planning.
- **Alternatives:** Retain the broad two-step follow-up ranking; treat a click on a glyph edge as a translated center; rewrite the parsed target from history; use a target-specific coordinate or color exception; reject all endpoint/target proximity.
- **Evidence:** Replay falsified D-003-0007 by regressing a previously preserved route. Official Campaign 15 then directly falsified both the painted-edge click `(41,16)` and the unpainted bounding-box corner `(41,15)` as relocations: each left the endpoint center and mediator unchanged. The narrower repair preserves the full known official Level 1 route and replaces that failed Level 2 sequence with a four-action chain into the recorded Level 3 frame. No-op consequence, successful role-transfer, dedicated-separation, and incidental-restoration regressions are covered. The visual-policy suite reports 50 passed; Ruff and strict mypy are clean.
- **Why:** The failure is an observation-geometry error, not evidence that broader search is needed. A coordinate within the observed endpoint hitbox cannot certify relocation, while the exact residual target ring can certify that one differently colored endpoint center is the contaminant. Reusing perception's merge predicate keeps action selection and reparsing isomorphic.
- **Consequences:** Production planning gains a bounded separation route without a game ID, fixed board coordinate, color exception, or walkthrough. Separation is only local evidence repair; it does not establish a solved mechanic, level completion, or official `WIN`.
- **Reopening condition:** Campaign 17 regresses the preserved Level 1/Level 2 route, the returned observation does not restore the certified sparse target, or a necessary relocation is rejected despite being outside the observed bounding box and parser merge boundary.

## D-003-0009 - Separate exact compound-target sector overlays and preserve valid bootstrap evidence

- **Status:** ADOPTED; OFFICIAL MECHANISM OBSERVED
- **Stage:** 10
- **Date:** 2026-08-25
- **Commit:** pending
- **Decision:** Treat a secondary same-color target component as an endpoint/target overlay only when removing exactly one observed endpoint center leaves exactly the marker-colored sector of a complete compound target. When direct progress is exhausted, activate and separate that certified endpoint with the smallest parser-readable displacement, then retain the target-identity constraint against exact same-sector rejoining. Permit the separation after either post-collapse reacquisition or a visible role transfer when ordinary continuation is absent. Accept the consequence only when every target coordinate retains its observed color, every raw component touching the target remains inside its exact same-color sector, and the old marker center is cleared. Recover a marker-bootstrap active color from one unique marked endpoint at the submitted coordinate even when its already-solved mediator/target group is no longer complete.
- **Alternatives:** Treat the seven-cell component as an independent target; accept any endpoint near a target as contamination; reconstruct a target from history; repeat open-space bootstrap actions; add a target coordinate/color exception; immediately add deeper unrestricted search.
- **Evidence:** Campaign 17 preserved the earlier route, reached level 3, solved two visible level-3 groups locally, and ended after 43 official submissions at returned `NOT_FINISHED` with one remaining group. The final raw residual was a bootstrap at `(6,43)` whose returned frame contained one unique marked active endpoint, but the old bootstrap reader searched only complete embedded groups. Separately, the remaining group's endpoint center at `(44,12)` formed a seven-cell target component consisting exactly of that center plus the six marker-colored cells of its complete twelve-cell compound target; projection certifies separation to `(41,12)`. Focused replay with the repair matches official actions 1-40 and first diverges at action 41 to certified separation `(41,12)`. On the exact final official frame, bootstrap localization returns active color 0, selects activation `(44,12)`, and projects the same separation `(41,12)`. The 56-test visual-policy suite passes; exact negative tests reject a retained marker bridge and swapped target-sector colors; Ruff, format, strict mypy, and diff checks pass. Recording SHA-256 `2685d5e81cb4b95c5784169a9e9a4a1675c4b3a94cb447fc5dd186c4ac864965`.
- **Why:** The returned observation distinguishes a parser-level overlay and a valid localized bootstrap; neither supports a broader mechanic rewrite. Exact set equality and the existing parser merge relation keep the repair observation-grounded, while per-action reobservation remains authoritative.
- **Official observation:** Campaign 18 executed the planned marker-14 separation at action 41. The returned frame cleared the exact sector overlay, preserved every target-sector color and raw component boundary, and earned the marker-14 target-identity constraint, which remained active through action 54. The repaired bootstrap at action 54 localized active color 0 at `(6,43)` exactly as predicted. The later failure remains distinct from this overlay/bootstrap mechanism; B-003-0011 records its refined target-role diagnosis. Level 3 and `NOT_FINISHED` remained authoritative.
- **Consequences:** A locally solved group no longer forces an unsupported bootstrap when a remaining group's only immediate continuation is exact target separation, and a localized bootstrap can transfer into that group without losing the separation continuation. Approximate restoration cannot earn target identity. A successful local separation still does not establish level completion or official `WIN`.
- **Reopening condition:** Focused replay regresses any action before the Campaign 17 residual, the official separation is not observed, the exact certificate accepts an approximate overlay, or Campaign 18 exposes a distinct planning horizon after target identity is restored.

## D-003-0010 - Scope connector-role aliases to the same observed marker group

- **Status:** ADOPTED; OFFICIAL MECHANISMS OBSERVED; LATER RESIDUAL IS B-003-0014
- **Stage:** 10
- **Date:** 2026-08-25
- **Commit:** `ec8e27f45090a408e1ae942f12407680fbb0542f`
- **Decision:** During collision/readability planning for one composite marker group, do not treat a raw target candidate as independent evidence when every observed cell of that component is contained in the same group's observed mediator plus inferred endpoint-to-mediator centerline raster. Preserve the component when it is the raw target of another complete ordinary group, and always preserve exact composite target proxies. When a staging certificate uses a shallow projection that cannot rerender connectors, carry the starting observation's already-filtered target regions through that projection only if role-switch reparsing preserves the exact same target center and cells. Execute only the first selected action and recompute the target surface from the next official observation.
- **Alternatives:** Protect every parser target role even when it is wholly generated by the same group's connector topology; globally discard raw target candidates near connectors; persist a target-alias identity across official observations; add a deeper or target-specific action bridge; inspect target game source.
- **Evidence:** Campaign 18 action 51 exposed a raw color-1, area-11 target candidate centered at `(42,10)` whose 11 observed cells were all contained in marker 14's mediator/connector raster. It was not a certified raw target of another complete group. The frozen policy treated its 72-cell bounding region as independent protection, removed every strict marker-14 continuation, deferred to marker 9 at action 52, and eventually exhausted. The narrow filter preserves official actions 1-51 and first diverges at action 52 to ordinary relocation `(35,6)`, reducing marker-14 potential `410 -> 386`. Exact local audit passes placement, endpoint separation, static, mediator, connector, non-goal target, target-identity, unique-reparse, and consequence gates. The 61-test visual-policy suite passes; negative tests retain partial overlaps, composite proxies, other complete-group raw targets, and reject target remapping during projection carry. Ruff and strict mypy pass.
- **Why:** The residual is a local descriptive-role conflict, not evidence that an independent target may be erased or that unbounded lookahead is required. Exact containment plus other-group and composite-target preservation supplies the smallest observation-grounded discriminator. Limiting carry to one projection and requiring target identity equality prevents a stale planning surface from becoming persistent world state.
- **Consequences:** Production logic remains game-ID and coordinate blind. The action-52 move is a new local prediction, not official progress; its consequence must be observed before any projected continuation is reconsidered. No outside-group staging fallback or open-loop action sequence is added.
- **Reopening condition:** The official action-52 consequence does not preserve the predicted group, a genuine independent target is suppressed despite the other-group/composite guards, target geometry changes during role-switch projection, or a later residual requires temporal co-movement evidence rather than exact same-frame containment.

## D-003-0011 - Separate target role from palette identity and retain failed exploration roots

- **Status:** ADOPTED; OFFICIAL MECHANISMS OBSERVED; LATER RESIDUAL IS D-003-0012
- **Stage:** 10
- **Date:** 2026-08-25
- **Commit:** `8c31b734401d6d5c6360facf95389ac18c13804e`
- **Decision:** Keep same-color mediator/target identity as the primary affine relation. If it is absent, accept a palette-changed target role only when exactly one ordinary hollow target is readable; preserve ambiguity when multiple mismatched targets or equally supported mediator relations exist. During cross-level transfer, retain each affine receipt whose support error remains within the existing 6.0 ceiling rather than allowing one noisier receipt to veto the whole retained set. For null-signature exploration, bind the first state/action/kind decision of an episode; when its consequence reaches `GAME_OVER`, exclude that exact root after a same-level `RESET`. Clear episode-local attempted identities on reset so a different frame context remains eligible, but clear the failed-root ledger only on genuine level progress.
- **Alternatives:** Continue same-color target matching and generic probes; choose the nearest of multiple mismatched targets; loosen the affine support-error ceiling; discard every prior when one is noisy; persist raw coordinates across levels; treat `GAME_OVER` as completion; reset all episode evidence and replay the same branch; encode the observed target color, coordinates, or game ID.
- **Evidence:** Campaign 19 verified 240 artifacts and officially reached level 5, but ended without `WIN` after 3,000 non-reset actions, 48 resets, and 48 raw `GAME_OVER` observations. On the first level-5 transition, endpoint displacement `(-4,-14)` and mediator displacement `(-2,-7)` retained the already-supported arity-2 relation. Exactly one ordinary hollow target was readable at `(53,28)`, but its palette differed from mediator color 0. One prior receipt above support error 6.0 suppressed all otherwise reliable transfer. The fallback then repeated the same five coordinate choices twelve times per life and returned to the exact initial frame after reset. Recording SHA-256 `4416bb7a4062da9a967e233529c89a21d3faca83178a4ea7267cd0a9c0c922f7`; trace manifest SHA-256 `30d303c9125011be8923b80540d95745f1921cae81551491602482a884214ae9`.
- **Why:** Palette equality is one observed identity cue, not the target role itself. Exact uniqueness supplies the smallest observation-grounded alternative without choosing among competing targets. Filtering at the unchanged error ceiling preserves reliable mechanics without upgrading noisy evidence. An exact failed episode root has already returned its consequence; repeating it after an exact reset is not a discriminating action.
- **Consequences:** Exact replay preserves Campaign 19 actions 1-61 and first diverges at action 62 to `(60,28)`, the first action of a transferred arity-2 plan for target `(53,28)` with support error 0.5. Campaign 20 officially observed that transfer: `(60,28)`, role exchange `(43,34)`, and completion `(46,28)` retained the affine relation and placed its mediator on the palette-changed target. Its five failed lives used five distinct roots, after which bounded exhaustion failed explicitly. The environment nevertheless remained `NOT_FINISHED`, level 4; D-003-0012 addresses the distinct remaining-group reacquisition residual. Multiple mismatched targets and equally supported mediator relations still fail closed. Exploration-root memory is level-scoped and bounded to 64 entries; reaching that capacity latches explicit exhaustion instead of replaying an unrecorded failure. Decreasing level counters also fail closed. No game ID, target coordinate, target color, or walkthrough enters production logic.
- **Reopening condition:** The official `(60,28)` consequence does not retain the predicted affine group, the unique hollow target is not causally relevant, a reliable prior is incorrectly filtered, a failed episode root repeats after same-level reset, or the level-scoped exclusion survives a genuine level transition.

## D-003-0012 - Reacquire remaining affine groups after bounded local closure

- **Status:** ADOPTED; OFFICIAL CONSEQUENCE PENDING
- **Stage:** 10
- **Date:** 2026-08-25
- **Commit:** `858650c4b6494faaeee5e8ef24cd1bf27257e82d`
- **Decision:** When an ordinary affine plan reaches its local target but the official environment remains `NOT_FINISHED`, carry that target center for one bounded remaining-group reacquisition. Exclude the mediator already at that center and every mediator coincident with another visible target; among at most twelve endpoints, require one unique arity-2..6 subset whose centroid explains a remaining mediator within L1 error 1.5, including ambiguity across arities. A candidate role exchange is legal only when both prospective endpoint recolors remain distinct under the parser's exact connectivity and small-component merge relations; select only one uniquely nearest safe candidate. Treat only an exact two-endpoint exchange between the clicked endpoint and the unique pre-action active endpoint, with unchanged other endpoints and mediators, as successful causal evidence. Build subsequent radial plans from the actual mover sequence and reject full-glyph static or pairwise prospective collisions in both each immediate active color and every intermediate post-switch fixed color before submitting the first action.
- **Alternatives:** Emit another free-space coordinate probe after local closure; select the nearest endpoint without checking prospective recolor merges; infer completion from target contact; globally assign every mediator to a different target despite observed sequential reuse of one target; queue a projected multi-action continuation; encode Campaign 20 coordinates, colors, or game identity.
- **Evidence:** Campaign 20 action 64 returned `NOT_FINISHED`, level 4 after endpoints `(60,28)` and `(46,28)` placed their mediator on target `(53,28)`. Another group `(52,55)`, `(34,55)`, `(41,47)` still explained mediator `(42,52)`. The frozen policy chose `(53,17)`, disturbed the solved group, then used placements that merged endpoint and mediator components. Five distinct lives ended in raw `GAME_OVER`, and the sixth stopped at explicit root exhaustion. In the clean action-64 frame, `(41,47)` is the nearest fixed endpoint but its future active-color component is at cell separation one from mediator `(42,52)`; `(34,55)` is the unique nearest readable exchange. Campaign recording SHA-256 `2dba21f16c225383b47ece4d0217b72f2b229eeca32f45b54525830b3a479392`.
- **Official observation:** Campaign 21, frozen at `2702c4d5a1f8e604cb806bdb7d811a4df0c3da4d`, matched Campaign 20 through action 64, returned the exact safe role exchange at action 65 `(34,55)`, and preserved readable consequences for radial supports `(57,18)`, `(60,37)`, and `(42,29)` through action 70. The role exchange, arity-3 transfer, dynamic-color collision checks, and recomputation boundary were therefore observed. The state remained `NOT_FINISHED`, level 4 after both visible child mediators coincided with `(53,28)`; that distinct flat-target interpretation failure is B-003-0014.
- **Why:** Local affine closure is evidence about one relation, not the official level or game state. A target-scoped one-action continuation preserves the completed relation while asking the smallest discriminating question about the remaining group. Exact parser isomorphism prevents action selection from authorizing a component merge that perception would later misread as disappearance.
- **Consequences:** Exact Campaign 20 replay matches non-reset actions 1-64 and first diverges at action 65 to role exchange `(34,55)`. A synthetic returned swap preserves all endpoint centers and mediators, clears the one-shot reacquisition state, does not set probe failure, and uniquely transfers the retained affine form to the remaining arity-3 group. The audited radial supports are `(57,18)`, `(60,37)`, and `(42,29)`; their immediate active-color and later fixed-color forms remain parser-distinct from retained and future endpoints. Only action 65 is selected; every later consequence remains unearned until official observation. Arbitrary recoloring, cross-arity centroid ties, unsafe future recolors, and already-satisfied visible groups fail closed. The 76-test visual-policy suite, Ruff, strict mypy, format, diff, and nine-test secret checks pass.
- **Reopening condition:** A later replay regresses action 65, accepts arbitrary recoloring or a cross-arity tie, permits an unsafe intermediate recolor, or disturbs another visibly satisfied group. Failure of the distinct flat target interpretation does not reopen the observed reacquisition mechanism.

## D-003-0013 - Treat an exact child-mediator cover as a bounded affine hierarchy

- **Status:** ADOPTED AS AN OBSERVED PROBE; JOINT PARENT-CENTROID SUFFICIENCY FALSIFIED BY CAMPAIGN 22
- **Stage:** 10
- **Date:** 2026-08-25
- **Commit:** `cfc1d245750574b6545b964c31142d77c24e14a2`
- **Decision:** When multiple same-palette mediators, one differently paletted raw target, exactly one active endpoint, and one unique disjoint global cover of every endpoint by centroid-supported child groups are all visible, defer the flat singleton-target interpretation and test one second-order affine hierarchy. Assign each child mediator a distinct symmetric support whose exact floor centroid is the parent target. Search bounded parent shells and fair child/support alternatives under one deterministic 131,072-unit budget shared by exact-cover recognition, layout generation, projected reparsing, and sequential consequence checks. Preserve every observed raw/composite target region and unrelated static cell. Treat a connector as movable only when one color forms a contiguous mediator-to-endpoint bridge on at least two child legs. Reparse every projected intermediate and require the same endpoint count, mediator count, full visible-target signature, and unique hierarchy. Submit only one action before reobservation. Search exhaustion or no layout is a receipted deferral that skips flat reuse and selects one parser-safe alternate role binding or bounded probe; it is not campaign termination. Claim final supports only when the returned mediators occupy the planned distinct supports and their centroid is the parent target.
- **Alternatives:** Reuse the lone mismatched target independently for every child; co-locate child mediators; choose one-to-one child targets that are not observed; enumerate arbitrary interaction sequences; ignore unrelated centerline occupancy; treat synthetic closure or high confidence as official progress; encode the target coordinates, colors, game ID, or Campaign 21 action sequence.
- **Evidence:** Campaign 21 action 64 placed the arity-2 child mediator on the mismatched target without level progress, and action 70 placed the arity-3 child mediator there too, collapsing two readable child mediators into one while the official state remained `NOT_FINISHED`. On the untouched action-61 level-entry frame, the new exact cover contains arities 2 and 3, plans distinct supports `(59,31)` and `(47,25)` around parent target `(53,28)`, and certifies the nine-action sequence `(58,37)`, `(43,34)`, `(60,25)`, `(52,55)`, `(42,22)`, `(41,47)`, `(47,31)`, `(34,55)`, `(52,22)`. All projected intermediates preserve five endpoints, two mediators, the sole raw target, the full visible-target signature, and the unique hierarchy. The shared search has 37,471 of 131,072 units remaining on that frame.
- **Official observation:** Campaign 22, frozen at `9c513fa7375e97606a337f26fe63e8cfa99be693`, executed those nine hierarchy actions as zero-based recording rows 62-70, physical JSONL lines 63-71. The final `(52,22)` consequence returned the predicted distinct child supports `(59,31)` and `(47,25)`, whose centroid is the parent target `(53,28)`, but the official environment remained `NOT_FINISHED` with `levels_completed=4/6`. Its exact mechanics residual is `distinct child mediators reached the predicted parent centroid but the official environment remained NOT_FINISHED`. The next step, zero-based row 71 and physical line 72, reopened the same semantic relation under a new frame-specific hierarchy identity, clicked the already-active endpoint `(52,22)`, left the hierarchy geometry unchanged, and changed only HUD cell `(0,10)` from 0 to 5. The hierarchy detector and projected geometry were observed; their sufficiency for level progress was falsified.
- **Why:** Campaign 21 falsified the flat many-to-one target assignment but preserved exact lower-layer mechanics. A unique global cover plus a differently paletted parent supplies the smallest observation-grounded layer discriminator. Distinct supports preserve the multiplicity whose collapse was directly rejected. Exact per-action reparsing and recoverable budget deferral keep planning within the learner's evidence and authority boundaries.
- **Consequences:** Exact replay preserves all Campaign 21 actions 1-61 and first diverges at action 62 to `(58,37)` rather than the rejected flat starts `(60,28)` or `(57,18)`. Campaign 22 then observed the complete hierarchy without official progress, so the returned lower-layer relations remain evidence while the claim that their joint parent centroid is sufficient is removed. The next repair must retain the official rejection across frame-specific mechanic identities and must reject zero-displacement progress actions. Production still contains no target game ID, known coordinate, campaign label, or walkthrough token.
- **Reopening condition:** Met by Campaign 22 remaining `NOT_FINISHED` after the complete returned hierarchy. This reopens only joint parent-centroid sufficiency; the observed child relations, exact-cover recognition, one-action recomputation boundary, and returned distinct-support geometry remain bounded evidence unless separately contradicted.

## D-003-0014 - Test exact non-active-child isolation after joint hierarchy rejection

- **Status:** ADOPTED; CHILD-ONLY SUFFICIENCY FALSIFIED BY CAMPAIGN 25
- **Stage:** 10
- **Date:** 2026-08-25
- **Commit:** `20078a1a0c6e549017fa02e4ac3b50da7e781a0f`
- **Decision:** Retain Campaign 22's returned lower-layer affine evidence but persist its official rejection of joint parent-centroid sufficiency across frame-specific mechanic identities. On the clean level-entry scene, test the smaller compositional alternative by projecting exactly one non-active child to the sole readable target while keeping the other child's endpoints and mediator geometrically unchanged. Require one unique child, mover assignment, exact projected child centroid, and parser-safe endpoint and mediator identities. Preserve the parent target's raw surface and identity in the validation surface; permit only the selected child's expected mediator or connector occupancy across that surface when the returned flat-target-style geometry remains exactly reparsable. Reject zero-displacement progress actions, unrelated static changes, component merges, target-identity loss, ambiguity, or budget exhaustion. Submit only the first action and recompute from each official consequence.
- **Alternatives:** Repeat the rejected joint hierarchy under a new frame hash; isolate both children simultaneously; move the already-active child because it is nearest; erase the parent target from structural validation; forbid every connector crossing of a target despite prior returned flat-target frames; enumerate arbitrary interactions; encode the observed coordinates, colors, game identity, or a Campaign 23 action sequence.
- **Evidence:** Campaign 22's nine-action hierarchy ended with distinct supports whose parent centroid exactly matched `(53,28)` yet returned `NOT_FINISHED`. The immediate repeated click at `(52,22)` produced no geometric displacement and consumed one visible HUD step. Across seven complete level-5 lives, `GAME_OVER` arrived after exactly 60 non-reset submissions; the sequences differed, so the evidence supports a bounded resource horizon rather than an identical-play claim. Campaign 23 officially returned the first two child-isolation actions, but an implicated connector-certificate mismatch cleared the plan before the terminal isolated-child consequence.
- **Official observation:** Campaign 23, frozen at `cf6acbd46dfc46e946e9a6ed76dc0a819142a63e`, returned role exchange `(34,55)` at zero-based recording row 62 / physical line 63 with no mechanics residual. It then returned relocation `(53,36)` at row 63 / line 64 with the selected arity-3 endpoints, mediator `(48,46)`, active role, frozen arity-2 sibling, parent target, and target surface all in the predicted state. The receipt nevertheless recorded `planned child-isolation consequence was not structurally readable`, because the selected connector signature failed under the frozen raster rule. The next submitted action was `(43,34)`, rather than the queued `(41,47)`. The interrupted run remained `NOT_FINISHED`, `levels_completed=4/6`; it did not observe the terminal isolation, level progress, or `WIN`.
- **Campaign 25 official observation:** Campaign 25, frozen at `b2d482b0c85ec2d433a6b0c7e769ce1e2d1c12d1`, officially returned all six child-isolation actions through terminal `(60,24)`. The final frame exactly preserved the sibling and target, fully reparsed the selected child with its mediator at the parent target, and returned `NOT_FINISHED`, `levels_completed=4/6`, `win_levels=6`. This falsifies only child-only sufficiency. It does not erase the returned child mechanics, establish a different composition, advance the level, or complete the game.
- **Why:** The Campaign 22 official residual implicates the composition hypothesis, not the already-observed affine child mechanics. Isolating one non-active child while freezing the other is the smallest action-grounded discriminator between a joint parent target and a child-specific target. Carrying the raw target identity while allowing only expected connector occupancy prevents the validator from rejecting the same flat-target geometry that the official environment previously returned.
- **Consequences:** Repair commit `20078a1a0c6e549017fa02e4ac3b50da7e781a0f` retains semantic hierarchy rejection independently of frame identity, rejects zero-displacement progress, and validates exact endpoint, mediator, center-marker, connector, sibling, and target-surface state from every projected-and-reparsed step. The settled Campaign 21 clean frame produces the six-action sequence `(34,55)`, `(53,36)`, `(41,47)`, `(46,24)`, `(52,55)`, `(60,24)`, with the selected child mediator ending at `(53,28)` and 106,434 of 131,072 search units remaining. Its pre-Campaign23 verification passed 100 visual-policy tests, 11 focused child-isolation tests, nine secret tests, and source-quality checks. Campaigns 23 and 24 exposed connector rounding and exact occlusion-lineage defects without reaching the discriminator. Campaign 25 reached it and preserved an exact official failed-mechanism result: child-only sufficiency is now rejected while the lower child mechanics remain bounded evidence. `GameState.WIN` remains unobserved.
- **Reopening condition:** Met by Campaign 25 remaining `NOT_FINISHED` after the complete exact isolated-child consequence. Reopen only child-only sufficiency; retain the returned child mechanics and investigate a distinct composition from a certified state.

## D-003-0015 - Raster oriented endpoint-to-mediator connectors with exact ties toward the endpoint start

- **Status:** ADOPTED; LOCAL AND CROSS-CAMPAIGN FRAME VERIFICATION PASS; COMMITTED
- **Stage:** 10
- **Date:** 2026-08-25
- **Commit:** `b7a606673e87ea0d25c69e086bf7d8b00ed5bf68`
- **Decision:** Treat connector construction as an oriented endpoint-to-mediator operation. Raster each coordinate by exact integer nearest rounding; when a displacement lies exactly halfway between two cells, choose the cell toward the endpoint start. Use the same oriented raster in projection, safety, and returned-frame connector evidence, and retain exact connector-signature equality rather than weakening the certificate.
- **Alternatives:** Python floating-point `round` with ties-to-even; half-up rounding independent of orientation; reverse mediator-to-endpoint orientation; permit either tie cell; ignore the failed connector leg; relax exact connector signatures; encode the Campaign 23 coordinates or game identity.
- **Evidence:** On Campaign 23 step 62 / zero-based recording row 63, the leg from endpoint `(53,36)` to mediator `(48,46)` crossed an exact tie. The frozen banker-rounded projection selected `(50,41)`, while the official frame contained `(51,41)`. That one missed cell split returned evidence for the leg, reducing the frozen recognizer's selected connector certificate from thirteen cells to seven even though endpoint, mediator, active-role, frozen-sibling, hierarchy, and target-surface certificates all matched. A read-only audit of 666 readable official level-4 frames across Campaigns 21-23 found that exact nearest rounding with ties toward the endpoint start matched every unobstructed connector cell; banker and orientation-independent half-up rounding had counterexamples.
- **Why:** Planning and perception must use the same observation-grounded, oriented raster relation. Exact integer arithmetic removes platform-sensitive floating-point and ties-to-even behavior while preserving the strict certificate that distinguishes a readable connector from destructive overlap.
- **Consequences:** The repaired projection retains child-isolation signature `affine-child-isolation:861a43265b195cdf82a5e278` and sequence `(34,55)`, `(53,36)`, `(41,47)`, `(46,24)`, `(52,55)`, `(60,24)`. Recorded-frame replay certifies both returned Campaign 23 actions with no structural residual and keeps the queued continuation. The exact visual-policy suite passed 103 tests; the focused child-isolation module passed 13 tests; all nine repository secret tests, Ruff lint and format, strict mypy, compilation, JSON parsing, and diff checks passed. This is local and cross-campaign frame verification only: Campaign 23 remained `NOT_FINISHED`, level 4, and observed neither terminal isolation nor `WIN`.
- **Reopening condition:** An unobstructed official connector contradicts endpoint-start tie handling; projection and returned-frame evidence diverge under the same orientation; or the rule admits a component merge or target-surface loss that exact certification should reject.

## D-003-0016 - Do not fall from a readable exhausted hierarchy into an uncertified coordinate probe

- **Status:** ADOPTED; LOCAL AND FULL VISUAL REGRESSION PASS; COMMITTED
- **Stage:** 10
- **Date:** 2026-08-25
- **Commit:** `b7a606673e87ea0d25c69e086bf7d8b00ed5bf68`
- **Decision:** When a readable affine hierarchy has no certified child-isolation or joint layout and every parser-safe role binding is exhausted, fail closed with an explicit target-preserving-continuation residual. Do not fall through to the generic coordinate probe. Keep the marker-bootstrap probe separate because it runs under a different, localized evidence gate.
- **Alternatives:** Continue generic open-space probing; check only the selected center cell; permit a mostly-background endpoint footprint; enumerate target-specific forbidden coordinates; suppress the lost target from later perception.
- **Evidence:** After Campaign 23's connector mismatch cleared the intended plan, the policy tried safe role exchanges and alternate layouts, then submitted generic coordinate probe `(53,17)` at trace step 74. Its translated endpoint footprint covered four cells of the complete twelve-cell composite target centered at `(57,18)`: `(54,17)`, `(54,19)`, `(55,16)`, and `(55,20)`. The five-entry target-surface signature present through official observations 61-74 fell to four entries in observations 75-76. The probe's center passed `scene.is_open`, but the full glyph did not preserve the observed target. This damage is distinct from the earlier connector-certificate residual and did not establish progress.
- **Why:** A readable hierarchy and its target surfaces are stronger evidence than an unstructured coordinate fallback. Exhausting certified layouts or role bindings does not authorize consuming that evidence, especially under a finite official action horizon.
- **Consequences:** The production policy now raises a bounded `PolicyError` before generic probing at this boundary. A focused regression forces both hierarchy layouts and every role binding unavailable, proves the generic probe is never called, and preserves the input frame. The exact visual-policy suite passed 103 tests, with nine repository secret tests and source-quality checks also clean. The repair is generic and contains no game ID, target coordinate, or walkthrough. It is a safety repair, not a completion claim.
- **Reopening condition:** A target-preserving generic hierarchy probe is later certified end to end; the guard blocks a required already-certified hierarchy action; or an official consequence shows that the readable hierarchy itself disappeared before the guard.

## D-003-0017 - Match official mediator-over-endpoint z-order and retain only exactly certified occluded lineage

- **Status:** ADOPTED; COMMITTED AND VERIFIED
- **Stage:** 10
- **Date:** 2026-08-25
- **Commit:** `f10abedad58366ad24719b90b235a36dc38943df`
- **Decision:** Render projected child endpoints before their recomputed mediator, matching the effective mediator-over-endpoint order observed officially. Carry a prior child identity across a temporarily unreadable consequence only when the selected mutable raster, every non-left-column board cell, endpoint center marker, visible endpoint residue, mediator, frozen sibling, connectors, and target surfaces match one predeclared projection exactly. Permit changes only in the observed left-edge lifecycle HUD column. Keep occlusion distinct from structural readability, require the terminal state to reparse fully, and latch the exact `(level, relation, plan, phase)` before any unrelated fallback when an assessed hierarchy consequence or queued precondition does not match.
- **Alternatives:** Retain endpoint-over-mediator projection; reject every projected overlap; accept partial endpoint shapes approximately; ignore arbitrary nonselected board changes; discard the hierarchy and infer unrelated flat mechanics; encode the recorded action sequence.
- **Evidence:** Campaign 24 rows 62-64 returned the first three child-isolation consequences with exact certificates. Row 65 returned `(46,24)` with target and frozen sibling exact, but official mediator cells `(51,36)` and `(52,37)` replaced the stationary endpoint's outer color. The endpoint centered at `(53,36)` lost two shell cells and the arity-3 hierarchy became unreadable. Campaign 24 then lost hierarchy lineage, damaged target surfaces, repeated a six-coordinate source-free cycle five times, and reached `GAME_OVER`. Read-only replay regenerates the unchanged six-action plan, exactly matches official rows 62-65 outside cumulative x=0 HUD changes, certifies only that two-cell occlusion, and keeps `(52,55)` queued. Mutation and search-exhaustion regressions reject extra selected, frozen, connector, target, or non-HUD board changes while preserving separately classified search-budget uncertainty.
- **Why:** Planning and consequence certification must reproduce observable composition order. A predictable occlusion should not erase reliable causal lineage, but neither should partial recognition authorize historical reconstruction or a fallback that consumes the official resource horizon.
- **Consequences:** The selected child can continue through a uniquely projected intermediate occlusion without globally relaxing shape equality. Any ambiguity or protected-raster mismatch stops before a new mechanic or source-free probe. Exact lineage identity is exposed in the bounded policy snapshot. No child-isolation terminal, level progress, or `WIN` is claimed by this repair.
- **Reopening condition:** A later official frame contradicts the observed overlap order or left-column HUD boundary, the exact certificate admits competing hidden assignments, or the protected-raster gate rejects an otherwise independently certified necessary consequence.

## D-003-0018 - Restore the exact pre-discriminator hierarchy before testing a distinct composition

- **Status:** ADOPTED; OFFICIALLY VERIFIED IN CAMPAIGN 26
- **Stage:** 10
- **Date:** 2026-08-25
- **Commit:** `3312f6d80d943f7c6213f1dc0bae37a27e759293`
- **Decision:** For a child-isolation action, compare the returned visible endpoint and mediator counts with the exact projected per-action counts rather than requiring raw before/after count equality. When a fully certified child-at-target consequence returns `NOT_FINISHED`, preserve that relation-level falsification and queue one bounded inverse replay derived only from the original forward plan and its exact prior-state certificates: reverse every pre-terminal forward action, then restore the original active-role center. Give the entire inverse one distinct `affine-child-recovery` identity. Require every returned protected raster, selected relation raster, visible count, sibling, connector, target, and active-role certificate to match exactly; latch lineage and stop on any mismatch. After exact restoration, clear only the concrete recovery execution and test the distinct joint composition without retesting child-only sufficiency.
- **Alternatives:** Treat any endpoint-count change as lineage loss; accept arbitrary count changes; continue joint planning from the altered child-at-target state; click a generic role binding without restoring its certified geometry; hard-code Campaign 25's coordinates; forget the child-only rejection after rollback or reset.
- **Evidence:** Campaign 25 terminal row 67 returned the exact projected arity-3 child at the target and de-occluded the fifth endpoint, changing the readable count from four to five. The protected board and all 139 selected relation cells matched exactly; only the frozen raw equality `len(before.endpoints) == len(after.endpoints)` rejected the consequence and caused `PolicyError`. Read-only search found no certified joint continuation from any partial inverse state under the existing planner; the exact six-action rollback restores the known pre-discriminator state, where the previously certified joint plan is again available. This is a lower bound for restoring that known state, not a global minimality claim over speculative geometries.
- **Campaign 26 official verification:** Frozen commit `063b1421a24450fcdd84ac04d1cd086bbd995671` returned all six exact recovery actions at submissions 68-73, restored the certified pre-discriminator hierarchy and active binding, retained the child-only rejection, and then returned all nine actions of the distinct equal-weight joint plan at submissions 74-82. Submission 82 placed the two child mediators at their predicted parent centroid `(53,28)`, but the official environment returned `NOT_FINISHED`, `levels_completed=4/6`, `win_levels=6`. No reset, `GAME_OVER`, target damage, or lineage loss occurred during recovery or the joint discriminator. The later evaluator `PolicyError` does not revise those official consequences.
- **Why:** A prediction-certified de-occlusion is expected evidence, not loss of identity. Once a discriminator falsifies one composition, the smallest auditable continuation is to return through the learner's own conserved trace to a state where the distinct alternative is defined. This preserves the failure rather than smoothing it into a retry or unrelated probe.
- **Consequences:** The final candidate passed 124 exact visual-policy tests, including successful six-action inverse provenance, 4-to-5 endpoint de-occlusion, first-step and terminal raster corruption, queued-precondition corruption, `GAME_OVER`/`RESET` lifecycle behavior, and joint continuation after restoration. Ruff, format, strict MyPy, compilation, and diff checks pass. Campaign 25 replay matches all 67 official selections, retains lineage, queues six recovery actions under one signature, and selects the exact first inverse. Production contains no target ID, campaign label, or recorded coordinate sequence. This is verified replay and synthetic evidence, not official progress.
- **Reopening condition:** Any official inverse consequence differs from its exact certificate; recovery reaches `GAME_OVER`; the original state is not restored exactly; the child-only rejection is lost; the distinct joint plan is unavailable after restoration; or the subsequent official consequence exposes a smaller implicated residual.

## D-003-0019 - Recover the equal-weight hierarchy before testing arity-weighted endpoint composition

- **Status:** ADOPTED; OFFICIALLY EXECUTED IN CAMPAIGN 27; ARITY-WEIGHTED COMPLETION SUFFICIENCY FALSIFIED
- **Stage:** 10
- **Date:** 2026-08-25
- **Commit:** `ea25385a0a516d0718431b0eeca1e854e07feee8`
- **Decision:** Preserve the official child-only and equal-weight joint rejections. From the exact equal-weight terminal, traverse only the inverse of that certified nine-action joint plan until the protected row-73 hierarchy and active binding are restored. Then test one distinct composition in which each child mediator is weighted by its observed endpoint arity, equivalent to flattening all five endpoints without erasing their two-child hierarchy. Keep recovery and weighted planning under separate signatures, require an exact protected-raster certificate before every queued action, submit only one action before reobservation, and stop at the first mismatch. Derive all coordinates from the current observation and conserved plan certificates; do not encode a campaign sequence, game identifier, or target coordinate.
- **Alternatives:** Treat the two child mediators as equally weighted again; discard the hierarchy and probe unrelated coordinates; move directly from the altered post-joint state without exact recovery; count every visible object equally regardless of its child relation; enumerate all possible interactions; or treat a local projection as official progress.
- **Evidence:** Campaign 26 submission 82 officially returned `NOT_FINISHED` after the two child mediators reached their equal-weight parent centroid, falsifying only that completion hypothesis. The hierarchy retains unequal observed arities two and three, so arity-weighted composition is a distinct observation-derived relation. Commit `ea25385a0a516d0718431b0eeca1e854e07feee8`, source SHA-256 `75138045a7f4a06eeeb947522bb67206fb12e6d17d56927f54fa232521a0bc39`, implements generic exact recovery followed by `affine-weighted-hierarchy:ab316b07112bbc772f9b5f6f`. Read-only receipt `C:\a\arc3-b003-play\campaign26-75138045-action82-recovery-weighted-plan-replay-receipt.json`, file SHA-256 `642b3b1e0b7d853d44f7b7ac3d7c7753ba467625645dd5fe2f68816b8aa1f491`, payload SHA-256 `d16aa266db51f6ced986b07221073005213658dcdb10b353161e0151a32cbf7d`, matches all 82 historical submissions and first diverges at submission 83 to `(34,55)` instead of recorded `(60,25)`. It certifies nine recovery actions to protected hash `sha256:e2bab5b0964c3c52fc01c81ba29791531136a4c95cad347ab7466b9ce67228f6`, then nine weighted actions using supports `(47,19)` and `(57,34)`, weights two and three, ending at target `(53,28)` and protected hash `sha256:a3a625a0beb6993982533da09f99fc0a3aee4aa8a38bd230077cb259502cea14`. The replay inspected no game source and issued no environment action.
- **Campaign 27 official verification:** Frozen commit `db5c70ed93970ef16eab10349e634c203dc8a625` matched Campaign 26 through submission 82. Submissions 83-91 returned the exact equal-weight inverse and restored protected hash `sha256:e2bab5b0964c3c52fc01c81ba29791531136a4c95cad347ab7466b9ce67228f6` with active binding `(25,35)`. Submissions 92-100 returned the nine-action arity-weighted plan `(42,16)`, `(43,34)`, `(52,22)`, `(52,55)`, `(60,29)`, `(41,47)`, `(60,39)`, `(34,55)`, `(51,34)`. Submission 100 completed supports `(47,19)` and `(57,34)` with weights two and three at target `(53,28)`, but the official environment returned `NOT_FINISHED`, `levels_completed=4/6`, and `win_levels=6`. Submissions 101-109 exactly restored the protected origin, endpoint colors, and active binding; submissions 110-113 made four safe role exchanges and each remained `NOT_FINISHED`. No reset, `GAME_OVER`, `WIN`, target damage, or hierarchy-lineage failure occurred. The evaluator failed closed only after step-113 candidates were recorded; no submission 114 was selected or sent.
- **Campaign 27 replay and integrity:** Read-only receipt `C:\a\arc3-b003-play\campaign27-db5c70e-2ce40400-integrity-replay-receipt.json`, 11,560 bytes, file SHA-256 `993b64b2eea7a346624d50094ecdecfd61c88271a8cc7d53f6efb21445749fd0`, payload SHA-256 `3b2e479b64134f7663dd99129a4430281af0b8e4363b40c8127556470ff166c3`, matches all 113 official submissions and reproduces the post-action-113 `PolicyError`. It verifies the 680-event chain, 188 unique blobs, 114 recording rows, 394 returned frames, and all action, state, level, and mechanics bindings with zero mismatch. Recording SHA-256 is `2ce40400d5001b33469a8c671fbaea1ddd94de7d5c3326a83676fe92c2abb7ef`; trace tail is `sha256:ac74b4f78b640f95836ce566e0fbb72dde0b3ed06f6154db0e36c14a5412451a`.
- **Why:** Equal parent weighting discards the endpoint multiplicity that distinguishes the two child relations. After its official failure, the smallest currently certified discriminator is to retain the hierarchy while carrying that observed multiplicity into composition. Exact recovery keeps the comparison anchored to a known state and avoids interpreting post-failure geometry as a new start.
- **Consequences:** Arity-weighted endpoint composition now joins child-only arity-three isolation and equal-weight child composition as a bounded officially falsified sufficiency hypothesis. Its exact recovery remains supported. The failed terminal does not falsify coordinate control, child identity, hierarchy identity, endpoint arity, rollback, or every other composition. The next policy must retain all three failures, repair only the overbroad child-only rejection scope, and test the structurally distinct arity-two sibling only after a complete offline certificate. `GAME_OVER` remains failure evidence; only official `GameState.WIN` completes the game.
- **Reopening condition:** New official evidence shows the Campaign 27 terminal certificate was not reached; recording/trace integrity fails; the arity-weighted relation was misidentified; or a later official transition establishes a narrower contextual condition under which that exact relation succeeds. Local projection, replay, or a new formula alone does not reopen it.

## D-003-0020 - Scope child-only rejection to stable structural strata

- **Status:** ADOPTED; OFFICIALLY VERIFIED IN CAMPAIGN 28; BOTH CHILD STRATA FALSIFIED AND EXACTLY RECOVERED
- **Stage:** 10
- **Date:** 2026-08-25
- **Commits:** Mechanics `894adbbc2a16a5043237a468d232af5805ac77c5`; evaluator evidence `a1f85dbb4607016f555358bb936343ee8ed99861`
- **Decision:** Retain the officially falsified arity-three child-only, equal-weight, and arity-weighted hypotheses, but bind child-only failure to the parent relation plus the selected child's stable structural stratum. The stratum carries arity, mediator palette and center marker, normalized mediator and endpoint glyphs, endpoint center markers, and stable connector color plus supported-leg degree. It excludes absolute placement, object references, connector raster paths, and endpoint active-role color. Structurally identical siblings deduplicate; distinct strata remain independently testable. Prefer the inactive child before any child failure to preserve the observed discriminator order, then prefer an active unfalsified child so a direct test omits an unnecessary role exchange. Retain failed strata across same-level `RESET`, clear them only on genuine level progress, use exact forward and inverse raster certificates, and fail closed rather than falling from a readable exhausted hierarchy into raw role or coordinate probes.
- **Alternatives:** Keep relation-wide rejection; key by arity alone; include endpoint coordinates, object references, active-role color, or connector raster paths in the key; always perform a role exchange; treat a same-level reset as erasing failure; test visible-node weighting before the smaller sibling discriminator; or encode the target game, campaign, or audited coordinates in production.
- **Evidence:** The settled mechanics passed 38 child-isolation tests and 104 complete visual-policy tests. These cover distinct and identical strata, connector-color distinction, connector layout invariance, reset retention, level clearing, exact active arity-two terminal and three-action inverse, returned-raster and queued-precondition corruption, exact terminal `GAME_OVER`, all-family exhaustion, and recognition/layout exhaustion without uncertified fallback. The evaluator repair passed 37 completion/public-contract tests, labels new mechanical `PolicyError` exhaustion `FAILED_MECHANISM` without revising sealed legacy receipts, and restores recording-derived `GAME_OVER`/`WIN` counts plus final trace metadata without weakening the authoritative completion conjunction. Ruff, format, strict MyPy, compilation, and diff checks pass. Two independent reviews found no remaining P0/P1. Production `visual_causal.py` contains no target ID, campaign label, frozen recording hash, or audited coordinate literal.
- **Commit-bound replay:** Read-only receipt `C:\a\arc3-b003-play\campaign27-a1f85dbb-2ce40400-child-stratum-divergence-replay-receipt.json`, file SHA-256 `1ba06cf96b3a4dde50de5bc75290f3ee9140bc5c7f48863cf66de1df8e0b5dc1`, payload SHA-256 `9a0694c84e7f413b2ba2de996df350509d23b8ef97d5967db674dbc02d9af80f`, binds clean detached commit `a1f85dbb4607016f555358bb936343ee8ed99861` and Campaign 27 recording SHA-256 `2ce40400d5001b33469a8c671fbaea1ddd94de7d5c3326a83676fe92c2abb7ef`. It matches submissions 1-73 and first diverges at submission 74 from recorded `(58,37)` to the unfalsified direct arity-two action `(50,22)`. The certified forward plan is `(50,22)`, `(43,34)`, `(56,34)` and its inverse is `(43,34)`, `(50,22)`, `(25,35)`, restoring protected hash `sha256:e2bab5b0964c3c52fc01c81ba29791531136a4c95cad347ab7466b9ce67228f6` and active binding `(25,35)`. At divergence one of two child strata is rejected; equal and weighted relation rejections are both still zero because the repaired policy branches before those Campaign 27 actions. The replay issued no environment action, inspected no game source, consumed no holdout, and makes no completion claim.
- **Campaign 28 official verification:** Frozen commit `4a9ebb4798003c5c1d3d6eedae352fc414a998fe` matched the earlier official trace through submission 73. Submissions 74-76 returned the complete direct arity-two child discriminator. Submission 76 placed that child at the parent target with the exact protected certificate, but the official environment returned `NOT_FINISHED`, `levels_completed=4/6`, and `win_levels=6`. Submissions 77-79 returned its exact inverse and restored the protected pre-discriminator hierarchy and active binding. The distinct equal-weight terminal at submission 88 and arity-weighted terminal at submission 106 also returned exact `NOT_FINISHED`; submissions 89-97 and 107-115 respectively restored the certified origin. No reset, `GAME_OVER`, `WIN`, target damage, or hierarchy-lineage failure occurred. Both stable child strata are now rejected by their own official consequences rather than by inherited evidence.
- **Campaign 28 replay and integrity:** Receipt `C:\a\arc3-b003-play\campaign28-4a9ebb4-154f3e1f-integrity-replay-receipt.json`, 8,635 bytes, file SHA-256 `45da1c1537f734f2ac9b34b131d01ed1df57eec742c3a35a98e09e73cf9a1222`, payload SHA-256 `2de56975166d4d88c5bce93218347e4ab6c519ef908230ef8702738963d93158`, verifies the 692-event sealed trace and all 115 selected, submitted, mechanics, and consequence bindings. Recording SHA-256 is `154f3e1f862dfd47c08607fcfa84546bb9f0b23e85841fe4242772ed9d1637fd`; trace tail is `sha256:fe333f47f01705c799e278f27fd882fb855d8ab4264ff1b40501b2df2fadf806`. The independent receipt reports `trace_manifest_object_hash_verified=false`, while the authoritative public verifier and exact replay pass; that bounded verifier discrepancy remains explicit rather than being relabeled true.
- **Why:** Campaign 25 supplied exact official failure evidence only for the arity-three child. Extending that result to a visually distinct arity-two sibling would manufacture evidence. A stable observation-derived stratum is the smallest identity that preserves actual failure while leaving the implicated sibling question open. Stable connector semantics distinguish readable relations without letting movable path geometry reopen a falsified hypothesis.
- **Consequences:** B-003-0020 is resolved by the official arity-two consequence and exact recovery. Child-only sufficiency is now falsified separately for both observed structural strata; equal-weight and arity-weighted completion sufficiency remain separately falsified. These failures do not falsify coordinate control, hierarchy identity, rollback, or every possible composition. Campaign 28 is sealed `FAILED_MECHANISM` evidence, not completion; only official `GameState.WIN` completes the game.
- **Pre-Campaign 28 integrity preflight:** The live OneDrive worktree run is preserved as `FAILED_INFRASTRUCTURE`: 36 integrity cases passed and one repository-integrity case emitted `unscannable_candidate` because candidate bytes changed during its bounded read; no secret or policy finding was reported. A clean detached checkout of commit `896d5556a415bcbb14bb695b7c5314a2dd8a7d75` passed all 37 secret, repository-integrity, and policy cases. Its deterministic manifest-aware offline integrity receipt `C:\a\arc3-b003-preflight-896d555-integrity.json` passed archive, policy, secret, source-identity, and supply-chain checks with zero findings; file SHA-256 `14d5c537f390005f5bd473603b6b4bcc0c66d6765c0f4982e598b657663be0ae`, embedded receipt SHA-256 `679abe5c78eed105181fd7cb01263d3176e2223a99a086dcb7e6ef4d4f23c1d4`. The explicit package-only Git-blob receipt `C:\a\arc3-b003-preflight-896d555-package-only.json` returned exit `0` with `package_only_passed=true`, binding the clean commit and index to 377 immutable blobs with zero findings; file SHA-256 `3a10ec22794cd17aa7f3d46e06f3bbbf53bf870a92ba7968f5f3870ec60457a4`, embedded receipt SHA-256 `4f1fd462993e09677bbbc7983b9b2a7c2f824b71dbbc4b784dab8f447096b1f5`. Its public-identifier field is intentionally not evaluated in package-only mode and is covered by the separate manifest-aware receipt. The detached action-semantics scan also passed with zero findings and receipt hash `sha256:0c21392cb273165bec218b42b09e94d6ce6840504d21579bf4f0f820a553c99a`.
- **Reopening condition:** The sealed replay or official bindings fail verification; later official evidence establishes that either terminal certificate was not reached; a failed stratum reopens without level progress; or a new observation-derived child stratum is suppressed without its own evidence. Only an official returned `GameState.WIN` completes the game.

## D-003-0021 - Test visible-node hierarchy composition after the observed lower-order families failed

- **Status:** ADOPTED; CLEAN CHECKPOINT VERIFIED; OFFICIAL CONSEQUENCE DEFERRED BY STAGE GATES
- **Stage:** 10
- **Date:** 2026-08-25
- **Commit:** `ad3c604cbaa8ca97e451d58c8fa24dd15c73f611`
- **Decision:** Preserve the two child-stratum, equal-weight, and endpoint-arity-weighted failures. The next bounded hierarchy hypothesis weights each child by its observed endpoint arity plus its one visible mediator, giving the currently observed children weights three and four without encoding those values as target-specific constants. Derive supports, weights, target, actions, and all forward/inverse certificates only from the current observation. Reject only this hierarchy family on an exact terminal `NOT_FINISHED` or `GAME_OVER`, retain the rejection across same-level reset, clear it only on actual level progress, and stop on any certificate or lineage mismatch.
- **Alternatives:** Retry any officially falsified family; count every board object; enumerate interactions; use audited coordinates or campaign identity; treat a synthetic projection as progress; or launch another official campaign before completing the workflow's earlier learner gates.
- **Evidence:** The clean implementation passed 109 complete visual-policy tests and 38 child-isolation tests; six post-format visible-node tests also passed. Ruff lint and format, strict MyPy, compilation, and diff checks pass. Formatting preserved the Python AST digest `e52bc7587f89bddb9ab1d2717d3f9933334a879a59b193897ceea1f8c602ae46`. Production source SHA-256 is `6d1072d4709ed0de6a462c0705b1d67bd61b29f314cd700022d9c4aaaffeda29`; test SHA-256 is `6ca584a4b08ea28dc84e780ef4ab9d8c48e21f6822537c522623c96cff23e8e9`.
- **Noncanonical replay boundary:** Worktree receipt `C:\a\arc3-b003-play\campaign28-worktree-dd17a8b8-154f3e1f-visible-node-submission116-replay-receipt.json`, file SHA-256 `c63ee384e093a7bd12aa91ea79417a6b6ce4170c9309632e3d2eacbf32ec0b57`, payload SHA-256 `a18baf0096b2fcd36b4d6cc613e83eb18c4f2083b97d6182d522225d6f4c1c30`, derives a nine-action candidate whose supports are `(49,20)` and `(56,34)` and whose observation-derived weights are three and four. It is explicitly noncanonical because it predates the final Stage 02-09 and production-reachability integration commit. It issued no environment action and must be superseded by a clean commit-bound replay before Campaign 29.
- **Why:** The smallest remaining readable residual is that each mediator is itself a visible node in the composed relation. This differs from counting endpoints only while preserving the already learned hierarchy. It is a bounded discriminator, not an assertion that the mechanic is solved.
- **Consequences:** B-003-0021 remains open. Campaign 29 is paused until the exact workflow gates are repaired, the production route is proven to invoke the mechanical policy, and a clean final replay plus integrity preflight binds the launch commit. No official progress or `WIN` is claimed.
- **Reopening condition:** A clean replay cannot derive the plan from observations; a certificate fails; production cannot reach the policy; Stage 02-09 evidence invalidates the learner integration; or an official consequence exposes a smaller implicated residual.

## D-003-0022 - Pause official play and supersede the defective Stage 08 diagnostic protocol

- **Status:** ADOPTED; REPAIR IN PROGRESS
- **Stage:** 02-09
- **Date:** 2026-08-25
- **Decision:** Do not launch Campaign 29 merely because a target-game continuation exists. Preserve the first complete Stage 08 matrix as `FAILED_MECHANISM` diagnostic evidence, then freeze a v0.2 protocol before rerunning it. The replacement must bind the exact Stage 01 Build 002 baseline, independently freeze seeds and budgets, keep level resource costs stable within each sequence, randomize action mapping, exercise the actual mechanical learner lifecycle and production-facing route, compute observation-grounded per-channel receipts, and test literal H1-H3 without rewriting their thresholds after seeing results.
- **Alternatives:** Promote the structurally complete v0.1 matrix to `PASS`; use its eight synthetic wins as completion evidence; silently change the baseline, seed family, or budgets; accept self-reported retention; or continue official play while Stages 02-09 remain pending.
- **Evidence:** Diagnostic receipt `C:\a\arc3-b003-stage08-dd17a8b8-20260825-01\stage08-dd17a8b8-synthetic-matrix-audit-receipt.json`, file SHA-256 `d77d4b3cf4046ca1e4acd2bb1e465194bf04b10dbfaa75a7e4a009bc059f040a`, payload SHA-256 `f528472ce295d578522063292710fa13f1202266a03251d2dd6b7adafce728bc`, covers 120 sequences and 1,200 rows with complete deterministic receipts. Only eight sequences reached synthetic `WIN`; 101 ended at reset budget and 11 at action budget. H1 and H3 exploratory-pressure medians tied their comparators rather than being strictly lower. H2 relied on non-grounded self-reporting and did not meaningfully exercise reopen/repair. The v0.1 adapter used bespoke navigation, immediately resolved residuals, omitted several consequence channels and mechanic modes, made erroneous global reopening impossible, and did not instantiate the production `VisualCausalPolicy` or `MyAgent`. Its actual Build 002 comparator commit `753b0e007222a973a2c8a6d7ce14a395135d3c5f` differed from the Stage 01 frozen baseline `5448c53f3b7e08f606cf292e6068f3f9c9db16d4`.
- **V0.2 preregistration freeze:** Commit `24c34dbd25a946446550de8f00529cabbf587026` froze `build-003-curriculum-protocol.v0.2.json`, `build-003-heldout-seeds.v0.2.json`, and `build-003-preregistration-amendment.v0.2.json` before any v0.2 result or development smoke. It binds exact baseline commit `5448c53f3b7e08f606cf292e6068f3f9c9db16d4` and tree `700718c09c2a1532cea16526b290f57be0120371`, restores 192 sequence actions, 48 actions per level attempt, ten resets, and ten wall seconds, and freezes a new independently derived 30-case domain. Independent verification recomputed every seed and case identifier. The final 30-by-4 matrix remains unexecuted.
- **Why:** Workflow order and evidence identity matter: an official target-game action can be real while the claimed learner comparison behind it remains unverified. The matrix is useful diagnostic evidence but cannot establish the workflow hypotheses in its current form.
- **Consequences:** Stage 08 is `FAILED_MECHANISM`; Stages 02-09 remain incomplete. The learner lifecycle, v0.2 protocol, exact comparator, adversarial tests, and production reachability must pass their own acceptance gates before the official target is reopened. Synthetic success remains synthetic and cannot substitute for official `GameState.WIN`.
- **Reopening condition:** A preregistered v0.2 matrix with immutable receipts passes or honestly falsifies each literal hypothesis; independent audit confirms the exact baseline and observation-only learner lifecycle; production reachability and integrity gates pass; or a hard external boundary makes the required evaluation unavailable.

## D-003-0023 - Bind matrix status to literal hypotheses and independently reconciled action evidence

- **Status:** ADOPTED; IMPLEMENTED AND PUSHED; FINAL V0.2 MATRIX NOT YET EXECUTED
- **Stage:** 02-09
- **Date:** 2026-08-25
- **Commits:** Protocol `a642a4c63aa1d90605646c948a44140234b48001`; production reachability `6080f9937017c03db9995f912669a72746a524ba`; observation-only lifecycle `ca86e33a047e5e6b6689e509cea899f6c5bd07a6`; literal gates `d68815fc421758b90ef805d2bdea706deceec2ed`
- **Decision:** A complete selector set or 1,200 keyed rows is structure only. Matrix `PASS` now requires the exact v0.2 protocol identity, literal H1-H3 decisions, complete paired rows, replay determinism, replay-linked action receipts, reconciled worker and runner environment-action/reset totals, and zero infrastructure or policy-error rows. Missing H2 reopening assessment is `NOT_MEASURED`, not zero. A v0.1 invocation cannot qualify as the v0.2 final matrix. Matrix `PASS`, if earned, remains synthetic H1-H3 evidence only and cannot establish overall Workflow acceptance or target-game completion.
- **Alternatives:** Retain structural-only `PASS`; infer H1-H3 from favorable means; treat missing reopening assessment as zero; trust worker receipt counts without transition links; omit resets from reconciliation; allow v0.1 selectors to satisfy the v0.2 predicate; or treat a synthetic `WIN` as the official target `WIN`.
- **Evidence:** The lifecycle checkpoint adds observation-grounded ten-channel consequences, passive confirmation, local repair and two-failure implicated reopening, transfer confirmation, additive/conditional/gating/override/delayed event accounting, CLEF metrics, and action links. The production checkpoint proves `MyAgent -> VisualCausalPolicy -> MechanicalLearner -> MechanicLedger` reachability and preserves single-owner consequence/cancellation semantics. The gate checkpoint adds exact v0.2 identity, anti-result, missing-assessment, replay/receipt, policy-error, action, reset, and transcript-counter regressions. Thirty-six focused evaluation tests pass with only the separately preserved legacy authoritative-WIN acceptance excluded; seven decision-gate regressions pass; affected Ruff, format, strict MyPy, and compilation pass. No v0.2 heldout/final matrix or official target action was run by these checkpoints.
- **Why:** The preserved v0.1 matrix showed that deterministic structure can coexist with an invalid learner comparison and failed hypotheses. Status must follow the preregistered claim rules and actual returned evidence rather than file count or self-report.
- **Consequences:** B-003-0022 narrows to the still-open frozen-budget curriculum, Stage 09, final-matrix, and audit gates. Official Campaign 29 remains paused. The final v0.2 matrix may honestly end `PASS`, `FAILED_MECHANISM`, or `FAILED_INFRASTRUCTURE`; none of those labels substitutes for official target `GameState.WIN`.
- **Reopening condition:** Independent review finds a literal decision-rule mismatch; a worker or reset can again obtain receipt completeness before its returned consequence; row and transcript counters can diverge without failing; v0.1 can qualify as complete v0.2; or a complete anti-result can still be labeled `PASS`.

## D-003-0024 - Require complete development curricula and close the named adversarial lifecycle gaps before final evaluation

- **Status:** ADOPTED; DEVELOPMENT AND ADVERSARIAL GATES PASS; FINAL V0.2 MATRIX UNEXECUTED
- **Stage:** 02-09
- **Date:** 2026-08-25
- **Commits:** Hot path `276f8af7f9a52f01c45295f109922e117e04056c`; transition lifecycle `5296afe7b9e0744b7a69540680a1896f4723117b`; occluded identity `0ea67e07281f79ba31e1c9b26377fec85125d497`; five-seed gate `ec7b7d964930bc1dadfe7561200e98593a74ef57`; relevance boundaries `8c87244169c7b97468308dc9868d70c3643921af`; resource lifecycle checkpoints `493a3e610cbcc4fe2d63cf5144b77ecea5c62f44` and `614d055252050a1ff0e2e408a0fe68604aad9302`
- **Decision:** Keep the preregistered v0.2 budgets and final seed set unchanged. Treat a development repair as eligible for the final freeze only after every declared development curriculum reaches authoritative synthetic `WIN`, the full learner remains behaviorally distinct from both per-level reset and BLA-only variants, and each named Stage 09 lifecycle boundary either passes an observation-grounded regression or is preserved as an explicit limit. Exact resource failure roots must constrain every action-selection path; an override discriminator must install a localized `OVERRIDE` mechanic rather than merely emit a receipt; and true level transitions must archive prior-level repair state while retaining earned game-scoped base mechanics.
- **Alternatives:** Increase budgets; replace difficult seeds; accept nine of ten levels; infer a hidden singleton through player occlusion; count a discrimination receipt without changing the ledger; avoid a fatal root only in the probe selector; silently discard level-local residuals; or promote development success to final or official completion.
- **Evidence:** Commit `0ea67e0` changed the first v0.2 development curriculum from nine of ten levels to synthetic `WIN` at ten of ten in 169 actions and two resets. Commit `ec7b7d9` then required all five declared development seeds to reach synthetic `WIN`. The post-resource development matrix at `C:\a\arc3-b003-dev5x4-493a3e6` contains 20 sequences and 200 rows: full BLA+CLEF won 5/5 sequences and all 50 levels in 721 actions with four resets; BLA-only won 3/5 and completed 35 levels in 797 actions with six resets; per-level reset won 0/5 and completed seven levels in 960 actions with 22 resets; exact Build 002 won 0/5 and completed one level before five wall-clock bounds. Matrix SHA-256 is `fb35cfa947e52c00f50b70b02f97b4106678987ef0e51abfe8af0598a82e23c0`; rows SHA-256 `1cbf8866848f75be156fc1813332df637c5b896215f7b279d69a78a7d0cdbbc8`; receipts SHA-256 `43cfb9a724c6d2bdb6b223c76e4705546b28316d4233104c43a18f0bacda9e5f`; report SHA-256 `c621d7a53e51a6e6c691e1f18cb29e0182c0a368fa80dfa7a54d00a90054a607`. After the final resource repair, 17 focused lifecycle cases plus all five development-seed WIN cases passed in 29.08 seconds; an independent integration rerun passed the same ten test functions in 34.31 seconds. Ruff and strict MyPy passed. No final seed, public holdout, official environment, credential, or target source was accessed.
- **Why:** Synthetic success is useful only when the learner actually exercises the claimed lifecycle and the failure semantics are reflected in the active ledger. Frozen budgets and complete development coverage prevent a later favorable final result from concealing a selected-seed or receipt-only repair.
- **Consequences:** Stages 02-07 and the named Stage 09 adversarial suite may advance on bounded development evidence. Stage 08 remains open until the exact frozen 30-by-4 matrix is executed once and independently audited. The measured development loss from disabling persistence or CLEF is selection evidence only until the final matrix reproduces or falsifies it. Override evidence is occurrence-only and exact-resource-context localized; failed-root memory remains bounded to eight; coordinate-only fallback is not synthesized and fails closed.
- **Reopening condition:** Any declared development seed regresses below `WIN` under the frozen budget; an exact failed root is replayed through another selector; override evidence leaves the competing additive repair active or rewrites the base mechanic; prior-level residual or repair state remains active after a true transition; or the final matrix contradicts the development comparison.

## D-003-0025 - Seal the final matrix with an independent source-bound auditor

- **Status:** ADOPTED; AUDITOR IMPLEMENTED AND PUSHED; FINAL MATRIX UNEXECUTED
- **Stage:** 08
- **Date:** 2026-08-25
- **Commit:** `0bb6ddeb2c0febebff992a3d69ac1b0e93b05eda`
- **Decision:** The final matrix runner must refuse a dirty Build 003 checkout and record its exact commit, tree, runner hash, and result-ledger hash. A separate command, writing to a distinct empty output root, must reconstruct the exact v0.2 30-case by four-variant by ten-level result from canonical receipts; bind the exact clean Build 002 comparator; reject duplicate, missing, reordered, replacement, infrastructure, policy-error, replay-link, action/reset, source-hash, and literal-decision inconsistencies; hash the source matrix before and after its read-only audit; and seal its own receipt without overwriting prior evidence.
- **Alternatives:** Trust the runner's self-assessment; audit only file counts; allow a dirty worktree; store the Build 003 branch name without commit/tree identity; reuse the matrix output directory; omit failure rows; or infer replay correctness from deterministic flags alone.
- **Evidence:** `scripts/audit_build003_curriculum_matrix.py`, `src/arc3/evaluation/build003_matrix_audit.py`, and fabricated valid/adversarial tests were integrated at `0bb6dde`. Eight test functions passed from an explicit fresh Windows temp root in 36.88 seconds after the default pytest temp root failed before setup with `PermissionError`; Ruff and strict MyPy passed. The auditor validates all stored flags, digests, action links, counters, exact row derivations, and paired H1-H3 decisions, but the matrix format does not retain full environment transcripts and therefore cannot independently re-simulate transitions. Frozen Build 002 rows similarly retain counter/link attestations rather than learner-style link objects.
- **Procedural finding:** During auditor development, an early broad search may have read the frozen v0.2 manifest bytes. It returned no manifest lines, seed values, cases, outputs, or results; no cases were derived, imported, or executed; all auditor tests used fabricated injected cases; and no seed-set amendment or replacement followed. Preserve this as a pre-result procedural disclosure rather than claiming literal zero-byte access.
- **Why:** The v0.1 diagnostic proved that complete-looking structure can coexist with the wrong comparator, defective lifecycle, and failed hypotheses. A source-bound independent reconstruction narrows the remaining authority gap without claiming more replay evidence than the stored format supports.
- **Consequences:** The final v0.2 matrix remains unexecuted until the implementation and assurance tools are frozen in one clean commit. Its status may still be `PASS`, `FAILED_MECHANISM`, or `FAILED_INFRASTRUCTURE`; the audit cannot manufacture a favorable result and no synthetic status can substitute for official target `GameState.WIN`.
- **Reopening condition:** The auditor can accept a changed source matrix, dirty or mismatched Build 003 identity, wrong Build 002 tree, replacement row, incomplete counter chain, rewritten paired decision, or nonexclusive audit output; or the final stored format omits evidence needed by a claimed conclusion.

## D-003-0026 - Freeze the implementation before the one-shot v0.2 matrix

- **Status:** ADOPTED; IMPLEMENTATION FROZEN; FINAL MATRIX UNEXECUTED
- **Stage:** 08 and 10
- **Date:** 2026-08-25
- **Commit:** `83df5520478cc209c06d9ce4e658c90786914544`; tree `3d1a45995b004947900de3114449a7e42bd80a87`
- **Decision:** Freeze Build 003 implementation at the exact clean commit and tree above. From this point, make only evidence and documentation commits unless a later result explicitly reopens the implementation. Execute the preregistered v0.2 30-case by four-variant matrix exactly once from this freeze, with no selector, replacement, or external worker-storage override, and then run the independent auditor into a separate empty root. Keep Campaign 29 paused until that literal matrix result, audit, final package, integrity, and documentation gates are preserved.
- **Alternatives:** Continue changing production after inspecting final synthetic results; bind the freeze only to a branch name; treat a noncanonical replay as launch authority; weaken the package-only no-child-process guard; repeat or subset the final matrix; or resume official play before the achievable gates are complete.
- **Evidence:** Clean detached canonical replay receipt `C:\a\arc3-b003-play\campaign28-83df552-154f3e1f-canonical-replay-receipt.json` has file SHA-256 `a31eccb229279a965c7a66dc93c80f40b061ee21401591577fe7f6dda2c42bb2`, payload SHA-256 `1ef595d61ec435680e105b6d91eceacf4debe807d146336de6444e5258d3e85b`, and receipt hash `78cb8c95703d6217c9c668fb49f6c433f9765f447f41e0214f86edf6117460fe`; it matched all 115 recorded selections and consequences and cancelled the next observation-derived `ACTION6 (52,14)` without submission. Production action-semantics receipt `C:\a\b003-action-semantics-final-83df552.json` has file SHA-256 `4615ff438676d992f09e6cbbfdda78bf72443806939c1fe31bf6929d3a2c965d`, receipt hash `7b7ca339652c2a124f52c2f80c9f67fd7bee69352d4f3574ce1ad6df4f51c557`, and zero findings. Matched `MyAgent` profile `C:\a\b003-profile-final-83df552.json` has file SHA-256 `caa48466570b4c55ac1fe324071cc7be000ea0b9222d6d043510e0033d256596` and embedded receipt SHA-256 `9e7f61e3b662eee9343e4f05b7983ff33d1a196ebfd2393debc1600dd9503676`; every Build 003 cycle reached the mechanical route, maximum cycle time was 0.0559292 seconds, and maximum peak RSS was 77,959,168 bytes. Build 003 was faster than the matched Build 002 route but used 26,386,432 more peak RSS bytes. This is synthetic performance and reachability evidence only, with zero official interactions and no gameplay or RHAE claim.
- **Preserved failure:** The earlier `91c5f26` Linux package-only release receipt remains `FAILED_MECHANISM` and its guard remains `FAILED_BOUNDARY`, despite 1,012 passing tests. The selected development-performance test transitively launched a policy child and produced exactly 725 denied child-RSS reads, equal to 721 environment actions plus four resets. Commit `83df552` explicitly excludes that process-backed test from the no-child-process package-only profile while ordinary CI retains it; the guard was not weakened.
- **Why:** The final paired result must be evaluated against a source identity chosen before its outcomes are known. Canonical replay, static action semantics, and matched production-route profiling establish the narrow readiness boundary without converting synthetic evidence into official progress.
- **Consequences:** The next execution is the exact final v0.2 matrix at `C:\a\arc3-b003-stage08-v02-final-83df552-01`, followed by the independent audit at `C:\a\arc3-b003-stage08-v02-final-83df552-01-audit`. Preserve `PASS`, `FAILED_MECHANISM`, or `FAILED_INFRASTRUCTURE` literally. Campaign 29 remains unstarted; the authoritative official state remains `NOT_FINISHED`, `levels_completed=4/6`, and `GameState.WIN` unobserved.
- **Reopening condition:** A sealed matrix, independent audit, final package, integrity check, or official consequence identifies a specific implementation defect that cannot be documented honestly without changing source. Any reopening must be ledgered before a code change and cannot erase the one-shot matrix result or prior official failures.

## D-003-0027 - Preserve the final v0.2 failed mechanism and repair only the independent auditor

- **Status:** ADOPTED; ONE-SHOT RESULT SEALED AS FAILED_MECHANISM; FIRST AUDIT SEALED AS FAIL; ASSURANCE-ONLY REOPENING
- **Stage:** 08
- **Date:** 2026-08-25
- **Source identity:** Documentation checkpoint `0ad2011094961876798516af21652eb4be6fbebc`, tree `36da08fbd612edfb6620ce5cf2dc96237742f55b`, containing frozen implementation `83df5520478cc209c06d9ce4e658c90786914544`.
- **Decision:** Preserve the first exact v0.2 30-case by four-variant matrix unchanged and do not rerun, replace, subset, or post-select it. Preserve the first independent audit unchanged as a failed assurance receipt. Reopen only the auditor implementation to recognize the two declared learner metrics and reconstruct valid per-level rows independently of the truthful aggregate incomplete-link flag. Add regressions, then audit the same immutable matrix into a new empty root. The repaired audit must still fail on the 534 genuine link-completeness findings and cannot earn Stage 08 `PASS` or change any paired result.
- **Matrix evidence:** Root `C:\a\arc3-b003-stage08-v02-final-83df552-01`; receipt file SHA-256 `a01c3dfca5c18d6282e978c07de035932dd28de21491e7a7ea04f8354a0fc8a6`; rows SHA-256 `8195b470bf4287f4d3246ee82ab349b39a20fb26697c6e714ef1e1aed748f8c0`; sequence-receipts SHA-256 `d5d7d865a4a4b52c42d01601b274744b3603893d8e07495b61d16fd69a9c2d60`. The complete 1,200-row, 120-sequence matrix returned `FAILED_MECHANISM`: 47 synthetic `WIN`, 43 `ACTION_BUDGET`, and 30 `WALL_CLOCK_BUDGET`; H1 `FAIL`; H2 `NOT_MEASURED`; H3 `FAIL`; replay determinism 1.0; receipt completeness 0.615; zero infrastructure and policy-error rows. Wall time was 133.42792640000698 seconds.
- **Audit evidence:** Root `C:\a\arc3-b003-stage08-v02-final-83df552-01-audit`; audit-receipt file SHA-256 `3f694030d1c7e9a5e2e3e6770a16ec9c8fadba0ee896bbe2098d3b61a66a0214`; report file SHA-256 `f112a31e436ffb46a92aab26ce452971794cc0e3dff67d9ca15eea3a7f563172`; sealed payload SHA-256 `7af34ca1e851bfd46a3e643e687d58b780c667de9a6965447219d69e71486bdc`. It returned `FAIL` with 1,692 findings. Independent diagnosis classifies 1,158 as auditor defects: 900 known-field schema mismatches for `resource_discrimination_actions` and `restoration_ambiguities_resolved`, plus 258 row-derivation errors caused by treating an aggregate incomplete-link flag as all-level invalidity. The remaining 534 are genuine matrix evidence failures: 462 per-level action/receipt count mismatches and 72 truthful aggregate incomplete-link attestations. Thirteen of fourteen checks passed. Exact coverage, identities, source hashes, canonical reconstruction, literal decisions, and source-root integrity passed; sequence replay links and counters failed. The pre/post matrix-root manifest hash was identical at `747e4c35b13f68714548b8af196ced026e555f8c33491dff5d942b5baab88389`.
- **Why:** The final matrix is evaluation evidence, not an opportunity for adaptive tuning. Its literal anti-result and missing receipts must remain immutable. A narrowly corrected auditor is still required to distinguish those genuine failures from its own schema and reconstruction defects; that improves evidence accuracy without changing or repeating the experiment.
- **Consequences:** Stage 08 remains `FAILED_MECHANISM`, not `PASS`. The first audit remains immutable. Only assurance code and its tests may change before a second audit of the existing matrix. The result is synthetic only and does not measure ARC3 RHAE, consume the public holdout, advance the official target, or establish `GameState.WIN`. Campaign 29 remains gated on the corrected audit plus remaining release, integrity, CI, and documentation work.
- **Corrected audit resolution:** Assurance-only commit `d3d8eddb85549550ddc6629de4aa32e202ef897a` added the two declared counters and preserved valid per-level reconstruction when a sequence truthfully attests aggregate incompleteness. Eleven focused tests, Ruff, format, strict MyPy, compilation, and independent review passed. A first relocated-source retry is preserved at `C:\a\arc3-b003-stage08-v02-final-83df552-01-audit-v2-d3d8edd` with four additional absolute-path binding failures; its receipt file SHA-256 is `7b4c5d27405fe42b88c28580e69d940a9c196de321565db88fe5c22d43a98519`. The final source-projected audit at `C:\a\arc3-b003-stage08-v02-final-83df552-01-audit-v3-d3d8edd-source-projected` independently verified the recorded `0ad2011` matrix-source commit/tree after proving the clean `d3d8edd` head differed only in the three ledger files, the auditor, and its test. It returned `FAIL` with exactly 534 genuine findings: 462 per-level count mismatches and 72 aggregate incomplete-link attestations. Every other audit check passed, the matrix root was unchanged, audit-receipt file SHA-256 is `518cc61bdf981a2c6537bda321c27d46d66deb95974f0bedc922db040e0a0c89`, report file SHA-256 is `86f1a33c0b6ca8cbdb122619d6a9d5554d430651c3c6160865a2d5889994d046`, and sealed payload SHA-256 is `fda2cc01f12746abbb98b44d8d3513161366e0b8b3c4be8876d65eccb564dde3`.
- **Reopening condition:** A final-package or official-environment gate exposes a concrete source defect that prevents safe execution. The sealed final matrix and first audit remain immutable even if a later assurance tool is repaired.

## D-003-0028 - Reopen only the synthetic-lab anchor boundary exposed by exact-freeze packaging

- **Date:** 2026-08-26
- **Stage:** 10
- **Decision:** Preserve the exact `83df552` release-verifier `FAILED_INFRASTRUCTURE` and standalone guard `FAILED_BOUNDARY` receipts. Repair only the generic fallback-anchor generator so its six semantic anchors are distinct, and add the exact failing root seed as a solvability regression. Do not change the mechanical learner, official evaluator/runner, target policy, matrix, holdout state, or any official-game trace.
- **Reason:** The integrated verifier reached 84 percent before its fixed 2,400-second host ceiling; every other package check passed. The uninterrupted exact-source recovery then collected 1,022 tests with zero guard attempts and exact source projection, but Hypothesis found root seed `262795363413301024`. Its `toggle-door-key` case aliased key, locked door, and target at one coordinate, making the key unreachable. The evaluator correctly rejected the generated case as unsolvable.
- **Verification:** The exact regression and the full property file pass. Seventeen lab catalog, transition, integration, and property tests pass together. Ruff lint, Ruff format, and strict MyPy pass for the implicated files. This is synthetic generator correctness evidence only.
- **Consequence:** `83df552` remains the immutable source identity for the final one-shot matrix and its receipts, but it is no longer the final package/play candidate. A new candidate must be frozen only after replay and production-drift checks, then package, cold-start, integrity, secret, and CI gates must be repeated. The matrix is not rerun or rewritten.

## D-003-0029 - Preserve the 83df552 production freeze after confirming the repaired lab is outside the payload

- **Date:** 2026-08-26
- **Stage:** 10 and 11
- **Decision:** Supersede only D-003-0028's tentative requirement for a new production/play freeze. Keep `83df5520478cc209c06d9ce4e658c90786914544` as the competition payload and Campaign 29 source. Classify `48bd18d09ec82c06150dbcba7b72370781eb21de` as a post-freeze synthetic-assurance repair and current-head CI source. Preserve both package failures and the repaired regression without rebuilding the one-shot matrix or changing the official-play policy.
- **Evidence:** Independent ZIP inspection found exactly 114 members in `C:\a\o3f83\package-a\arc3-first-party.zip` and zero `src/arc3/lab` members. The repair commit changes only the two synthetic-lab files and Build 003 ledgers relative to its parent; the already-recorded auditor change is also outside the agent route. Agent, mechanical learner, official runner, package configuration, dependency lock, and the 114-member executable payload are unchanged from `83df552`.
- **Why:** A source-closure failure in an excluded synthetic generator must be repaired and covered by CI, but it does not alter the executable competition agent or justify moving an official-play freeze selected before the final matrix. Rebuilding from the assurance head would change outer commit-bound metadata while leaving the executable payload behavior unchanged.
- **Consequence:** Campaign 29 remains bound to exact `83df552`, its canonical replay, action-semantics scan, and matched profile. The final report must distinguish the `83df552` package verifier timeout and genuine lab test failure from the `48bd18d` assurance repair and current-head CI result.

## D-003-0030 - Accept the exact-freeze package sub-gates without erasing verifier failures

- **Date:** 2026-08-26
- **Stage:** 10 and 11
- **Decision:** Record the exact `83df552` deterministic offline package, cold start, package-only integrity, and generated-log secret scan as passing sub-gates. Preserve the integrated release verifier as `FAILED_INFRASTRUCTURE` and the standalone guarded recovery as raw `FAILED_BOUNDARY`, classified `FAILED_MECHANISM` for the genuine synthetic generator defect. Do not describe the integrated verifier or sealed artifact set as a clean pass. Keep Campaign 29 paused until this evidence is committed and pushed and current-head CI passes.
- **Package evidence:** Exact clone `C:\a\s3f83`; output `C:\a\o3f83`; two byte-identical package builds. Candidate SHA-256 `d83efdd5c08b082ec8f1402120e75645b15e960fa954e42eb33fd116ee875ee8`; first-party payload `27bfe4fd15459d20fa1750c4e4bbe69cb1873ded13db26b43030f7866def1e16`; manifest `81a53eb9a26a8d251370b652478493d5fb309610ed5e37f23fad183d7792f94c`; SBOM `45d81bef6a94c987a3e5db078a2cab6f48fbe5aa7762bed84111850507255106`; build receipt `c2cc69ece59767a0999b1b4c9e3bd03103c41341ca08236943ebb6b7bf322423`. Cold start passed in 3.0803145 seconds with 73,826,304 sampled peak RSS and zero network/process attempts. Package-only integrity receipt SHA-256 is `5ff2932b4cd5de6868652ec4b4d87f668207a53f8be4aaa12cb0ba4d513effd2`; it records `package_only_passed=true` and top-level `passed=false` only because public-identifier semantics are `NOT_EVALUATED` by scope. Twenty-two persisted redacted logs yielded zero secret findings or redactions; this does not claim every raw sealed byte was scanned.
- **Preserved failures and scope:** Integrated receipt `C:\a\o3f83\release-verification-receipt.json`, file SHA-256 `35e84860f5c167677cfaa1de45fa903e099fff895a73e0ff81de0e366fd72ddb`, remains `FAILED_INFRASTRUCTURE` after the guarded suite exceeded 2,400 seconds. All 45 declared artifact hashes currently match, but its sealed set remains explicitly incomplete. Standalone recovery `C:\a\o3f83\package-only-test-guard-recovery.json`, file SHA-256 `a2b21eeb5c4a7ac7448ecfd9abdaf77902cf8ceaba12d0dde462c68d23d31a1a`, remains raw `FAILED_BOUNDARY` with one genuine test failure and zero guard attempts. The full static lock-only receipt `C:\a\i3f83.json`, file SHA-256 `6d83a04b1a2e652f60ad8dbb8cf401c6baa24f3bfb12b961a848cd39d5c33ffa`, has top-level `passed=false`: source, policy, archive, and secret sub-checks pass, while supply chain is `NOT_EVALUATED` by explicit scope with 60 nonblocking warnings.
- **Why:** Package fitness and a verifier wrapper's aggregate status are distinct claims. The successful deterministic package and runtime sub-gates are real evidence, while timeout and synthetic-generator failures remain immutable evidence. The excluded repaired lab does not change any of the 114 executable payload members.
- **Consequence:** The exact package/integrity/secret work no longer remains `PENDING_EVIDENCE`; current-head CI and committed documentation are the remaining prelaunch gates. No holdout, credential, terms, upload, submission, paid service, or official action was used.

## D-003-0031 - Preserve Campaign 29 and test the proximity-assigned composite-sink hypothesis

- **Date:** 2026-08-26
- **Stage:** 10
- **Status:** RESOLVED BY OFFICIAL FAILED_MECHANISM; EXACT NON-EDGE INVERSE RECOVERY OBSERVED
- **Source result:** Campaign 29 from frozen production commit `83df5520478cc209c06d9ce4e658c90786914544`.
- **Decision:** Preserve the visible-node result as `FAILED_MECHANISM`, including its one official `GAME_OVER`, legal reset, exact retry, terminal `NOT_FINISHED`, and exact inverse. Reopen production only for a bounded composite-sink discriminator: two exact 21-cell disks sharing one carrier color and carrying two distinct residual colors identify an exact two-color ring containing those residual colors, while unique agreement among Euclidean, Manhattan, and squared-distance rankings supplies only a provisional child-to-sink assignment. The observed carrier examples delimit sink candidates; they do not prove that the dynamic child groups instantiate the carrier-elimination operator. Require two independent examples, a unique metric-stable proximity assignment, exact target-relative layouts, and complete per-action raster certificates. Test the paired placement one action and one official consequence at a time. An arity-two-only `NOT_FINISHED` does not reject paired sufficiency; only the exact paired terminal may falsify this proximity-assigned hypothesis.
- **Alternatives:** Try another arbitrary scalar weighting; co-locate both child mediators at the already-falsified raw parent target; assign children directly to rings by a metric-sensitive nearest-target rule; treat illustrative composite rings as decoration; or use recorded coordinates, colors, level identity, or target identity as production constants.
- **Evidence:** Campaign 29 ended at official raw state `NOT_FINISHED`, `levels_completed=4/6`, after 140 submissions: 139 `ACTION6` and one legal `RESET`. Submission 121 returned `GAME_OVER`; submission 122 reset; submission 131 reached the exact visible-node terminal and remained `NOT_FINISHED`; submissions 132-140 restored the protected origin. The sealed recording SHA-256 is `c9f7422dcb96a24a47ce734c83a32aa35d3d7816ef8b94c7750e81d6814f692e`; failure-receipt SHA-256 is `6e4ec80f9556967ec3d438a48f5cc403491d3b4bec1526326dcd22d668ec9935`; trace-chunk SHA-256 is `52ff2645a99a9202fd9efca3ce74f4192cd99c96d331ec9161939ecd82544b3b`; replay verification passed. The final official frame contains two independent exact carrier examples, `{0,8}+{0,9}->{8,9}` and `{0,11}+{0,14}->{11,14}`. Source-pair aggregate proximity uniquely and metric-stably assigns the arity-two and arity-three child groups to those two payload rings. Recorded-frame search found a complete nine-action forward and exact nine-action inverse with the raw parent target and unrelated rings preserved. These coordinates are replay evidence only.
- **Implementation review:** Independent review found that the first bridge planner repeated 70 of 111 projected-state validations and took 14-17 seconds, and that its language overstated a proximity assignment as an exact carrier law. Per-search immutable-state memoization plus a sound terminal cross-child geometry prefilter reduced the same plan to 41 unique projected parses and about five seconds under the shared 512-combination ceiling. Claims, residuals, and the hypothesis identity now say `proximity-assigned composite-sink`; the exact carrier-mask examples remain observations, not a proven dynamic correspondence.
- **Replay gate:** A read-only worktree replay matched all 140 Campaign 29 submissions and consequences, including the intermediate `GAME_OVER` and legal `RESET`, derived `ACTION6 (13,34)`, cancelled it unsubmitted, and issued no environment action. The generic replay-audit contract now preserves legacy Campaign 28 evidence while separately accounting for Campaign 29's raw `NOT_FINISHED` state, official score-boundary `GAME_OVER`, 139 non-reset actions, and one reset. Its sealed v0.2 audit is `C:\a\arc3-b003-play\campaign29-83df552-reset-aware-audit.json`, file SHA-256 `5dcc6c84dffd18c17f8abb53503c067d335945e98799624655c750bc6f1b789c`, object hash `a1527d537784b77dccc71f1c031dc23fcdd844ff82348690c60781dce11dfb20`. A clean exact-commit replay remains required before launch.
- **Replay gate resolution:** The required clean exact-commit replay passed before Campaign 30 launch from detached commit `6c32d2a65e61a7b1160fd1c09e8f991078fc9477`, tree `724d62e18fd73315702bb8b69424da3ffbb59915`: receipt `C:\a\arc3-b003-play\campaign29-6c32d2a-c9f7422d-canonical-replay-receipt.json`, file SHA-256 `5ced2bff2e10c9638c0deaad69acf26b9334f91e5606ed882d531620996908e2`, payload SHA-256 `039735fc147adf3759f0afebab1ef75daeeb85fcc5630aaeaef58833e1cb23f1`, replay hash `8df4aea46c85722bffc40e23c372af301724f2b5ab650c1ad38206752de80672`, and zero environment actions.
- **Why:** The bridge rule reuses exact visible structure and a prior observation-grounded mediator-to-signature-matched-sink mechanic. It explains both composite examples and both child assignments, while the four failed parent-sufficiency families did not test it. The current certificate incorrectly assumes every hierarchy sink is the unique raw parent target and therefore cannot express the observed standalone composite sinks.
- **Consequences:** Add a generic bridge detector, factored sink certificate, family-scoped failure state, exact forward/inverse planning, and tests. Do not modify the sealed matrix, consume a holdout, inspect the target source semantically, or claim progress before an official level transition or `WIN`. Preserve the homogeneous raw parent ring throughout the bridge route.
- **Campaign 30 official result:** Exact commit `6c32d2a65e61a7b1160fd1c09e8f991078fc9477` reached the paired terminal after nine forward actions. Both child mediators occupied their proximity-assigned composite rings while the raw singleton target and unrelated static examples remained unchanged; the official consequence was `NOT_FINISHED`, `levels_completed=4/6`. Nine inverse actions then restored every gameplay-field cell at `x=1..63`; only nineteen cells in the depleting left-edge band at `x=0`, `y=19..37` differed. Campaign 30 ended `FAILED_MECHANISM` after 158 submissions (157 `ACTION6`, one legal `RESET`), one `GAME_OVER`, zero `WIN`, and final raw state `NOT_FINISHED`. This falsifies paired composite-sink sufficiency, not the observed movement relation.
- **Reopening condition:** Exact projected replay cannot preserve all targets, static pixels, hierarchy identity, active-role transitions, or the full inverse; an official intermediate consequence violates its certificate; or the exact paired terminal returns `NOT_FINISHED` or `GAME_OVER`. Only official `GameState.WIN` resolves target completion.

## D-003-0032 - Test the residual-linked mixed-support conjunction

- **Date:** 2026-08-26
- **Stage:** 10
- **Status:** ADOPTED; OFFICIAL FAILED_MECHANISM AND FULL INVERSE OBSERVED; RECORDING COMPLETE
- **Decision:** After the paired composite-sink family is officially rejected and exactly recovered, admit one narrower mixed-support hypothesis only when the observation contains one unique singleton raw-target color, exactly one witnessed composite residual palette containing that color, and the already-provisional child-to-example assignment is unique under every declared proximity metric. Place the child assigned to the uniquely linked example at the singleton raw target and place every other child at its own nonmatching composite sink. Require a complete projected forward and inverse, exact protected raster and target surfaces, readable endpoint/connector lineage, and one submitted action per official reobservation. Reject the family only when its exact terminal returns `NOT_FINISHED` or `GAME_OVER`; an intermediate mismatch rejects the plan, not the family. Retain terminal rejection across same-level reset and clear it only on real level progress.
- **Alternatives:** Enumerate the swapped sink permutation; probe the unrelated `{9,12}` or `{6,7}` rings; co-locate both mediators without a merged-lineage certificate; repeat a child-only or already-rejected parent family; use a fixed coordinate, palette value, target identity, game ID, or campaign sequence.
- **Evidence:** Campaign 30 rejected the state with both child mediators on the two composite sinks and restored the protected origin. Earlier preserved observations separately rejected arity three at the singleton raw target with arity two at origin and arity two there with arity three at origin; the mixed conjunction has not been tested. Prior level-3 observations associate the arity-two branch with residual palette `{8,9}` and the arity-three branch with `{11,14}`; in the level-4 origin, the raw singleton palette `{14}` is contained in exactly one of those witnessed residual palettes. Thus the observation-derived candidate keeps arity two on the `{8,9}` sink and sends arity three to the singleton `{14}` target. This palette-containment link is an inference and the inherited proximity assignment remains provisional.
- **Resource boundary:** Campaign 30's recovered frame shows a left-edge band that grows with submitted actions, clears on reset, and was full at a prior `GAME_OVER`. Interpreting it as a time/resource counter is inferred, but it makes arbitrary enumeration materially unsafe. The mixed route is bounded to the smallest certified conjunction and must preserve a full inverse.
- **Implementation evidence:** The generic detector requires one unique residual link, a metric-stable child/example assignment, distinct raw-parent and linked-sink centers, exact target surfaces, and a complete forward/inverse. It has no game ID, campaign number, fixed coordinate, fixed palette value, or level-number branch. The frozen Campaign 30 recovery frame produces a nine-action forward and nine-action exact inverse with supports `(13,28)` and `(53,28)`; those coordinates are replay evidence only. Terminal-only rejection, intermediate mismatch and `GAME_OVER` non-rejection, exact terminal `NOT_FINISHED`/`GAME_OVER` rejection, same-level reset retention, level-progress clearing, palette permutation, translated geometry, ambiguity, corrupt raster, and duplicate-bridge-support refusal are covered. The full visual-policy suite passed 113 tests; the stable combined learner/replay/CLI suite passed 154 tests in 287.90 seconds; Ruff, format, strict MyPy, compilation, and diff checks passed.
- **Read-only trace gate:** A new sealed-trace replay path validates the original trace projection, aliases, manifest and tail hashes, compressed-copy containment, all six-event cycles, blobs, frame links, reset lifecycle, submissions, consequences, and regenerated durable mechanics receipts. It operates through an immutable temporary copy and proves the source projection unchanged; it never reconstructs SDK recording rows. Current-policy replay matched all 950 Campaign 30 events, all 158 historic actions, all 158 durable receipts, and 493 frames, then derived `ACTION6 (13,34)` under `affine-residual-linked-hierarchy:22e0f6df83e485277c91b9da`, cancelled it unsubmitted, and restored zero pending learner actions. Recording and run completeness remain false. This direct diagnostic emitted no receipt file; a clean exact-commit CLI receipt remains required before Campaign 31.
- **Clean exact-commit gate resolution:** The CLI replay passed at detached commit `b7d10cd53e7b6138a063d5a2124ab3f0c985e792`, tree `0fa6b0a3b44a7b0b12a1fb68726f86f98d154816`: receipt `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-6c32d2a-campaign30-sealed-trace-replay-b7d10cd.json`, file SHA-256 `a563e7467f93125aa62dc2f1f9b0c679c4fd888fc462d8ec0b9249278d6977af`, payload SHA-256 `885b7c8217cfcbd41180411850b3bdc15cd1a68fbb628c964b1719ee1efad3a5`, replay hash `44131fce61ee820fca2eb4381afa965af29aeae2588e68afd5879a7c95d699f1`, and zero environment actions. It matched all 950 events, 158 submissions, and 158 regenerated mechanics receipts, then staged and cancelled the same candidate. A prior attempt failed closed without a receipt when concurrent ignored bytecode creation changed the bounded repository projection; the stable rerun passed without deleting or editing files. Together with 54 passing exact-clone integrity cases and the zero-finding action-semantics receipt, the mechanism/replay gates were complete before launch. Exact repair commit `0434b3c95b7c1e3bd98488e9277df5c82f8c5850`, tree `fb77bc0c752f9b1932f6aa6c7b46e069f1e216da`, subsequently passed all five required CI workflows and all nine jobs.
- **Campaign 31 official result:** From clean exact commit `0434b3c`, submission 167 reached the certified residual-linked mixed terminal and returned raw `NOT_FINISHED`, `levels_completed=4/6`. The receipt states that the linked child reached the raw parent and every other child its nonmatching composite sink, while the authoritative environment did not complete. Submissions 168-176 completed the full inverse; recording-frame comparison matched every `x=1..63` cell to the pre-hypothesis frame, with only twenty consumed cells in the left-edge band. The run ended `FAILED_MECHANISM` after 176 submissions (175 `ACTION6`, one legal `RESET`), one `GAME_OVER`, zero `WIN`, final raw state `NOT_FINISHED`, and official score boundary `GAME_OVER`. Public verification passed all 293 declared artifacts. The complete 177-row SDK recording aligns one-to-one with the initial observation and every submitted action, consequence state, level count, and session GUID.
- **Independent review:** Four pre-commit P2 findings were repaired: missing game identity in the trace receipt, escaping compressed-copy paths, mixed-family aliasing of an already-falsified bridge support tuple, and a copied-manifest time-of-check/time-of-use gap. The final reviewer reported no remaining actionable finding. The focused replay/CLI suite passed 41 tests after hardening.
- **Consequences:** Production may add a palette-equivariant and translation-equivariant mixed-support detector, planner, family lifecycle, exact inverse, and adversarial tests. No official progress, completion, access change, or generalization claim is earned until an official consequence returns it.
- **Reopening condition:** Projected combined-state safety is ambiguous or incomplete; a returned intermediate violates the exact certificate; the terminal remains `NOT_FINISHED` or reaches `GAME_OVER`; or a level transition/`WIN` establishes a different authoritative consequence.

## D-003-0033 - Add bounded hosted-runner variance margin to the guarded package-only suite

- **Date:** 2026-08-26
- **Stage:** 10 and 11
- **Status:** ADOPTED; EXACT REPAIR AND CURRENT-HEAD CI PASS; PRIOR TIMEOUT PRESERVED
- **Decision:** Increase only the `build001-package-only` guarded test command ceiling from 2,400 to 2,700 seconds. Pin that value in the package-only plan regression. Keep the separately evidenced full-profile 2,400-second ceiling, the 60-minute workflow job ceiling, all 1,056 collected guarded tests, all package-only boundary exclusions, timeout-to-`FAILED_INFRASTRUCTURE` classification, and the expected sealed-private-surface `BLOCKED_EXTERNAL` verdict unchanged.
- **Evidence:** Pull-request run `32953824616` and push run `32953819904` bind the same commit `b7d10cd53e7b6138a063d5a2124ab3f0c985e792`, tree `0fa6b0a3b44a7b0b12a1fb68726f86f98d154816`, plan `sha256:bb7dd5a69bd3d09480b404381aa06ae96e4f1637587982d51a8964edc3be2c48`, test command, 2,400-second ceiling, and all twenty-two package artifact hashes. The PR runner stopped at 95 percent after 2,400.0343565 seconds with `timeout_triggered=true` and `FAILED_INFRASTRUCTURE`; its 1,165-byte stdout is an exact prefix of the passing run. The duplicate push runner completed 1,053 passed and three skipped in 2,382.0408983 verifier seconds, only 17.9591017 seconds below the old ceiling, and returned the expected `BLOCKED_EXTERNAL`. The jobs took about 41 minutes, leaving more than eighteen minutes below the unchanged outer ceiling.
- **Receipts:** Failed PR receipt `C:\a\arc3-ci-b7d10cd-package-win-pr-fail\release-verification-receipt.json`, file SHA-256 `04daa6ab6d9c6537add994312594c9a7708b38d7d0fb77673e613ee64b8bccc8`, receipt hash `5636858b236bca21ce2d09a99324d3e55ac6283e03987f7ea26057478c5aab93`; passing push receipt `C:\a\arc3-ci-b7d10cd-package-win-push-pass\release-verification-receipt.json`, file SHA-256 `a4d290383734a0a306de34f14546fb359b166790a10824c7a074452ca1e7569a`, receipt hash `f25254ecd2ce322a55baef95c260896c529d2a39dcdc5846bce1aaaeee0cd956`.
- **Verification:** In a detached non-OneDrive worktree, all seventeen package-only verifier tests pass in 7.98 seconds; Ruff lint and format, strict MyPy on the verifier, and `git diff --check` pass. The first live-worktree attempt is preserved as `FAILED_INFRASTRUCTURE`: pytest's shared temp root denied enumeration and OneDrive changed candidate bytes during a bounded scan.
- **Consequences:** Exact repair commit `0434b3c95b7c1e3bd98488e9277df5c82f8c5850`, tree `fb77bc0c752f9b1932f6aa6c7b46e069f1e216da`, passed five of five required workflow runs and nine of nine jobs. The Windows package-only pull-request receipt completed the unchanged 1,053-passed/three-skipped guarded scope in 2,505.1840711 verifier seconds and returned the expected private-surface `BLOCKED_EXTERNAL`; the push receipt did the same in 1,740.549541 seconds. Neither timed out. Campaign 31 was therefore launched from the exact repaired head. The earlier `b7d10cd` pull-request timeout remains immutable `FAILED_INFRASTRUCTURE` evidence.
- **Reopening condition:** A current-head package-only runner still reaches 2,700 seconds, the guarded selection or sealed-boundary verdict changes, the 60-minute outer ceiling becomes limiting, or evidence shows a test defect rather than runner variance.

## D-003-0034 - Test the unique external residual-chain family

- **Date:** 2026-08-26
- **Stage:** 10
- **Status:** ADOPTED; IMPLEMENTATION AND EXACT PRELAUNCH GATES PASSED; OFFICIAL CONSEQUENCE PENDING
- **Decision:** After the exact residual-linked mixed-support terminal has officially failed and been fully inverted, admit one further observation-grounded family only when all of the following are unique: the existing metric-stable child-to-bridge assignment; one singleton raw target; exactly two carrier-elimination examples; occurrence of the raw color in exactly one source disk; one carrier-offset-mask counterpart in the other example; and occurrence of that counterpart residual in exactly one additional exact sparse target after every bridge output is excluded. Choose distinct child supports and use the raw target exactly once. Fail closed on any ambiguity. Before official play, require a target-, raster-, connector-, and lineage-safe complete forward and inverse.
- **Family lifecycle:** Prior mixed-family rejection is a prerequisite. Reject the external residual-chain family only when its exact certified terminal returns `NOT_FINISHED` or `GAME_OVER`. An intermediate mismatch rejects the current plan. The retry-capable bridge, residual-linked, and external late hierarchy families retain one same-level retry after a preterminal `GAME_OVER` and legal `RESET`; a repeated preterminal failure rejects the plan. Equal-weight, weighted, and visible-node hierarchy families retain their historical immediate-failure lifecycle. Real level progress clears level-local rejection; official `GameState.WIN` alone completes the target.
- **Evidence:** Campaign 31's returned observation supplies a unique external-chain candidate with support centers `(15,15)` and `(53,28)`. Those coordinates, their palette values, and the observed level are evidence only. No game ID, campaign number, level number, coordinate, color, or target identity became a production decision constant. Commit `83dbe5211451f65e45c8a768b92ae7c5ed305b0a`, tree `71fd0ebdd5def55770e4dbfe3780c7e3241c53fb`, implements the generic detector, a collision-safe nine-action forward and exact inverse, ambiguity rejection, terminal-only family rejection, and bounded retry lifecycle. Five focused compatibility/lifecycle cases, all 120 visual-policy tests, and 54 exact detached integrity, secret, policy, action-semantics, and production-reachability cases passed. Ruff lint/format and strict MyPy passed. Manifest-aware integrity passed; package-only integrity returned `package_only_passed=true` with its intentionally out-of-scope public-identifier top level false; action semantics passed with zero findings.
- **Compatibility gates:** The first sealed replay at `79c9f28` failed closed at Campaign 31 submission 121 and emitted no receipt because retry authority retroactively included an older equal-weight hierarchy family. The only receipt mismatch was the preserved top-level residual, but post-`RESET` state also would have diverged. Commit `0d0cd86` narrowed retry authority to the external family and passed the sealed replay, but its later full-policy gate returned 119 passed and one failed because the established bridge-family retry contract had been removed. Commit `83dbe52` admits retry only for the bridge, residual-linked, and external late families while preserving immediate failure for the historical equal-weight family; all 120 policy tests then passed. One initial `83dbe52` replay invocation was rejected before execution because a campaign-audit-only argument was incompatible with sealed-trace mode; it emitted no output or receipt and changed nothing. The corrected exact detached replay matched all 1,058 events, 176 historic actions, and 176 durable mechanics receipts, staged `affine-external-residual-linked-hierarchy:95c73601f21a60f8029fa0fd`, cancelled it unsubmitted with zero pending actions, preserved final recorded `NOT_FINISHED` at 4/6, and issued no environment action. Receipt `C:\a\arc3-b003-c32-gate-output-83dbe52\campaign31-sealed-trace-replay-83dbe52.json` has file SHA-256 `aababa5c9b81d22e685f313e1c298522dad7792249d826afa5816437e9cd10db`, payload SHA-256 `76febb32d9dd3f8dc16964d3f31ef1abcbad5cc5310fd5f7de1fcae247426093`, and replay hash `67664265b17bd5155f6a846db9c3da460731e0f1277caa0f6cbb2f9829413a7c`.
- **Alternatives rejected:** Exhaustive interaction enumeration; repeating either child-only, parent-composition, bridge, or mixed-support terminal; choosing an extra sparse target by coordinate, color literal, game identity, or campaign order; accepting multiple counterpart masks or residual targets; submitting a route without a certified inverse.
- **Consequences:** Commit `83dbe52` is the frozen Campaign 32 implementation head; later evidence/documentation changes do not alter its runtime bytes. Five of five exact-head workflows and nine of nine jobs passed. B-003-0029 remains open only for authorized official discrimination. Preserve Campaign 31 as `FAILED_MECHANISM`; the passing replay and CI are prelaunch evidence only and establish no level transition, completion, or `WIN`.
- **Reopening condition:** Any uniqueness premise fails; the proposed supports collide; a projected action changes protected target, raster, connector, or lineage evidence; no complete inverse exists; the resource/reset lifecycle cannot be bounded generically; or an official consequence falsifies the family.

## D-003-0035 - Preserve Campaign 32 as failed-mechanism evidence and reopen only the implicated hierarchy scope

- **Date:** 2026-08-26
- **Stage:** 10
- **Status:** ADOPTED; CAMPAIGN 32 SEALED AS FAILED_MECHANISM; TARGET REMAINS OPEN
- **Decision:** Preserve Campaign 32 without replaying, rewriting, or promoting it. The exact external residual-chain support configuration is rejected as sufficient because its certified terminal returned official `NOT_FINISHED` at 4/6 after one legal preterminal retry. Retain the verified coordinate-displacement mechanic, cross-level transfer through level 4, target/support parser, protected-raster certificates, complete inverse, and recording/trace infrastructure. Reopen only the smallest hierarchy-composition scope implicated by the returned residual. Repair the stale preterminal-retry latch before any further live play, but do not present that bookkeeping repair as progress.
- **Official evidence:** Frozen commit `83dbe5211451f65e45c8a768b92ae7c5ed305b0a`, tree `71fd0ebdd5def55770e4dbfe3780c7e3241c53fb`, produced 201 submissions: 199 `ACTION6` and two legal `RESET`s. Submission 182 returned preterminal `GAME_OVER`; submission 183 legally reset; submission 192 reached the exact external terminal and returned `NOT_FINISHED`, `levels_completed=4/6`; submissions 193-201 completed the certified inverse. The final raw state remained `NOT_FINISHED`, the score boundary remained `GAME_OVER`, and zero `WIN` occurred. Completion receipt `sha256:b87c019ab67e887bda6103d511578885baa368e8a9a5813c1318258db8402d2f` truthfully records `completion_observed=false` and `receipt_complete=false`.
- **Evidence integrity:** The sealed trace contains 1,208 events and 201 replay-linked mechanics receipts. Trace chunk SHA-256 is `4d9b3ce76cfa096185c7a9801c60223e88b4ba44cd3f5b879dcc0bf8a8d65a2a`; tail event hash is `65a541dc22ee75c5a9d0111c3680624b6af1d809ecff31fc890b7cc77ee4214e`. The complete 202-row official recording SHA-256 is `8a9b3bea1620ef4eb9807dfdaba3e7f24ce05d92bf52c3dcefdb35e09c4139d2`. Public verification passed all 327 declared artifacts, and independent hashing found zero missing or mismatched artifacts. The logical run receipt hash is `7f36c601284723715e8db17f5c1543da85c767744949c390896c1a9f363928db`; its physical JSON SHA-256 is `3c5f15f68c8b9d7e08422f8ed640d706f0514611eff07a6f67931fa2069e9d35`.
- **Exposure and authority boundary:** The hash-linked exposure ledger now contains 61 development events, zero holdout events, tail `sha256:b8052f2f277203808353b48575364fa83374244e8582bc3d30dc11f0ca51adb5`, and file SHA-256 `02656761e355a7aceec9514a9848c5ae0ffe43eedd7f79db2e0e18c4996a83d9`. No terms, credentials, sealed holdout, submission, money, merge, release, or external message was used.
- **Lifecycle residue:** The final restored policy snapshot reports `hierarchy_preterminal_retry_count=1` even though the external relation is rejected and no plan remains active. This does not change the official terminal evidence. The next implementation must clear the reserved signature on exact terminal `NOT_FINISHED` rejection and exact recovery, with regression coverage and sealed replay compatibility.
- **Consequences:** B-003-0029 is resolved only for external residual-chain sufficiency. Build 003 remains active because official `GameState.WIN` has not been observed. A next discriminator must be unique, observation-derived, palette/translation/identity agnostic, bounded, reversible, and replay-certified before another authorized development run. Synthetic, offline, replay, confidence, and exact-restoration evidence remain non-completion evidence.
- **Reopening condition:** Only an integrity discrepancy in the sealed Campaign 32 artifacts may reopen this evidence classification. A later mechanism may reuse surviving mechanics, but it may not relabel the rejected external terminal as progress or erase either `GAME_OVER` consequence.

## D-003-0036 - Preserve Campaigns 33 and 34 and test the unique carrier-source occlusion relation

- **Status:** ADOPTED; IMPLEMENTATION REOPENED; OFFICIAL PLAY PAUSED
- **Stage:** 10 and target-game continuation
- **Date:** 2026-08-26
- **Source identity:** Campaign 33 commit `7df40464152cdba927d2156007747379e104f788`; Campaign 34 commit `1315a603f2cfd50eaa0a32c7eb4c2bac4276b111`, tree `bde1d396f9f94b2c4fdd8832127d92a8f857b9f6`.
- **Decision:** Preserve Campaign 33 and Campaign 34 as immutable `FAILED_MECHANISM` official evidence. Reopen only the hierarchy residual to test one further observation-derived family: after every tested child, centroid, sparse-sink, and recombined-sink family has been rejected or operationally exhausted, identify the singleton raw target color in exactly one carrier-sharing source disk and the exactly one source disk in the other witnessed bridge example with the same exact carrier-offset mask. Assign the raw-linked child to the raw source center and the other child to that exact-mask counterpart center. Treat those two complete 21-cell source disks as narrowly assigned, reversible occlusion surfaces; every other observed cell, target, endpoint lineage, and action boundary remains protected. Do not launch another official campaign unless the complete forward route, every transient projected state, the exact inverse, sealed-trace replay, clean-source gates, and CI all pass.
- **Alternatives:** Stop merely because the sparse-sink tuple family was exhausted; choose arbitrary occupied objects; normalize away carrier-mask orientation; reuse a rejected sink; enumerate coordinates; inspect target-game source; or submit an uncertified direct action.
- **Evidence:** Campaign 33 ended raw `NOT_FINISHED`, score-boundary `GAME_OVER`, at `4/6` after 219 submissions and zero `WIN`. Campaign 34 ended raw `NOT_FINISHED`, score-boundary `GAME_OVER`, at `4/6` after 237 submissions and zero `WIN`; its complete external-own terminal returned `NOT_FINISHED` and its inverse restored the protected origin. The recovered Campaign 34 frame contains exactly two carrier-elimination examples. The unique raw-color source and exact carrier-offset counterpart are selected structurally, not by stored palette or coordinate constants. A read-only projected-raster check found 124 active-child and 128 sibling layouts after removing only the two assigned 21-cell occlusion surfaces from static occupancy; sampled paired finals retained two readable hierarchy mediators and the raw target. This is implementation-entry evidence only, not a certified route and not completion.
- **Why:** The failed sparse-target hypotheses falsified destinations, not the already witnessed source identity chain. Exact carrier-offset equality is a narrower discriminator than palette overlap, and the source surfaces can be evaluated as reversible renderer occlusions without claiming that an offline projection is an official result.
- **Consequences:** Prior failures, resets, exposure rows, and artifact hashes remain unchanged. Holdout consumption remains `0/1`. `GameState.WIN` remains unobserved, `OFFICIAL_ARC3_RHAE = NOT_MEASURED`, and `NO_ARC3_GENERALIZATION_CLAIM`. Campaign 35 is not authorized by this ledger entry alone; it requires a frozen clean implementation and all stated launch receipts.
- **Reopening condition:** The relation is ambiguous under exact source masks; any transient loses parser identity, changes an unassigned static cell, lacks an exact inverse, or fails replay/CI; the official terminal returns `NOT_FINISHED` or `GAME_OVER`; or a new official observation uniquely implicates a narrower residual.

## D-003-0037 - Add bounded outer CI headroom without reducing verification

- **Date:** 2026-08-26
- **Stage:** 10 and 11
- **Status:** ADOPTED; EXACT-HEAD VERIFICATION PASSED; PRIOR TIMEOUTS PRESERVED
- **Decision:** Increase only the `ARC3 CI` quality-job outer ceiling from 60 to 75 minutes. This supersedes only the outer-60-minute clause preserved in D-003-0033 and B-003-0028; their package-only command ceiling, scope, receipts, and failure evidence remain unchanged. Preserve the complete Linux/Windows matrix, locked environment, Ruff lint and format gates, strict MyPy, the full coverage command, runtime doctor, coverage thresholds, and failure classification unchanged. Do not remove, skip, shard, stub, or narrow any test to obtain a green result.
- **Evidence:** At exact commit `5fccb5751fdbbd30e42e473fb3a33a7e699c0931`, tree `1c604b40302635b818c8b451654f9d66568befee`, pull-request ARC3 CI run `33015837950` passed on both Linux and Windows. Duplicate push run `33015834403` passed Linux but its Windows coverage step was cancelled at the configured 60-minute job ceiling; an unchanged rerun again passed Linux and again cancelled Windows at that ceiling. Every setup, dependency, lint, format, and strict-type step passed in both cancelled Windows attempts, and no test failure was reported. The successful exact-head Windows job completed 1,813 passed and 20 skipped in 3,493.53 seconds; the prior `1315a603` baseline completed 1,809 passed and 20 skipped in 3,453.11 seconds. The 40.42-second delta is only 1.17 percent, while the successful job itself finished within seconds of the outer cap.
- **Why:** Repeating an unchanged near-boundary run would manufacture no new mechanism evidence. The outer workflow ceiling, not an inner test timeout or failed assertion, is the demonstrated limiting surface. A 15-minute margin is bounded, preserves the entire verification scope, and remains below the project's longer package/evaluation budgets.
- **Consequences:** The two exact `5fccb575` cancellations remain immutable `FAILED_INFRASTRUCTURE` evidence. Campaign 35 remains unlaunched until a new exact commit containing only this bounded ceiling repair plus its ledger provenance passes every required workflow/job and its unchanged production bytes remain bound by clean-clone replay and integrity receipts.
- **Resolution evidence:** Exact repair commit `d59f605f64feda19bdf89e2035dfcb9b32a6da0f`, tree `81263eb69c845ed84a9c0a60630b876d993aea53`, passed five of five required workflows and nine of nine jobs: push ARC3 CI `33024166133`, pull-request ARC3 CI `33024168803`, push package-only `33024166120`, pull-request package-only `33024168829`, and pull-request Build 002 package/cold start `33024168838`. The push Windows coverage job ran 62m40s and passed beyond the old ceiling; no command, test scope, platform, coverage threshold, or production byte was reduced.
- **Reopening condition:** Any job reaches 75 minutes; the test count, command, coverage threshold, or platform matrix changes; a test or runtime-doctor failure appears; or the repaired exact head does not pass every required workflow and job.

## D-003-0038 - Preserve Campaign 35 and learn only the observed residual-foreground source layer

- **Date:** 2026-08-26
- **Stage:** 10 and target-game continuation
- **Status:** ADOPTED; CAMPAIGN 35 FAILED_MECHANISM PRESERVED; NARROW IMPLEMENTATION REOPENED
- **Source identity:** Campaign 35 frozen commit `d59f605f64feda19bdf89e2035dfcb9b32a6da0f`, tree `81263eb69c845ed84a9c0a60630b876d993aea53`.
- **Decision:** Preserve Campaign 35 as immutable `FAILED_MECHANISM` evidence and reopen only its first nonmatching carrier-source consequence. The full-mediator overlay model predicted that the assigned 21-cell source would be completely covered. The official raster instead retained exactly the source's complementary non-carrier residual layer above the mediator and occluded its carrier-colored layer. Revise the projection and action-boundary certificate to retain the unique relation-derived residual offsets as foreground whenever the assigned child mediator is centered on its own source. Do not add a game ID, campaign number, level number, coordinate, palette value, or target identity constant. Do not reopen any rejected sink family or infer that the unobserved carrier-source terminal succeeds.
- **Official evidence:** Campaign 35 submitted 240 official actions: 238 `ACTION6` and two legal `RESET`s. It observed two earlier `GAME_OVER` consequences, raw final `NOT_FINISHED`, score-boundary `GAME_OVER`, `levels_completed=4/6`, and zero `WIN`. The carrier-source family itself began only at submissions 238-240 with three setup actions. All three returned `NOT_FINISHED`; the third lost parser lineage before the remaining six forward actions or any inverse action was submitted. Its 241-row recording and 1,442-event trace verify completely. Run receipt hash is `sha256:a5af82bd9718c94f446cf9d49ca7bbbec31c21ada287cfa5422b4ad1c1d5721b`; recording SHA-256 is `197846fc40e26bc863786bcdb86aa30169565bfb84026ab35bbdeeb14b1295a5`; trace tail is `sha256:a42bcc1750bc69e0ec5a44fe7c8e085c970afe1860e2ed74d29a55131c3b2c81`.
- **Residual diagnosis:** Against the exact pre-action public observation, the first two setup consequences matched the protected projected raster. The third differed at exactly ten non-HUD cells, all the observed non-carrier residual layer of the assigned source; three additional differences were the independently changing `x=0` resource band excluded from the protected hash. The official parser therefore exposed four endpoints and a 23-cell merged object where the full-overlay model expected five endpoints and two ordinary 21-cell mediators. This is a localized z-order residual, not evidence that the unsubmitted terminal would win or fail.
- **Safety boundary:** A revised candidate must predict every residual-foreground transient from the source's unique palette complement to the relation's carrier color, retain logical endpoint lineage while a source masks ordinary parser identity, preserve all unassigned source, target, static, and resource-independent cells, and prove the entire forward and exact inverse before launch. It must reproduce the observed Campaign 35 consequence from an immutable recording, replay all 240 historic public-development actions without issuing an environment action, and pass clean-source integrity, source-quality, and exact-head CI gates.
- **Implementation evidence:** The exact Campaign 35 row-240 fixture now matches every gameplay cell at `x=1..63`; its uncompressed 64x64 bytes have SHA-256 `10502941cabb15b6379a2995c39f0e1b67b5225349f91bfa007d72866cd78688`. The generic mask is the unique source palette complement to the relation carrier color. Applying the same source-relative renderer order at the unobserved counterpart is explicitly a bounded transfer candidate, not established behavior. Only three of eighteen route boundaries need planner-derived masked lineage: forward A4 with zero visible active endpoints, inverse R6 with one, and inverse R7 with zero. All other actions retain the ordinary parser-based runtime precondition. Each exceptional action still requires exact predecessor and expected protected hashes, parser counts, target identity, family prefix, active-count direction, and purpose-specific click safety. The next model-derived A4 predecessor hash is `sha256:f6c31353caf376e90064ef8a7d229e7a12f7ccc79ff572859a230ebdf61fbd87`; its projected consequence hash is `sha256:fb3f98761464e7e151066ae4bd78be6a8cf5fc304319725f17269451cb7a0d06`, with five endpoints and one mediator.
- **Repair iterations:** The first local patch incorrectly retained the carrier-color offsets and failed the exact official-frame regression. A subsequent all-action masked-lineage bypass was rejected during read-only review as broader than the implicated residual. The first three-transition predicate then failed closed on the legitimate inverse role swap with one visible active endpoint. The final predicate records the exact required visible-active count per exceptional boundary and keeps the other fifteen generic checks. None of these local iterations issued an environment action or changed immutable Campaign 35 evidence.
- **Local verification:** The exact observed-frame case passed in 55.04 seconds. Five focused carrier-source projection, selection/recovery, palette/translation, cross-source/unsafe-inverse, and `GAME_OVER`/reset lifecycle cases passed in 82.99 seconds. All 134 visual-policy tests passed in 454.94 seconds. Ruff lint and format, strict MyPy, JSON parsing, and `git diff --check` passed. An independent read-only audit found no remaining material safety issue. These are local candidate gates only; clean exact-commit trace replay, integrity, CI, and official consequence remain pending.
- **Consequences:** Holdout consumption remains `0/1`; `GameState.WIN` remains unobserved; `OFFICIAL_ARC3_RHAE = NOT_MEASURED`; `NO_ARC3_GENERALIZATION_CLAIM`. Campaign 36 is not authorized by this decision alone. Only the smallest residual-foreground continuation may be prepared, and official play remains gated on frozen receipts.
- **Reopening condition:** The residual foreground is not uniquely derivable from the assigned source, any predicted transient changes an unassigned cell or loses logical action lineage, the exact inverse is unavailable, frozen public-development trace replay or CI fails, or an official consequence returns a narrower residual, level transition, `GAME_OVER`, or `WIN`.

## D-003-0039 - Separate exact masked lineage from readable observed-effect promotion

- **Date:** 2026-08-26
- **Stage:** 10 and target-game continuation
- **Status:** ADOPTED; WORKTREE STRICT REPLAY PASS; EXACT-COMMIT GATES PENDING
- **Decision:** Keep the Campaign 35 residual-foreground raster certificate as exact plan-lineage evidence, but do not promote it into a generic observed coordinate-transform fact when the carrier-source hierarchy remains structurally unreadable to the ordinary parser. In that narrow state, preserve the unreadable hierarchy residual and prior mechanics receipt while keeping the exact plan live. Leave terminal family disposition, recovery, reset, action selection, strict receipt equality, and every non-carrier-source hierarchy unchanged. Do not add a cross-version receipt exception or rewrite the historical receipt.
- **Failed gate preserved:** The first clean exact-`09d740f` replay matched all 240 historical actions and receipts 1-239, then failed closed at receipt 240 and emitted no replay receipt. The mismatch contained 23 derived interpretation leaf/list differences: generic coordinate-transform observation and explanation were added, one passive-support receipt appeared, and the old unreadable residual was cleared. Action, receipt and prediction IDs, before/after frame hashes and states, level counters, changed-cell count, source references, and all other fields were equal. Using the repository's newline-terminated canonical artifact bytes, the historical receipt hash is `sha256:11e9ecccdf3399d0808ea53032d2d111194b477350cbff685e6083c1cf9604b2`; the over-promoted regenerated hash is `sha256:7d2e379c7b369652ae394f28e8e97660a9f4e55f385f01bf40981a566ac6943f`. No environment action occurred and the immutable trace remained unchanged.
- **Why:** Exact hidden-raster agreement can justify continuing a pre-certified reversible plan without establishing that parser-masked objects were independently observed as a generic transform. Conflating those boundaries would turn model-supported lineage into stronger observation evidence. Keeping them separate preserves the BLA residual and CLEF readability boundary while retaining the smallest action-serving repair.
- **Repair failure preserved:** The first separation patch also narrowed the terminal family-disposition predicate. The focused route test passed the exact-frame case but failed its terminal rejection assertion. That overreach was removed; exact terminal `NOT_FINISHED`/`GAME_OVER` disposition continues to use the protected raster certificate, while only observed-effect interpretation uses the narrower readable boundary.
- **Local verification:** The corrected focused selection/recovery test passed in 70.21 seconds. The full visual-policy suite passed 134/134 tests in 388.93 seconds after the repair. Ruff lint and format and strict MyPy passed. A complete worktree replay then matched all 1,442 trace events, all 240 submissions, and all 240 durable mechanics receipts. It staged the exact unsubmitted next action `ACTION6 (52,55)` under plan `affine-carrier-source-occlusion-hierarchy:c741a4d9d27a238cd433900c`, with five plan actions remaining, and cancelled it with zero pending learner action. Candidate snapshot hash is `sha256:26507ea8544599b82f51b92b5e1d3ce1c8422142d5ab6e5c024808a5c3555e1d`. This is read-only worktree evidence, not an official consequence or the required clean exact-commit receipt.
- **Consequences:** Campaign 36 remains gated on a frozen commit/tree, clean exact-checkout replay and integrity receipts, exact-head CI, and then an authoritative official consequence. Holdout consumption remains `0/1`; completion remains false; `OFFICIAL_ARC3_RHAE = NOT_MEASURED`; zero `WIN` was observed.
- **Reopening condition:** Any historical receipt still differs; the next unsubmitted action or cancellation changes; the masked raster is not exact; the plan latches lineage loss; terminal family disposition changes; exact-head verification fails; or the official environment returns a narrower residual, progress, `GAME_OVER`, or `WIN`.

## D-003-0040 - Add a second bounded variance margin to the guarded package suite

- **Date:** 2026-08-26
- **Stage:** 10 and target-game continuation
- **Status:** ADOPTED; EXACT-COMMIT GATES PASSED; CAMPAIGN 36 EXECUTED; PRIOR TIMEOUT PRESERVED
- **Decision:** Increase only the `build001-package-only` guarded test-command ceiling from 2,700 to 3,000 seconds and pin that value in the existing package-plan regression. Preserve the separately scoped full-profile 2,400-second ceiling, the package workflow's 60-minute outer job ceiling, all 1,077 collected guarded tests, every boundary exclusion and process/path guard, timeout-to-`FAILED_INFRASTRUCTURE` classification, and the expected private-only `BLOCKED_EXTERNAL` terminal. Do not rerun the failed workflow as if a green duplicate erased the demonstrated ceiling failure.
- **Failure evidence:** Exact commit `d406e1b632ae781cf97ca720397a61de9637c4c4`, tree `a739d5bf373166b732a423c7df44f28b932d3d0e`, pull-request package workflow `33035269356`, Windows job `98396513754`, and plan `sha256:2f354c68171c15d28e29617a22c05dedf5e4f4154d12ca525d7094c49791f4ad` reached 2,700.0342362 seconds. The receipt returned `FAILED_INFRASTRUCTURE`, `timeout_triggered=true`, successful process-tree cleanup, and no remaining child process. All 1,077 outcome glyphs had been emitted as 1,074 passes and three skips, but final reporting and the guard receipt were absent, so the suite is not relabeled complete. Failure receipt file SHA-256 is `5b66b14048a48502e31cfb75a5eff624759846e6e5ea81b4e54698aa09c2a0ff`; embedded receipt SHA-256 is `sha256:5b5e553e0fb6f3b1a967955801b10ec9d9535ba34db6c022bdb2229f59b50eda`.
- **Paired evidence:** The simultaneous push Windows job `98396509998` at the same commit, tree, plan, command, selection, and ceiling passed the guarded suite in 1,676.0844144 verifier seconds, emitted the complete zero-attempt guard receipt, and returned only the expected private-surface `BLOCKED_EXTERNAL`. Its receipt file SHA-256 is `35dfb3ddc479eeb3de08c44f38d6fbc5ee45480994070365638e5925416f3fd0`; embedded receipt SHA-256 is `sha256:d6d92db310a0b5316eac1f20256790cfbb068f2b551562c02a4d0f4577b65f08`. The failed 1,203-byte stdout is an exact byte prefix of the passing 244,837-byte stdout, and all source, index, plan, package, integrity, and substantive artifact identities agree. This supports hosted-runner variance rather than a test assertion or source difference.
- **Why:** D-003-0033 explicitly reopens if a current-head runner reaches 2,700 seconds. A further 300-second command-only margin addresses the observed boundary while leaving roughly ten minutes for setup, postchecks, artifact upload, and cleanup under the unchanged 60-minute outer ceiling. No test is removed, skipped, sharded, stubbed, or reclassified.
- **Local verification:** All 17 package-only verifier-plan tests passed in 7.10 seconds. Ruff lint and format checks passed for the verifier and its focused regression; strict MyPy passed for the verifier; the run-state JSON parsed; and `git diff --check` passed. These are source-level checks only, not the required clean exact-commit or hosted-runner evidence.
- **Consequences:** The failed PR artifact remains immutable `FAILED_INFRASTRUCTURE`; the passing duplicate does not erase it. Campaign 36 remains blocked until a new exact commit passes local verifier-plan tests, clean-source replay/integrity, and all five hosted workflows and nine jobs. Holdout consumption remains `0/1`; no environment action was issued by this repair.
- **Resolution evidence:** Commit `657f0406542d289b73540d4bb66c8190d2c4869e`, tree `a2c644da9e35f1283cc0e26cde64b03ce1acc6c0`, passed the 17 focused plan tests, a clean exact-checkout 54-case integrity/reachability gate, manifest-aware and package-only integrity, action semantics, and strict Campaign 35 replay of all 1,442 events, 240 actions, and 240 mechanics receipts. Five of five hosted workflows and nine of nine jobs passed. Both exact-head Windows package jobs completed under the new 3,000-second command ceiling; the PR job required 47m06s and therefore crossed the old 2,700-second boundary without reducing verification. Campaign 36 then launched only from the detached clean exact source.
- **Reopening condition:** Any runner reaches 3,000 seconds; the 60-minute outer package job ceiling becomes limiting; the selection, guard receipt, expected `BLOCKED_EXTERNAL` boundary, or package identities change; or a real assertion/source failure appears.

## D-003-0041 - Preserve Campaign 36 and learn only the observed carried-source translation

- **Date:** 2026-08-27
- **Stage:** 10 and target-game continuation
- **Status:** ADOPTED; CAMPAIGN 36 FAILED_MECHANISM PRESERVED; NARROW IMPLEMENTATION REOPENED
- **Source identity:** Campaign 36 frozen commit `657f0406542d289b73540d4bb66c8190d2c4869e`, tree `a2c644da9e35f1283cc0e26cde64b03ce1acc6c0`.
- **Decision:** Preserve Campaign 36 unchanged and reopen only its first nonmatching carrier-source recovery consequence. The endpoint inverse itself was exact, but the assigned two-color filled source translated with the recomputed child-mediator center instead of remaining fixed. Admit only a relation-specific carried-source certificate that proves the unique assigned source footprint moved one-to-one by the mediator displacement, its old footprint cleared, the selected endpoint and active role matched, and every unrelated source, target, static, and gameplay cell remained unchanged. Rebase only the implicated remaining recovery lineage from that returned observation. Do not weaken generic hierarchy raster checks, promote a parser-masked carry into a generic observed coordinate-transform fact, or pre-assume that every source is carried.
- **Official evidence:** Root `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-657f040-campaign36` is verified `FAILED_MECHANISM`. It contains 254 official submissions: 251 `ACTION6` and three legal `RESET`s; three `GAME_OVER` returns; raw final `NOT_FINISHED`; score boundary `GAME_OVER`; `levels_completed=4/6`; and zero `WIN`. The 255-row recording SHA-256 is `4973df6030a8989f0f5795639285553d1b43df7db3ed6a00620324a2ab738da0`; the 1,526-event trace chunk SHA-256 is `733c386acd9e3096f0c07ea93e9a0234ffda589db2df190b585890986220c32a`; trace tail is `sha256:a5a551e557d25b26387f97966bb55de5cb8218d094c394da6aede0512aabbbf6`; run receipt hash is `sha256:6b6f89bff82825e8e8e1e774e2b938e0913cfcb73757612de01c34980f782273`; completion receipt hash is `sha256:d95f042b0ba62fee114411b86f78adb2eae61611765cca09fd4780a89fdb55c6`.
- **Residual diagnosis:** After the complete nine-action carrier-source forward route officially returned `NOT_FINISHED`, first recovery `ACTION6 (34,55)` restored the selected endpoint from `(14,53)` exactly. The expected static-source inverse had protected hash `sha256:6ea58f0176511e38d4390586e5fcd701cb237bd78bf9a1bc9003a0df5c090a9e`; the official return had protected hash `sha256:bd9c1cd80f7af28303970b32bfb6e4e58a0d2e675ed4494115a4c984a9d67b2f`. The unique color-14 residual footprint moved byte-exact by `(+6,0)` into the recomputed mediator footprint while its old cells cleared, leaving five readable endpoints and zero ordinary mediators. This falsifies static source restoration, not endpoint transport or all hierarchy lineage.
- **Safety boundary:** A revised candidate must reproduce the immutable Campaign 36 consequence exactly, fail closed on any recolor, dropped or added source cell, wrong displacement or destination, uncleared old footprint, unrelated cell mutation, other-source/target change, or ambiguous match, and leave every non-carrier recovery unchanged. It must replay all 254 historic submissions and mechanics receipts without an environment action, select and cancel one next evidence-linked action, pass clean exact-source integrity and CI, and only then resume authorized development play.
- **Consequences:** Holdout consumption remains `0/1`; completion remains false; `OFFICIAL_ARC3_RHAE = NOT_MEASURED`; `NO_ARC3_GENERALIZATION_CLAIM`; no target-game source was semantically inspected. A carried-source certificate is mechanism evidence only. Only an official `GameState.WIN` completes the target.
- **Reopening condition:** The exact translation is non-unique, any unrelated protected cell changes, remaining recovery cannot be safely rebased, replay or CI fails, or the next official consequence reports progress, `GAME_OVER`, a narrower residual, or `WIN`.

## D-003-0042 - Deliver only the exact carried source to its uniquely linked raw target

- **Date:** 2026-08-27
- **Stage:** 10 and target-game continuation
- **Status:** ADOPTED; EXACT B307B6B REPLAY AND INTEGRITY PASSED; HOSTED CI PENDING; OFFICIAL PLAY PAUSED
- **Decision:** When and only when an exact Campaign 36-compatible recovery consequence certifies one carried source, permit a target-delivery continuation if exactly one relation support has a non-carrier source palette equal to the palette of exactly one monochrome raw target, and the existing child-isolation planner independently supplies a complete target-centered layout for that same child. Rebase role exchanges through current endpoint identities and replace the stale inverse queue atomically with that finite route. Do not add game, campaign, level, coordinate, or palette decision constants; the frozen coordinates below are test evidence only.
- **Observation chain:** The implicated assigned source is the only carrier-source support whose non-carrier residual palette equals the unique raw target palette. Campaign 25 already observed the same arity-three child layout ending at that target and returned `NOT_FINISHED` without cargo. Campaign 36 newly established that the source follows its mediator. The resulting model-derived route is `ACTION6 (53,36)`, `(23,58)`, `(46,24)`, `(23,48)`, and `(60,24)`; its first move is the smallest nonhorizontal carry discriminator. This chain makes carried cargo the narrow new variable and does not establish that delivery succeeds.
- **Safety boundary:** Every queued step requires one exact carried-support identity, predecessor and consequence protected-raster hashes, active-role continuity, one consistent delivery plan identity, and preserved sibling dynamics, other source, target ring, and all stationary occupied cells. The terminal geometry is a co-centered 21-cell carried disk inside a disjoint 12-cell hollow target ring; every target-ring cell remains byte-identical. Any recolor, dropped or added carried cell, unrelated obstacle overlap, target mutation, split plan identity, unexpected state flag, ambiguous relation, or raster mismatch fails closed. An official `WIN` is authoritative; terminal `NOT_FINISHED` is bounded failure evidence and terminal `GAME_OVER` enters the existing legal reset/recovery lifecycle.
- **Repair evidence:** The first target-fit predicate wrongly required the carried disk to contain the hollow ring and therefore produced no route; it was replaced by exact co-centered disjointness with target preservation. Independent review then found that future mutable footprints could hide an unrelated static obstacle; stationary occupied cells are now excluded explicitly and a one-cell future-footprint regression fails closed. A smaller compatibility review found two unused state flags and split plan IDs were not rejected; both flags must now be false and all five steps share one `visual-carrier-source-delivery:` identity. These were local preflight repairs and issued no environment action.
- **Local verification:** The final exact Campaign 36 focused case passed in 70.28 seconds with coverage disabled after an earlier coverage-enabled version passed in 357.28 seconds. It verifies stale-queue replacement, five exact consequence hashes, the preserved 12-cell ring, co-centered 21-cell source, future-obstacle rejection, corrupted carried-cell failure, malformed flag/plan rejection, terminal `NOT_FINISHED`, authoritative `WIN`, and `GAME_OVER`/reset behavior. All six carrier-source tests passed in 115.03 seconds; all 135 visual-policy tests passed in 444.91 seconds. Ruff lint and format, strict MyPy, Python compilation, and diff hygiene pass. An independent read-only audit found no remaining P0/P1 issue after the stationary-occupancy repair.
- **Exact-source verification:** Commit `b307b6b534be533eee065733757cd7d790b1d393`, tree `c9c71627b0ff150b75dcf8dccda200b0a7a6b50e`, passed clean detached replay of all 1,526 events, 254 submissions, and 254 mechanics receipts with zero mismatch and zero environment actions. It selected unsubmitted `ACTION6 (53,36)`, left four route actions, and cancelled with zero pending action; receipt file SHA-256 is `cbbf3527be68a8913668194c9da294db2623a09a2c10d3d72fe1cbebd6469de9`, payload SHA-256 `b07e15b6dc8156d95140d9bbe8bd726e6beb24506ae01440f436c8c05dc1612d`, and replay receipt hash `sha256:82efe970296124097a8e711e4b487215c8de706d49debb87a3485ad5ede57e67`. The current clean-clone integrity/reachability/action suite passed 119 tests with three platform skips. Manifest-aware integrity passed all source, policy, archive, secret, and supply-chain checks; package-only integrity passed with public identifiers intentionally `NOT_EVALUATED`; action semantics passed with zero findings. The first manifest-aware scan omitted the explicit manifest hash and is preserved as failed preflight `manifest-identity-unbound`; the explicitly bound rerun passed.
- **Consequences:** Campaign 37 remains unlaunched until the evidence-bound head is pushed and every required hosted job passes. Holdout consumption remains `0/1`; `GameState.WIN` remains unobserved; `OFFICIAL_ARC3_RHAE = NOT_MEASURED`; `NO_ARC3_GENERALIZATION_CLAIM`.
- **Reopening condition:** Historical receipt equality changes; the exact next action differs; carried identity or raster continuity becomes ambiguous; any target, sibling, other-source, stationary-cell, or trace-identity invariant fails; a required replay/integrity/CI gate fails; or the official environment returns progress, `GAME_OVER`, a narrower residual, or `WIN`.

## D-003-0043 - Preserve Campaign 37 and test detachment only after observed target promotion

- **Date:** 2026-08-27
- **Stage:** 10 and target-game continuation
- **Status:** ADOPTED; CAMPAIGN 37 FAILED_MECHANISM PRESERVED; EXACT 0173AB4 REPLAY AND INTEGRITY PASSED
- **Source identity:** Campaign 37 frozen commit `1fde5c1dbed04f5318c85cf3f8a7fab669a51cd9`, tree `0dbf755fd0baf144ce9602876b3cfdf3aa0d228d`.
- **Decision:** Preserve Campaign 37 unchanged and reopen only the final carried-source delivery consequence. The five exact delivery actions all returned official `NOT_FINISHED` at `4/6`. The final contact consumed the hollow target interpretation and produced one target-centered mediator while retaining the delivered source geometry, so direct-delivery sufficiency is rejected. Permit exactly one inverse discriminator at the observed predecessor active center only after an exact final `NOT_FINISHED` raster and unique target-to-mediator promotion. Accept only two mutually exclusive protected-raster/count outcomes: the source returns with the child, or the source remains deposited while the child detaches. The first rejects repeated delivery and unrelated fallback; the second clears stale carrier lineage and permits only fresh planning from the returned observation. Any other consequence latches lineage loss. `WIN` remains authoritative and `GAME_OVER` remains failure/reset evidence.
- **Official evidence:** Root `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-1fde5c1-campaign37` is verified `FAILED_MECHANISM`. It contains 259 official submissions: 256 `ACTION6` and three legal `RESET`s; three `GAME_OVER` returns; raw and metric final `NOT_FINISHED`; score boundary `GAME_OVER`; `levels_completed=4/6`; and zero `WIN`. Run receipt hash is `sha256:7d4018f8d575c10d7a1a6759142998aafd529a8f96e75be048ee606ca683de7d`; completion receipt hash is `sha256:8cd7f98ba23bee7dc687eb027ca3fb325b157241300fe1a08f45cc6321e6e6da`; recording SHA-256 is `672746f3d1c0a2466b3e8a80194a840b18f00e8bf6ab687598140782bf4770af`; trace chunk SHA-256 is `7240ffc6bc88566a2126fb586eab3bbe316af97fa4e7cd7c1b2c27f5a47a6e16`; trace tail is `sha256:478718ca9a67bd569f52906164e2de4f582f9c954cd0d249288b524db3495055`. The exposure ledger has 71 development events and zero holdout events.
- **Launch preflight preserved:** The first Campaign 37 invocation stopped before exposure or environment action with `FAILED_INFRASTRUCTURE` because the reused editable environment resolved `arc3` from the dirty root worktree rather than the clean launch clone. Binding process-local `PYTHONPATH` to the clean exact clone repaired source identity without altering the frozen source. The official run then launched only from that exact clone. The failed preflight is not erased by the successful correction.
- **Residual diagnosis:** The five new official actions were `(53,36)`, `(23,58)`, `(46,24)`, `(23,48)`, and `(60,24)`, derived by the generic relation planner rather than production constants. Their final protected scene changed from five endpoints, no mediator, and a 12-cell hollow target centered at `(53,28)` to five endpoints, one 19-cell mediator at that center, no hollow target, and the active endpoint at `(60,24)`. The delivered 21-cell source remained co-centered and disjoint from the former target ring. Exact inverse `(23,48)` is therefore the smallest action that distinguishes continued attachment from deposition; neither outcome, nor `WIN`, has yet been observed.
- **Safety boundary:** Detachment projection is optional future work and may never suppress an otherwise valid direct delivery that could return `WIN`. A probe is enqueued only after the exact observed `NOT_FINISHED` promotion. It must retain the same plan lineage, one carried support, predecessor active center, protected before hash, two distinct exact consequence hashes, and expected endpoint/mediator counts. Missing projection, missing promotion, corrupt raster, ambiguous branch, coordinate mutation, or terminal-state mismatch fails closed. No game ID, campaign, level, coordinate, or palette constant enters production logic.
- **Repair evidence:** The first local patch made the future detachment certificate mandatory for every final delivery. Independent review identified that this could suppress a potentially winning direct action when only the speculative inverse was unavailable. The corrected implementation isolates all detachment-only projection/certificate failures and leaves delivery admissible; no-probe `WIN` is honored, while no-probe or unpromoted `NOT_FINISHED` stops with no fallback. Failed test-only iterations involving a derived `VisualScene.targets` field and a cached fixture were repaired without environment use.
- **Local verification:** On final bytes, the two focused lifecycle/generation tests passed in 102.93 seconds, all seven carrier-source tests passed in 164.85 seconds, and all 136 visual-policy tests passed in 499.24 seconds. Ruff lint and format, strict MyPy, Python compilation, and `git diff --check` pass. Independent read-only review found no remaining P0/P1 issue. A nonblocking wording inconsistency remains in the post-carried fail-closed `PolicyError`; the receipt states the correct rejection and behavior does not repeat or fall back.
- **Exact-source verification:** Commit `0173ab4ba1954b69b46f32cd17cc54bc15a9f47f`, tree `ebca31dc69a8d95c348f6187f7c2adf54290925b`, strictly replayed every Campaign 37 event, submission, and mechanics receipt with zero mismatch and zero environment action, selected then cancelled exact inverse `(23,48)`, and left no pending action or prediction. Replay file SHA-256 is `1e9a00c281b62a286e169557dc8efb8b07c6bdfc1fa1429ecdc4ed1cb1267811`; payload SHA-256 is `72962b71b6f4fddf66a8865e55b031e3a287d6b6daa765d2ac1fd519fa80f4db`; receipt hash is `sha256:633cb9c5bfd173c264f7857921089a71ceb9a11e8de828e6f27fda451e162b3b`. The clean-clone 122-case integrity/reachability/action selection passed 119 with three Windows platform skips. Corrected manifest-bound integrity file/receipt hashes are `d6fe45978e74282e2441e1b843cd1ce33c36309ed3d030d090c3072a33541a40` / `sha256:ecee0a74b7462aeacf021bd70246b94cf45e56f8e1d18512630eebbcd1bd17cc`; package-only hashes are `7dc5fd50cf84283b489c2dbae1e1a295629427b8a32049f216d649d69cc8ab3b` / `sha256:7b15a6f1e9db3213c8c7aa35bbf0f6ab11a4fc2c4fbd804e4d7081ab5ff272d8`; action-semantics hashes remain `4615ff438676d992f09e6cbbfdda78bf72443806939c1fe31bf6929d3a2c965d` / `sha256:7b7ca339652c2a124f52c2f80c9f67fd7bee69352d4f3574ce1ad6df4f51c557`.
- **Preserved gate failure:** The first manifest-aware and package-only scans passed `upstream.lock.json` where `uv.lock` was required and correctly failed supply-chain inventory. Their file/receipt hashes are `44e2f588d0cbdde49a7eba029f8f11455e4766f429c00b77f88692173f5bcc4e` / `sha256:4f0f1a63bd2387431fefd432bc9836003db7e4c3e3797a1e64a534c710fc719c` and `9f47bcde8c003620c277db10d3c018e004b9f6716c5ed2e0fc6c197a0f9bc4f9` / `sha256:8559a4994ef41a0db9417436a30557420c97d452743f3d15994ebbc19dff06ca`. The corrected passing receipts preserve these failures rather than overwriting them.
- **Consequences:** Campaign 38 remains gated only on committing and pushing this evidence head, repeating the replay/integrity bindings at that final exact head, and passing every required hosted job. Holdout consumption remains `0/1`; `GameState.WIN` remains unobserved; `OFFICIAL_ARC3_RHAE = NOT_MEASURED`; `NO_ARC3_GENERALIZATION_CLAIM`.
- **Reopening condition:** Historical receipt equality changes; exact replay does not select the predecessor inverse; the optionality boundary suppresses delivery; carried/deposited branches overlap or accept a corrupt raster; any target, source, sibling, stationary, lifecycle, or trace invariant fails; exact-head verification fails; or the official environment returns a narrower residual, progress, `GAME_OVER`, or `WIN`.

## D-003-0044 - Preserve the exact 3,000-second failure and restore the package timeout reserve

- **Date:** 2026-08-27
- **Stage:** 10 and target-game continuation
- **Status:** ADOPTED; LOCAL REPAIR GATES PASSED; EXACT-COMMIT AND HOSTED GATES PENDING
- **Decision:** Increase only the `build001-package-only` guarded `package-safe-test-suite` ceiling from 3,000 to 3,300 seconds and only that workflow's outer job ceiling from 60 to 65 minutes. Pin the 3,300-second command ceiling, 65-minute job ceiling, exact 600-second nominal reserve, and separately scoped full-profile 2,400-second ceiling. Preserve the complete guarded selection, every boundary exclusion and path/process/source-projection guard, failure-evidence upload, timeout-to-`FAILED_INFRASTRUCTURE` classification, and expected private-only `BLOCKED_EXTERNAL` terminal. Do not rerun or relabel the failed source head as though the passing duplicate erased it.
- **Failure evidence:** Exact commit `2986c7864ede3b651f68b3ffb85c837a7eb7ef44`, tree `14d9f4fb0250721f1a08c8138ea899318bbad471`, push package workflow `33061793807`, Windows job `98485992398`, and plan `sha256:4683f43558e2a567c76a3336f8ef19cb8cf07bd33ed0e07bc83c826b94bb4fd1` reached 3,000.0182591 seconds. The receipt returned `FAILED_INFRASTRUCTURE`, `timeout_triggered=true`, successful pipe drain and process-tree cleanup, and zero remaining PIDs. Its 1,182-byte stdout emitted 1,053 pass glyphs and three skip glyphs through 93 percent plus 48 further tests, with zero failure/error glyphs; 23 of 1,079 collected tests remained before reporting and guard-receipt finalization. Failure artifact `9643987446` has GitHub digest `d19b655b50f0e85794e4831e2d0dff794374f1526a96b659f5640242b55507b5`; receipt file SHA-256 is `e44a1596e25d2777470fd8a0b3dd463535221e8a1a74dc2dc33a5ec508d2440c`; embedded receipt SHA-256 is `sha256:e4a6784fd17e340efe039e90aa642a57d3fd57a0dfd47137a8e1b8942c6a3544`.
- **Paired evidence:** Simultaneous pull-request Windows job `98485989264` used the identical commit, tree, index, plan, command, selection, and 3,000-second ceiling and completed all 1,079 items as 1,076 passes and three skips in 1,685.9914728 verifier seconds. It emitted the complete zero-attempt guard receipt and returned only the expected private-surface `BLOCKED_EXTERNAL`. Its 245,235-byte stdout begins with the failed stdout byte-for-byte. Passing artifact `9643355463` has GitHub digest `550dcbbe794dbfc20b3d024c908d2956433d051fc0fd8251dcbd91c839bd521e`; receipt file SHA-256 is `4b0e85a94c47d88e83ebc7a44f52f731050c1b99f99573012ef6338c1715480b`; embedded receipt SHA-256 is `sha256:b2f5bc54a37670e9cdbbd9f789d2299ae198374b9809eb4a9ab8cbceb85ecf84`. All package and integrity payloads are identical; only the passing artifact adds the completed guard receipt.
- **Why:** D-003-0040 explicitly reopens when a runner reaches 3,000 seconds. Linear progress projects the interrupted suite near 3,065 seconds before finalization, so another bounded 300-second command margin covers the demonstrated variance. Raising the outer ceiling by the same five minutes preserves the existing ten-minute nominal reserve instead of making the 60-minute workflow boundary the next limiting surface. No learner, test scope, exclusion, guard, package, or verdict mechanism changes.
- **Local verification:** Nineteen focused package-plan and full-plan-boundary tests passed on final formatted bytes in 7.69 seconds. Ruff lint and formatting passed for the verifier and both regressions; strict MyPy passed for the verifier; JSON and workflow YAML parsed; diff hygiene passed. One new regression increases rather than reduces the guarded test count. No environment action, public inventory access, gameplay, credential use, or holdout access occurred.
- **Consequences:** Exact `2986c78` remains an immutable 8-of-9 hosted-gate result with one required `FAILED_INFRASTRUCTURE` job; its eight passing jobs cannot authorize Campaign 38. A new exact repair head must repeat strict Campaign 37 replay, clean-source integrity and action gates, and all five hosted workflows and nine jobs before public-development play resumes. Holdout consumption remains `0/1`; completion remains false; zero `WIN` was observed.
- **Reopening condition:** Any runner reaches 3,300 seconds; the 65-minute outer package ceiling becomes limiting; the guarded selection, exclusions, guard receipt, source projection, upload behavior, expected `BLOCKED_EXTERNAL` boundary, or package identities change; or a real assertion/source failure appears.

## D-003-0045 - Preserve carried target inverses and test only the untouched attachment

- **Date:** 2026-08-27
- **Stage:** 10 and target-game continuation
- **Status:** ADOPTED; EXACT 6055E7F REPLAY AND LOCAL GATES PASSED; FINAL EVIDENCE HEAD AND HOSTED CI PENDING
- **Source identity:** Campaign 38 frozen commit `9590c03d4d7490f01b09326e0f59feaf8bdb67a1`, tree `57d363d70faccc16be053ac9724c40a488116e6f`; Campaign 39 frozen commit `c8f0cfc03eecb16711a55078aeedffcbc5382381`, tree `cd900d8aa50760aa4359b16ee3f0f4617ceca43c`.
- **Decision:** Preserve both official continuations unchanged. Campaign 38's exact target inverse proved that the delivered source remained attached. Campaign 39's exact alternative delivery and inverse moved the same 21-cell source rigidly again while the target ring remained unchanged, rejecting repeated target delivery and both tested target inverses as sufficient. Reopen only the sole still-untested carrier-source attachment. Permit one finite historical-inverse discriminator followed immediately by its exact restoration and prior-role restoration. Derive the support, endpoint lineage, predecessor, branch rasters, and coordinates from accumulated observation history; add no game, campaign, level, palette, target, or coordinate constant. Stop on `WIN`, level progress, `GAME_OVER`, any third raster, or failed restoration.
- **Official evidence:** Campaign 38 submitted 260 official actions, including 257 `ACTION6` and three legal `RESET`s; final raw state was `NOT_FINISHED`, score boundary `GAME_OVER`, `levels_completed=4/6`, and zero `WIN`. Its verifier passed 412 declared artifacts with zero error. Campaign 39 submitted 263 actions, including 260 `ACTION6` and three legal `RESET`s; all three new actions returned `NOT_FINISHED` at `4/6`, final score boundary was `GAME_OVER`, and zero `WIN` occurred. Its verifier passed 417 artifacts, 1,580 hash-linked trace events, 264 recording rows, all 263 action/consequence/receipt mappings, and zero mismatch. Campaign 39 run receipt hash is `sha256:6475c6f5f48536bb51a281a00081e9d6895360bf153997e62ff31a3e0438f653`; recording SHA-256 is `fbbb740b8229d7caa0c8dd235f5dcebc88338c20133ca9124914b3ef6005ece1`; trace tail is `sha256:c6aff1fa14864841d325ee9375c56c3467229aa9eda00b048df11e100fe0b95e`.
- **Residual diagnosis:** The three Campaign 39 actions were `(60,24)`, `(46,24)`, and `(23,58)`. The first two performed exact redelivery and role exchange. The final inverse translated the complete carried disk from center `(53,28)` to `(45,39)` without changing any target-ring cell. This is direct official attachment evidence, not deposition or completion. One separate split-source support remains untouched. Its current endpoint and exactly observed predecessor define the smallest remaining discriminator.
- **Candidate boundary:** On the frozen final observation, role exchange to the untouched support must alter only the two 12-cell endpoint shells. The exact historical inverse then has only two accepted protected outcomes: 76 non-HUD changes with the split source stationary, or 97 with its complete footprint translated by the carrier displacement. Those branches differ on 31 cells. The inverse corridor intersects the target ring, previously carried source, and its carrier at zero cells. The candidate must then reverse the move and restore the prior active role byte-exact. Any ambiguity, extra mutation, third outcome, stale lineage, or incomplete restoration fails closed.
- **Implementation and local verification:** Exact implementation commit `6055e7f2fb96e47e65f71de5f440abe524b72cfd`, tree `e0fa546c36a4a118ef8ed92377a1bfc48bcf604c`, derives the discriminator only after the immutable Campaign 39 receipt. It initially queues only role selection and the discriminator; the exact returned raster earns a branch-naming receipt before only that branch's two-action restoration is queued. A focused fixture covers stationary and newly carried outcomes, corrupt protected raster, full restoration, post-receipt atomicity, `WIN`, `GAME_OVER`/reset, level progress, and cancellation. It passed in 138.33 seconds; eight carrier-source tests passed in 201.58 seconds; all 137 visual-policy tests passed in 475.98 seconds. Ruff lint/format, strict MyPy, compilation, and diff hygiene passed. Independent final review reported no P0/P1 finding after four narrower preflight issues were repaired.
- **Exact replay:** A clean detached clone strictly matched all 1,580 Campaign 39 trace events, 263 submissions, and 263 mechanics receipts, then selected and cancelled unsubmitted `ACTION6 (20,42)` with one queued plan action remaining and zero environment actions. Receipt `C:\a\arc3-b003-gates-6055e7f\campaign39-sealed-trace-replay-6055e7f.json` has file SHA-256 `6c10f78f164a6563ac848945871c172c41aec35b74aac3f54ca1ff782dc03322`, payload SHA-256 `637a8c70eb388a443bf55fd905aea5a11530021ffc360b7fb14c5b5cfd1dae65`, and replay receipt hash `sha256:1c9542e98c62e99b5c87890d1545c47dada6fec0fbf77b617a06fcb772561b4c`. Two earlier command-shape attempts were rejected before trace access and emitted no receipt: module-style invocation, then one trace-mode invocation carrying a recording-only argument.
- **Exact integrity:** The 122-case clean-source selection completed 117 passes and five host/platform skips. Manifest-bound integrity passed all checks with zero findings; file/receipt SHA-256 are `3f973c3c58e71586bafcf3b33442a925a67d362d178b571e6a04eb40dc43d6aa` / `sha256:872d44b4daa1433658af7aa81048761c77f80eaa3dd6c10cabcc6db2569f134a`. Package-only integrity passed its declared scope with zero findings and intentionally left public identifiers `NOT_EVALUATED`; file/receipt SHA-256 are `ff6b04b9649aea4d879d1e6661fbd1e52d00416cf78c4287d8bcca2f5caf08b8` / `sha256:ccb8b2813b3df0f7f9de5a9ee507727e5224ec4600a8a2748544f7722523ffd6`. Action semantics passed with zero findings; file/receipt SHA-256 are `4615ff438676d992f09e6cbbfdda78bf72443806939c1fe31bf6929d3a2c965d` / `sha256:7b7ca339652c2a124f52c2f80c9f67fd7bee69352d4f3574ce1ad6df4f51c557`.
- **Authority and consequence:** Exposure remains 75 development events and zero holdout events. No target-game source was semantically inspected. No terms, credentials, money, submission, merge, release, or external representation was used. Replay, exact projection, restoration, CI, and confidence remain non-completion evidence. Only official `GameState.WIN` completes the target.
- **Reopening condition:** Historical receipt equality changes; the unique untouched relation, exact predecessor, two branch rasters, safe corridor, inverse, or restoration is not derivable; any local or hosted gate fails; or the official environment returns progress, `GAME_OVER`, a narrower residual, or `WIN`.

## D-003-0046 - Preserve both observed attachments and test one paired cargo delivery

- **Date:** 2026-08-27
- **Stage:** 10 and target-game continuation
- **Status:** ADOPTED; EXACT D0D210D REPLAY AND LOCAL GATES PASSED; HOSTED LAUNCH GATE FAILED; OFFICIAL PLAY PAUSED
- **Source identity:** Campaign 40 frozen commit `02d078ed1477a47a0d205cd7ee45a8d66125b2e9`, tree `e2364ccf01811bf0f7003fec66dfd1014fef39c4`.
- **Decision:** Preserve Campaign 40 unchanged and retain both exact carrier-source attachments. Admit one next family only: move the raw-linked carried source to the unique monochrome raw target and its exact carrier-mask counterpart to the previously observed unique external residual-linked target. Reuse the already observed endpoint-to-support relation, but rebase endpoint identity and role exchanges through the exact restored carried-source state. Both translated source disks are the only newly added variable. Do not enumerate alternative sinks, interactions, palette permutations, or arbitrary coordinates.
- **Official evidence:** Root `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-02d078e-campaign40` is verified `FAILED_MECHANISM`. It contains 267 submissions: 264 `ACTION6` and three legal `RESET`s; 264 `NOT_FINISHED` consequences, three `GAME_OVER` consequences, and zero `WIN`; raw final `NOT_FINISHED`; score boundary `GAME_OVER`; `levels_completed=4/6`. The four new actions were `(20,42)`, `(43,34)`, `(20,42)`, and `(23,58)`. The exact discriminator matched only the carried-source projection, then the two restorations returned the complete gameplay raster to Campaign 39 with zero mismatch over all 4,032 non-HUD cells. The shared gameplay-raster SHA-256 is `58773d2b09486b79650b08d1b4b0a274434e2d4fbe8abcc4a475764e3125d61b`; only five left-HUD cells changed.
- **Evidence integrity:** The public verifier passed all 423 declared artifacts and the complete trace/recording links. Recording SHA-256 is `982f9feb5065ae7b7357492b1faef41f0adee54eae285928f5a3a9472908b270`; trace manifest hash is `sha256:b9e798912f883446f7fc18d660ce49405c4d59ee325dcd81a23a2dcc46a5c62f`; trace tail is `sha256:fe511012a68759b2f161dd8dd2e482999144d24942da5173264860950958a93c`; run receipt hash is `sha256:268d4a07c2bbd34c2a375cb6e1feec1535211686fa6cdb42a02b16897dc56c2a`; completion receipt hash is `sha256:ae02b6ec321d57bc0f56a2732d1bf9e8bf5c1536d24407c31e1c9738ea5c2919`.
- **Replay boundary:** The exact old-policy strict replay regenerated all 267 submissions, consequences, and mechanics receipts, then raised the expected target-delivery `PolicyError` while staging the next candidate. Because no candidate was returned, the canonical verifier emitted no PASS receipt. It issued zero environment actions and did not alter the sealed trace. The Campaign 41 implementation must convert precisely this boundary into one exact selected-and-cancelled candidate without changing any prior receipt.
- **Safety boundary:** Require all carrier-source supports to be independently certified carried; a unique replay match for current latent endpoint positions and active role; the previously rejected external residual-chain relation; unique raw and counterpart destination supports; in-bounds, disjoint translated sources; no endpoint/source, cross-child, unrelated occupied-cell, or protected-target collision; exact protected-raster and visible-count certificates at every role exchange and movement; and a complete inverse to the Campaign 40 gameplay raster before the first action is selectable. `WIN` or real level progress terminates the old-level plan. `NOT_FINISHED` at the exact paired terminal queues only the exact inverse. `GAME_OVER` remains immutable failure evidence and enters the legal reset lifecycle.
- **Implementation and local verification:** Exact implementation commit `1195da156d227290404771f9d77ec62a69c1c533`, tree `1bbef9354c9f8ad2caec56e69474bb115651118b`, reconstructs the previously rejected external residual-chain relation, binds the raw and counterpart sources to those child-specific external supports, and derives every action from the frozen replay state. The exact fixture has 136 and 60 raw layouts. A bounded 16,320 layout/order enumeration found 168 safe routes across 84 terminal endpoint assignments: the destination mapping is unique, but the action route is not, so production uses a deterministic sorted first-safe tie-break. Counts are exactly five endpoints/zero mediators through forward step nine, five/one at the paired terminal, and five/zero throughout the inverse; a shared initial-boundary hash closes the ten-action inverse to the Campaign 40 raster. The final combined lifecycle/equivariance run passed 2/2 tests in 215.70 seconds, all 137 visual-policy tests passed in 638.24 seconds, and Ruff lint/format, strict MyPy, compilation, and diff hygiene passed. Independent final review found no P0/P1 issue. Policy/test SHA-256 are `1a1e105f19589b0f77397d4d6b6a35ec132994668a144557a9f345c07ad64386` / `e1b67581a6fd3626ce1776bca7b65116d81de513ced699228228a61b5d7e8954`.
- **Preserved preflight failures:** An initial local prototype incorrectly targeted each child's already rejected own bridge sink; independent review stopped it before commit, replay, or official action. The first corrected external search then failed closed under an overstrict baseline-count equality, and a tempting four-endpoint/two-mediator projection was rejected because it lost the visible active endpoint. Later red tests exposed exact parser-boundary, inverse-cycle, terminal level-progress, and post-RESET assertion gaps before the final green tree. These are local diagnostic evidence only. Git also reported a non-fatal `FAILED_INFRASTRUCTURE` permission warning while attempting to prune stale read-only worktree metadata after commit `1195da1`; the commit and tree verified, and no cleanup was attempted.
- **Preserved strict-replay failure:** Exact `43e7a482195289cc0721cd9345eb945f50071f1c`, tree `e90de2c99644101a2a0dcdfcc539e59eb189430a`, matched the first 266 mechanics receipts, then disagreed at submission 267 only in `$.residual`: the new paired-staging sentence had replaced the sealed historical residual `exact endpoint recovery ended with an observation-certified source attachment; no unrelated continuation is authorized`. Sealed/regenerated receipt hashes were `sha256:3d55f47ecf4586f5bc20e0e2df98b015443b79be5ea68de46e4c80bd5cf3b5b6` / `sha256:7f764a48a9884263759c012252dcc20854a30084b563e2ffadb740464da1354e`. No output replay receipt was emitted and zero environment actions occurred.
- **Repair and exact replay:** Exact `f65003b2b246bfaa6efb7979cfda35e12a948c29`, tree `2347ad5a5e5352bb3eb8f608c759a74e172020ee`, preserves the durable historical residual and derives paired staging only after receipt append. The focused lifecycle regression passed 1/1 in 210.22 seconds and the formatting, lint, strict-type, compilation, and diff gates passed. Its clean sealed replay matched all 267 submissions, consequences, and mechanics receipts, selected unsubmitted `ACTION6 (8,38)` with nine plan actions remaining, then cancelled it with receipt count still 267, learner pending count zero, and zero environment actions. Replay file/payload/receipt SHA-256 are `b74431a34b70a1a78ab75413ba647473177414bd73364b6d959c2864a87a8160` / `5e9b28a2a3305308b8ccebeed480907e27ea324d8a09ed16238a63eb59a9de5b` / `ba706a40a05a1007cf4f44dcc58bad2dbd2a4788c94347669f4a451eb292be87`.
- **Clean-source selection and integrity:** The exact detached source collected 122 integrity, policy, production-reachability, and action-semantics cases; 119 passed and three expected Windows/POSIX/symlink platform cases skipped in 36.05 seconds. Manifest-bound integrity passed with zero findings (file/receipt SHA-256 `c4a83f0692c3d3aba87f1f78add291eedbea0085f270f831e60a6bef91b43ba0` / `c70a56c475fdab6d75e79b479ff030310c36bd82bd67f430c59a8a562efa2c67`). Package-only integrity returned `package_only_passed=true` with zero findings; top-level `passed=false` only because public-identifier semantics are intentionally not evaluated in that mode (file/receipt `e71f62b5e092de05c95da38674394fba31f066509925482b9c03261e1153b032` / `cceee8fa510e2640672ea86def4f94a2ea0e8703e71af66400247d99b38b335b`). Action semantics passed with zero findings (file/receipt `4615ff438676d992f09e6cbbfdda78bf72443806939c1fe31bf6929d3a2c965d` / `7b7ca339652c2a124f52c2f80c9f67fd7bee69352d4f3574ce1ad6df4f51c557`).
- **Final exact-head evidence:** Commit `d0d210df6fafa513fd2e6a500f6709f5c81816cb`, tree `c035fbfce532a97b2015c78e8ccacbc06e3e3e06`, repeated strict replay of all 267 submissions and receipts, selected and cancelled `(8,38)`, and issued zero environment actions. Its replay file/payload/receipt SHA-256 are `9553232bf8c43cb5b4542afcf736a5cdae85fcb2a7d02162e8308d554f9acc78` / `ffdb22c957745af19b05520ab8b6d0459280310310a50e7f132eb10d240a8da8` / `62681bb7ebeff395d66e590b926916d212aca51e2a824620aac39d8746986efa`. The exact clean-source selection passed 119 of 122 cases with three platform skips; manifest-bound, package-only, and action-semantics gates passed with zero findings. A read-only launch audit passed with receipt `sha256:dec77b369a6c94679a9d33ff3090342a99b1869132e0282d3d9438047ceb834b`, confirmed 77 development exposures and zero holdout exposures, and issued no environment action.
- **Hosted launch-gate result:** The exact head reached terminal state in all five required workflows and nine jobs, but only six jobs passed. Both Windows package-only jobs reached the exact 3,300-second guarded ceiling at 93 percent with no assertion failure and preserved `FAILED_INFRASTRUCTURE`; the push Windows ARC3 CI job independently exceeded the configured 75-minute job ceiling after 1,801 passes and 20 skips with no assertion failure. The exact pull-request duplicate completed 1,818 passes and 20 skips in 4,418.19 seconds and passed runtime doctor. Campaign 41 was not launched. D-003-0047 and B-003-0043 preserve the bounded repair and immutable failures.
- **Consequences:** Exact candidate head `d0d210df6fafa513fd2e6a500f6709f5c81816cb` is strict-replay and clean-source gate verified but failed its hosted launch gate and remains officially unobserved. The new bounded time-budget repair head, its exact replay/integrity reruns, hosted CI, and an official consequence remain pending. A complete local paired layout, exact synthetic projection, strict replay, or green CI is not completion. Holdout consumption remains `0/1`; no target-game source was semantically inspected; `OFFICIAL_ARC3_RHAE = NOT_MEASURED`; only official `GameState.WIN` completes the target.
- **Reopening condition:** Current latent state is non-unique, either destination link is ambiguous, any translated source or protected-raster gate fails, the inverse is incomplete, historical replay changes, a required exact-head gate fails, or the official environment returns progress, `GAME_OVER`, a narrower residual, or `WIN`.

## D-003-0047 - Preserve Campaign 41 gate failures and raise only demonstrated hosted time ceilings

- **Date:** 2026-08-27
- **Stage:** 10 and 11
- **Status:** ADOPTED; LOCAL VERIFICATION PASSED; EXACT REPAIR HEAD AND HOSTED GATES PENDING; OFFICIAL PLAY PAUSED
- **Source identity:** Campaign 41 evidence head `d0d210df6fafa513fd2e6a500f6709f5c81816cb`, tree `c035fbfce532a97b2015c78e8ccacbc06e3e3e06`.
- **Decision:** Preserve all three non-successful hosted jobs as immutable infrastructure evidence. Raise only the package verifier's `package-safe-test-suite` ceiling from 3,300 to 3,900 seconds and its workflow outer ceiling from 65 to 75 minutes, preserving a 600-second nominal reserve. Raise only the ordinary ARC3 CI outer ceiling from 75 to 90 minutes. Do not change learner or gameplay bytes; shrink, shard, skip, stub, or reclassify any test; change platforms, coverage, runtime doctor, package guards, evidence upload, or failure semantics; or infer that an unexecuted tail passed.
- **Package evidence:** Pull-request job `98706462417` and push job `98706398101` independently timed out at 3,300.0115123 and 3,300.0146267 verifier seconds. Both reached 93 percent plus additional pass glyphs, emitted no assertion or error evidence, successfully drained pipes and cleaned process trees, and left zero remaining PIDs. Their receipts remain `FAILED_INFRASTRUCTURE`; the push receipt file/internal SHA-256 are `217b303767cfdd826980d5572c2e944ec57c9f856c450da10bcd5d65eec6266b` / `9881216a1fa592c11bbf709eac1ab3902eb2427a74afd18e0a904a86e41e38a4`, and the pull-request receipt hash is `sha256:06f85a6a5c1d8585c7bfc836c3dcca717e33926bb951e6970c3ec57449b01da4`. The 3,900-second ceiling is a bounded allowance for the observed remaining tail, not a claim that it will pass.
- **Full-CI evidence:** Push Windows job `98703379113` was definitively cancelled because it exceeded the configured `1h15m0s` job maximum, after 1,801 passes and 20 skips in 4,459.59 seconds with no assertion failure. The exact pull-request duplicate job `98703384643` completed the unchanged command with 1,818 passes and 20 skips in 4,418.19 seconds, emitted coverage, passed runtime doctor, and finished in 4,464 seconds, only 36 seconds inside the former cap. A 90-minute outer ceiling is the smallest conventional 15-minute increment and changes no command or acceptance criterion.
- **Local verification:** Nineteen focused verifier/workflow tests passed in 6.61 seconds. Assertions pin the 3,900-second guarded ceiling, 75-minute package job, exact 600-second nominal reserve, 90-minute ordinary-CI job, unchanged full coverage command, and unchanged runtime doctor. Ruff lint/format, strict MyPy, Python compilation, JSON parse, both workflow YAML parses, and diff hygiene passed. A first concurrent live-tree run is preserved as `FAILED_INFRASTRUCTURE`: 17 tests passed and two plan-construction cases rejected OneDrive bytes that changed during a bounded snapshot; the unchanged sequential rerun passed all 19. No environment action occurred.
- **Authority boundary:** No official environment action may be sent from this repair or from the passing duplicate. Holdout access remains zero, the intended Campaign 41 run root remains absent, and only an exact repair head that passes strict replay, local integrity, all five workflows, and all nine jobs can earn authorized development play. Green gates still cannot substitute for official `GameState.WIN`.
- **Reopening condition:** The repair changes guarded selection or learner bytes; the 600-second package reserve is not preserved; a focused assertion, replay, integrity gate, workflow, or job fails; or a repaired exact head passes all gates and earns official continuation.

## D-003-0048 - Preserve Campaign 41 and test only the complementary paired-terminal active role

- **Date:** 2026-08-28
- **Stage:** 10 and target-game continuation
- **Status:** ADOPTED; RESOLVED BY VERIFIED OFFICIAL CAMPAIGN 42 FAILED_MECHANISM
- **Source identity:** Campaign 41 frozen commit `6f117d2c265e7922f430e4ff0afcc61b7fad27cb`, tree `7c37a06eba90175567d97cf0a64aa280272bbc17`.
- **Decision:** Preserve the exact paired carried-source placement as `FAILED_MECHANISM`: both observation-derived sources reached their uniquely linked external-chain destinations and the official environment remained `NOT_FINISHED` at `4/6`. Retain the two independently observed source attachments, translation mechanics, endpoint lineage, collision-safe forward route, and exact inverse. Reopen only the active-role variable at the paired terminal. Recreate that terminal, select the complementary child endpoint once without moving either endpoint group or cargo, and classify only the exact role-only consequence. Do not enumerate other endpoints, destinations, or interactions.
- **Official evidence:** The run root is `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-6f117d2-campaign41`. It contains 287 submissions: 284 `ACTION6` and three legal `RESET`s; the 20 post-Campaign-40 actions were ten forward and ten inverse actions, and every one returned `NOT_FINISHED` at `4/6`. Forward action 277 reached the paired terminal with five endpoints, one mediator, zero parsed targets, and returned `NOT_FINISHED`; its frame hash is `sha256:0f9e8ca269660d7297306a51d6909b6563158d841475b40d9a0b301ce13b013e`. The complete inverse restored every gameplay cell at `x=1..63`; only 21 consumed status cells at `x=0` differed. Final raw state was `NOT_FINISHED`, score boundary `GAME_OVER`, completion observed was false, and zero `WIN` occurred.
- **Evidence integrity:** Public verification passed all 453 declared artifacts. The 288-row recording SHA-256 is `d3b3b7fdef431f0bfc86bdee5375631e2cff6a12796dd70091ff1d60824b2b7b`; the 1,724-event trace chunk SHA-256 is `678adff60bda3c290451f2625d69964de969d46a0a904f62f54566442b55c583`; trace manifest hash is `sha256:a3215f1fcc048146312a665fe1be9b3c60a4d8891cb362c4cb2a74770ffb76f7`; trace tail is `sha256:2d0651bd48b33a2e964ae6b2f5ae8818fb9c706f6d92a6b100640cf68b5f5e87`; run receipt hash is `sha256:f15ccba685e1af28ad86765c4da5bc263b344084711d3a9be7f5cd3beb188a08`; completion receipt hash is `sha256:0b08e8622f4f0c04dbb03936cf422574f41ca1ff6b5d2da1ca6b23b875df19c3`.
- **Prelaunch gate resolution:** The narrow timeout repair at exact `6f117d2` repeated strict Campaign 40 replay, clean-source selection, bound integrity, package-only integrity, action semantics, and launch audit with zero environment actions. All five required hosted workflows and all nine jobs passed under the repaired ceilings. The earlier exact-`d0d210d` timeouts and cancellation remain immutable `FAILED_INFRASTRUCTURE` evidence.
- **Smallest next discriminator:** At the exact paired terminal, the active endpoint belongs to the second placed child and exactly one complementary endpoint belongs to the first placed child. Derive both from the existing route and returned observation. Submit one `ACTION6` role exchange to the complementary endpoint. On exact `NOT_FINISHED`, require a 24-cell endpoint-shell-only change with cargo, target, static raster, endpoint positions, and counts unchanged; restore the prior active endpoint, verify the exact terminal, then run the already certified inverse. `WIN` is authoritative, real level progress clears the old-level plan, `GAME_OVER` is failure/reset evidence, and any third raster or ambiguous lineage fails closed.
- **Why:** Geometry and cargo placement have now been falsified only under one terminal active-child orientation. The complementary role is the sole one-action variable that can be changed while holding every placed object, destination, and carried source fixed. This is a bounded discriminator, not a claim that active role controls completion.
- **Implementation and local verification:** Exact implementation commit `0a8b07cd0dd78de495e380a755060fa14748f298`, tree `740e7a2efc95b2e6c0c28cea3328902236dccc40`, derives the complementary and current active endpoints from retained paired-route lineage. It recreates the existing ten-action paired terminal, admits exactly one complementary role exchange, accepts only the exact role-shell consequence, restores the prior role, and reuses the certified inverse. A focused lifecycle regression passed 1/1 with 136 deselected in 285.92 seconds. Direct assertions cover a nonmatching role-restoration consequence and unavailable `ACTION6`; Ruff lint and format, strict MyPy, compilation/diff hygiene, and independent review passed. These are implementation checks only and issued no environment action.
- **Strict replay and clean-source gates:** From clean exact source, sealed Campaign 41 replay matched all 1,724 trace events, 287 submissions, and 287 mechanics receipts. It selected the first unsubmitted route action `ACTION6 (8,38)` with nine successors, cancelled it, returned learner pending count to zero, and issued zero environment actions. Replay output `C:\a\arc3-b003-gates-0a8b07c-campaign42\campaign41-sealed-trace-replay-0a8b07c.json` has file/payload/replay-receipt SHA-256 `a4922489f99531a1721818029f1e6f65801aa14ff1febf3ef91ce3e92468030a` / `c614e5b52eb43dc4bcced9a59f56ecdea6ebf3fb8bf3433a6ac9ffa1cdf4ea81` / `652e84e03307e1ad977e183ec86d5dc29eff4cab724ffda3ee4c316811cbfe61`. The exact clean-source selection collected 122 cases: 119 passed and three expected Windows platform cases skipped in 39.99 seconds. Manifest-bound integrity passed with zero findings (file/receipt SHA-256 `ff0185c9fbf45754d1cfeb50449381012b9eff2edee01b40807865ab71d59ba6` / `129d6bdd4e0c064795a56b9c0760e017a034a6d380fb11e8eae90e43097db55f`). Package-only integrity returned `package_only_passed=true`, zero findings, and the expected `NOT_EVALUATED_PUBLIC_IDENTIFIERS` full-scope boundary (file/receipt `1a1b7636243be46016d1013bfb1b964fb97320eb9301568915a0c403f5ebac47` / `4fa271b586a53d7eb16e644b95df0b80b3ac813507c7adb7bb0ebb1f90e40080`). Action semantics passed with zero findings (file/receipt `4615ff438676d992f09e6cbbfdda78bf72443806939c1fe31bf6929d3a2c965d` / `7b7ca339652c2a124f52c2f80c9f67fd7bee69352d4f3574ce1ad6df4f51c557`).
- **Gate and official consequence:** Ledger-bound exact head `0a7cde458c6d9fe76789fc680c583537aebb3996`, tree `adba06d9afe5fd35c16ac1007d7a06e8d67a1ada`, repeated strict replay and every bound local gate, then passed all five required workflows and all nine hosted jobs. Authorized development-only Campaign 42 recreated the exact paired terminal, submitted the complementary role exchange, and received the exact 24-gameplay-cell endpoint-shell exchange with cargo, positions, and all other gameplay cells unchanged. The official consequence was `NOT_FINISHED` at `4/6`; restoring the prior active role restored the paired terminal gameplay body exactly. The role-only hypothesis is therefore rejected. No `WIN` occurred.
- **Authority boundary:** Exposure now contains 81 development events and zero holdout events. No target-game source was semantically inspected; no terms, credentials, money, holdout, submission, upload, merge, release, or external representation was used. Synthetic projection, replay, restoration, and confidence remain non-completion evidence. Only official `GameState.WIN` completes the target.
- **Reopening condition:** Historical replay changes; the exact paired terminal, complementary endpoint, role-only raster, restoration, or inverse is not uniquely derivable; any local or hosted gate fails; or the official environment returns progress, `GAME_OVER`, a narrower residual, or `WIN`.

## D-003-0049 - Preserve Campaign 42 role falsification and carry the complementary handoff through one lineage reconstruction

- **Date:** 2026-08-28
- **Stage:** 10 and target-game continuation
- **Status:** ADOPTED; CAMPAIGN 42 FAILED_MECHANISM PRESERVED; CAMPAIGN 43 IMPLEMENTATION PENDING
- **Source identity:** Campaign 42 frozen commit `0a7cde458c6d9fe76789fc680c583537aebb3996`, tree `adba06d9afe5fd35c16ac1007d7a06e8d67a1ada`.
- **Decision:** Preserve the exact complementary terminal-role exchange as a genuine but insufficient mechanic: it changed only the two 12-cell endpoint shells and returned official `NOT_FINISHED` at `4/6`. Do not repeat or broaden terminal-role enumeration, and do not manufacture a cargo swap by relabeling source-support tuples. Reopen only whether the complementary active-role handoff can be retained while the already observed endpoint movements are reconstructed through their predecessor lineage, after which exactly one target-relative delivery is recomputed from the returned crossed carried-boundary observation. Every crossing must arise from physical endpoint/child transitions whose projected raster is checked stepwise. Production may contain no target ID, campaign, level, coordinate, or palette constant.
- **Official evidence:** Root `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-0a7cde4-campaign42` is verified `FAILED_MECHANISM`. It contains 305 submissions: 301 `ACTION6` and four legal `RESET`s; 301 `NOT_FINISHED` and four `GAME_OVER` consequences; zero `WIN`; score boundary `GAME_OVER`; final raw post-reset state `NOT_FINISHED`; `levels_completed=4/6`; completion false. Public verification passed all 477 declared artifacts with zero error. Recording SHA-256 is `4b89220f5932ee1c3860f2a817edfc8d112840d295598b47325c0a7400cffee7`; run-receipt file/canonical hashes are `76ac25c05fa4ce659adf2ccb280feeb83a35157215ff57e3df2208666b76fd5e` / `sha256:d660fecdf655faf55f1bc106911fefd7700c84fa659178c4f7da4b0883fc9b49`; completion-receipt hash is `sha256:8c0b51b5498925ce402f64300d4abea4d83f9e3ecea32ce39b23f3aeb2d13a64`; trace-manifest hash is `sha256:da8505ee5311ac403c3a555c2261e206e10e0db115e7baa13be10ff39b855c24`; trace chunk SHA-256 is `4d150ffa80b154938964ffad66591cce8affa744ea4f1903a1e6f877e1f8cb56`; trace tail is `sha256:ff5799c183b5f598a2202e72d00a34e68610ed9852556eb3d72e1680b6a5bb40`.
- **Preserved launch failure and correction:** The first invocation stopped before exposure, official-root creation, or environment action with `FAILED_INFRASTRUCTURE` because the evaluator imported `arc3` from the dirty live OneDrive worktree. The exact detached clone itself was clean. Binding process-local `PYTHONPATH` to that clone repaired source identity without changing frozen bytes; the corrected launch asserted the imported module path and scrubbed credential variables. The failed preflight is not relabeled or erased.
- **Role consequence:** Campaign 42 shared the complete 288-observation Campaign 41 prefix. Its new role action changed exactly 24 non-HUD cells: twelve shell cells at each of the two terminal endpoints, with no position, cargo, target, or static-raster change. Restoring the prior role returned the Campaign 41 terminal body exactly apart from the monotone status column. Both orientations returned `NOT_FINISHED`; terminal role alone is falsified.
- **Resource diagnosis:** Each complete level-4 life ended at exactly the sixtieth `ACTION6`. After reset, the 64-cell left status column is empty; after action ordinal `n`, its filled prefix has length `n + [n>=8] + [n>=23] + [n>=38] + [n>=53]`. Action 60 fills all 64 cells and returns `GAME_OVER`; legal `RESET` restores the initial gameplay raster and meter. Campaign 42's inverse was interrupted at this exact budget boundary, not by a spatial hazard, so no paired-route failure is inferred from that `GAME_OVER`.
- **Smallest next discriminator:** From a fresh life, replay only the observation-certified 23-action prefix to the carried-source boundary, the certified ten-action paired delivery, and the now-observed one-action complementary handoff. Retain that handoff instead of restoring the old active role. Project one ten-action predecessor-map reconstruction of the carried-boundary endpoint geometry with the crossed active/lineage binding, requiring every returned raster. If action 44 exactly matches that provisional crossed carried boundary, recompute exactly one target-relative delivery from the returned observation; the known planner needs ten actions on the uncrossed state, so a route no longer than the 16-action observed reserve can complete before action 60. This continuation is model-derived, not yet official or certified. `WIN` stops successfully; level progress preserves the receipt and clears the old-level plan; `GAME_OVER`, an unexpected raster, non-five endpoint core, off-lineage relocation, exhausted preflight reserve, or failed restoration fails closed and preserves evidence.
- **Authority boundary:** Exposure now contains 81 valid development events and zero holdout events. No target-game source was semantically inspected; no terms, credentials, money, holdout, submission, upload, merge, release, or external representation was used. Only official `GameState.WIN` completes the target.
- **Reopening condition:** The route or inverse is not uniquely derivable from the sealed lineage; resource accounting differs on replay; any local or hosted gate fails; or the official environment returns progress, `GAME_OVER`, a narrower residual, or `WIN`.

## D-003-0050 - Treat action-60 resource exhaustion as a pre-effect interruption and replay only the exact rejected lineage

- **Date:** 2026-08-28
- **Stage:** 10 and target-game continuation
- **Status:** ADOPTED; EXACT IMPLEMENTATION COMMIT AND CLEAN-SOURCE GATES PASSED; LEDGER-BOUND EXACT HEAD, HOSTED GATES, AND OFFICIAL CONSEQUENCE PENDING
- **Decision:** Preserve every Campaign 42 failure and rejection. Interpret its final `GAME_OVER` narrowly as exhaustion of the uniquely learned edge resource before the submitted gameplay action took effect, because the returned protected gameplay raster equals the required pre-action raster rather than the distinct predicted post-action raster. Mint one fresh-reset authorization only when the full edge history, level, reset epoch, retained role-handoff lineage, context hash, rejected carrier-source relation hash, root-plan hash, exact setup count, exact crossed-forward count, protected body, and endpoint/mediator counts all agree. Permit the planner to regenerate only that exact rejected root candidate without deleting or weakening any failed relation or plan signature. Consume the authorization only when the exact crossed continuation is installed; clear the token and all executable lineage state on any mismatch, cancellation, `UNKNOWN`, terminal transition, or invalid reset.
- **Why this is the smallest implicated repair:** Campaign 42 already observed a deterministic resource meter and a legal reset to the exact initial gameplay raster. Its sixtieth action changed the edge meter to full but left the protected gameplay body at the action's required input hash. Treating that return as the predicted post-action body incorrectly rejected a continuation the environment never executed. The repair does not reopen the carrier-source family generally, enumerate layouts, infer success, or encode the target game, campaign, level, coordinate, palette, or resource length. It carries only hashes, exact observed counts, and the learned edge trajectory.
- **Candidate behavior:** A fresh life must reconstruct the exact carrier-source root, carried boundary, paired terminal, complementary role handoff, crossed predecessor, and recomputed delivery under the learned resource ceiling. Every setup action revalidates the exact edge prefix and reserves the token-bound remaining setup plus crossed-forward count. The returned crossed boundary must match its protected raster before delivery is derived. `WIN` remains authoritative; real level progress clears old-level state; `GAME_OVER` remains failure/reset evidence; any third raster, altered meter trajectory, stale context, count mismatch, cancellation, or `UNKNOWN` clears the entire replay execution.
- **Preserved failed diagnostics:** A surface-overlap route was rejected because two independently carried source projections assigned conflicting values to one cell. Early local versions admitted a latent endpoint only at generation but not replay, omitted the exact edge trajectory, retained replay authority across extra resets, drifted three historical receipt strings, and checked only the next setup action rather than the complete token-bound budget. Independent review then found that cancelling the first post-reset setup action or selecting on `UNKNOWN` could clear the token while retaining a stale queue. Each defect was repaired before commit or environment use; the final cancellation/`UNKNOWN` regression drives a real token-authorized root selection and proves that token, queue, context, relation, signatures, counts, and lineage clear while failed sets remain unchanged. A later pre-commit integrity-test attempt is preserved as `FAILED_INFRASTRUCTURE`: pytest could not create a nested `C:\a` base temp because its explicit parent was absent, producing 35 setup errors and one coupled reachability assertion. Creating only that named parent and rerunning the unchanged selection passed all 36 cases in 4.52 seconds.
- **Local verification:** Strict read-only replay over the immutable Campaign 42 trace matched all 1,832 events, 305 submissions, 305 consequences, and 305 regenerated mechanics receipts with zero mismatch and zero environment action. It selected unsubmitted `ACTION6 (8,38)` under relation `affine-carrier-source-occlusion-relation:2ba41c99ff9899b8bb3cf492` and root plan `affine-carrier-source-occlusion-hierarchy:5567a2c902222a06ad64c3f8`, retained every hierarchy rejection count, then cancelled with zero pending action or learner prediction. Candidate snapshot SHA-256 is `sha256:2dce45a8a9809d2954b3d420655035ae2aa1397a5889dd1f96aadbf40ea5c4ee`. Focused resource and lifecycle tests passed, the final visual-policy suite passed all 143 cases in 694.93 seconds, and Ruff lint/format, strict MyPy, Python compilation, diff hygiene, and production constant scans passed. Independent read-only review found and then verified repair of the cancellation/`UNKNOWN` P1; no P0-P2 remained. Final policy/test SHA-256 are `f282190924cc2b1fc503613abd04ab664dd71cda11c8e9e1f67c5715fe6d8c31` / `c4b0c671291962c7f1d428dc4080b6707c95cdce6c1019597f0160a26dc03458`.
- **Exact implementation and sealed-replay evidence:** Checkpoint commit `c2a08b0272f51d02d065c3ad808864755e961360`, tree `fb6ea181b8ae646557f6a431c01cf190c5a233ea`, was pushed with the required human-direction footer. A clean exact clone imported `arc3` only from that clone and replayed all 1,832 Campaign 42 trace events, 305 submissions, and 305 mechanics receipts with zero mismatch and zero environment action. It selected and cancelled only unsubmitted `ACTION6 (8,38)`, preserved every rejection count, and emitted `C:\a\arc3-b003-gate-evidence-c2a08b0-campaign43\campaign42-sealed-trace-replay-c2a08b0.json`; file/payload/replay-receipt SHA-256 are `761276e5d7fa5e4620c1e2906bb8a3d03e426dd6f66ac816532f791769b70fe4` / `acdd5690263c667c525fce6c3722e877a9f0c6f961acca12c24ee10b7ec1e3b7` / `df42d83b56c76871f4f471a496ba32bcbb4e9634739553bba700e94589c1e25f`. The first exact replay invocation failed closed with exit code 1 and no receipt because its requested output was inside the source clone; moving only the output to a sibling evidence root repaired the invocation. Git also reported the known non-fatal inability to delete stale worktree metadata `.git/worktrees/arc3-b003-replay-a1f85dbb` after the checkpoint commit; commit, tree, push, and source cleanliness were independently reverified, and no destructive cleanup was attempted.
- **Clean-source and static-integrity evidence:** The exact clean source collected 122 integrity, action-semantics, and production-reachability cases; 119 passed in 31.38 seconds and three documented Windows/POSIX/symlink platform cases skipped. Manifest-bound integrity passed with zero findings (file/receipt SHA-256 `6cf4614b392762b4740e6942a28fe66d2aa1ecdb6802f5387ae7905277f99b97` / `a2344e39f85fc015842fb61f48c0cd8c0541f69428891b6ba9128a201cac8d43`). Package-only integrity returned `package_only_passed=true`, zero findings, and the expected `NOT_EVALUATED_PUBLIC_IDENTIFIERS` full-scope boundary (file/receipt `aa7394d909390029682b34eea55d530773f2a03c86b2ec926e9d68c2f79fb866` / `a0b15fa2a572ad5c697e25e4ceac2c91530c7774842a9c30db6df2e157253e25`). Action semantics passed with zero findings (file/receipt `4615ff438676d992f09e6cbbfdda78bf72443806939c1fe31bf6929d3a2c965d` / `7b7ca339652c2a124f52c2f80c9f67fd7bee69352d4f3574ce1ad6df4f51c557`). The source clone remained exactly clean at the same commit and tree. These gates authorize no environment action by themselves; the ledger-bound exact head, detached read-only launch audit, and all five hosted workflows/nine jobs remain required before Campaign 43 play.
- **Authority and consequence:** This is candidate-only local evidence. No Campaign 43 official action, public exposure event, holdout access, terms acceptance, credential use, spending, submission, merge, release, external message, or target-game source inspection occurred. Completion remains false at official `NOT_FINISHED`, `4/6`, zero observed `WIN`; replay and tests cannot substitute for `GameState.WIN`.
- **Reopening condition:** Any historical receipt changes; the exact token-bound relation, root plan, context, meter trajectory, setup/crossed counts, protected rasters, or cancellation lifecycle differs; any clean-source, integrity, package, action-semantics, launch-audit, or hosted gate fails; or authorized official play returns progress, `GAME_OVER`, a narrower residual, or `WIN`.

## D-003-0051 - Preserve the exact fe0484f hosted test-interface and package-timeout failures and apply bounded repairs

- **Date:** 2026-08-28
- **Stage:** 10 and exact-head launch gate
- **Status:** ADOPTED; FAILED HOSTED GATE PRESERVED; TEST-ONLY AND PACKAGE-TIMEOUT REPAIRS PASSED LOCALLY; CORRECTED EXACT HEAD AND GATES PENDING
- **Source identity:** Ledger-bound candidate `fe0484feb36812e66f46e1e97b7969a5cfa21779`, tree `7ab28a2df958e50eb30c887d9ce4be821c66d734`.
- **Hosted failure evidence:** Both push and pull-request Ubuntu CI jobs and both Windows CI jobs failed the same single test after otherwise completing the full unchanged suite. Jobs `98822910653` / `98822898520` each report one failure, 1,834 passes, and ten skips in 2,458.81 / 3,586.54 seconds. Jobs `98822910466` / `98822898798` each report one failure, 1,824 passes, and twenty skips in 5,304.93 / 5,266.43 seconds. Every failure is `tests/unit/test_visual_causal_child_isolation.py::test_child_isolation_rejects_only_the_failed_layout_after_sibling_displacement`, where the direct test hook called `_begin_reset_epoch()` without its newly required `observation`. Downloaded log SHA-256 are `ee479bbba7d49f2abf2ce8bbc021ad27d6844dc825cecea60859fd5fb4b43a3f`, `5de1aa5f07bac965672b132c42358841fb9bb8345109cc06a8b85f3e200c385d`, `1e56fffa02d9fe8483324c205ee0acd49effad1606c06f989b6bc5a36a3bb5e3`, and `5bac5a49f940ddcbfcce3e1c66810645496f8b3f999cdcceae5b18d56c56dd2c` respectively under `C:\a\arc3-b003-ci-failures-fe0484f`.
- **Package consequence:** Ubuntu push/PR package jobs `98833518574` / `98833696036` inherited the same test failure and returned `FAILED_MECHANISM` after 2,416.844977073 / 3,088.031914344 seconds; log SHA-256 are `51d69f43011bc3c70b9e499e830051d1111855e1cbf3164735aeaa1bf417500d` / `80ce2631d0f88e9291e286e07ac02dfe52e9211685807c786cd8fe2d0a5c9fa3`, and receipt file/canonical hashes are `12b9d37219e8b1ea205f64a409bfefcc5bd443ea429dfef54bc39c56b4e4e612` / `sha256:a502e9207301e631da87d85124480c85d15432b75a52d7bec600c1fa781c9058` and `042b7f95c723f12a53bba250828846a57de6f733eb57570dd120da4376722b94` / `sha256:e6b9df165aa4b712e1e96f04b1a55b00c8cab99e20397a79fe8e08d1ad714f1f`. Independently, Windows push/PR jobs `98833518405` / `98833696264` reached the exact 3,900-second guarded-suite ceiling near the test tail and returned `FAILED_INFRASTRUCTURE` after 3,900.0121631 / 3,900.0015037 seconds with successful pipe drain, process-tree cleanup, and zero remaining PIDs. Their log hashes are `7077b25c9ccf850a17f5bbcbc74af367673cb9c7969d27fee4fca98a2a626da5` / `91e175da458f5b7b26c259833b4d25b972cfbae3e0a70f034d25d0efc35054b3`; receipt file/canonical hashes are `e13f9a0fd1012338c962425aa0db74fe478090f55bdaaeb7c1bcd80ec00d347f` / `sha256:1fed7c7d11d247e65fe7c17c3a54cf4ee42ef88c42c1fb25d27d221712726120` and `7c615fff8153b95f1dd35526923e65c716f05a3cb735b8fc12fcdbe54fb21e16` / `sha256:055f58b6e8c11484cf7c2493a4c7c962d6c2fe6ca650501317b99a4f63c1e41e`. All four receipts preserve `private_kaggle_surfaces=BLOCKED_EXTERNAL` and `sealed_artifact_set.complete=false`; none authorizes play. Build 002 cold-start job `98822857258` passed.
- **Smallest repair:** Change only the missed direct test hook to `_begin_reset_epoch(displaced)`. `displaced` is the current synthetic observation and supplies the frame required to seed the episode resource baseline. All six other production/test invocations already pass an `Observation`; production learner bytes are unchanged. Separately raise only `package-safe-test-suite` from 3,900 to 4,200 seconds and only its outer package workflow from 75 to 80 minutes, retaining the exact 600-second nominal reserve. Preserve the full selection, boundary policy, classifications, uploads, and unchanged 90-minute ordinary CI ceiling.
- **Local verification:** The exact failed test passed 1/1 in 8.05 seconds. The complete child-isolation module passed 38/38 in 153.78 seconds, and the combined visual-policy plus child-isolation selection passed 181/181 in 839.24 seconds. The final timeout-plan bytes passed all nineteen focused verifier/workflow tests in 15.08 seconds. The fifteen-node push-tail selection completed 15/15 in 130.02 seconds; the one PR-only preceding Campaign 43 lifecycle node passed in 78.94 seconds, putting the locally observed worst remaining PR tail near 209 seconds and leaving about 91 seconds inside the bounded 300-second increase. This supports the smallest increment but does not guarantee hosted passage. Ruff lint/format, strict MyPy for the verifier, Python compilation, run-state JSON parse, both workflow YAML parses, diff hygiene, and an exhaustive reset-epoch call-site scan passed. Independent read-only review confirmed the one test argument and the bounded 4,200/80-minute guard pair are the smallest semantically consistent repairs.
- **Authority boundary:** Exact head `fe0484f` failed its hosted launch gate and authorizes no Campaign 43 environment action. The intended official run root remains absent; exposure remains 81 development events and zero holdout events. No terms, credentials, holdout, spending, upload, submission, merge, release, or external message occurred.
- **Reopening condition:** Any focused, replay, source, integrity, package, action-semantics, detached-audit, or hosted gate fails at the corrected exact head; the unchanged guarded suite still times out; or authorized official play returns progress, `GAME_OVER`, a narrower residual, or `WIN`.

## D-003-0052 - Preserve the exact c9b221c pull-request Windows CI cancellation and raise only its outer allowance

- **Date:** 2026-08-28
- **Stage:** 10 and exact-head launch gate
- **Status:** ADOPTED; EXACT C9B221C FAILED_INFRASTRUCTURE PRESERVED; 95-MINUTE REPAIR PASSED LOCALLY; NEW EXACT HEAD AND HOSTED GATES PENDING
- **Source identity:** Corrected Campaign 43 evidence head `c9b221c509d8543ff6a96eda6f06af8329c34014`, tree `299551af56429d830f4faa5c2b11deb2897d6894`.
- **Hosted gate evidence:** Exact `c9b221c` passed four of five required workflows and eight of nine jobs. Push package workflow `33172117150` passed Windows job `98851550454` in 1:10:49 and Ubuntu job `98851550652` in 0:53:22. Push ARC3 CI `33172117151` passed Windows job `98851550389` in 1:28:20 and Ubuntu job `98851550686` in 1:00:38. Pull-request package workflow `33172120779` passed Windows job `98851562187` in 1:09:48 and Ubuntu job `98851562443` in 0:53:03. Build 002 cold-start workflow `33172120776` passed job `98851562088` in 0:00:41. Pull-request ARC3 CI `33172120773` passed Ubuntu job `98851562419` in 0:53:53 but its Windows job `98851562553` was cancelled at the configured 90-minute outer job limit after 1:30:09. No job was rerun, manually cancelled, or relabelled.
- **Failure classification:** Windows pull-request coverage reached 97 percent and reported 1,810 passes and 20 skips in 5,356.06 seconds before the runner injected `KeyboardInterrupt` in coverage collection; 15 tests, the coverage receipt, and runtime doctor remained unfinished, with no assertion failure. Its exact push sibling completed the unchanged command with 1,825 passes and 20 skips in 5,245.73 seconds, emitted 80 percent coverage, passed runtime doctor, and completed the whole job in 5,300 seconds. The cancelled whole job consumed 5,409 seconds. This is immutable `FAILED_INFRASTRUCTURE` evidence and a failed launch gate, not a mechanism failure or near-pass authorization.
- **Smallest repair:** Raise only `.github/workflows/ci.yml` `timeout-minutes` from 90 to 95 and update only its pinned regression expectation. Preserve the full test command and selection, coverage collection, runtime doctor, both platforms, fail-fast behavior, package workflow and its 4,200-second/80-minute guard, evidence uploads, learner bytes, and gameplay logic. This explicitly supersedes D-003-0051's historical decision to leave ordinary CI at 90 minutes; that prior sentence remains preserved as the decision applicable before this new exact-hosted evidence.
- **Why 95 minutes:** Five additional minutes give 300 seconds beyond the failed ceiling and 400 seconds over the exact passing sibling's 5,300-second whole-job duration. The measured remaining 15-test local tail was 130.02 seconds, exact hosted job-to-job variance was at least 109 seconds, and passing post-test/doctor/cleanup consumed about five seconds. Their bounded 244.02-second allowance leaves about 55.98 seconds inside the increment. This is a defensible allowance, not proof; only a new exact hosted pass resolves the burden.
- **Local verification:** The focused timeout-contract regression passed 1/1 with 18 deselected in 8.27 seconds under Python 3.12.14. The first complete-module attempt passed 13 cases and produced six setup errors, all because pytest could not scan the shared host temp root (`WinError 5`); no assertion failed. The unchanged module rerun with an explicit isolated `C:\a` base temp passed all 19 cases in 13.13 seconds. Ruff lint and formatting passed for the changed test, JSON and workflow YAML parsed, and `git diff --check` passed. The initial `uv` invocation also failed before collection because `uv` was absent from the live PowerShell PATH; using the already verified detached-clone Python environment repaired only the invocation. Both infrastructure failures remain preserved and changed no source or test semantics.
- **Authority boundary:** No Campaign 43 official action was issued. The intended run root remains absent and exposure remains 81 development events and zero holdout events. No target source, holdout, terms, credentials, money, upload, submission, merge, release, or external representation was used. The new timeout cannot substitute for exact hosted passage or authoritative `GameState.WIN`.
- **Reopening condition:** The new exact head changes learner/gameplay bytes; replay, integrity, package, action-semantics, detached launch audit, or any hosted job fails; any job reaches 95 minutes; the full selection, coverage, doctor, matrix, or package boundary changes; or authorized official play returns progress, `GAME_OVER`, a narrower residual, or `WIN`.

## D-003-0053 - Preserve the exact fc27f27 pull-request Windows package timeout and raise only its guarded and outer ceilings

- **Date:** 2026-08-28
- **Stage:** 10 and exact-head launch gate
- **Status:** ADOPTED; EXACT FC27F27 FAILED_INFRASTRUCTURE PRESERVED; 4,500-SECOND/85-MINUTE REPAIR PASSED LOCALLY; NEW EXACT HEAD AND GATES PENDING; OFFICIAL PLAY PAUSED
- **Source identity:** Campaign 43 timeout-repair evidence head `fc27f27695511136cc2eaabf826125c6402e5042`, tree `b4088f30abd27b0b81c7a4d1dc2e814fae603bec`.
- **Exact pre-hosted evidence:** Strict Campaign 42 replay matched all 1,832 events, 305 submissions, and 305 regenerated mechanics receipts, selected and cancelled only `ACTION6 (8,38)`, and issued zero environment actions; file/payload/receipt SHA-256 are `76625f8b9e52c0ca71e5355859e503d06343f07f9907139ef54e4aaf7dd1d436` / `1441983265db569975246d1591821085ba2cd0980c39553e2c32f20bb9659c36` / `458901f23f59ee81833019ccaf707b0ad160d2ddfbaa59308ac9921318980a3f`. The clean exact source passed 119 of 122 selected cases with three expected platform skips. Manifest-bound integrity passed with file/receipt hashes `43c624aaf4ce60f7fe320b2eb11c7646f1d3e8cb3628d4e41d94aad1d2325d26` / `a2f49e22716d810c850490afa3ce12123ab8d1a8fe0d375eb107a09ba2c5f53c`; package-only integrity returned `package_only_passed=true` with file/receipt hashes `64f368d5de0f3626cd5dfc29f3bfb77ad054c1b511096a1b85e76f0569a38445` / `0ec837d712eee086ebf202917f13bf187c67d56895d6a22664a39f1cb51cfb15`; action semantics passed with file/receipt hashes `4615ff438676d992f09e6cbbfdda78bf72443806939c1fe31bf6929d3a2c965d` / `7b7ca339652c2a124f52c2f80c9f67fd7bee69352d4f3574ce1ad6df4f51c557`. The detached prelaunch audit passed with file/receipt hashes `65d01e7a81a2e6e8fe5990ee8c5f6c381b9e273b72b3ceccbadb38e9b1fe460b` / `df12722c1773f5d9ce9718088acb4b7e1bf6f2390106670c7da79f75a47c8db4`.
- **Hosted gate evidence:** Exact `fc27f27` passed four of five required workflows and eight of nine jobs. Package push run `33179880547` passed Ubuntu/Windows jobs `98878095610` / `98878095941`; ARC3 CI push run `33179880580` passed Ubuntu/Windows jobs `98878095950` / `98878096188`; Build 002 cold-start run `33179883758` passed job `98878107776`; ARC3 CI pull-request run `33179883785` passed Windows/Ubuntu jobs `98878107880` / `98878108208`, confirming the 95-minute D-003-0052 repair. Package pull-request run `33179883751` passed Ubuntu job `98878107477`, but Windows job `98878107642` failed at the internal 4,200-second package-safe-suite ceiling. No job was rerun, manually cancelled, or relabelled.
- **Failure classification:** Windows package job `98878107642` reached `4,200.0349605` verifier seconds after emitting 1,070 pass and three skip glyphs, leaving 15 of 1,088 outcomes, with empty stderr and no assertion failure. Timeout handling drained both pipes, cleaned the process tree, and left zero remaining PIDs. The overall receipt is immutable `FAILED_INFRASTRUCTURE`; its derivative guard-validation `FAILED_MECHANISM` and `sealed_artifact_set.complete=false` are consequences of that timeout and are not package success. The extracted receipt file/canonical SHA-256 are `4ddb7bbed1681773c9b1c5261aa3079e5d3f28dd8d9ca3401f1c0fdbad272854` / `46eaec2940a21935b6a55a3bb267bc171a168900dd52c7af094307c1d52d69cc`; timed-test stdout SHA-256 is `f21de013b2120bd9543dd68e664f7ee1ab6a4f1ad7c9d11808c4f58721bcec7c`; curated-evidence file/canonical SHA-256 are `a579eee1c79816e85d10c2feb4bd015bfbc602c5ab103be197a0e9015a85cad0` / `0703528b20d2944e08bdff5bc6d64781800118fe86e0b550bd85a0593acb4211`; uploaded artifact `9691637396` has digest `e6a3e5b110b400c2c50dae17017bbf9edfa54b3c57e74f5c84ccc9c3e1590738`. The exact push sibling passed the same guarded selection in `3,649.7352024` seconds with 1,085 passes and three expected skips. Prior exact `c9b221c` Windows passes took `4,077.2512087` and `4,134.8475294` seconds, leaving only 122.75 and 65.15 seconds under the old ceiling.
- **Smallest repair:** Raise only `package-safe-test-suite` from 4,200 to 4,500 seconds and only `.github/workflows/build001-package-only.yml` from 80 to 85 minutes, updating their pinned regression expectations. Preserve the exact 600-second nominal outer reserve, complete test selection, boundary policy, process supervision, classifications, evidence uploads, both platforms, learner bytes, gameplay logic, full-profile 2,400-second timeout, and ordinary CI's now-verified 95-minute ceiling.
- **Why 4,500/85:** A 300-second increment is the smallest conventional five-minute increase beyond the exact internal timeout. The exact-prefix near-tail output, clean teardown, passing exact sibling, and marginal prior exact passes justify one bounded retry but do not prove that 4,500 seconds is sufficient. Only a new exact-head Windows pull-request package pass resolves this burden.
- **Local verification:** The first complete-module attempt passed 18 cases and reported one `candidate-mutated-during-scan` failure when the bounded integrity reader observed live OneDrive bytes changing; no timeout assertion failed. The unchanged module rerun with a fresh isolated `C:\a` base temp passed all 19 cases in 13.45 seconds. Ruff lint and formatting, strict MyPy for the verifier, both workflow YAML parses, run-state JSON parse, and diff hygiene passed. The live-worktree scanner race remains preserved as `FAILED_INFRASTRUCTURE` and changed no test or source semantics.
- **Authority boundary:** No Campaign 43 official action was issued. The intended run root remains absent and exposure remains 81 development events and zero holdout events. No target source, holdout, terms, credentials, money, upload, submission, merge, release, or external representation was used. This infrastructure repair cannot substitute for authoritative `GameState.WIN`.
- **Reopening condition:** The repair changes test selection, boundary semantics, learner/gameplay bytes, full-profile timeout, ordinary CI, or the 600-second reserve; replay, integrity, action-semantics, detached launch audit, or any hosted job fails; a package job reaches 4,500 seconds or its workflow reaches 85 minutes; or authorized official play returns progress, `GAME_OVER`, a narrower residual, or `WIN`.

## D-003-0054 - Preserve Campaign 43 as a verified crossed-lineage delivery failure and isolate the post-deposit mediator

- **Date:** 2026-08-28
- **Stage:** 10 and target-game continuation
- **Status:** ADOPTED; VERIFIED OFFICIAL CAMPAIGN 43 FAILED_MECHANISM PRESERVED; CAMPAIGN 44 ONE-SHOT MEDIATOR DISCRIMINATOR PENDING
- **Source identity:** Campaign 43 exact commit `766c84e1306c89f1c13b012229b4a875998ccda1`, tree `845b0ea34ec735dece2e6323597946dfb09db5f6`.
- **Exact launch gates:** Clean exact-source replay matched all 1,832 Campaign 42 events, 305 submissions, and 305 regenerated mechanics receipts, selected and cancelled the next action, and issued zero environment actions; file/payload/receipt SHA-256 are `8cee7cd70b5ef1b24922ab979c18db408d8dd3c6f71af2a8179348a812c2e18d` / `8418445a360af300df0528ac3a73dd0f7c380b7d9fcacf253c7aae04d1b2379b` / `9e42bc1397ee641b0f63e53f896048589a4f95d5c238b92f4742563a3e25ae19`. The 122-case clean-source selection returned 119 passes and three expected platform skips. Manifest-bound integrity, package-only integrity, and action semantics passed with file/receipt hashes `12397fc2379b6b7cb37caab82de34ed4ac6cea36b4318572c9409c916450e03d` / `c1a142378020ba38a01c8b2c993177126228c5ba0bf0e290a544d8540e7571b1`, `632d2e5bd427fe2e2bbbde08dd482cc06d9d590b0b092a4b5bb7008e8ef7d961` / `902cf0a789bd1861d8ad61250d0374872f6127073fbd4139ecb400afa25f8cf4`, and `4615ff438676d992f09e6cbbfdda78bf72443806939c1fe31bf6929d3a2c965d` / `7b7ca339652c2a124f52c2f80c9f67fd7bee69352d4f3574ce1ad6df4f51c557`. The sanitized neutral-CWD launch audit passed with file/receipt hashes `236950890d894a92db3d5df57ab987b54de9ef82e2750985f421c6da52122fc8` / `da5535528971544987138b3a4d4fdba00d41facc4b3de4e411f9d32ff67e9517`, 81 development exposures, zero holdout exposure, zero credential/control names, and zero environment actions.
- **Hosted gate:** All five workflows and all nine jobs passed without rerun or cancellation: package push `33187861251`, ARC3 CI push `33187861496`, package pull request `33187866108`, ARC3 CI pull request `33187866284`, and Build 002 cold start `33187866243`.
- **Official evidence:** Verified root `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-766c84e-campaign43` contains 509 declared artifacts, one run, and zero verification errors. The run ended `FAILED_MECHANISM` with claim `MECHANISM_NOT_OBSERVED`: raw final `NOT_FINISHED`, score boundary `GAME_OVER`, `levels_completed=4/6`, 366 submissions comprising 361 `ACTION6` and five legal `RESET`s, 361 `NOT_FINISHED` and five `GAME_OVER` consequences, and zero `WIN`. Recording, replay, and run evidence verify; `receipt_complete=false` correctly preserves the absent authoritative completion. Run-receipt file/canonical hashes are `08cbb0c189894a2d9b7a70915f3cef49d6b9725029883a91f6ffb838c16dca39` / `sha256:501cf12ed8df48cba7887fa2a3d52b31f2ee6c623332bd78b49430199896cca0`; completion-receipt hash is `sha256:c33134ba2eeb517dde01a50a708e82c6ff61d5ecde3ae93c21d80e7eb914bef7`.
- **Mechanism decision:** At episode `ACTION6` ordinal 54, official submission 359, the recomputed crossed-lineage delivery returned `NOT_FINISHED` with the exact predicted protected body `sha256:fffe76fb1431850bd702ef8c2caad70d50a33f99fcf4e1c148a4609fb02bd641`. Reject only the hypothesis that this exact handoff-preserving crossed-lineage paired-source delivery is sufficient to complete the level. Retain the supported movement, lineage, protected-raster projection, object-count, and edge-resource mechanics.
- **Smallest residual:** The returned frame changed the unique former hollow target into a mediator at the same observation-derived center and footprint: before, color 14, area 12, one target and zero mediators; after, color 14, area 19, zero targets and one mediator. No recorded submission tested that mediator. Campaign 44 may, only after the exact action-54 certificate, derive the sole mediator from the returned observation and submit one `ACTION6` at its current rounded center. Production may store no game ID, campaign, level, palette, or coordinate constant. `WIN` or level progress accepts the discriminator; `NOT_FINISHED` rejects only mediator access/activation sufficiency; ambiguity, resource mismatch, unexpected raster, or `GAME_OVER` fails closed. Do not repeat or enumerate other interactions.
- **Diagnosis receipt:** `C:\a\arc3-b003-gate-evidence-766c84e-campaign43-timeout4500\campaign43-postrun-diagnosis-766c84e.json`; file/canonical hashes `ab1de22bb6a0063321882772168467ea31e3927b7a6ec4153de4f70ef0d964b9` / `sha256:9c90bf73ad293161083c4773a992c650eb5deef3feba82fcd16232b195ba6dbe`.
- **Authority boundary:** Exposure advanced from 81 to 83 development events with zero holdout events; file SHA-256 is `1c6b1156241609db1f2332d5f72cfcac5b0d18e16106cb57e1dba71be3c4a380`. No credential value was inspected or used, no target source or holdout was inspected, and no terms, spending, submission, merge, release, or external representation occurred. Completion remains false until returned `GameState.WIN`.
- **Reopening condition:** Any Campaign 43 receipt or protected-body certificate fails verification; Campaign 44 cannot derive exactly one mediator from the returned observation; replay, integrity, package, action-semantics, sanitized launch, or hosted gates fail at its exact head; or authorized official play returns progress, `GAME_OVER`, a narrower residual, or `WIN`.

## D-003-0055 - Freeze the one-shot post-deposit mediator discriminator behind an exact reopening-prefix contract

- **Date:** 2026-08-28
- **Stage:** 10 and target-game continuation
- **Status:** ADOPTED; LOCAL IMPLEMENTATION CHECKPOINT COMMITTED; LEDGER-BOUND EXACT HEAD, CLEAN-SOURCE GATES, HOSTED GATES, AND OFFICIAL CONSEQUENCE PENDING
- **Implementation identity:** Checkpoint `a688ec0a717ad867a96e349570673e0ca8372263`, tree `368916da16c1a91514ca655a2a271c3f63f9907d`. Production policy, exact replay engine, CLI, focused regressions, and the preserved Campaign 43 ledger state are committed with the required human-direction footer. Git emitted the known non-fatal permission warning while deleting stale metadata for `.git/worktrees/arc3-b003-replay-a1f85dbb`; commit identity and tracked-worktree cleanliness were independently verified, and no destructive cleanup was attempted.
- **Policy decision:** Only after the exact crossed-delivery consequence and resource lineage, derive a unique former-target mediator from the returned observation and stage one `ACTION6` at its current center. A selected but cancelled action earns no attempt. Any submitted access action records a non-epistemic attempted-hypothesis key that persists across same-level `RESET` and prevents reconstruction of the same discriminator. Only an exact `NOT_FINISHED` consequence adds the epistemic rejection key; `GAME_OVER`, `UNKNOWN`, or resource-lineage mismatch fail closed without claiming access insufficiency. Level progress clears the level-local key, `WIN` terminates successfully, and inverse recovery is reported active only when inverse actions are actually queued. No game ID, campaign, level, palette, or coordinate constant is present in production.
- **Replay boundary:** The sealed reopening receipt verifies immutable integrity, action-state structure, and receipt-consequence linkage for all 2,198 Campaign 43 events and 366 action cycles; exactly reproduces current-policy actions and mechanics receipts through submission 358; binds the historical submitted action and official consequence at submission 359; permits only the declared residual-field revision there; and checks the seven historical suffix cycles only for structure and linkage. It explicitly does not claim reopened-policy action or mechanics-receipt semantic equivalence for submissions 360-366. The staged and cancelled candidate must differ from the historical suffix and have a nonempty plan signature beginning `affine-crossed-post-deposit-mediator-access:`.
- **Local verification:** The exact Campaign 43 resource/lifecycle regression passed in 805.27 seconds; the target-retained inverse fallback passed in 631.58 seconds; and the full unchanged visual-policy module passed all 144 tests in 4,668.02 seconds. The replay/CLI selection passed all 59 tests in 23.47 seconds. Ruff format and lint, strict MyPy over all three changed runtime modules, compilation, JSON parsing, and diff hygiene passed. Source/test SHA-256 are `825482afb3dbc4618c2181ae1773803ec3a4f8667aa3dc0f6723f10fd0dda5c6`, `0ecfd198be5f41818802b27f094dccdf5817ba6a2ec03f6139dd217344304133`, `48e1f53eabf05fcc9a75cc4cd088b3070139b8834e40444e8b79f355a40386a7`, `eade6741185f66b5fcd451bd8d6b20df2910c0f7fa45ca4be5dd924efacc86c0`, `8627126942640b5ed7daf6517541b82df05df28e3d34c901f4ccf9f5ea7e2f04`, and `c139522c5c2345addafe63d15a0d8ecd21345085cd7b5f56af84a41ea04bfd82` for policy, replay engine, CLI, and their three test modules respectively. Three independent read-only audits found no remaining P0-P2 issue after repair.
- **Preserved infrastructure failures:** The first final replay/CLI attempt reached three passes but 56 setup errors because Windows denied enumeration of pytest's shared default temp root. The unchanged selection passed from a fresh explicit `C:\a` basetemp. A live OneDrive scan separately returned 327 `candidate-mutated-during-scan` findings and therefore is not a secret-scan pass, although its 119-file production-policy scan had zero findings. A byte-stable `HEAD` plus nine-file overlay at `C:\a\arc3-b003-c44-precommit-snapshot-20260828-1715` then scanned 564 tracked candidates with zero secret findings and the same 119 policy files with zero findings; the sole live-only candidate was the owner's pre-existing untracked pytest fixture and remained untouched and unstaged.
- **Authority boundary:** This is local candidate evidence only. Replay selected and cancelled an unsubmitted action and issued zero environment actions. No public exposure event, official action, holdout access, credential use, terms acceptance, spending, upload, submission, merge, release, external message, or target-game source inspection occurred. Completion remains false at official `NOT_FINISHED`, `4/6`, zero observed `WIN`.
- **Reopening condition:** A clean exact-head replay fails any integrity, prefix, boundary, residual-only, suffix-structure, or plan-family check; clean-source integrity, package, action-semantics, sanitized launch, or any hosted job fails; or authorized Campaign 44 play returns progress, `GAME_OVER`, a narrower residual, or `WIN`.

## D-003-0056 - Preserve the exact a8327c1 Ubuntu package boundary failure and repair only its test fixture

- **Date:** 2026-08-28
- **Stage:** 10 and exact-head launch gate
- **Status:** ADOPTED; EXACT HOSTED FAILURE PRESERVED; TWO-LINE TEST-ONLY REPAIR CHECKPOINT COMMITTED; LEDGER-BOUND EXACT HEAD AND ALL GATES PENDING
- **Failed source identity:** Ledger-bound Campaign 44 candidate `a8327c194b41633e21b30c6217541fd393553b4b`, tree `f2fd646842d9cf569e6cd5c8866910f2e77e6010`.
- **Exact local gate evidence:** From a clean detached clone, sealed Campaign 43 reopening-prefix replay verified all 2,198 events and 366 action cycles, matched current-policy actions and mechanics receipts through submission 358, bound the historical action and official `NOT_FINISHED` consequence at submission 359, permitted only the declared residual-field revision there, and selected then cancelled only unsubmitted `ACTION6 (53,28)` under `affine-crossed-post-deposit-mediator-access:623e0a7f688fa1b555f39b40`. The receipt issued zero environment actions. The exact 122-case clean-source selection passed 119 with three expected platform skips; manifest-bound integrity, package-only integrity, action semantics, and the sanitized Campaign 44 launch audit passed their declared scopes with zero findings or actions. These gates did not override the later hosted failure and did not authorize play.
- **Hosted failure evidence:** All five required workflows and all nine jobs reached terminal state: three workflows and seven jobs passed; both Ubuntu package-only jobs failed. Push run/job `33211502102` / `98985547374` and pull-request run/job `33211504595` / `98985555183` each completed all 1,107 guarded tests with `pytest_exit_code=0`, no timeout, clean source revalidation, successful process cleanup, and zero remaining PIDs, but the guard correctly returned `FAILED_BOUNDARY` after recording exactly `{"event":"subprocess.Popen","path":"child-process"}`. The byte-identical guard file and receipt hashes are `3cd9dd52298b932de48d3501de48669722710e134e742da0c2d069c669da660d` and `sha256:d9151e4524e0d776179154a9785224e27d2e58d705d0ef7ebe60cd309b7cde3a`. Push/PR release receipt file hashes are `83617262002a17d5e7f3c6e5da7a25f6f3f03d71d823d8a2567dfed9ef8282c7` / `da9a41ee166f34a7d44243511791fd6c61410ab05f83de91b5ee75fbb50c29ea`. Both Windows package siblings passed the same 1,107-item selection with zero attempts; both ordinary CI matrices and Build 002 cold start passed. The final snapshot is `C:\a\arc3-b003-hosted-a8327c1\final-hosted-status.json`, SHA-256 `a16d9dd50abfe8f4beb27b0d692c01d9e30c8aaa083639b9a858fbb8839da368`.
- **Causal diagnosis:** The newly added receipt-contract test invokes `replay_cli.main()`. Receipt assembly calls `platform.platform()` in `scripts/replay_build003_mechanical_recording.py`; on Ubuntu, CPython falls back to `uname -p` through `subprocess.check_output`, emitting the one audited `subprocess.Popen`. The guard denies it. `platform` catches the resulting `PermissionError` as `OSError`, so pytest correctly completes while the independent boundary receipt still fails. The same test did not invoke `main()` at the prior passing head, and the Windows platform path does not require this probe.
- **Smallest repair:** In only `test_cli_main_emits_the_prefix_reopening_schema_and_count_contract`, replace the host-dependent `platform.platform()` lookup with the existing `monkeypatch` fixture returning `synthetic-platform`, and assert that exact runtime value in the emitted receipt. Do not allowlist `uname`, patch `subprocess`, weaken the audit hook, or exclude the test file. Production, guard, selector, learner, replay engine, official evaluator, workflow, and timeout bytes remain unchanged; the test stays inside the exact guarded selection.
- **Repair checkpoint identity:** Commit `8b6cb4c78e645ff8e23b6f157eec5b8b46fc3b60`, tree `e1641221158280b2838662b8b097cbc2dd5a6fbf`, contains exactly the two-line test repair plus this preserved failure decision, burden, and run-state evidence. The commit carries the required human-direction footer. Git again reported the known non-fatal permission warning while deleting stale `.git/worktrees/arc3-b003-replay-a1f85dbb` metadata; commit and tree identity were independently re-read, and no destructive cleanup was attempted.
- **Local repair verification:** The exact repaired node passed 1/1 and the full replay-CLI module passed 21/21. The module plus the two package selection/plan contract nodes passed 23/23 in 15.03 seconds. Ruff lint and format checks passed; `git diff --check` passed; `build001_test_selection` still selects the file among 111 files and excludes it from neither the boundary nor process-capable sets. The repaired test file SHA-256 is `a91024db60b77c5241b0a5c12a691c66d532e4fbb64b87c0a587e04e9a083e01`. Independent read-only review found no P0-P2 issue and verified that unrelated child-process attempts remain visible and denied.
- **Authority boundary:** Exact `a8327c1` failed its hosted gate and authorizes no Campaign 44 action. The prospective run root remains absent; exposure remains 83 development events and zero holdout events. No terms, credentials, holdout, spending, upload, submission, merge, release, external message, or official environment action occurred during diagnosis or repair. Completion remains false at official `NOT_FINISHED`, `4/6`, zero observed `WIN`.
- **Reopening condition:** The repaired test leaves the exact guarded selection, production bytes, or no-child policy; any exact-head replay, local/static/sanitized gate or hosted job fails; the Ubuntu guard records any attempt; or authorized Campaign 44 play returns progress, `GAME_OVER`, a narrower residual, or `WIN`.

## D-003-0057 - Seal Campaign 44 as a failed access discriminator and narrow to one post-access composite action

- **Date:** 2026-08-28
- **Stage:** 10 and target-game continuation
- **Status:** ADOPTED; CORRECTED CAMPAIGN 44 GATES PASSED; OFFICIAL FAILED MECHANISM SEALED; CAMPAIGN 45 LOCAL CANDIDATE VERIFIED; EXACT-HEAD GATES PENDING
- **Corrected exact-head proof:** Commit `51630b75e209a6c8c71fbb8d9231f5c382f129a2`, tree `72c90c09d77b0c5e399ec3506c8cb8a6b3b47936`, repeated the sealed Campaign 43 reopening-prefix replay and every clean-source/static/sanitized gate. Replay file/payload/receipt hashes are `bd96d509f3cb4c65fbb6fcb617a0fe2f679c02a882683f1cb5d433e03cbe8cd5` / `ed4afce781b1a66637bb3424e3fb1e482c76a2cad8eb163335ac4459bc5c24ed` / `5f86a65645c2c87893e0f89255ffc4835dc92214a3a61f22ffe83eb6b6671ee1`. Manifest-bound, package-only, action-semantics, and sanitized-launch file/receipt hashes are `470380ea12271d97d52a59597ad2aa446aafd7e2a77e63d371e7f49548b4fbf1` / `61afe0a18ed0e592163dad49773a072365cbb53eb41065be37aa90da11e3e8ff`, `d69015fb251bbae4872ed0fc7ae2d2d627b00b6cf262ca65452e18415ce351f2` / `43e04370422ce66c9cf1a7e053a7b5a6f7ab148d84d66120bc635986f36a4cae`, `4615ff438676d992f09e6cbbfdda78bf72443806939c1fe31bf6929d3a2c965d` / `7b7ca339652c2a124f52c2f80c9f67fd7bee69352d4f3574ce1ad6df4f51c557`, and `60ad4a79f88ca12b4d201cd7d08b7b4e29ca889e333e583ba9834209689effd7` / `7364ff5ed034c382adea4104d607510f5c261a0847a090b4fa1c9599fc7c9193`. All five hosted workflows and all nine jobs passed; both Ubuntu package guards recorded zero attempts. The final hosted snapshot SHA-256 is `c4696c2a98ec7be640e23070e36d0839df01028e49bdd982bb69ec19cfdf2733`.
- **Official Campaign 44 evidence:** Verified root `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-51630b7-campaign44` contains 504 declared artifacts, one run, and zero verification errors. The exact development-only run ended `FAILED_MECHANISM` with raw final `NOT_FINISHED`, score boundary `GAME_OVER`, `levels_completed=4/6`, 360 submissions comprising 356 `ACTION6` and four legal `RESET`s, four `GAME_OVER` consequences, and zero `WIN`. Run-receipt file/canonical hashes are `67e27f0518a63a72e2cb676734f9b2b86f501fe6fbf61751810322d3d6929294` / `sha256:ba1b55a102e8add206cf05561331b61548d4bc08eabea12e0b6555de0e7fb7f9`; the completion-receipt hash is `sha256:45f94aa50bb1393c530e009e80157dce15f6893c128e02ace40f9a7e20a716d4`. Recording SHA-256 is `0563ef4e0f7093c86fd102b0dd9b96e4a623c51999f255bc46afcb06a63197ae`; trace chunk, manifest-object, and tail hashes are `341aa3c999e424b6e2ea0bdd442d72813c5e206617c12994239b06de17404381`, `d6b3ee93e55c41213ec80ad2648293a0c9a29e770b93f698114703a6f78a6052`, and `73612b3d9e6f481722cba0b9be2f081855760ed39f7b38db01599b5f0e7563e5`.
- **Mechanism decision:** The exact one-shot access action on the unique post-deposit mediator returned `NOT_FINISHED`. Reject only mediator-access sufficiency. Do not reject the retained crossed-route movement, source/endpoint lineage, protected-raster projection, resource boundary, or the observation that access caused a material 51-cell transition: endpoints changed from five to four, the prior active endpoint disappeared, and the returned frame formed exactly one mediator and one target with the same rounded center and overlapping bounding boxes.
- **Smallest residual:** Only after that exact access consequence, derive the unique co-centered mediator-target composite from the returned observation and stage one `ACTION6` at its common rounded center. Bind the parent access key, exact before/after protected rasters, full object signatures, five-to-four endpoint transition, current resource action count, exhaustion boundary, level/reset lineage, and strict one-action reserve. Production stores no game ID, campaign, level, palette, or coordinate constant. The derived plan family is `affine-crossed-post-access-composite-exit:`.
- **Lifecycle boundary:** Selection followed by cancellation earns no attempt. Submission records the exact composite hypothesis as attempted across same-level reset. `WIN` or level progress terminates old-level state. Exact `NOT_FINISHED` rejects only that composite-exit sufficiency and authorizes no repeat. Ambiguity, mismatch, `UNKNOWN`, `GAME_OVER`, `NOT_PLAYED`, reset, unavailable `ACTION6`, or exhausted reserve fails closed without manufacturing a broader rejection.
- **Local candidate evidence:** After correcting only closure-message precedence, the exact Campaign 43/44 resource lifecycle regression passed 1/1 in 892.30 seconds; the full visual-policy module passed 144/144 in 4,641.66 seconds; and the combined replay/CLI selection passed 59/59 in 23.77 seconds. Strict MyPy, compilation, Ruff format/lint, JSON parsing, diff hygiene, and a production target/campaign/coordinate constant scan passed. An independent read-only audit replayed all 2,162 sealed Campaign 44 events and 360 submissions/receipts exactly, selected and cancelled only `affine-crossed-post-access-composite-exit:fa118b8ca54cc8a6a298e8e8`, observed zero environment actions, and found no P0-P2 issue. Policy, replay-engine, CLI, visual-test, and replay-CLI-test SHA-256 are `4b64a587f77609c0fc3ab8312f7f90b705498debab112684d9347cb74abb0701`, `68eb3f4b513612c45c26ad56bd0380ac78727ee31ef156609999e793c11a207f`, `a8411ab1eab013afe9b6b1a5d4dfb100d23d3b0b86b53078baafeffd58f912ef`, `f5da99efa65870f5106c5b5a7e81de4b7f1a25ed548af1c761f881237d2c52a9`, and `1368235d5b318eada5e253b2db3cec19fdbf391025854dd975f105c89bfb2116`. Exact Git-bound replay/gates, push, and hosted gates remain mandatory before play.
- **Authority boundary:** The exposure ledger now contains 85 valid development events and zero holdout events, SHA-256 `e9237a804411bc2603cdb9e8dd9d70ba1c26ed677cacc193623afef2b4fc085d`. No terms, credentials, holdout, spending, upload, submission, merge, release, external message, or target-game source inspection occurred. Campaign 44 did not complete the target; only returned `GameState.WIN` can do so.
- **Reopening condition:** Any Campaign 44 artifact, trace, or receipt identity fails verification; Campaign 45 cannot replay all 360 submissions and mechanics receipts exactly; the composite is not unique under its full certificate; any local, exact-source, sanitized, or hosted gate fails; or authorized official play returns progress, `GAME_OVER`, a narrower residual, or `WIN`.

## D-003-0058 - Bind Campaign 45 to exact sealed replay and a checked-in development-only prelaunch audit

- **Date:** 2026-08-28
- **Stage:** 10 and target-game continuation
- **Status:** ADOPTED; CAMPAIGN 45 IMPLEMENTATION AND AUDIT PRODUCER CHECKPOINTED; INTERMEDIATE EXACT REPLAY/STATIC GATES PASSED; LEDGER-BOUND EXACT, SANITIZED, AND HOSTED GATES PENDING
- **Implementation identity:** Campaign 45 production, replay, CLI, and regression checkpoint `75fdc98df11cd5c3a554f959a0ae72dcddfc0560`, tree `8c9fc0f0a86f8f7f98800705e338248a4c2cb3c5`, is pushed with the required human-direction footer. It preserves the generic `affine-crossed-post-access-composite-exit:` family and contains no target, campaign, level, palette, or coordinate constant in production.
- **Intermediate exact replay:** A clean detached clone of `75fdc98` replayed all 2,162 Campaign 44 trace events, 360 submissions, and 360 regenerated mechanics receipts; preserved 356 `ACTION6`, four `RESET`, four `GAME_OVER`, 356 `NOT_FINISHED`, raw final `NOT_FINISHED`, and `4/6`; staged and cancelled only `ACTION6 (52,28)` under `affine-crossed-post-access-composite-exit:fa118b8ca54cc8a6a298e8e8`; and issued zero environment actions. Replay file/payload/receipt hashes are `3bf4b5d2ddd1b7d1aa072d19366c0bd0e7330aabb1b876c0967429b8821c503e` / `a1f42844bc024bca2c01cb80151380e12dd8dc3cc1673828ab6ce472826d29cf` / `f36a4bac7bb04944fb82abc51cda24088a397079418d718d8e0afb659bfa164f`; the candidate snapshot hash is `b79478fa7c1a288bba1bacf5589a2c86735278154d1f3366baf93842e740b785`.
- **Preserved invocation failures:** Before the successful replay, three exact-clone invocations failed before replay and emitted no candidate receipt: one clone invocation omitted the required `-B` isolation, one supplied a campaign-only value where the parser required a complete evaluation identity, and one supplied a shortened malformed manifest digest. They are invocation-level `FAILED_INFRASTRUCTURE`, changed no trace or source byte, issued no environment action, and are not erased by the corrected command.
- **Intermediate static gates:** The exact `75fdc98` clone passed the 122-case clean-source selection with 119 passes and three expected platform skips, plus 61/61 replay/CLI cases. JUnit SHA-256 values are `b99ed406602f3fb1c9581f46817039abbdfeec1f427c8f6cc239e4ed5688d83c` and `3bd777500d1351f5141cb45ddc60a5d39b87ca0971835d962a0a525259fce82c`. Manifest-bound integrity passed with file/receipt hashes `5d5a426d1cb175a3ab96cfd35387483da0dcb6f9e5c7e00264d1e0d01d1a77d6` / `3270daa4bd2bdaef0f2eb95b0f9b6c1b9c3d31fd001871c3872762833b1a788d`; package-only integrity returned `package_only_passed=true` with `e0699565c1c82bd043058e4de43b5c89368fd488d9a9332a4d40ff8dfb6aea78` / `836f79e0a2c1e6d0437e22a7813c478293ae59706fc8f0881014a46ccf53f363`; action semantics passed with `4615ff438676d992f09e6cbbfdda78bf72443806939c1fe31bf6929d3a2c965d` / `7b7ca339652c2a124f52c2f80c9f67fd7bee69352d4f3574ce1ad6df4f51c557`.
- **Audit provenance decision:** The prior sanitized Campaign 44 receipt was valid for its exact historical launch but no reusable checked-in producer could reproduce that audit contract for a new campaign. Do not substitute an inventory-only evaluator invocation or an ad hoc unversioned wrapper. Commit `5eae0b6f3a60f337137a18a05b1a2b76520f1910`, tree `720adeeb09901e1c2a04adb2bd68ab9ec0e3b7e1`, adds `scripts/audit_build003_prelaunch.py` and twelve focused tests. The producer verifies detached clean source/import identity, pinned SDKs, parser-only development/mechanical controls, exact non-holdout cache inventory, the canonical chain-valid exposure ledger with zero holdout events, sanitized environment variable names without reading values, empty neutral working directory, absent intended run root, and an external exclusive receipt; it cannot acquire, start a session, submit an action, or make a network request. Producer/test SHA-256 are `0ed54696a7ed41bbb999575197249ae1cf3a9aa9d2482adf7deab9b1f1c87c02` and `73c18067307f9d5a14c0029531537bdec0db57859332531f42e79a6b827d3f2b`.
- **Audit implementation verification:** The twelve focused tests passed independently in 1.39 and 1.50 seconds. Ruff lint/format, strict MyPy, AST no-action-surface inspection, and diff hygiene passed. No real Campaign 45 audit receipt is claimed from the audit-producer checkpoint because the final launch identity must include this ledger decision and burden; a fresh clean detached clone of the new ledger-bound head must produce the authoritative audit receipt.
- **Authority boundary:** Both exact replay and audit work were read-only with respect to the official environment. Exposure remains 85 valid development events and zero holdout events, file SHA-256 `e9237a804411bc2603cdb9e8dd9d70ba1c26ed677cacc193623afef2b4fc085d`. No evaluator session, environment action, target source, credential value, terms acceptance, holdout access, spending, upload, submission, merge, release, or external representation occurred. Neither intermediate gate success nor the audit producer is progress or completion.
- **Reopening condition:** The new ledger-bound head fails strict Campaign 44 replay, any clean-source/static gate, the checked-in sanitized audit, or any hosted job; the audit observes a credential/control name, nonempty neutral directory, existing run root, source/import drift, cache/manifest/ledger drift, or holdout exposure; authorized official play returns progress, `GAME_OVER`, a narrower residual, or `WIN`; or any claimed completion lacks returned `GameState.WIN`.

## D-003-0059 - Preserve the exact e1f9b01 package-boundary failure and repair only its synthetic manifest fixture

- **Date:** 2026-08-29
- **Stage:** 10 and exact-head launch gate
- **Status:** ADOPTED; EXACT E1F9B01 LOCAL GATES PASSED AND HOSTED FAILURE PRESERVED; TEST-ONLY REPAIR PASSED THE LOCAL PACKAGE GUARD; NEW EXACT HEAD AND ALL GATES PENDING; OFFICIAL PLAY PAUSED
- **Source identity:** Campaign 45 ledger-bound evidence head `e1f9b01360a6bc88a4ed21d9393c5ce1d417489e`, tree `034054eacf66d3c19ecd8d12ca2767d5a97fbfd3`.
- **Exact replay and launch audit:** A clean detached clone replayed all 2,162 Campaign 44 events, 360 submissions, and 360 regenerated mechanics receipts; preserved 356 `ACTION6`, four `RESET`, four `GAME_OVER`, 356 `NOT_FINISHED`, raw final `NOT_FINISHED`, and `4/6`; selected and cancelled only `ACTION6 (52,28)` under `affine-crossed-post-access-composite-exit:fa118b8ca54cc8a6a298e8e8`; and issued zero environment actions. Replay file/payload/receipt SHA-256 are `6412e046c0c320655138798eb30f11cc15c668fcb84388685d68e32be58084ac` / `9e42433fc4f56c2c0d91d55f37b8d3849defb5a0accca7432a1ccc29cdba6bf9` / `3a3f2e23fcb383e546499befaef1c743a3e04040814f01916dc9cb29e2b11a16`. The checked-in audit ran from a fresh empty neutral directory under isolated Python and a scrubbed control/credential environment, bound the exact source, development-only target, 85-event chain-valid exposure ledger with zero holdout, 15-entry non-holdout cache, absent prospective run root, parser controls, and exact intended launch arguments, and issued zero environment actions. Its file/receipt SHA-256 are `26211ed7e88d3c40b7211e9a48667314e02dd6afb476fba20ad6703b67521e03` / `3a7bc636a13d24e7da7c54b4d63d39ccd0a05fba219ba210b6423e9d02a8a2d7`.
- **Exact local gates:** The clean source passed 119 of 122 focused cases with three expected platform skips and 61/61 replay/CLI cases. JUnit SHA-256 are `1293ed6204e140eb1c973fb1bb3ef51b80cfd6f898fa34c245f69dc34fa2c23f` and `624fcb78e4f9b2a160473b674e8fbc1b848142266115144d8ebef996c2207a46`. Manifest-bound integrity passed with file/canonical hashes `614870890a2aef38ec10f52a28658cc7c26bb6a06bed9e1a9a2e7c15cd3d2673` / `de659bfbc88e42d5a74a68db5fc3662468ca7407c7566cc33dd55da9dfd9f78f`; package-only integrity returned `package_only_passed=true` with `ef057957fdff86d5b0a51e08cbe2e6c0e21c5fdf42aaead7425136376bc784e1` / `65239888628f3ef53c6843a9efe9ebfd6407fa04252e0d1d29e5432d173b08d6`; action semantics passed with `4615ff438676d992f09e6cbbfdda78bf72443806939c1fe31bf6929d3a2c965d` / `7b7ca339652c2a124f52c2f80c9f67fd7bee69352d4f3574ce1ad6df4f51c557`.
- **Hosted failure:** All five required workflow runs and nine jobs are terminal. ARC3 CI push and pull request plus Build 002 cold start passed, but both package-only workflows failed on both platforms, for three of five workflows and five of nine jobs passed. Each failing job completed the guarded command without timeout but denied four identical `open` attempts on `docs/evaluation/public-game-partitions.v0.1.json`; the guard correctly returned `FAILED_BOUNDARY`, the release verifier returned `FAILED_MECHANISM`, and the source projection remained exact. Run IDs are `33232012141`, `33232012125`, `33232012109`, `33232010744`, and `33232010783`; failed job IDs are `99047679681`, `99047679806`, `99049614682`, and `99049614875`. The consolidated immutable snapshot is `C:\a\arc3-b003-hosted-e1f9b01\final-hosted-status.json`, SHA-256 `b415d6271e1d2daa65302892af70133cfe7abe1ee140c58ce48e5d859c7ffa86`.
- **Failure cause:** The production audit and package guard behaved as designed. The synthetic producer test itself hashed and statted the protected checked-in manifest, so four selected test cases crossed the package-only path boundary. This is a test-fixture defect and launch-gating failure, not an official gameplay consequence and not evidence against the learner mechanism.
- **Smallest repair:** Keep the evaluator `--manifest` argument bound to the exact checked-in manifest path, but give the synthetic test a fixed fake manifest digest and byte length and intercept only `_require_stable_file` for that exact manifest path while asserting its label, length, digest, and two producer checks. Delegate every other stability check to production behavior. Do not exclude the test, allowlist the protected path, weaken the guard, or alter production audit, learner, replay, CLI, workflow, timeout, or gameplay bytes.
- **Local repair evidence:** The changed test file SHA-256 is `abf46c626471faa9e5ea28a4815156826240d638b3346058e3ac5fc65d5e1375`. All twelve audit tests passed normally; the four formerly failing nodes passed under the unchanged package guard with zero boundary attempts and receipt `sha256:3d1e69aff78fbdc5a8e813593f3bc35639a66792abd974c799bb8c13fc3962a4`; the full twelve-test audit module also passed under that guard with zero attempts and receipt `sha256:3194e5a50aa7ffcf5324c141e4f5e26f4db02b584984b003f48fb8a5de698bb2`. Ruff lint/format, strict MyPy, and diff hygiene passed. This local repair is not launch authority.
- **Authority boundary:** The prospective Campaign 45 run root remains absent; exposure remains 85 valid development events and zero holdout events. Replay, audit, static gates, hosted CI, and the repair issued zero official environment actions. No target source, holdout, terms, credentials, spending, upload, submission, merge, release, or external representation occurred. Completion remains false at official `NOT_FINISHED`, `4/6`, zero `WIN`.
- **Reopening condition:** The repair reads any protected package-only path, weakens a boundary, changes production/gameplay bytes, fails exact Campaign 44 replay, any local/static/sanitized gate, or any of the five hosted workflows/nine jobs; or later authorized official play returns progress, `GAME_OVER`, a narrower residual, or `WIN`. Only a fresh repaired exact head that passes every gate may authorize Campaign 45 play.

## D-003-0060 - Seal Campaign 45 as a failed centroid discriminator and test only the embedded composite marker

- **Date:** 2026-08-29
- **Stage:** 10 and target-game continuation
- **Status:** ADOPTED; EXACT CAMPAIGN 45 GATES AND OFFICIAL FAILED MECHANISM SEALED; CAMPAIGN 46 IMPLEMENTED AND LOCALLY VERIFIED; FROZEN-HEAD GATES PENDING
- **Corrected exact-head proof:** Commit `3f2ab0fc0819a45c682f48125777cbb7f63ef97f`, tree `b6ded104b7bbe7fe118d294aa5c6412e55669766`, repeated strict Campaign 44 replay, the 122-case clean-source selection with 119 passes and three expected skips, all 61 replay/CLI cases, manifest-bound and package-only integrity, action semantics, and the checked-in sanitized prelaunch audit. Replay file/payload/canonical hashes are `089af587fb25ade546f0900fd04766620d6088003b2ded20caa5aa389b64ca59` / `377370a8c0948a4d8dab1c9f0b3ad3b64d64f86b190d0bf162bb6b019dcad1c` / `20622812a034ba5eaa47c1c8fcfdb6ce3ad3a5a95e247d4ecab50dab746c32b5`; audit file/canonical hashes are `dc104a91f0d67f2306bb9c9165c376f221bf673161299823328b4fdf0afc4a88` / `34a159c1dd6fa7e52009897729f08ab9a5ddaa331f6c8001cc2f3aa75446156e`. All gates issued zero official environment actions.
- **Hosted proof:** ARC3 CI push `33236254987` and pull request `33236256661`, package push `33236255016` and pull request `33236256665`, and Build 002 cold start pull request `33236256686` all succeeded. Nine of nine jobs passed. All four package guards selected the repaired audit test, preserved exact source, and recorded zero denied attempts. Evidence root is `C:\a\arc3-b003-hosted-3f2ab0f`.
- **Official Campaign 45 evidence:** Verified development-only root `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-3f2ab0f-campaign45` contains 505 declared artifacts with zero verification errors. The run ended `FAILED_MECHANISM`: raw/metric final `NOT_FINISHED`, score boundary `GAME_OVER`, `levels_completed=4/6`, 361 submissions comprising 357 `ACTION6` and four legal recovery `RESET`s, four `GAME_OVER` consequences, and zero `WIN`. Run-receipt file/canonical hashes are `40fd286842d388c7a5b9113d949d9c83a99ca3361a044d658b6c0a42a1857d6a` / `sha256:1159984c00044197be7d09bb25546099f96008eb3363e6da181f8ca1080da731`; completion receipt is `sha256:16539428c414fba12078d08a022d345e1c4b9befcd663251f7d2b56d6221e95f`. Recording SHA-256 is `c7f6c3cde3f0e366ed35388d2777a00a2cf8614b3e1ed07a7de373e90db82209`; trace manifest/chunk/tail hashes are `sha256:bb8c5bf915af2a4e693ec88c81015edf2b3b06b998c5065c7d09674c23b2069d` / `c34c8c0fe7fdc53f3a8147423db8715136c7a6fd95bd8c66cc9b9df7f6c12505` / `sha256:79a6999f22e4432becf3b07ca01cabba9e89f23113d659b5bf1d26f5d56c633b`.
- **Mechanism decision:** Campaign 45 submitted exactly one `ACTION6` at the composite's rounded centroid. Stable final-frame comparison `sha256:c106f093edd937cb5d0419e4a3896aca87d5324c53fa2931bb7b022f74bdd2ad` to `sha256:f9646e28b5a9ecba68291b8c11908b232497ce7870eeb10b6a217cc8e7abb7a2` changes only left-edge resource cell `(0,59)` from `0` to `5`; no gameplay-body, object, topology, legal-action, progress, or terminal change occurred. Reject only rounded-centroid exit/completion sufficiency. Preserve the preceding action's 50-cell composite transformation, crossed-delivery lineage, endpoint mechanics, and learned action-resource trajectory.
- **Multi-frame evidence rule:** Official consequences may contain animation frames. Causal attribution and candidate derivation consume `observation.frames[-1]`, the stable final frame used by production. Consecutive first-frame deltas would incorrectly assign the preceding 50-cell animation to Campaign 45 and must not support a receipt or claim.
- **Smallest residual:** In the unchanged stable composite, all four surviving endpoint shells share exactly one non-body center-marker color. Exactly one cell of that marker color lies inside the unique co-centered mediator-target overlap, and it differs from the rejected centroid. Campaign 46 may derive and submit one action at that embedded marker under the parent composite, exact object/protected-raster signatures, edge-resource prefix, reset epoch, action count 56, and exhaustion boundary 60. The plan family is `affine-crossed-post-access-composite-marker:`. Production stores no game, campaign, level, palette, or coordinate constant.
- **Lifecycle boundary:** A selected then cancelled marker earns no attempt. Submission records the exact marker hypothesis across same-level reset. `WIN` or level progress terminates the old-level plan. Exact `NOT_FINISHED` rejects only marker-keyed sufficiency and authorizes no repeat or unrelated fallback. Ambiguity, marker loss/duplication, protected-body or resource-prefix drift, unavailable action, insufficient reserve, `UNKNOWN`, `NOT_PLAYED`, or `GAME_OVER` fails closed; `GAME_OVER` permits only legal reset. Do not reopen the rejected centroid.
- **Authority boundary:** Exposure contains 87 valid development events and zero holdout events, file SHA-256 `9ada7d8b6344989a0cd8325a9bdde39ca985bdab3fbfdf49493b9d78e1133fba`. No terms, credentials, holdout, target source, spending, upload, submission, merge, release, external representation, or completion is claimed. Only returned `GameState.WIN` completes the target.
- **Reopening condition:** Campaign 45 evidence identity fails verification; stable-frame replay cannot reproduce all 2,168 events and 361 mechanics receipts exactly; the marker is not unique or distinct from the rejected centroid; any exact local/static/sanitized or hosted gate fails; or authorized Campaign 46 play returns progress, `GAME_OVER`, a narrower residual, or `WIN`.

## D-003-0061 - Preserve Campaigns 46-49 and exclude cargo routes that overwrite assigned target surfaces

- **Date:** 2026-08-29
- **Stage:** 10 and target-game continuation
- **Status:** ADOPTED; FOUR OFFICIAL FAILED MECHANISMS SEALED; COLLISION-FREE CAMPAIGN 50 CANDIDATE LOCALLY VERIFIED; EXACT REPAIR-HEAD GATES PENDING
- **Official evidence:** Campaign 46 (`5fc270e`) ended `NOT_FINISHED` at `4/6` after 362 submissions (358 `ACTION6`, four `RESET`), Campaign 47 (`62f7d12`) ended `NOT_FINISHED` at `4/6` after 375 submissions (370 `ACTION6`, five `RESET`), Campaign 48 (`777be35`) reached `5/6` and ended `NOT_FINISHED` after 701 submissions (691 `ACTION6`, ten `RESET`), and Campaign 49 (`9cc3e77`) reached `5/6` and ended `NOT_FINISHED` after 407 submissions (402 `ACTION6`, five `RESET`). Each score boundary returned `GAME_OVER`; each run has zero `WIN` and `completion_observed=false`. Run-receipt hashes are `sha256:42fad2d2ff04022208ff0431259abdbd8b5691db26927169aab8c04bb1d6e1a7`, `sha256:c6f03fc5995b14bf520a4fc6c0da48a6d4a4f0d0ac0e1e771c8b98d111d29035`, `sha256:07c4035d61fc7258e6646e6b6769e5ca3bc9bcb29c03d9bc95ed1e67eb98024c`, and `sha256:b02c9cf94a7f5c3f553056c4a656fedb0a312f56fb5ed0e90f8e85dc51fc59ec`.
- **Narrowing:** Campaign 46 rejects only embedded-marker exit sufficiency. Campaign 47 preserves the returned nonmatching consequence and stops when composite hierarchy lineage becomes unreadable. Campaign 48 establishes genuine level progress while exhausting only its bounded coordinate roots. Campaign 49 rejects only one same-child cargo route whose endpoint footprint intersects an already assigned target surface; it does not reject the retained multi-source mechanics, earlier successful levels, or every cargo successor.
- **Campaign 49 cause:** At final-level episode action 11/global submission 407, `ACTION6 (12,50)` placed an endpoint footprint over assigned ring cell `(12,52)`. The model projector restored that protected cell to palette 6, while the official returned footprint kept it palette 0. The mismatch is a geometric route-safety defect, not evidence that the environment completed or that the entire mechanic family failed.
- **Smallest repair:** Exact commit `ce3fa49e2dcb17ba94c59293608383b89fbb6f45`, tree `ce0ec497a2b7a17f612f8396b46d938b43bed1a3`, unions every assigned target cell and rejects any successor whose endpoint footprint intersects that surface. Production stores no game, campaign, level, coordinate, or palette constant. The full visual-policy module passed 150/150; Ruff and strict MyPy passed.
- **Prospective replay:** Clean exact replay matched the 401-submission safe Campaign 49 prefix, generated 27 successors, staged and cancelled only `ACTION6 (11,41)` under `affine-same-child-composite-cargo:86601e2fc1d86ff31572d615`, and issued zero environment actions. Receipt path is `C:\a\arc3-b003-gate-evidence-ce3fa49-c50-r1\campaign49-safe-prefix-replay-ce3fa49.json`; file/canonical hashes are `64c07d4aea38b18a8221b3bc3209a161d66204f259f7d378a65c810ff0b98f0f` / `sha256:0b8f24087606f0e2c6886a4541e52e136b70dfabdddf5693a2357c7a5d08d231`.
- **Authority boundary:** The exposure ledger contains 95 development events and zero holdout events, SHA-256 `372770e0adf31a5f9d2611aec056e3fb407bdd96ce71a53d8bfbfa86643c4eb7`. No Campaign 50 action has occurred. No holdout, target source, terms, credentials, spending, upload, submission, merge, release, or external representation is authorized or claimed. Only returned `GameState.WIN` completes the target.
- **Reopening condition:** Any Campaign 49 evidence or prefix-replay identity fails; a successor intersects an assigned target surface; the exact local, sanitized, or hosted gate set fails; authorized play returns a nonmatching consequence, progress, `GAME_OVER`, or `WIN`; or any completion claim lacks an observed official `GameState.WIN`.

## D-003-0062 - Repair only the two Windows launch-gate timing defects exposed at ce3fa49

- **Date:** 2026-08-29
- **Stage:** 10 and exact-head launch gate
- **Status:** ADOPTED AND RESOLVED BY A65A3A0 EXACT LOCAL, SANITIZED, AND HOSTED GATES; CE3FA49 FAILURES PRESERVED
- **Hosted evidence:** All five `ce3fa49` workflow runs and nine jobs are terminal. Push ARC3 CI `33272436447` and PR cold start `33272438736` passed. PR ARC3 CI `33272438745` failed only Windows job `99153246040`: one executor-recovery fixture returned `TIME_BUDGET` after 149.6335 ms under a default 100 ms search wall-clock limit; 1,865 tests passed and 20 skipped. Package push `33272436388` and PR `33272438711` failed only Windows jobs `99153240021` and `99153246016`: the guarded suite timed out at 4,500.0251 and 4,500.0175 seconds after 1,113 of 1,128 tests. Ubuntu passed. Package evidence artifacts are `9721435957` and `9721436717`.
- **Failure classification:** The planning failure is a load-sensitive test-fixture clock, not a production search failure. The package receipts correctly remained `FAILED_INFRASTRUCTURE`; the workflow correctly refused to normalize them to the expected private-boundary `BLOCKED_EXTERNAL`. Six jobs passed and three failed. No official environment action occurred and the prospective Campaign 50 root remains absent.
- **Smallest repairs:** Only `tests/unit/test_planning_execution.py::_plan` sets `enforce_time_budget=False`, retaining finite node/depth bounds and elapsed telemetry while leaving every production/default budget unchanged. Only the package-safe verifier allowance rises from 4,500 to 4,800 seconds and the workflow ceiling from 85 to 90 minutes, preserving the explicit 600-second outer reserve. Expected-status, guard, selection, package payload, learner, policy, replay, and gameplay semantics are unchanged.
- **Local verification:** The planning/search/property slice passed 10/10. The combined planning and release-verifier slice passed 72 tests with three expected platform skips. The exact package timeout-contract module passed 19/19. Ruff lint/format and diff hygiene passed. These local results are not launch authority.
- **Launch boundary:** Commit and push one ledger-bound repair head, repeat the Campaign 49 safe-prefix replay plus clean-source, replay/CLI/audit, manifest-bound, package-only, action-semantics, and sanitized prelaunch gates from a fresh detached clone, then require all five hosted workflows and all nine jobs to pass. Do not launch Campaign 50 on any failure. Only after that exact gate may authorized development-only play resume toward official `WIN`.
- **Resolution evidence:** Exact `a65a3a00cd4989a6bd97009a2c625afe6f9fc643`, tree `163d2b948424e4f4c1ae850fe869f57eff6f2340`, replayed the 401-submission Campaign 49 prefix and cancelled only the related `ACTION6 (11,41)` successor with zero environment actions. It passed 119 clean-source cases with three expected platform skips, 74 replay/CLI/audit cases, manifest-bound, package-only, action-semantics, full visual-policy, Ruff, strict MyPy, and corrected checked-in prelaunch audit gates. ARC3 CI push `33276871633`, ARC3 CI PR `33276873891`, package push `33276871664`, package PR `33276873865`, and cold-start PR `33276873866` all passed; all nine jobs succeeded. Campaign 50 launched only after the final hosted completion at 2026-08-29T23:18:25Z. The earlier `ce3fa49` three-job Windows failure remains immutable infrastructure evidence.

## D-003-0063 - Preserve Campaign 50 and narrow connector precedence to its observed structural phase

- **Date:** 2026-08-29
- **Stage:** 10 and target-game continuation
- **Status:** ADOPTED; OFFICIAL FAILED MECHANISM SEALED; NARROW CAMPAIGN 51 REPAIR REPLAYED LOCALLY; EXACT COMMIT GATES PENDING
- **Official evidence:** Campaign 50 at exact `a65a3a0` ended `FAILED_MECHANISM` with raw final `NOT_FINISHED`, score boundary `GAME_OVER`, `levels_completed=5/6`, 421 submissions comprising 416 `ACTION6` and five legal `RESET`s, five `GAME_OVER`, and zero `WIN`. The complete 422-row recording, 2,528-event trace, 600-artifact public verifier, run receipt `sha256:4216451931e5b54ca91bdb31d9b55b2e25f0bde432e03040d0861edfe4a85be7`, and completion receipt `sha256:c5d4500c10f0edcba5864779b4c42611b52137168af3420d3607d36896671924` verify. `completion_observed=false`; this is not completion.
- **Failure cause:** At final-level episode action 25/global submission 421, `ACTION6 (29,39)` preserved every assigned target, source, endpoint, and mediator surface. The only protected-raster mismatches were `(28,50)` and `(28,51)`: the official returned binary-child connector retained palette 1 while the projector placed transported residual palette 10 above it. The returned state remained `NOT_FINISHED`; fail-closed hierarchy-lineage loss then stopped the policy.
- **Rejected broad repair:** An unrestricted own-child connector-over-transported-residual rule was falsified before any environment action. Exact replay first diverged at Level 4 submission 370, where binary-child cells `(21,30)` and `(22,30)` had to remain residual 9, and direct Level 5 comparison found submission 402 cells `(12,35)` and `(12,36)` also had to remain residual 9. New collection, fixed-link ingress, vertical geometry, identical offsets, and arity two alone are therefore insufficient discriminators. The diagnostic reused the eventual corrected replay path, so the failed JSON was overwritten before sealing; this is observed unsealed diagnostic evidence, preserved reproducibly by fixed regressions rather than claimed as an immutable receipt.
- **Smallest retained repair:** Apply connector precedence only for an arity-two child after at least one other hierarchy child is already delivered, and only at transported-residual cells on that same child's connector raster. Protect static cells, retained sources, assigned targets, endpoint footprints, and mediator centers. Production stores no game, campaign, level, palette, or coordinate constant. Fixed official-shaped regressions preserve the Level 4 and submission-402 residuals while requiring the terminal child-specific two-cell precedence; translation/palette equivariance, Campaign 49 collision safety, and the full Campaign 48 route also pass. Paired projection timing found no material regression.
- **Prospective replay:** Frozen production/test hashes `cd025fad50e21babd39caf218775cdb9079aadaf14b918cd7e451111e2e79bc4` / `cd9a38d3c2a34a8c93e529b30bb3131fadeb3e7b6864d03fe32c09ee86aba20a` replayed all 421 actions, exact durable mechanics receipts, and returned consequences. Submission 421 was accepted with no lineage loss; only related `ACTION6 (28,60)` under `affine-same-child-composite-cargo:86601e2fc1d86ff31572d615` was staged and cancelled with seven route actions pending. No adapter or environment action was constructed. Receipt path `C:\a\arc3-b003-campaign50-replay-diagnosis-a65a3a0\campaign50-patched-projector-replay.json` has file/canonical hashes `11fcb3a7099d1be7ce8aad770aa4892f9027bc95e739771459e89bc26365eaa9` / `sha256:a2ba038a8a1dc0bc66c3144d361273eba869c94c3d440249c52dfc1406a2924b`.
- **Authority boundary:** The exposure ledger contains 97 valid development events and zero holdout events, file SHA-256 `9f89eaf473ae941d87be11df0abc4aa5f2ae9659cea2ba46231889b76db7106a`. The provisional Campaign 51 repair has issued zero environment actions. No holdout, target source, terms, credentials, spending, upload, submission, merge, release, or external representation is authorized or claimed. Only returned `GameState.WIN` completes the target.
- **Launch boundary:** Commit and push one ledger-bound repair head, then from a fresh clean detached clone repeat the full 421-action replay and cancel only the related successor; pass clean-source, replay/CLI/audit, manifest-bound, package-only, action-semantics, sanitized prelaunch, and all five hosted workflows/nine jobs at that exact head. Any failure blocks Campaign 51. Authorized development-only play may resume only after every gate passes and must continue while the environment reports `NOT_FINISHED`.
