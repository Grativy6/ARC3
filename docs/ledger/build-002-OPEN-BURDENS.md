# ARC3 Build 002 open burdens

Append-only record. Resolved burdens remain present with their original observation and resolving
evidence. Later success must not overwrite a failed attempt, superseded artifact, or uncertainty.

## B-002-0001 — Exact private Kaggle execution surface is unavailable

- Status: OPEN; `BLOCKED_EXTERNAL`.
- Opened: 2026-08-24T03:26:38Z.
- Burden: the exact private competition wheels, framework input, gateway, scorer, accepted-terms
  runtime, and hidden environments are not available under current authority.
- Consequence: public/local package and toolkit evidence cannot prove private compatibility or
  create a Kaggle-public, semi-private, official-private, or official RHAE result.
- Current safe work: complete and verify every local/offline deliverable, including the exact
  adapter, lifecycle, package, notebook entry point, and structural output.
- Resolution condition: a later owner-authorized official evaluator returns an authenticated
  receipt. Do not request credentials, accept terms, upload, or submit in Build 002.

## B-002-0002 — Local toolkit and Kaggle score-cap formulas conflict

- Status: OPEN; source discrepancy.
- Opened: 2026-08-24T03:26:38Z.
- Burden: current ARC-AGI 0.9.9 methodology/code cap a completed level's ratio at 1.15, while the
  current Kaggle data-page formula describes a 1.0 ratio cap before squaring. Both then aggregate
  levels/games, but the cap difference can change numerical results.
- Consequence: any ten-game public score must identify the exact local scorer and be labeled
  `local-public`, not official RHAE.
- Resolution condition: the exact official evaluator returns the score or official sources
  converge under a newly pinned identity. Build 002's earlier result remains source-bound.

## B-002-0003 — Public toolkit and staff-sample versions diverge

- Status: OPEN; compatibility burden.
- Opened: 2026-08-24T03:26:38Z.
- Burden: the current public ARC-AGI package is 0.9.9, while the latest observed official staff
  sample used 0.9.6. The exact private runtime package set is unavailable.
- Consequence: test the adapter against the pinned public version and fail closed on any discovered
  incompatibility; do not call that private parity.
- Resolution condition: exact official runtime dependencies are published or an authorized
  private run produces a complete compatibility receipt.

## B-002-0004 — Exact private `submission.parquet` parity is unavailable

- Status: OPEN; `BLOCKED_EXTERNAL` at the private schema/scorer boundary.
- Opened: 2026-08-24T03:26:38Z.
- Burden: the public starter supports a four-column structural artifact, but private framework and
  scorer validation are not locally available.
- Consequence: report local structural validity precisely; never imply upload acceptance or scorer
  parity.
- Resolution condition: an owner-authorized official notebook execution validates the artifact.

## B-002-0005 — One-shot public evidence is authorized but not yet consumed

- Status: OPEN; gate pending.
- Opened: 2026-08-24T03:26:38Z.
- Burden: all source, offline, lifecycle, package, notebook, output, integrity, and frozen-identity
  gates must pass before the one authorized ten-game public run begins.
- Current state: `0/1` runs started; no Build 002 public result exists.
- Failure rule: a preflight failure leaves authority unconsumed; durable intent immediately before
  the first and only upstream scorecard open consumes it. A later failure must be sealed without
  retry, while environment `make` interactions remain independently counted.
- Resolution condition: either the frozen run completes once and is independently verified, or a
  hard blocker is recorded without opening an environment.

## B-002-0006 — Competition package projection initially omitted new runtime modules

- Status: OPEN pending exact repair receipt.
- Opened: 2026-08-24T03:26:38Z.
- Observation: an in-progress package-candidate suite reported three failures because the existing
  payload builder did not include the new `src/arc3/competition/**` package. This is a packaging
  integration failure, not gameplay or scorer evidence.
- Required repair: include all new competition modules and the v0.2 runtime declaration in the
  deterministic payload, then rerun the exact candidate and cold-start suites. Preserve the failed
  invocation/receipt once its exact command and output path are available.
- Resolution condition: clean-source package verification proves the modules are present,
  importable offline, manifest-bound, and included in deterministic A/B archives.

## B-002-0007 — Cold-start isolation must not borrow host dependencies

- Status: OPEN; verification pending.
- Opened: 2026-08-24T03:26:38Z.
- Burden: Build 001's available local sandbox mechanism could inject host site-packages through a
  `.pth` path and used a canary wheelhouse/canned Parquet fixture for some tests. Those checks are
  valuable but are not Build 002's required sealed cold start.
- Required evidence: an offline install/import and entry-point run using only packaged
  dependencies/assets/configuration, plus a complete payload/license/secret manifest.
- Resolution condition: exact frozen package passes from an isolated environment without host
  dependency injection or network access.

## B-002-0008 — Public-run performance and failure distribution are unknown

- Status: OPEN; measurement not yet authorized by the preflight gate.
- Opened: 2026-08-24T03:26:38Z.
- Burden: no Build 002 total score, completed game/level count, per-level score, action efficiency,
  runtime, peak memory, allocation/reserve result, or failure taxonomy exists yet.
- Resolution condition: seal the one authorized run or record a pre-consumption hard blocker.
- Claim boundary: implementation tests, synthetic fixtures, or extrapolations cannot resolve this
  burden.

## B-002-0009 — Public-source preview exposure prevents a pristine/unseen claim

- Status: OPEN; epistemic contamination recorded without mechanical run consumption.
- Opened: 2026-08-24T03:55:35Z.
- Observation: while checking for a terms-free static Kaggle asset surface, an unexpected
  third-party search-result excerpt displayed source-derived public-game claims, baseline
  examples, and a metric table covering the public set. The claims were not validated or adopted.
- Exact receipt: `docs/evidence/002-00-public-source-preview-contamination.json`.
- Measured boundary: zero environment `make`, reset, action, or scorecard interactions; zero
  static assets acquired or opened; zero game-source files opened; zero production-policy changes
  derived from the excerpt.
- Authority: the explicit Build 002 mechanical consumption boundary remains un-crossed (`0/1`
  runs started), and Build 001 remains immutable `SEALED_UNCONSUMED` historical evidence.
- Consequence: any later Build 002 public run must be labeled
  `local-public-source-preview-exposed`, not pristine or unseen. The controller may not use the
  third-party claims as game-specific rules, tuning data, or hard-coded behavior.
- Resolution condition: this burden is permanent provenance. A later run may close the
  measurement burden but must retain this limitation; it cannot erase or supersede the exposure.

## Failed attempts and superseded evidence registry

No material attempt may be removed after repair. Each new entry must record the exact command,
source/config identities, exit status, concise failure, artifact path/hash, repair, and whether the
attempt consumed public authority. B-002-0006 is the first known in-progress package failure;
its exact invocation is pending append-only capture in the validation ledger.

## Status update — B-002-0005 and B-002-0008

- Recorded: 2026-08-24T08:37:44Z.
- Status: terminal for this Build 002 run as `BLOCKED_EXTERNAL_BEFORE_CONSUMPTION`.
- Resolution evidence:
  `docs/evidence/002-07-frozen-preflight-blocked-external.json` and
  `docs/evidence/002-08-public-run-disposition.json`.
- Authority: `0/1` runs started; no scorecard-open intent, environment make, reset, or action
  occurred.
- Measurement consequence: total RHAE, official RHAE, completed games/levels, per-game/per-level
  scores, actions versus human baselines, gameplay wall time/peak memory, allocation/reserve, and
  gameplay failure taxonomy are all `NOT_MEASURED`.
- Residual: the authorization was not consumed. Any later use requires a new explicit owner
  direction; Build 002 did not silently convert non-consumption into standing permission.

## Status update — B-002-0006

- Recorded: 2026-08-24T08:37:44Z.
- Status: RESOLVED for the local package projection; original failure preserved.
- Repair evidence: current package contains `104` exact Git-bound first-party members, including
  `src/arc3/competition/**`, `src/arc3/competition_runtime.py`, and
  `src/arc3/competition-runtime.v0.2.json`.
- Final payload SHA-256:
  `726e595523a9b737a3b000b6d4d088a8e9289c1e6fd1da03297b79876311356f`.
- Final package status: `PACKAGING_PASS`.
- Residual: exact private platform compatibility remains B-002-0001/B-002-0004, not resolved by
  local projection.

## Status update — B-002-0007

- Recorded: 2026-08-24T08:37:44Z.
- Status: RESOLVED for native Linux local cold-start isolation; external-platform residual remains.
- Evidence: CI run `32706714450`, cold-start receipt file SHA-256
  `e97df685246b87936813eed74cf442b3c516192dad4a4d9fd83e6bfb116b519a`.
- Proven locally: 31 exact Linux wheels, hash-required/no-index install, no host `.pth` bridge,
  exact four generated notebook code cells, deterministic startup projections, and structural
  Parquet output.
- Residual: the safe framework/gateway fixtures are not Kaggle's private surfaces; Python socket
  guards are not OS-enforced containment.

## B-002-0010 — Initial scorecard opening was outside the one-shot seal

- Status: RESOLVED; failed mechanism preserved.
- Opened: 2026-08-24 during one-shot harness audit.
- Observation: the first durable harness marked authority immediately before the first environment
  `make`, but the official lifecycle opens its one scorecard earlier. A scorecard-open failure
  could therefore occur without a durable consumption receipt.
- Repair: move consumption to durable `scorecard.open_intent` immediately before upstream
  scorecard open; keep each environment make separately intent-counted.
- Resolving source: commit `2039c554d1fb06d89250cfed13cbd226b44d30c3`.
- Residual: none in the local lifecycle mechanism. Exact private framework behavior remains
  externally blocked.

## B-002-0011 — Infrastructure failures during final verification

- Status: RESOLVED or superseded locally; all attempts remain provenance.
- Long pytest temporary root:
  - failure: Windows path length exceeded in deep terminal-artifact fixtures;
  - repair: short `C:\q8f` basetemp; clean full suite passed;
  - unsealed fields: exact failed argv, count, and hash.
- Parallel A/B package build:
  - failure: shared `.venv` mutation produced Windows `Access Denied`;
  - repair: sequential isolated builds;
  - unsealed fields: exact failed argv, path, and hash.
- OneDrive integrity scan:
  - failure: `candidate-mutated-during-scan` after `ctime` churn despite stable content hashes;
  - repair: clean non-OneDrive clone;
  - unsealed fields: failed receipt path and hash.
- Profiler `2s` timeout:
  - failure: outer bound was too small for the frozen workload;
  - repair: coherent `1800s` worker timeout;
  - unsealed fields: exact failed argv and receipt hash.
- Incorrect profiler source SHA:
  - expected `9d395d57d08ffc0106760597061d746dc384c59a`;
  - actual `9d395d51fd3ae9ca35df90a2ddbccbd0338dade4`;
  - status `FAILED_INFRASTRUCTURE`;
  - receipt SHA-256
    `570bbcb3472999c5f8f8e2e50d079cdf89e8a01461a18e607c583a9ece85e2ff`.
- None consumed public authority.

## B-002-0012 — Build 001 package-only CI collected a Build 002 protected-manifest fixture

- Status: REPAIRED LOCALLY; hosted final confirmation was pending when recorded.
- Opened: 2026-08-24 from CI run
  `https://github.com/Grativy6/ARC3/actions/runs/32702015799`.
- Observation: both Ubuntu and Windows returned `FAILED_MECHANISM`, because
  `tests/competition/test_run_build002_holdout.py` intentionally copies the protected manifest
  while Build 001's package-only verifier forbids that semantic surface.
- Repair: commit `0385d238ab85477ce6f995f7182855a7b3473f5d` gives that exact test an explicit
  boundary exclusion and reason in `scripts/package_only_pytest.py`.
- Local repair evidence: `2 passed`.
- Residual: final hosted package-only conclusion is appended in the validation ledger. Do not mark
  this resolved by assumption while any exact-head run remains non-terminal.
- Claim boundary: the failure did not consume a public run and does not revise Build 001.

## B-002-0013 — Package-only integrity is not full competition integrity

- Status: OPEN by design.
- Observation: final integrity producer receipt
  `d0d32f777cadc5a5116d12d9096f97e638af7f99857b8f0632f0bca6c177c5b5`
  has `package_only_passed=true` and
  `full_competition_integrity_status=NOT_EVALUATED_PUBLIC_IDENTIFIERS`.
- Consequence: report package-only static/source/supply-chain/archive/secret success, but do not
  state that a full public-identifier or official competition scan passed.
- Resolution condition: an authorized exact competition surface evaluates the remaining semantics
  without violating the one-shot boundary.

## B-002-0014 — Source discrepancies remain permanent provenance

- Status: OPEN as provenance; controlling identities are frozen.
- Preserved conflicts:
  - two incompatible initial hashes for one alleged Kaggle metadata response;
  - Windows checkout versus raw Git-blob hashes;
  - local toolkit `1.15` score cap versus Kaggle page `1.0`;
  - toolkit `0.9.9` versus staff-sample `0.9.6`;
  - starter five-daily-submission text versus live metadata one daily/two scored;
  - inclusive upstream action loop versus ARC3's deterministic internal cap;
  - ACTION7 documentation versus generic ARCEngine action typing.
- Current policy: exact raw source identities and the fresh metadata response control local
  implementation. No correction erases superseded evidence.
- Resolution condition: official sources converge or an owner-authorized evaluator supplies exact
  receipts. Earlier source-bound results remain source-bound.

## B-002-0015 — Exact competition surface and first honest RHAE remain unavailable

- Status: OPEN; `BLOCKED_EXTERNAL`.
- Missing: exact private wheels, framework input, gateway, scorer, accepted-terms runtime,
  independently pinned ten-game static asset provenance, OS-enforced evaluation network
  containment, and official evaluator acceptance.
- Local work completed: adapter, modes, governor, lifecycle, profiling, deterministic package,
  generated notebook, native Linux cold start, and structural Parquet validation.
- Public evidence disposition: stopped before consumption; `0/1`.
- Official result: none.
- Required later owner actions, each still separately gated: accept applicable terms, provide or
  authorize an official Kaggle runtime, upload, submit, or merge. Build 002 performed none.

## Final failed-attempt and superseded-evidence registry

- Preserved: initial pre-make rather than pre-scorecard consumption mechanism.
- Preserved: package projection that initially omitted `src/arc3/competition/**`.
- Preserved: `2s` profiler timeout.
- Preserved: incorrect expected profiler SHA failure with receipt
  `570bbcb3472999c5f8f8e2e50d079cdf89e8a01461a18e607c583a9ece85e2ff`.
- Preserved: parallel shared-`.venv` package race and `Access Denied`.
- Preserved: OneDrive `ctime` churn and `candidate-mutated-during-scan`.
- Preserved: long Windows pytest basetemp path failure.
- Preserved: Build 001 protected-manifest package-only CI failure and exact repair.
- Preserved: superseded `44af464` profile/package/preflight artifacts.
- Preserved: source-preview exposure; any future run label remains
  `local-public-source-preview-exposed`.
- Preserved: Build 001 remains `PARTIAL` and historically `SEALED_UNCONSUMED`.

## Status update — B-002-0012

- Recorded: 2026-08-24T09:05:27Z.
- Status: RESOLVED for the original protected-manifest collection fault; later failures remain
  separate evidence.
- The `0385d238` exclusion repaired the exact Build 002 holdout-fixture collection fault without
  weakening Build 001's scanner or authority boundary. The later Build 001 package-only run
  `32706714371` did not provide a green hosted conclusion because it exposed two distinct issues:
  an unconfigured packaged `MyAgent` governor on both operating systems and, on Linux, collection
  of the exact POSIX Agents integration test inside the protected package-only guard.
- Those later issues are tracked as B-002-0016. They do not reopen or erase the original
  protected-manifest diagnosis.

## B-002-0016 — Second 0385d238 package-only regression

- Status: REPAIRED LOCALLY; exact-head hosted confirmation pending at the evidence freeze.
- Opened: 2026-08-24 from Build 001 package-only workflow
  `https://github.com/Grativy6/ARC3/actions/runs/32706714371` at source
  `0385d238ab85477ce6f995f7182855a7b3473f5d` and tree
  `5b9a5610df4add0f0417f4f73161ff082af7b502`.
- Failure 1: the package startup probe instantiated `MyAgent` without first configuring its
  process-global tournament governor. Both Ubuntu and Windows ended `FAILED_MECHANISM` with the
  exact terminal error `MyAgent tournament governor was not configured`.
- Failure 2: the Linux protected package-only selection included
  `tests/integration/test_pinned_agents_framework.py`; that exact pinned Agents integration
  transitively exercises POSIX subprocess behavior outside the package-only guard. The ordinary
  CI selection remains the correct place for that test.
- Failed release producer receipts:
  - Ubuntu:
    `18541349e45d99de28c0f30db5edcf1b15c03c993e940eefde739f0950f596c8`;
  - Windows:
    `dd9800dc390fb107f5b15bafa2b948971df9b826c12ec38283b254244f5e9b4f`.
- Repair: commit `753b0e007222a973a2c8a6d7ce14a395135d3c5f` conditionally configures the
  tournament in the packaged probe and excludes only the POSIX exact integration from Build 001's
  protected package-only selector. Ordinary CI retains it.
- Local repair evidence: targeted selection `30 passed`; packaged startup passes. The exact local
  argv and standalone transcript hash were not sealed.
- Hosted state at freeze: Build 002 workflow `32708504639` passed; exact-head ARC3 CI
  `32708504627` and Build 001 package-only CI `32708504623` remained pending.
- Claim boundary: neither failure nor repair opened a scorecard, made an environment, consumed
  holdout authority, or revised Build 001.

## Terminal update — B-002-0015

- Recorded: 2026-08-24T09:20:26Z.
- Status: remains OPEN; `BLOCKED_EXTERNAL` at frozen source
  `753b0e007222a973a2c8a6d7ce14a395135d3c5f`.
- Current preflight producer receipt:
  `bb37fa65c0bf470ba54b2e6b82c14c01cafc8045d9697b1ac82893b2a241b189`;
  serialized source artifact:
  `15d748c6954705cabdfc37d0f993ec3e5d352558fb0741d7bd7cbd472e24e82e`.
- Result: the exact competition surface remains unavailable; the preflight stopped before arming,
  scorecard open, environment make, reset, or action. Consumption remains `0/1`, and every public
  gameplay metric remains `NOT_MEASURED`.
- The second `0385d238` regression and its `753b0e0` repair are now additionally preserved in the
  failed-attempt registry. None supersedes the external blocker.

## Status update — B-002-0013

- Recorded: 2026-08-24T09:12:27Z.
- Status: remains OPEN by design.
- Current package-only integrity producer receipt:
  `42aa847bc4443f100be9163b9bb9746ed30dc1e5d79692d20d7d1cfbc43da588`;
  serialized receipt:
  `9287f22b9a6d63cd8dd3540661f28b2115e9935488d24c38aeb58767c7ad1b3b`.
- It records `package_only_passed=true` and
  `full_competition_integrity_status=NOT_EVALUATED_PUBLIC_IDENTIFIERS`.
- The earlier `d0d32f77...` producer receipt remains preserved historical evidence; it is not the
  current freeze.

## Failed-attempt registry update — final preflight layout

- Recorded: 2026-08-24T09:12:27Z.
- Preserved superseded clean-worktree A/B-layout request and `BLOCKED_EXTERNAL` receipt:
  request `71cfd4e1...`, serialized receipt `607bcd21...`, producer `dc28361f...`.
- Preserved corrected-path/non-fresh-output failure:
  request `1a04da9d...`, serialized failure `252a42fa...`, producer `e16c44ff...`.
- Current fresh-output external stop:
  request `b842e2ce...`, serialized receipt `15d748c6...`, producer `bb37fa65...`.
- All three remained launch-free with zero public interaction and `0/1` authority consumed.

## Status update — B-002-0016

- Recorded: 2026-08-24T09:20:26Z.
- Status: RESOLVED.
- Hosted Build 001 protected package-only workflow `32708504623` passed on Ubuntu and Windows at
  exact commit `753b0e0`. The packaged agent was configured before construction, and the narrow
  POSIX integration exclusion remained confined to the protected package-only selector. Ordinary
  ARC3 CI `32708504627` retained the integration surface and passed on both platforms.
- Resolution does not revise the preserved `0385d238` failure or Build 001's `PARTIAL` result.

## B-002-0017 — GitHub Actions Node runtime deprecation warning

- Status: OPEN maintenance burden; non-fatal.
- Observation: implementation-freeze CI passed, but GitHub annotated `actions/checkout@v4`,
  `actions/upload-artifact@v4`, and `astral-sh/setup-uv@v6` as targeting deprecated Node.js 20 and
  forced them onto Node.js 24.
- Current consequence: none measured; every affected job passed.
- Resolution condition: adopt upstream action revisions that declare a supported runtime, then
  re-run ordinary and package workflows. Do not treat the warning as a failed Build 002 mechanism.

## B-002-0018 — Final Build 002 evidence documents lack a dedicated schema validator

- Status: OPEN, low-risk evidence-tooling burden.
- Observation: the final run state, public-run disposition, preflight receipt projection, and
  evidence index parse as JSON and are cross-hash checked, but the repository has no dedicated
  versioned validator for the Build 002 terminal document schemas.
- Current mitigation: exact artifact hashes, producer self-hashes, JSON parsing, focused tests,
  `git diff --check`, and human-readable ledger/report redundancy.
- Resolution condition: add a versioned terminal-evidence schema and regression tests in a later
  build without retroactively changing these receipts.
