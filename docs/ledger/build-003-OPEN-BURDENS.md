# ARC3 Build 003 open-burden ledger

Do not delete a burden when later evidence resolves it. Append a resolution with the resolving artifact and commit.

## B-003-0001 - Real OFFLINE target playability is not dynamically verified

- **Status:** OPEN
- **Stage:** 00, 10
- **Opened:** 2026-08-24
- **Owner:** shared
- **Burden:** The cached `r11l-495a7899` identity and development authority pass static checks, but Build 003 has not yet opened it through pinned OFFLINE execution.
- **Why it matters:** Static availability cannot establish that the real official environment can run now.
- **Current evidence:** `docs/evidence/003-00-source-and-base-preflight.json`.
- **Next discriminating action:** After synthetic gates and implementation freeze, revalidate the hash and open exactly this identity with `network_enabled=false` and no credentials.
- **Resolution condition:** The official OFFLINE adapter opens and returns a normalized frame, or an exact incompatibility receipt establishes `BLOCKED_EXTERNAL`.

## B-003-0002 - BLA/CLEF mechanism benefit is unmeasured

- **Status:** OPEN
- **Stage:** 02-09
- **Opened:** 2026-08-24
- **Owner:** Codex
- **Burden:** No Build 003 learner, curriculum, or paired ablation result exists yet.
- **Why it matters:** Framework alignment and passing component tests are not evidence of learning benefit.
- **Current evidence:** Source identity and pre-registration only.
- **Next discriminating action:** Implement the mechanism once, run the frozen four-variant matrix on all predeclared held-out seeds, and report paired outcomes including failures.
- **Resolution condition:** Acceptance gates support `MECHANISM_CONFIRMED_SYNTHETIC`, or the result is preserved as `PARTIAL`/`FAILED_MECHANISM`.

## B-003-0003 - No authoritative target-game completion exists

- **Status:** OPEN
- **Stage:** 10-11
- **Opened:** 2026-08-24
- **Owner:** shared
- **Burden:** No official returned `GameState.WIN` exists for the target. One historical random run completed one level but ended without WIN.
- **Why it matters:** A level transition, score, mechanic map, or synthetic pass is not the requested outcome.
- **Current evidence:** Build 000 Stage 15 trace; Build 003 preflight.
- **Next discriminating action:** Use the frozen learner at the official action boundary, preserve every failure/reset, and continue until WIN or an exact external/resource boundary.
- **Resolution condition:** A replay-linked receipt records final official state WIN, or the same exact blocking condition satisfies the persistent blocked-goal rule after independent work is exhausted.

## B-003-0004 - Target-guided development may overfit

- **Status:** OPEN
- **Stage:** 05-11
- **Opened:** 2026-08-24
- **Owner:** Codex
- **Burden:** Iterating from one public development target can accidentally create identity-, coordinate-, color-, layout-, or walkthrough-specific behavior.
- **Why it matters:** Such behavior would violate the workflow and provide no game-agnostic evidence.
- **Current evidence:** Production public-ID scan exists; no Build 003 code exists.
- **Next discriminating action:** Keep the target ID external, run static scans and remapping/metamorphic tests, and require generic causal features for every repair.
- **Resolution condition:** Final source scans and held-out synthetic compositions pass with zero target/public IDs or privileged imports.

## B-003-0005 - Official ARC3 RHAE remains unmeasured

- **Status:** BLOCKED_EXTERNAL
- **Stage:** 10-11
- **Opened:** 2026-08-24
- **Owner:** upstream/human
- **Burden:** Local OFFLINE play has no authoritative online/Kaggle scorecard, and credentials/terms/submission are not authorized.
- **Why it matters:** Local actions and completions must not be labeled official RHAE.
- **Current evidence:** Build 002 handoff; current official local-vs-online documentation.
- **Next discriminating action:** None in Build 003; report `OFFICIAL_ARC3_RHAE = NOT_MEASURED`.
- **Resolution condition:** A separately authorized official evaluator returns a scorecard bound to the frozen package.

## B-003-0006 - Frozen Windows launcher identity test fails on the current uv alias topology

- **Status:** OPEN / inherited `FAILED_INFRASTRUCTURE`
- **Stage:** 01, 10
- **Opened:** 2026-08-24
- **Owner:** host tooling
- **Burden:** `test_windows_direct_base_spawn_preserves_venv_identity_and_pid` reproducibly fails before Build 003 changes because `_runtime_identity` returns `verified=false` only for `direct_process_probe_exact`.
- **Why it matters:** The final regression result must preserve this known failure rather than attribute it to the learner or silently call the full suite clean.
- **Current evidence:** `docs/evidence/003-01-build-002-frozen-baseline.json`; the launcher probe reports the uv base Python through an unversioned lexical alias while direct execution reports its versioned resolved directory.
- **Next discriminating action:** Re-run the exact test at Stage 10 and compare the predicate-level receipt. Do not modify the frozen baseline or weaken runtime identity validation merely to obtain green output.
- **Resolution condition:** The final test passes on an authenticated stable launcher topology, or a separately scoped fix proves both launcher paths equivalent without weakening any identity predicate.

## B-003-0007 - Global composite-preservation guards regressed ordinary affine play

- **Status:** RESOLVED
- **Stage:** 10
- **Opened:** 2026-08-25
- **Owner:** Codex
- **Burden:** Campaign 14 ended after 29 official actions at `NOT_FINISHED`, `levels_completed=2/6`, when a cross-mediator separation guard learned from a composite level was applied to ordinary single-color affine groups. The policy exhausted its bounded same-group action set and raised `PolicyError`.
- **Why it matters:** A repair that preserves later mechanics but removes a valid earlier route is not progress toward authoritative `WIN`.
- **Failure evidence:** `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-b1a695ec-campaign14`; official recording SHA-256 `993031c8909e386c89a0fb35a9604c32e88260ddba151b082747709de4c43245`.
- **Local resolution evidence:** The guards are now scoped by observed composite-mediator identity. Disabling only the target-box gate did not repair the first divergent action; disabling ordinary inter-mediator separation did. The repaired policy exactly matches all 29 pre-transition actions from Campaign 13 and reaches its returned level-3 `NOT_FINISHED` observation. The visual-policy suite reports 40 passed; Ruff and strict mypy pass.
- **Official resolution evidence:** Campaign 15, rooted at `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-e87a2762-campaign15`, passed the Campaign 14 failure boundary and officially reached level 3. Its later compound-evidence failures are preserved separately as B-003-0008 rather than back-propagated into this resolved ordinary-affine regression.
- **Next discriminating action:** None for this burden; preserve the Campaign 14 and Campaign 15 traces as paired regression evidence.
- **Resolution condition:** Met by Campaign 15 reaching level 3 through ordinary affine play. Continued preservation of level-3 compound groups is governed by B-003-0008.

## B-003-0008 - Compound evidence loss and false failure memory blocked level-3 continuation

- **Status:** RESOLVED BY CAMPAIGN 17; LATER LEVEL-3 RESIDUAL PRESERVED SEPARATELY
- **Stage:** 10
- **Opened:** 2026-08-25
- **Owner:** Codex
- **Burden:** Campaign 15 reached level 3 but did not reach `WIN`. Observable compound mechanics were erased when a moved endpoint, prospective connector/tether raster, or translated mediator sector crossed a target region or merged with same-color scene structure. Separately, readable marker displacements were classified as unrecognized changes and falsely blacklisted, while an exact predicted local target collapse became unreadable after its expected disappearance and could not drive continuation to another unresolved group.
- **Why it matters:** Cross-level mechanic retention cannot serve progress when an action destroys the learner's own evidence, and false failure memory can exhaust otherwise valid same-group actions after `RESET`. Neither high confidence nor a local collapse is official completion; the controller must preserve bounded local progress and continue until the environment returns `WIN`.
- **Failure evidence:** Campaign root `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-e87a2762-campaign15`; official recording `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-e87a2762-campaign15\official-recordings\c2120ed7e4beb839e4d0\d5a89114-d780-4700-9741-1ce5c7a2a91d\r11l-495a7899-8f76d262-4475-4265-9851-5a4184c54c1a.jsonl`; SHA-256 `2a2b3da7fad9e5bb8d4fea3b2f33133bdf2e9076ac66ea49f02dc9c6744b2a14`. The run ended `FAILED_INFRASTRUCTURE` on `PolicyError`; the latest raw environment state was `NOT_FINISHED`, the score boundary reported `GAME_OVER`, and measured progress was 3/6 levels after 112 submissions: 110 non-reset actions and two `RESET` actions.
- **Local resolution evidence:** The bounded repair protects all observable target regions and prospective connector/tether rasters, applies the parser's own component-merge relation to endpoint and mediator-sector placement, recognizes structurally readable marker displacement as a known controllable effect, and carries observation-grounded exact local target contact forward as local continuation state. These changes do not establish a level completion or `WIN`.
- **Campaign 16 recheck:** The fresh campaign frozen at `0b87a923500041757213fc421e5e165530b0c11c` ended before level 3, so it neither resolves nor disproves the compound-preservation repair. It did verify that removing false blacklisting exposes a separate shallow-improvement cycle, preserved as B-003-0009. Campaign root `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-0b87a923-campaign16`; recording SHA-256 `9531e3137b8b08d7f178c6df0d33f248f6b87740bbb58471b131b992778891a0`.
- **Campaign 17 resolution:** The run frozen at `0ae945257a1f8ed68d9462f6b0f0bf4083b41c5b` reached level 3 through the repaired earlier route, preserved three readable compound groups, continued after one exact local collapse, and then exactly collapsed a second group. It ended on a distinct post-collapse overlay/reacquisition residual rather than the Campaign 15 evidence-loss or false-blacklist mechanisms. Campaign root `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-0ae94525-campaign17`; recording SHA-256 `2685d5e81cb4b95c5784169a9e9a4a1675c4b3a94cb447fc5dd186c4ac864965`.
- **Next discriminating action:** None for this burden. Preserve Campaign 15 and Campaign 17 as paired failure/resolution evidence; post-collapse continuation is B-003-0010.
- **Resolution condition:** A fresh official campaign does not falsely blacklist readable compound actions, continues after an exact local collapse, and advances beyond the Campaign 15 boundary without erasing an unresolved compound mechanic; authoritative completion still requires returned `GameState.WIN` under B-003-0003.

## B-003-0009 - Uncertified relocation geometry produced a shallow-improvement cycle

- **Status:** RESOLVED BY CAMPAIGN 17
- **Stage:** 10
- **Opened:** 2026-08-25
- **Owner:** Codex
- **Burden:** Campaign 16 completed two levels, then the repaired failure memory exposed a shallow-improvement cycle. The bounded policy ranked apparent relocations whose coordinates remained inside the current endpoint hitbox and did not separate an endpoint center that had merged into sparse-target evidence. It exhausted its same-group action set and raised `PolicyError` while the official state remained `NOT_FINISHED`.
- **Why it matters:** Retaining or looking ahead through more candidate actions is not useful when the candidate geometry itself does not certify relocation or preserve parser identity. The repair must remove the unsupported action and restore the exact observable target boundary before ranking further progress.
- **Failure evidence:** Frozen commit `0b87a923500041757213fc421e5e165530b0c11c`; campaign root `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-0b87a923-campaign16`; official recording `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-0b87a923-campaign16\official-recordings\c2120ed7e4beb839e4d0\3e7aa61a-60a6-4f13-919c-9cfbea353d9b\r11l-495a7899-12ce06a5-763d-4190-aebd-d28bf8e702c2.jsonl`; SHA-256 `9531e3137b8b08d7f178c6df0d33f248f6b87740bbb58471b131b992778891a0`. The recording contains 34 events for 33 non-reset action submissions and zero resets; `levels_completed=2/6`; raw and official final states were `NOT_FINISHED`.
- **Disproved first repair:** The broad D-003-0007 follow-up ranking regressed a preserved official replay route and was rejected before fresh official play. This narrows the burden: no evidence earned broader lookahead.
- **Refined diagnosis:** Official actions at the painted edge `(41,16)` and an unpainted bounding-box corner `(41,15)` both left the active endpoint center and mediator unchanged, so treating either coordinate as a new center invented improvement without relocation evidence. Separately, an endpoint center was joined to a sparse target; removing that center reconstructed the exact symmetric target ring, identifying a parser-level contaminant rather than a new target geometry.
- **Smallest local repair:** Reject relocation coordinates inside the current active endpoint's observed bounding box. For exactly certified endpoint-center/sparse-target contamination, select the smallest readable separation whose prospective center no longer merges with the residual target under the parser's exact small-component relation, then reobserve before continuing. Accept a consequence only when the predicted endpoint-center replacement or outer-color role transfer is visible; retain exact target restoration even when an ordinary improvement produced it.
- **Local verification evidence:** The visual-policy suite reports 50 passed; Ruff and strict mypy are clean. Historical replay preserves the full known official Level 1 route and replaces the failed Level 2 tail with the certified sequence `(59,49)`, `(59,46)`, `(39,16)`, `(49,48)` into the recorded Level 3 frame. No-op role transfers are now rejected, while actual role transfers and both dedicated and incidental target-identity restoration are covered. These results verify only the local repair and do not establish fresh official progress or `WIN`.
- **Official resolution evidence:** Campaign 17 preserved the earlier two-level route, executed the certified separation sequence, reached level 3, and continued for seventeen level-3 actions before a distinct residual. Campaign root `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-0ae94525-campaign17`; recording SHA-256 `2685d5e81cb4b95c5784169a9e9a4a1675c4b3a94cb447fc5dd186c4ac864965`.
- **Next discriminating action:** None for this burden; the distinct Campaign 17 residual is B-003-0010.
- **Resolution condition:** A fresh official campaign passes the Campaign 16 failure boundary without regressing the earlier two-level route; authoritative completion remains governed by B-003-0003.

## B-003-0010 - Compound-target sector overlay blocked post-collapse reacquisition

- **Status:** RESOLVED BY CAMPAIGN 18
- **Stage:** 10
- **Opened:** 2026-08-25
- **Owner:** Codex
- **Burden:** Campaign 17 locally solved two of three level-3 compound groups but did not reach level 4 or `WIN`. One remaining endpoint center was parser-joined to exactly one same-color sector of its complete compound target. The old contaminant certificate missed that relation, post-collapse reacquisition required an immediate direct relocation, and a subsequent open-space bootstrap recovered a marked endpoint from an already-solved group that the bootstrap reader rejected because no complete group contained it.
- **Why it matters:** Exact local completion must lead to the smallest readable continuation while the official state remains `NOT_FINISHED`. Treating a target-sector overlay as an independent target exhausts safe actions, while discarding a unique returned bootstrap endpoint erases valid causal evidence.
- **Failure evidence:** Frozen commit `0ae945257a1f8ed68d9462f6b0f0bf4083b41c5b`; campaign root `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-0ae94525-campaign17`; official recording `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-0ae94525-campaign17\official-recordings\c2120ed7e4beb839e4d0\8872dd93-7263-4c7c-8a2f-2d15e26f8f62\r11l-495a7899-9169ebee-d745-4730-b5f6-cb97a69dd8ed.jsonl`; SHA-256 `2685d5e81cb4b95c5784169a9e9a4a1675c4b3a94cb447fc5dd186c4ac864965`. The recording has 44 events: one initial reset observation and 43 non-reset submissions. Raw and official final state were `NOT_FINISHED`, `levels_completed=3/6`, with no reset and no observed `WIN`.
- **Exact diagnosis:** The remaining marker-14 group had endpoints `(58,32)`, `(44,12)`, `(27,52)`, mediator `(43,32)`, target `(50,12)`, and potential `3805`. A parsed seven-cell marker-14 target component was exactly the endpoint center `(44,12)` plus the six marker-14 cells of the complete twelve-cell compound target. The smallest projected parser-readable separation is `(41,12)`. Raw action 43 returned a unique marked endpoint at the submitted `(6,43)`, but `_marker_bootstrap_active_color` searched only endpoints inside complete groups.
- **Smallest local repair:** Certify only exact endpoint-center plus target-sector equality; bridge both post-collapse reacquisition and visible role transfer to a certified separation when direct improvement is absent; evaluate target rejoining against the same-color target sector; retain the resulting identity constraint only after exact target colors and raw component boundaries are observed; and accept one unique marked endpoint at the exact bootstrap coordinate even when its solved group is no longer complete.
- **Local verification evidence:** Repair commit `23b3b8b04eb00a8635e0c3efcfb9103c7dc1120a`. Focused replay matches Campaign 17 official actions 1-40 and first diverges at action 41 to `marker:14:separate:41,12`. The exact final official frame now localizes bootstrap active color 0, plans `marker:14:activate:44,12`, and projects separation `(41,12)`. The visual-policy suite reports 56 passed. Negative tests reject near-target similarity, retained marker bridges, and swapped target-sector colors. Ruff, format, strict mypy, JSON, and diff checks pass; independent final review found no remaining actionable issue.
- **Next discriminating action:** Freeze Campaign 18 from the verified repair. Require official observation of the separation and continued level-3 progress; any later distinct horizon remains a new burden rather than retroactively broadening this repair.
- **Resolution condition:** A fresh official campaign preserves the prior route, observes the exact target-sector separation, and continues without repeating the Campaign 17 reacquisition/bootstrap failure. Authoritative completion remains returned `GameState.WIN` under B-003-0003.
- **Official resolution evidence:** Frozen commit `98633c8f30d82ed0a92317a15a09566abe41ef56`; Campaign 18 root `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-98633c8f-campaign18`; official recording SHA-256 `2e478defe3b2af689e7266d45843fd72a6de93086b378cc5ea22bba6f22b315d`. Action 41 moved the certified contaminant from `(44,12)` to `(41,12)` and the returned frame passed the exact target restoration receipt. The identity constraint remained installed. Action 54's returned bootstrap frame localized active color 0 at `(6,43)` without residual. The subsequent exhaustion remains distinct from the overlay and bootstrap readers; B-003-0011 records how exact replay later localized its causal origin to a same-group target-role alias at action 51.

## B-003-0011 - Same-group connector role alias masked a legal continuation

- **Status:** RESOLVED BY CAMPAIGN 19; LATER LEVEL-5 RESIDUAL PRESERVED SEPARATELY
- **Stage:** 10
- **Opened:** 2026-08-25
- **Owner:** Codex
- **Burden:** Campaign 18 officially observed the repaired target separation and retained target identity, then deferred the remaining level-3 marker group and exhausted after 54 non-reset actions. Exact replay found that the apparent target blocking marker-14 progress was instead a raw component wholly contained in the same group's observed mediator/inferred connector raster. Treating that local role alias as independent target evidence removed a legal same-group action and caused the later orphan bootstrap and `PolicyError` while the official environment remained `NOT_FINISHED` at `levels_completed=3/6`.
- **Why it matters:** The learner must preserve independent target evidence while recognizing when its own observable connector topology receives a second descriptive role from perception. A deeper search would not repair that role conflict; it would plan around a false blocker and risk converting projection confidence into unsupported actions.
- **Failure evidence:** Frozen commit `98633c8f30d82ed0a92317a15a09566abe41ef56`; campaign root `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-98633c8f-campaign18`; official recording `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-98633c8f-campaign18\official-recordings\c2120ed7e4beb839e4d0\f2649d8b-c748-40e7-8dc5-8053977efa7f\r11l-495a7899-77287f8f-b6d0-4fba-8008-216a875c072d.jsonl`; recording SHA-256 `2e478defe3b2af689e7266d45843fd72a6de93086b378cc5ea22bba6f22b315d`; trace SHA-256 `ec60e614e97d7b7f32204bfaa9e8cb5c1f9358310894123f748a534e8fb72aa4`. The recording contains 55 events: one initial observation and 54 non-reset submissions, zero resets, raw and official final `NOT_FINISHED`, and no observed `WIN`.
- **Exact residual:** At official action 51 the remaining marker-14 group had endpoints `(46,3)`, `(50,34)`, `(37,12)`, mediator `(44,16)`, target `(50,12)`, and potential 410. A raw color-1, area-11 target candidate centered at `(42,10)` had all 11 observed cells inside that same group's mediator/connector raster and was not the certified raw target of another complete group. The frozen policy therefore deferred marker 14 at action 52. With only that role alias removed for same-group planning, replay matches official actions 1-51 and first diverges at action 52 to ordinary relocation `(35,6)`, which strictly reduces potential to 386 and reparses the same composite group.
- **Rejected local projection:** An initial shallow projection suggested activation `(46,3)`, stage `(31,12)`, switch `(37,12)`, relocate `(60,12)`, switch `(50,34)`, then `(59,14)` at potential 0. Exact frame re-extraction invalidated it: `_scene_after_marker_stage` translated the composite mediator to a generic object reference, causing later shallow edges to skip composite target and connector guards. The proposed final endpoint also overlapped a fixed endpoint. This route is not parser-safe, must not be played, and is preserved as failed local evidence rather than progress.
- **Smallest local repair:** For one composite group, exclude a raw target candidate only when every one of its observed cells is contained in that group's observed mediator plus inferred centerline raster and it is not the raw target of another complete group. Never exclude an exact composite target proxy. Carry the starting protected-target surface through only one shallow staging projection, require the same target center and cells after role-switch reparse, execute only the first action, and recompute from the official returned frame. No outside-group staging fallback or deeper action queue was added.
- **Local verification evidence:** Repair commit `ec8e27f45090a408e1ae942f12407680fbb0542f`. Exact Campaign 18 replay matches actions 1-51 and first diverges at action 52 to `marker:14:improve:35,6`. On the official action-51 frame, `(35,6)` is an ordinary candidate, is open, reduces potential `410 -> 386`, preserves endpoint separation, static clearance, every exact composite target, mediator and connector readability, and uniquely reparses the marker-14 group. Its structural consequence validator passes on the local projection. A re-extracted closed-loop projection reaches potential 0 through `(32,5)`, role transfer, `(58,15)`, `(58,20)`, role transfer, and `(60,11)`, but none of those future consequences is official evidence. The 61-test visual-policy suite, Ruff, strict mypy, format, diff, and nine-test repository secret scan pass; negative tests preserve partially intersecting targets, exact composite targets, other complete-group raw targets, and reject target-identity remapping during shallow carry.
- **Next discriminating action:** Freeze Campaign 19 from the verified repair and require the official action-52 consequence to preserve the predicted marker group before recomputing. Do not treat the projected tail as an action queue or as level completion.
- **Resolution condition:** A fresh official campaign observes the repaired same-group continuation and advances beyond the Campaign 18 boundary without losing independent target evidence; authoritative completion remains B-003-0003.
- **Official resolution evidence:** Campaign 19, frozen at `87364202a87772aea848de48ba1e3cdb9d14bfbf`, matched Campaign 18 actions 1-51, executed the repaired action 52 at `(35,6)`, finished the remaining level-3 group, and officially advanced to `levels_completed=4/6`. Its later level-5 bounded-probe/reset cycle is distinct and recorded as B-003-0012. Campaign root `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-87364202-campaign19`; verified recording SHA-256 `4416bb7a4062da9a967e233529c89a21d3faca83178a4ea7267cd0a9c0c922f7`.

## B-003-0012 - Palette-changed target role and all-prior veto caused a level-5 reset cycle

- **Status:** RESOLVED BY CAMPAIGN 20; LATER POST-CLOSURE RESIDUAL PRESERVED AS B-003-0013
- **Stage:** 10
- **Opened:** 2026-08-25
- **Owner:** Codex
- **Burden:** Campaign 19 officially advanced to level 5, then repeated five bounded coordinate probes twelve times before `GAME_OVER`, reset to the exact initial level-5 frame, and replayed the same 61-action episode. The level contained a clean transferred affine relation and exactly one readable hollow target, but the target palette no longer matched the mediator palette. Separately, one historical affine receipt above the existing support-error ceiling vetoed every reliable retained receipt. The resulting fallback probes had null plan signatures, so same-level reset retained neither the failed episode root nor a reason to choose a different branch.
- **Why it matters:** Retaining mechanics across levels is ineffective if one noisy receipt suppresses all reliable evidence or if target role is confused with palette identity. Replaying an exact failed episode consumes actions and lives without gaining information; neither four completed levels nor a complete-looking mechanic is `WIN`.
- **Failure evidence:** Frozen commit `87364202a87772aea848de48ba1e3cdb9d14bfbf`; campaign root `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-87364202-campaign19`; official recording `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-87364202-campaign19\official-recordings\c2120ed7e4beb839e4d0\a3fa6d43-6580-4bd0-857c-268be5930f37\r11l-495a7899-f92e3919-c638-4e4e-a111-6f6929db4d8f.jsonl`; recording SHA-256 `4416bb7a4062da9a967e233529c89a21d3faca83178a4ea7267cd0a9c0c922f7`; trace manifest SHA-256 `30d303c9125011be8923b80540d95745f1921cae81551491602482a884214ae9`. Verification passed with 240 artifacts and no errors. The recording contains 3,049 events: 3,000 non-reset submissions, 48 `RESET` submissions, and 48 raw `GAME_OVER` observations. Raw final state after the last permitted reset was `NOT_FINISHED`; the verified score boundary was `GAME_OVER`; `levels_completed=4/6`, `win_levels=6`, and completion was not observed.
- **Exact residual:** Level 5 began at action 62 with active endpoint `(25,35)`, fixed endpoint `(43,34)`, and mediator `(34,34)`. Campaign 19 action `(21,21)` moved the active endpoint by `(-4,-14)` and the mediator by `(-2,-7)`, exactly retaining the arity-2 affine relation. The frame exposed one and only one ordinary hollow target at `(53,28)`, with a palette different from the mediator. The frozen learner required same-color identity and then rejected transfer because its recent receipt window contained any support error above 6.0. Actions 62-121 repeated `[(21,21),(43,21),(21,43),(32,43),(43,43)]` twelve times; action 121 returned `GAME_OVER`; action 122 `RESET` restored the exact level-entry frame; action 123 repeated `(21,21)`.
- **Smallest local repair:** Preserve same-color target identity as primary. Only when no same-color target exists, permit exactly one readable hollow target to carry target role; two or more mismatched targets and equally supported mediator relations remain ambiguous. Filter retained affine receipts individually at the existing support-error ceiling instead of allowing one noisy receipt to veto every reliable prior. Record the first null-signature exploration decision of a failed episode and reject that exact state/action/kind root after same-level reset. Reset episode-local attempted identities, retain only context-bound failed roots across reset, and clear those roots only on genuine level progress.
- **Local verification evidence:** Repair commit `8c31b734401d6d5c6360facf95389ac18c13804e`. Exact Campaign 19 replay matches official actions 1-61 and first diverges at action 62 from fallback `(21,21)` to transferred-plan action `(60,28)`. The transfer re-reads active color 0, anchor `(43,34)`, arity 2, mediator color 0, and unique target `(53,28)` with support error 0.5; the remaining planned signature is `(60,28);(46,28)`, but only the first action may be submitted before official reobservation. The 71-test visual-policy suite, focused ambiguity/reset tests, Ruff, strict mypy, format, and diff checks pass. The nine-test secret scan passed with an explicit `C:\a` basetemp after the host's already-known default-temp ACL denial. Coordinate, activation, and marker-bootstrap roots are covered; one-candidate and capacity exhaustion fail explicitly; genuine level progress clears exclusions; counter regression fails closed. Multiple mismatched targets and tied mediator relations fail closed, and reliable transfer remains bounded by the existing 6.0 ceiling.
- **Next discriminating action:** None for this burden. Preserve Campaign 19 and Campaign 20 as paired failure/resolution evidence; post-local-closure continuation is B-003-0013.
- **Resolution condition:** Met by Campaign 20 passing the Campaign 19 level-5 cycle boundary, observing the transferred affine plan, and retaining distinct failed episode roots across same-level resets. Authoritative completion remains returned `GameState.WIN` under B-003-0003.
- **Official resolution evidence:** Campaign 20, frozen at `25afad247b6037aee1d6ab80f1ccc9b490930b52`, matched Campaign 19 through action 61 and executed the repaired level-5 actions `(60,28)`, `(43,34)`, and `(46,28)`. Those returned frames preserved the transferred arity-2 relation and placed its mediator exactly on the unique palette-changed target `(53,28)`. The environment remained `NOT_FINISHED`, level 4, so this was local mechanism confirmation rather than completion. Five later lives used five distinct exploration roots rather than replaying Campaign 19's exact failed episode. Campaign 20's distinct failure to reacquire the second affine group is B-003-0013.

## B-003-0013 - Local affine closure failed to reacquire the remaining group safely

- **Status:** RESOLVED BY CAMPAIGN 21; LATER COMPOSITION RESIDUAL IS B-003-0014
- **Stage:** 10
- **Opened:** 2026-08-25
- **Owner:** Codex
- **Burden:** Campaign 20 correctly completed one level-5 affine relation, but `NOT_FINISHED` left another relation unresolved. The policy cleared the local plan without arming ordinary-group reacquisition, moved the solved group away from its target with a free-space probe, and later chose endpoint placements that merged readable components. Five distinct bounded episode roots ended in `GAME_OVER`; after the fifth reset and a sixth three-action prefix, selection failed explicitly because every bounded root had already failed.
- **Why it matters:** A correct local mechanic does not complete a level. Retained mechanics must preserve completed structures, transfer the active role into a remaining group with the smallest readable action, and recompute from the returned frame. Blind probing after local closure can destroy the evidence that made progress possible.
- **Failure evidence:** Frozen commit `25afad247b6037aee1d6ab80f1ccc9b490930b52`; campaign root `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-25afad24-campaign20`; official recording `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-25afad24-campaign20\official-recordings\c2120ed7e4beb839e4d0\3d3e7048-42fa-40d9-9c7d-674d859c8a66\r11l-495a7899-902bd0c6-b08d-4c74-84b2-b67e7ea0ae70.jsonl`; recording SHA-256 `2dba21f16c225383b47ece4d0217b72f2b229eeca32f45b54525830b3a479392`; trace manifest file SHA-256 `2899425bf1fc8bf3ec9d292da9498014dcfca788c9a06c30e911dec0f1eb42d5`; evaluation manifest SHA-256 `8cd8c94f03c366dad0dec80d789da35ff07bf124c5a08df1bc1470670aaff6fe`. Artifact verification passed with 662 files and no errors. The raw recording contains 370 observations and 369 submissions: 364 non-reset actions, five `RESET` actions, and five observed `GAME_OVER` states. Every reset returned the exact initial level-5 frame. The last raw observation was `NOT_FINISHED`, `levels_completed=4/6`; completion was not observed.
- **Evaluator boundary:** The recovered verified scorecard records `official_run_state=GAME_OVER`, 369 official run actions including five resets, level actions `[3,10,13,35,308,0]`, score `43.36831875607386`, and `completed=false`; standalone official RHAE remains unavailable. The wrapper reports `FAILED_INFRASTRUCTURE` because selection raised `PolicyError: all bounded coordinate-probe episode roots already failed`. Its salvaged `metrics.game_over_events=0` contradicts the five raw recording states and is an evaluator-accounting defect, not evidence that those failures did not occur.
- **Exact residual:** After actions `(60,28)`, `(43,34)`, `(46,28)`, the solved endpoints `(60,28)` and `(46,28)` had centroid and mediator `(53,28)` on the sole target. A second fixed group `(52,55)`, `(34,55)`, `(41,47)` still had centroid `(42.33,52.33)` and mediator `(42,52)`. The learner nevertheless selected free-space `(53,17)` at action 65. Later `(59,31)` merged the active endpoint with the solved mediator, and `(41,47)` was structurally unsafe because its prospective active-color glyph was one cell from the remaining mediator. Multi-target ambiguity appeared only after that damage; replacing the observed sequential many-to-one target use with speculative one-to-one matching was therefore rejected as the root repair.
- **Smallest local repair:** When an ordinary affine action satisfies its local target but the official state remains `NOT_FINISHED`, retain that target center for one bounded reacquisition. Exclude the satisfied mediator and any mediator already coincident with another visible target; enumerate at most twelve readable endpoints and arities 2..6; require one unique endpoint subset across all tested arities whose centroid explains a remaining mediator within error 1.5; and choose the uniquely nearest role exchange whose two prospective recolors preserve component identity. Recognize only an exact clicked-endpoint/unique-active-endpoint swap as successful evidence. For radial continuation, bind each support point to its actual mover and reject full-glyph, static-component, and pairwise prospective collisions under both the temporary active color and the fixed color acquired after the next switch. Execute only one action before official reobservation.
- **Local verification evidence:** Repair commit `858650c4b6494faaeee5e8ef24cd1bf27257e82d`. Exact Campaign 20 replay matches all non-reset actions 1-64 and first diverges at action 65 from damaging free-space `(53,17)` to safe remaining-group role exchange `(34,55)` on frame `sha256:1f03ce474eb4b8f3cb5efb9c70424210c9ad70956ed5937abe6234ced1ba517f`. The nearest endpoint `(41,47)` is rejected by the observed component geometry. A synthetic returned exact role swap remains readable, does not set probe failure, and uniquely transfers the retained affine form to arity 3 with anchors `(52,55)` and `(41,47)`. The audited radial support sequence `(57,18);(60,37);(42,29)` remains parser-distinct in every immediate and post-switch color state. Arbitrary recoloring, cross-arity centroid ties, and already-satisfied visible groups fail closed. The 76-test visual-policy suite, focused regressions, Ruff, strict mypy, format, diff, and nine-test secret checks pass.
- **Next discriminating action:** None for this burden. Preserve Campaign 20 and Campaign 21 as paired failure/resolution evidence; the distinct composition residual is B-003-0014.
- **Resolution condition:** Met by Campaign 21 officially returning the safe `(34,55)` role exchange and every readable arity-3 support consequence without damaging the first relation. Authoritative completion remains returned `GameState.WIN` under B-003-0003.
- **Official resolution evidence:** Campaign 21 matched the prior official trace through action 64, returned action 65 `(34,55)` as the exact clicked-endpoint/unique-active-endpoint role exchange, inferred the remaining arity-3 relation, and returned readable consequences for `(57,18)`, role switch `(52,55)`, `(60,37)`, role switch `(41,47)`, and `(42,29)`. No component merge occurred. The official state after action 70 was still `NOT_FINISHED`, level 4, so this resolves only safe remaining-group reacquisition and radial execution. Recording SHA-256 `6ca07e11d4c294ce4429b2327a37c67b6c40927aa269791dc459442cba861211`.

## B-003-0014 - One palette-mismatched target was incorrectly reused as each child mediator's flat target

- **Status:** RESOLVED AS A FAILED HYPOTHESIS BY CAMPAIGN 22; CHILD-ISOLATION RESIDUAL PRESERVED AS B-003-0015
- **Stage:** 10
- **Opened:** 2026-08-25
- **Owner:** Codex
- **Burden:** Campaign 21 confirmed the Campaign 21 repair but did not advance beyond level 5. The learner applied the sole palette-mismatched target independently to two same-color child mediators. It first placed the arity-2 mediator at `(53,28)`, then safely transferred and placed the arity-3 mediator at the same coordinate. Both local relations closed, their visible mediators collapsed to one component, and the official environment still returned `NOT_FINISHED`.
- **Why it matters:** Uniqueness can identify a target role without identifying its compositional layer. A target whose palette differs from multiple child mediators must not be silently reused as an individual target for each child after official nonprogress falsifies that interpretation.
- **Failure evidence:** Frozen commit `2702c4d5a1f8e604cb806bdb7d811a4df0c3da4d`; campaign root `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-2702c4d5-campaign21`; recording `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-2702c4d5-campaign21\official-recordings\c2120ed7e4beb839e4d0\1e61572e-37d9-4a16-be59-a2da56fac203\r11l-495a7899-5d4ca9c6-cd2a-4c75-a792-c1fcd86141ee.jsonl`; recording SHA-256 `6ca07e11d4c294ce4429b2327a37c67b6c40927aa269791dc459442cba861211`; trace-manifest file SHA-256 `8224f887dae6004a382a598326807de06b9e6f63cdae719c1591d17fcc6b4df0`; evaluation-manifest file SHA-256 `2199bf46ba02341c8eaf34cd6e6ec9523dee6b718802251a4905e6dec7a8b19b`. Public artifact verification passed with 332 artifacts and no errors. The recording has 187 observations and 186 submissions: 184 non-reset actions, two `RESET` actions, and two raw `GAME_OVER` observations. The last raw observation was `NOT_FINISHED`, `levels_completed=4/6`; completion was not observed.
- **Evaluator boundary:** The verified score boundary is `GAME_OVER`, 186 official submissions including two resets, level actions `[3,10,13,35,125,0]`, local score `43.36831875607386`, and `completed=false`; standalone official RHAE is unavailable. The wrapper ended `FAILED_INFRASTRUCTURE` on `PolicyError: a local affine target is complete but no unique structurally safe remaining-group role exchange is readable`. Salvaged `metrics.game_over_events=0` contradicts the two raw official `GAME_OVER` observations and remains an evaluator-accounting defect.
- **Exact residual:** At the clean level-5 entry, endpoints partitioned uniquely into an arity-2 group around mediator `(34,34)` and an arity-3 group around mediator `(42,52)`. Both mediators had palette 0; the lone target at `(53,28)` had palette 14. Earlier completed ordinary levels paired child mediators with same-palette targets. The unique-mismatched-target fallback erased that layer distinction. Campaign 21 action 64 placed the first mediator at `(53,28)` without progress; action 70 placed the second there too, leaving one visible mediator component at the target and still no progress. Subsequent generic probes damaged the readable hierarchy; two lives ended in `GAME_OVER`, and the third failed closed after the flat roots were excluded.
- **Smallest bounded hypothesis:** Treat the observation as a candidate second-order affine relation only when multiple same-palette child mediators, one differently paletted target, and a unique disjoint endpoint partition are all readable. Preserve child mediators as distinct supports whose centroid explains the parent target; require parser-safe endpoint, mediator, and cross-group geometry; select one symmetric target-relative support layout; execute one action at a time and reobserve. This is a provisional interpretation, not official evidence, until the environment advances.
- **Local verification evidence:** Repair commit `cfc1d245750574b6545b964c31142d77c24e14a2`. Exact Campaign 21 replay preserves all actions 1-61 and first diverges at action 62 to `(58,37)`. The exact global cover has child arities 2 and 3; its distinct supports `(59,31)` and `(47,25)` have parent centroid `(53,28)`. The certified full sequence is `(58,37)`, `(43,34)`, `(60,25)`, `(52,55)`, `(42,22)`, `(41,47)`, `(47,31)`, `(34,55)`, `(52,22)`, with exact returned-frame revalidation required after every action. The shared deterministic search retains 37,471 of 131,072 units on the recorded frame. Ignored final actions, no-layout and exhausted searches, consequence-time exhaustion, unfair six-child support prefixes, synthetic target creation, unrelated one- and two-cell centerline blockers, target-region overlap, and flat-target collapse have focused regressions. The 87-test visual-policy suite, eleven focused review regressions, Ruff, strict mypy, compilation, diff checks, nine secret tests, and independent review pass. No target identifier or known coordinate appears in production. This does not establish official progress.
- **Next discriminating action:** None for this burden. Preserve Campaign 21's flat-target failure and Campaign 22's joint-hierarchy failure as paired evidence; B-003-0015 owns the smaller child-isolation discriminator and Campaign 23.
- **Resolution condition:** Met by Campaign 22 preserving a bounded official failed-mechanism result: the distinct child supports and their exact parent centroid were returned without level progress. `GameState.WIN` remains the only game-completion authority.
- **Official resolution evidence:** Campaign 22, frozen at `9c513fa7375e97606a337f26fe63e8cfa99be693`, executed all nine certified hierarchy actions. Its final `(52,22)` consequence returned distinct child supports `(59,31)` and `(47,25)` with parent centroid `(53,28)`, but the environment remained `NOT_FINISHED`, `levels_completed=4/6`. The exact mechanics residual is `distinct child mediators reached the predicted parent centroid but the official environment remained NOT_FINISHED`. This resolves B-003-0014 by falsifying the proposed joint parent-centroid sufficiency, not by advancing the level or completing the game.

## B-003-0015 - Joint parent-centroid and child-only sufficiency both failed officially

- **Status:** RESOLVED AS A PRESERVED FAILED MECHANISM; CAMPAIGN 25 RETURNED THE EXACT TERMINAL CHILD ISOLATION WITHOUT PROGRESS
- **Stage:** 10
- **Opened:** 2026-08-25
- **Owner:** Codex
- **Burden:** Campaign 22 falsified joint parent-centroid sufficiency. Campaign 23 exposed and locally repaired endpoint-oriented connector rounding. Campaign 24 exposed mediator-over-endpoint z-order before the terminal child discriminator. Campaign 25 then returned the complete exact child isolation with the sibling and target preserved, but the environment remained `NOT_FINISHED`, `levels_completed=4/6`. Child-only sufficiency is therefore also falsified. The returned lower-layer mechanics remain bounded evidence, and `WIN` remains unobserved.
- **Why it matters:** A correctly predicted geometric consequence is not evidence that the geometry is sufficient for level completion. Failure to retain a rejection admits zero-information repeats, while a projection/perception raster mismatch can discard a correct returned structure and force unrelated replanning. The official resource horizon makes both failures consequential. The learner must discriminate the implicated compositional layer without discarding reliable child mechanics or relaxing exact readability certificates.
- **Campaign 22 failure evidence:** Frozen commit `9c513fa7375e97606a337f26fe63e8cfa99be693`; interrupted local-public campaign root `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-9c513fa7-campaign22`; official recording `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-9c513fa7-campaign22\official-recordings\c2120ed7e4beb839e4d0\230b1ca8-6a47-4dad-adaa-c9789a478822\r11l-495a7899-1c7edbae-2d45-48d3-b387-7bb81a620e88.jsonl`, 10,751,052 bytes, SHA-256 `7d2e9d123865906d5f7a8d679199be5feff5fbcef9ffc1695418311eab3d8433`; active trace `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-9c513fa7-campaign22\t\c2120ed7e4beb839e4d0\active.jsonl`, 9,457,706 bytes, SHA-256 `578fbe962e674c2035cdaa97f357a04ec3e044e976c75edd06b668e2720b6dfa`; evaluation manifest `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-9c513fa7-campaign22\manifest.json`, 4,448 bytes, SHA-256 `ac6796126b0bb580af0c7c25d890fc61f1991c3f9a06a195c98d3f488d120241`, embedded manifest hash `889afcac9fcd2c53478e94e79c7bd5df2528d2b19e6227021d8b0a96cca88e07`. The recording contains 506 observations and 505 submissions: 498 `ACTION6`, seven submitted `RESET`, seven `GAME_OVER`, and zero `WIN`. Its final row is `NOT_FINISHED`, `levels_completed=4/6`, `win_levels=6`, after `(43,41)`.
- **Campaign 22 interruption boundary:** The evaluation manifest remains `IN_PROGRESS`, with no terminal scorecard, run receipt, sealed trace manifest, or standard replay receipt. The active trace nevertheless verifies read-only through all 3,029 event self-hashes and links, 530 unique referenced blobs, 505 submissions, and 505 consequences. Trace observations 0-504 exactly match recording rows 0-504, and every consequence exactly matches successor recording rows 1-505. Interruption therefore occurred after the final consequence and official row were durable but before the final normalized `observation.received` and `mechanics.action_receipt`, leaving 505 trace observations and 504 mechanics receipts. This is interrupted evidence, not a terminal evaluation or completion.
- **Campaign 22 exact residual:** Zero-based recording rows 62-70, physical lines 63-71, returned hierarchy actions `(58,37)`, `(43,34)`, `(60,25)`, `(52,55)`, `(42,22)`, `(41,47)`, `(47,31)`, `(34,55)`, `(52,22)`. The final returned supports `(59,31)` and `(47,25)` had exact centroid `(53,28)`, yet the official state remained `NOT_FINISHED`. Zero-based row 71, physical line 72, repeated `(52,22)` under `affine-hierarchy-mechanic:a55bed6f6fd23aa58318d443`; endpoint, mediator, target, and hierarchy geometry were unchanged, while the sole raw-frame delta was HUD cell `(0,10)` from 0 to 5. The step was labeled `PROGRESS` with no residual. Joint parent-centroid sufficiency and zero-displacement progress eligibility are therefore the implicated failed hypotheses.
- **Campaign 23 failure evidence:** Frozen commit `cf6acbd46dfc46e946e9a6ed76dc0a819142a63e`; interrupted local-public campaign root `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-cf6acbd-campaign23`; official recording `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-cf6acbd-campaign23\official-recordings\c2120ed7e4beb839e4d0\42e0bbd4-faab-427f-84bc-d5780087c57f\r11l-495a7899-1d938189-7780-46d7-af75-ed84a8df806b.jsonl`, 4,016,056 bytes, SHA-256 `1e4e500cb203ccefe7a7daa2534a6706043fa3e5f78c73b20a73151a0af7186e`; active trace `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-cf6acbd-campaign23\t\c2120ed7e4beb839e4d0\active.jsonl`, 1,524,350 bytes, SHA-256 `ed9e6f41c585f4053f47b784ac9b68e75aa20fa0cbe57658203fef17f1c651eb`; evaluation manifest `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-cf6acbd-campaign23\manifest.json`, 4,444 bytes, SHA-256 `325da60fd959156530475a81ccdc3d73ba7e903f43a1391b9a407469d2f7419b`, embedded manifest hash `4847b0554222aee32785c2a97ccefb79c4d91c2e942a048af1be5a709c75ff57`. The recording contains 77 observations and 76 submissions, all `ACTION6`: row 0 is one initial SDK `RESET` return with `full_reset=true`, not a submitted reset. There are zero submitted `RESET`, zero `GAME_OVER`, and zero `WIN`; the final row is `NOT_FINISHED`, `levels_completed=4/6`, `win_levels=6`, after `(53,36)`.
- **Campaign 23 interruption and integrity boundary:** The manifest remains `IN_PROGRESS`, with no terminal scorecard, run receipt, sealed trace manifest, or standard replay receipt. All 458 active events verify through schema, self-hash, chain-link, and unique-ID checks: 77 observations, 77 candidate generations, and 76 each of selections, submissions, consequences, and mechanics receipts. All 617 blob references resolve to 127 unique SHA-verified blobs totaling 1,110,689 bytes. Recording and trace have zero crosswalk mismatches across every observation/frame, action, mechanics action, and successor consequence. The first event hash is `sha256:99e34ae04590b84c3f32dbd482babd66afa47e2300e5d81a056a4998c19c75fc`. The final step-75 action was fully observed and receipted; the tail is step-76 `action.candidates_generated` with hash `sha256:706c8635c0f08c8204bbfbeca272e248dc37acf488e8b2916957c50c52aea212`, before any next selection or submission. This is complete interrupted evidence, not a sealed terminal evaluation.
- **Campaign 23 exact residual:** The clean level-entry scene generated child-isolation signature `affine-child-isolation:861a43265b195cdf82a5e278` and sequence `(34,55)`, `(53,36)`, `(41,47)`, `(46,24)`, `(52,55)`, `(60,24)`. Zero-based row 62 / physical line 63 returned `(34,55)` with no residual. Row 63 / line 64 returned `(53,36)` with the predicted arity-3 endpoints, selected mediator `(48,46)`, active endpoint, unchanged arity-2 sibling, unique hierarchy, parent target, and target surface, but receipt event `E-01787658016155768300-0000017b-d666670999954500b3249a14d6166571` recorded `planned child-isolation consequence was not structurally readable`. The frozen endpoint-to-mediator raster projected tie cell `(50,41)` through Python banker rounding; the official frame contained `(51,41)`, so returned connector evidence lost the bridging leg and failed exact signature equality. The plan cleared, and the next submitted action was `(43,34)` rather than queued `(41,47)`. This implicates connector rasterization, not the still-unobserved terminal child-isolation sufficiency.
- **Campaign 22 resource-exhaustion evidence:** Starting from the clean level-5 entry, each of seven complete lives ended in `GAME_OVER` after exactly 60 non-reset submissions. The `GAME_OVER` observations are zero-based rows `[121,182,243,304,365,426,487]`; submitted resets are rows `[122,183,244,305,366,427,488]`. Every terminal transition's only last-frame delta was HUD cell `(0,63)` from 0 to 5, and every submitted `RESET` restored the same clean representative scene with raw frame-blob hash `e8fa97c6ef0a46f53703793a5caa686f6d280430a92760f42bca5716b11311ee`. The seven action sequences differed, and terminal scene geometry was not uniformly identical; only the final two terminal frame hashes matched. The exact repeated evidence is the 60-submission horizon, terminal HUD delta, and clean reset. The eighth life contains 17 non-reset submissions and ends at the interrupted `NOT_FINISHED` row. Do not describe these as identical play.
- **Child-isolation repair before Campaign 23:** Persist the official joint-hierarchy rejection independently of frame-specific mechanic hashes and reject progress actions with zero geometric displacement. From the clean scene, enumerate only a generic exact-projected non-active-child isolation: select one unique child and mover assignment, place that child's mediator exactly on the sole target, and preserve every endpoint and mediator of the other child. Preserve the raw parent-target surface and identity while allowing only expected selected-child mediator or connector occupancy that reparses as the already-observed flat-target geometry. Require static safety, component identity, exact child centroid, uniqueness, and a bounded search; submit only the first action before official reobservation.
- **Local verification evidence:** Repair commit `20078a1a0c6e549017fa02e4ac3b50da7e781a0f`. The exact Campaign 21 clean-frame replay yields `(34,55)`, `(53,36)`, `(41,47)`, `(46,24)`, `(52,55)`, `(60,24)` and places only the selected child mediator at `(53,28)`, while 106,434 of 131,072 deterministic search units remain. Every projected step is reparsed and certified against exact endpoint outer shape/color, endpoint center marker, mediator shape/color/center marker, selected and frozen connector signatures, frozen-sibling state, and the raw/composite target surface. Stable center-marker changes alter the semantic relation key; mutable outer active-role swaps do not. `UNKNOWN` and `NOT_PLAYED` clear only the concrete plan without manufacturing semantic falsification. The pre-Campaign23 committed-source suite passed 100 tests, including 11 focused child-isolation tests, plus nine secret tests and source-quality checks. Campaign 23 subsequently exposed the P1 raster and fallback findings preserved below and in B-003-0016. These are bounded local, recorded-frame, and official-consequence checks, not official progress.
- **Campaign 23 smallest repair and verification:** Commit `b7a606673e87ea0d25c69e086bf7d8b00ed5bf68` keeps connector signatures exact, but rasters every connector in its existing endpoint-to-mediator orientation with exact integer nearest rounding and ties toward the endpoint start. A 666-frame audit of readable official level-4 frames from Campaigns 21-23 matched every unobstructed connector under this rule and found counterexamples to banker and orientation-independent half-up rounding. Recorded-frame replay retains the same six-action child-isolation signature and sequence, certifies both returned Campaign 23 actions without structural residual, and keeps `(41,47)` queued. The exact visual-policy suite passed 103 tests, including 13 focused child-isolation tests; all nine repository secret tests, Ruff lint and format, strict mypy, compilation, JSON parsing, and diff checks passed. This is not official progress.
- **Campaign 24 failure evidence:** Frozen commit `dac59f09c051fcfc4153a5ea765c40e3f26126e9`; interrupted root `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-dac59f09-campaign24`; recording `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-dac59f09-campaign24\official-recordings\c2120ed7e4beb839e4d0\2db4f7cf-ce0b-4e40-a800-194cf002940c\r11l-495a7899-558e28c4-2724-49ea-90b5-2b9141f39993.jsonl`, 5,632,179 bytes, SHA-256 `3f4d4fee1b2a856f0db5a3dc828e63d70fa95041fd956d88a4d149e9520c390f`; active trace `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-dac59f09-campaign24\t\c2120ed7e4beb839e4d0\active.jsonl`, 2,401,590 bytes, SHA-256 `3a34cac4125a1e70cd80d56b55059957f2858403a091fc85ce3e8aee9f5cb61e`; evaluation manifest `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-dac59f09-campaign24\manifest.json`, 4,448 bytes, SHA-256 `fb40e5677b254bc3ba69df1bb2e30a74ea79930e150f787b0fa089959098d378`, embedded hash `4cc2a60be9f3feab0d2a2b2c34e77153fb5c8e7764d49dd4e736839b87703571`. The recording contains 123 observations and 122 submissions: 121 `ACTION6`, one submitted `RESET`, one `GAME_OVER`, and zero `WIN`. Final row 122 is `NOT_FINISHED`, `levels_completed=4/6`, after the submitted reset.
- **Campaign 24 integrity boundary:** All 734 active events verify through schema, self-hash, chain link, and unique-ID checks. All 873 blob references resolve to 213 unique SHA-verified blobs totaling 1,830,597 bytes. Recording and trace have zero crosswalk mismatches across all 123 observations and 437 frames, all 122 selections and submissions, all 122 mechanics actions, and all 122 successor consequences. The submitted step-121 reset was fully observed and receipted; the tail is step-122 `action.candidates_generated`, hash `sha256:cf0d1d9190227eebed24998794faa8f8a6722b18adb5ea9340387ddf23321e3e`, before any next selection. The manifest remains `IN_PROGRESS`; this is interrupted evidence, not a sealed evaluation.
- **Campaign 24 exact residual:** The clean scene reproduced signature `affine-child-isolation:861a43265b195cdf82a5e278` and actions `(34,55)`, `(53,36)`, `(41,47)`, `(46,24)`, `(52,55)`, `(60,24)`. Rows 62-64 returned and certified the first three actions. Row 65 returned `(46,24)`, `NOT_FINISHED`, changed 99 cells, with receipt `E-01787660391373002600-00000187-c416eb48dc7f459db51a793f74fe696c` and residual `planned child-isolation consequence was not structurally readable`. The raw parent target and exact five-entry target surface remained unchanged, and the active endpoint reached `(46,24)`. Official mediator color 0 replaced endpoint color 3 at `(51,36)` and `(52,37)`, reducing the stationary endpoint shell from twelve to ten cells and preventing exact hierarchy recognition. Queued actions `(52,55)` and terminal `(60,24)` were never submitted.
- **Campaign 24 terminal boundary:** No frame satisfies the exact two-child isolation predicate. Rows with a color-0 mediator at `(53,28)` have only four readable endpoints and no exact arity-3 endpoint subset with centroid `(53,28)`. Campaign 24 therefore neither confirms nor falsifies terminal child-isolation sufficiency.
- **Campaign 25 repair verification:** Commit `f10abedad58366ad24719b90b235a36dc38943df` paints endpoints before the recomputed mediator and retains lineage only through one exact predeclared occlusion. Campaign 24 replay regenerates the same six actions; rows 62-65 have zero selected-raster and protected-board differences, with full-frame differences restricted to cumulative x=0 HUD receipts. Row 65 certifies only missing passive endpoint `(53,36)` and overlap `{(51,36),(52,37)}`, and queued `(52,55)` remains safe. Mutations of selected/frozen glyphs, connectors, target, overlap metadata, or any non-left-column board cell fail closed. A nonmatching assessed consequence or queued precondition latches exact level/relation/plan/phase identity before fallback; terminal completion still requires a fully readable hierarchy.
- **Campaign 25 official resolution evidence:** Frozen commit `b2d482b0c85ec2d433a6b0c7e769ce1e2d1c12d1`; evaluation root `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-b2d482b-campaign25`; official recording `official-recordings\c2120ed7e4beb839e4d0\ad3ea1db-a1f3-40f1-b972-94dbe2f687ec\r11l-495a7899-a57260a0-9b85-47de-b739-170856d723e1.jsonl`, 3,924,795 bytes, SHA-256 `f4b3d0d3189ba2ac299bc63ddef7d8a3a1498cb590210dbb1872123e66dfc38c`. The verified recording and sealed replay contain 68 observations and 67 `ACTION6` submissions, zero submitted resets, zero `GAME_OVER`, and zero `WIN`. Rows 62-67 returned all six exact child-isolation actions. Row 67 `(60,24)` changed 100 cells, fully reparsed the selected arity-3 child with its mediator at the parent target, preserved the sibling and target, and returned `NOT_FINISHED`, `levels_completed=4/6`, `win_levels=6`. The verified local scorecard reports level actions `[3,10,13,35,6,0]` and score `43.36831875607386`; it is not completion or official-private evidence.
- **Next discriminating action:** None for this burden. Preserve both compositional failures. B-003-0018 owns restoration from the exact child discriminator and the next distinct joint test.
- **Resolution condition:** Met by Campaign 25 preserving an exact official `NOT_FINISHED` failed-mechanism result at the complete child-isolation consequence. Only official `GameState.WIN` resolves game completion.

## B-003-0016 - Generic coordinate fallback consumed a preserved composite target after hierarchy deferral

- **Status:** RESOLVED; FAIL-CLOSED READABLE-HIERARCHY BOUNDARY VERIFIED THROUGH CAMPAIGN 24; LATER LINEAGE LOSS MOVED TO B-003-0017
- **Stage:** 10
- **Opened:** 2026-08-25
- **Owner:** Codex
- **Burden:** Campaign 23's first connector-certificate failure caused repeated hierarchy deferral. After parser-safe role bindings were exhausted, the policy fell through to a generic coordinate probe even though the hierarchy and five target surfaces were still readable. That probe moved the active endpoint onto an unrelated composite target and erased its observable identity.
- **Failure evidence:** Trace step 74 submitted `(53,17)` and the official successor remained `NOT_FINISHED`, level 4. The translated endpoint footprint intersected target-ring cells `(54,17)`, `(54,19)`, `(55,16)`, and `(55,20)` around composite target `(57,18)`. The exact child-isolation target-surface signature had five entries through observations 61-74 and only four after observations 75-76. The action changed 97 cells and carried no mechanics residual. Its center passed the generic mostly-background `scene.is_open` check; its full endpoint glyph was not target preserving.
- **Why it matters:** Exhaustion of a bounded hierarchy search does not authorize destruction of already-readable target evidence. A center-only openness heuristic is insufficient for an action whose consequence translates a multi-cell endpoint and its relation surfaces.
- **Smallest repair:** After target-protected hierarchy planning and parser-safe role exchange both return no continuation, stop explicitly before `_probe_coordinate`. Preserve the separate marker-bootstrap path. Do not weaken the hierarchy or target-surface certificate and do not invent a target-specific exclusion.
- **Local verification evidence:** Commit `b7a606673e87ea0d25c69e086bf7d8b00ed5bf68`. A focused synthetic hierarchy regression forces child and joint layout deferral, marks every parser-safe role binding exhausted, replaces the generic probe with a failing sentinel, and confirms selection raises `PolicyError` with `no parser-safe target-preserving continuation` while leaving the observation unchanged. The relevant three-test policy slice and the 103-test exact visual-policy suite pass; repository secret and source-quality checks are clean.
- **Campaign 24 resolution evidence:** The exact five-entry target-surface signature was preserved through every submitted child-hierarchy action at rows 62-65 and remained exact through row 68. No readable exhausted hierarchy reached the generic `_probe_coordinate` path. The first later target-surface loss occurred at row 69 under newly inferred flat mechanic `affine-transfer:060668f3012a0b86f35b`, after row 65's official z-order occlusion had already made the hierarchy unreadable. That later semantic-lineage gap is distinct and is carried by B-003-0017; it does not reopen the narrower readable-hierarchy fallthrough repaired here.
- **Next discriminating action:** None for this burden. Preserve the Campaign 23 failure and Campaign 24 target-preserving hierarchy actions as paired evidence; B-003-0017 owns occlusion lineage and post-loss cycling.
- **Resolution condition:** Met by the committed fail-closed regression and Campaign 24 preserving every target surface through all officially submitted readable-hierarchy actions.

## B-003-0017 - Official child occlusion erased hierarchy lineage and opened a source-free resource cycle

- **Status:** RESOLVED; CAMPAIGN 25 RETAINED EXACT LINEAGE THROUGH OCCLUSION AND REACHED THE TERMINAL DISCRIMINATOR
- **Stage:** 10
- **Opened:** 2026-08-25
- **Owner:** Codex
- **Burden:** Campaign 24's predictable mediator-over-endpoint occlusion made one certified arity-3 endpoint temporarily unreadable. The policy treated that structural residual as loss of the child relation, cleared the plan, and allowed unrelated flat-affine and later source-free probing. Reliable child mechanics were therefore not carried through a uniquely explainable visual occlusion, and the remaining resource horizon was consumed without observing the intended terminal discriminator.
- **Exact lineage evidence:** Row 65 action `(46,24)` used source mechanic `affine-hierarchy-mechanic:3d648e873f2ff6503d52e4e3` and returned `planned child-isolation consequence was not structurally readable`. Row 66 immediately used no source mechanic and only hypothesis `M-0001@1`. Rows 67-69 then used newly inferred flat mechanic `affine-transfer:060668f3012a0b86f35b`; later flat hashes replaced it. By rows 92-121, all thirty receipts carried no source mechanic and only `M-0001@1`.
- **Target and cycle evidence:** The exact five-entry target surface remained intact through row 68. Row 69 reduced it to four entries by losing composite target `(57,18)`; subsequent actions alternately obscured composite targets and reduced the raw parent target from twelve to ten cells. Rows 92-121 repeated `(42,17)`, `(42,28)`, `(42,17)`, `(53,17)`, `(42,28)`, `(42,39)` five times. The fifth cycle ended at row 121 with `GAME_OVER`; the sole final-frame delta was HUD cell `(0,63)` from 0 to 5. This level-4 life contained exactly 60 non-reset submissions. Submitted reset row 122 restored the exact clean entry frame `sha256:54517890aeb9985f61b806d47cbc50f927dd2acb65f42406a2afb27eeda0293b`.
- **Why it matters:** A learner that retains a mechanic only while every glyph remains fully reparsable can discard correct causal lineage under predictable occlusion, manufacture unrelated local mechanics from the damaged state, and spend the finite official horizon on repeated low-information probes. Neither a repeated coordinate cycle nor apparent mediator-at-target geometry is completion evidence.
- **Smallest repair:** Match official mediator-over-endpoint projection. Carry the prior child identity across an occluded consequence only when exact projected and returned pixels agree, the hidden cells equal the unique mediator/endpoint footprint intersection, the endpoint center marker and visible residue identify one prior endpoint, the sibling and target certificates remain exact, and no competing hierarchy assignment exists. Protect every non-left-column cell while allowing only the officially observed x=0 lifecycle HUD evolution. Continue only the already-certified child plan under per-action reobservation. If uniqueness fails, preserve the residual and fail closed before flat-affine or source-free fallback; do not relax shape equality globally or reconstruct arbitrary history.
- **Verification:** Commit `f10abedad58366ad24719b90b235a36dc38943df`. Campaign 24 read-only policy replay matches all 65 actions through the residual boundary and regenerates the six-action child plan. Rows 62-65 match the projected selected raster and protected board exactly; the official full-frame differences are only cumulative x=0 HUD cells. Row 65 uniquely certifies the two-cell occlusion and keeps `(52,55)` queued. Focused mutation, terminal, queued-precondition, parser-exhaustion, reset, and durable-lineage regressions pass. This is recorded-frame and local verification, not an official continuation or completion result.
- **Campaign 25 official resolution evidence:** The frozen Campaign 25 replay returned `(46,24)`, `(52,55)`, and terminal `(60,24)` under the same child plan after the Campaign 24 occlusion boundary. Every protected raster and target certificate matched; no target damage, flat-affine replacement, source-free probe, repeated cycle, reset, or `GAME_OVER` occurred. The terminal exact child discriminator was observed and remained `NOT_FINISHED`. This resolves the occlusion-lineage burden while opening the distinct visible-count and restoration burden B-003-0018.
- **Next discriminating action:** None for this burden. Preserve the Campaign 24 failure and Campaign 25 paired repair evidence; B-003-0018 owns the terminal de-occlusion and exact restoration.
- **Resolution condition:** Met by Campaign 25 retaining the child relation across the official occlusion without target damage or source-free cycling and observing the exact terminal child consequence. `GameState.WIN` remains the sole completion authority.

## B-003-0018 - Exact terminal de-occlusion was misclassified as hierarchy lineage loss

- **Status:** RESOLVED; CAMPAIGN 26 OFFICIALLY RETURNED THE EXACT RECOVERY AND TESTED THE DISTINCT EQUAL-WEIGHT JOINT COMPOSITION
- **Stage:** 10
- **Opened:** 2026-08-25
- **Owner:** Codex
- **Burden:** Campaign 25 returned the exact terminal child-isolation projection, including the expected reappearance of a previously occluded endpoint, but the frozen consequence gate required raw visible endpoint and mediator counts to remain equal before and after the action. The endpoint count correctly changed from four to five, so the gate discarded exact evidence, latched hierarchy lineage loss, and raised `PolicyError` before the already-falsified relation could be retained and a distinct composition tested.
- **Failure evidence:** Campaign 25 frozen commit `b2d482b0c85ec2d433a6b0c7e769ce1e2d1c12d1`; root `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-b2d482b-campaign25`; status `FAILED_INFRASTRUCTURE`; final official state `NOT_FINISHED`, `levels_completed=4/6`, `win_levels=6`; 67 official actions; zero resets, `GAME_OVER`, or `WIN`. Failure receipt `failures\r11l-495a7899-B5-mechanical-seed-7.json`, 816,084 bytes, SHA-256 `32226c5e72ceb751ba648e4fc03ece40bda6d23796290aaba4fafecd00d4f89a`, reports `PolicyError: hierarchy lineage was lost after a nonmatching returned consequence; no unrelated fallback is authorized`. The sealed trace has 404 events, tail hash `sha256:533123095227d8425f383f328363486bf4e100c4f9b3ac23d48054456982a1c7`, and manifest hash `sha256:9ec2dcf00baffef61b3b1cdedf5ddf6f355bbcd17691bde4db07bc28b4f24580`. Recording, trace, and scorecard verify, but `completion_observed=false` and the failure receipt is intentionally incomplete as a completion receipt.
- **Exact diagnosis:** At terminal `(60,24)`, all 139 selected relation cells and protected-board hash `sha256:aaf97efe6eddb218c9e9a2b9a5c8dd37d5cfc77b3375e6af4c7a1951e898d64c` matched the projection. The selected and frozen children, connectors, active role, sibling, and target were exact; `_child_isolation_was_observed` was true. The sole failed conjunct was equality of raw visible counts across the consequence. The projection itself expected five endpoints after the action, so this was a verifier defect, not ambiguous lineage.
- **Smallest repair:** Commit `3312f6d80d943f7c6213f1dc0bae37a27e759293` carries projected visible-count certificates per action. On exact terminal `NOT_FINISHED`, it rejects only child-only sufficiency, derives one inverse sequence from the original forward actions and prior exact state certificates, and restores the exact pre-discriminator raster and active role before permitting the distinct joint plan. All inverse steps share `affine-child-recovery:d1366359af531afdc1ab12a3`; any returned or queued mismatch latches lineage before fallback. No target ID or recorded coordinate sequence is present in production.
- **Verification:** The exact final visual-policy suite passes 124 tests in 1,039.03 seconds. Ruff lint and format, strict MyPy, compilation, and diff checks pass. Independent audit found no P0/P1 defect. Commit-bound Campaign 25 replay must remain attached as the pre-Campaign 26 gate; its precommit equivalent matched all 67 official selections, kept lineage intact after row 67, queued six exact inverse actions, and selected the first inverse without `PolicyError`.
- **Campaign 26 official resolution evidence:** Frozen commit `063b1421a24450fcdd84ac04d1cd086bbd995671`; root `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-063b142-campaign26`; status `FAILED_INFRASTRUCTURE`; final official state `NOT_FINISHED`, `levels_completed=4/6`, `win_levels=6`; 87 official `ACTION6` submissions; zero submitted resets, `GAME_OVER`, or `WIN`. Official submissions 68-73 returned the exact six-action child recovery, restored the protected pre-discriminator hierarchy and active binding, and retained the child-only rejection. Submissions 74-82 returned the complete distinct equal-weight joint plan; submission 82 placed both child mediators at their predicted parent centroid and returned `NOT_FINISHED`. This resolves the de-occlusion, lineage-retention, exact-restoration, and distinct-test burden. The evaluator later failed closed only after five further official `NOT_FINISHED` actions exhausted parser-safe target-preserving continuation.
- **Next discriminating action:** None for this burden. Preserve the Campaign 25 child-only and Campaign 26 equal-weight joint failures. B-003-0019 owns exact recovery from the equal-weight terminal and the arity-weighted discriminator.
- **Resolution condition:** Met by Campaign 26 officially returning every exact recovery consequence, restoring the known state and active binding, retaining the child-only rejection, and testing the distinct equal-weight joint hypothesis without retesting child-only sufficiency. `GameState.WIN` alone resolves game completion.

## B-003-0019 - Equal-weight hierarchy exhausted parser-safe continuation while arity-weighted endpoint composition remained untested

- **Status:** RESOLVED; CAMPAIGN 27 OFFICIALLY REACHED AND FALSIFIED THE ARITY-WEIGHTED TERMINAL, THEN EXACTLY RECOVERED
- **Stage:** 10
- **Opened:** 2026-08-25
- **Owner:** Codex
- **Burden:** Campaign 26 returned the exact equal-weight joint consequence and remained `NOT_FINISHED`. The official child-only arity-three and equal-weight failures were reliable, but the distinct hypothesis that each child mediator contributes according to its observed endpoint arity had not been tested officially.
- **Failure evidence:** Campaign 26 frozen commit `063b1421a24450fcdd84ac04d1cd086bbd995671`; official final state `NOT_FINISHED`, `levels_completed=4/6`, `win_levels=6`; 87 `ACTION6` submissions; zero resets, `GAME_OVER`, or `WIN`. Submission 82 satisfied the equal-weight relation, but the environment remained `NOT_FINISHED`. The two children had unequal observed endpoint arities two and three, so equal weighting and flattened-endpoint weighting remained distinct.
- **Smallest repair:** Commit `ea25385a0a516d0718431b0eeca1e854e07feee8` reversed the certified equal-weight plan to the protected origin and then tested arity-weighted support under a distinct `affine-weighted-hierarchy` identity, with exact per-action raster, count, target, role, and recovery certificates.
- **Campaign 27 official resolution evidence:** Frozen commit `db5c70ed93970ef16eab10349e634c203dc8a625`; root `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-db5c70e-campaign27`; final official state `NOT_FINISHED`, `levels_completed=4/6`, `win_levels=6`; 113 official `ACTION6` submissions; zero resets, `GAME_OVER`, or `WIN`. Actions 83-91 restored the exact protected origin. Actions 92-100 returned the complete arity-weighted plan; action 100 satisfied supports `(47,19)` and `(57,34)` with weights two and three at target `(53,28)`, but the official environment remained `NOT_FINISHED`. Actions 101-109 then restored the origin protected hash, endpoint colors, and active binding exactly. This resolves the execution and recovery burden while falsifying only arity-weighted completion sufficiency.
- **Verification:** Read-only receipt `C:\a\arc3-b003-play\campaign27-db5c70e-2ce40400-integrity-replay-receipt.json`, 11,560 bytes, file SHA-256 `993b64b2eea7a346624d50094ecdecfd61c88271a8cc7d53f6efb21445749fd0`, payload SHA-256 `3b2e479b64134f7663dd99129a4430281af0b8e4363b40c8127556470ff166c3`, matches all 113 official actions, reproduces the post-action-113 `PolicyError`, and verifies the 680-event trace, 188 blobs, 114 recording rows, 394 frames, and all action/state/level/mechanics bindings with zero mismatch.
- **Next discriminating action:** None for this burden. Preserve the arity-weighted failure and its exact recovery. B-003-0020 owns the remaining child-stratum rejection defect and next bounded discriminator.
- **Resolution condition:** Met by Campaign 27 officially reaching the arity-weighted terminal and returning its exact recovery without target damage, reset, `GAME_OVER`, or lineage loss. `GameState.WIN` alone resolves game completion.

## B-003-0020 - Arity-three child-only failure was overgeneralized to the untested arity-two sibling

- **Status:** RESOLVED; CAMPAIGN 28 OFFICIALLY FALSIFIED THE DISTINCT ARITY-TWO STRATUM AND EXACTLY RECOVERED
- **Stage:** 10
- **Opened:** 2026-08-25
- **Owner:** Codex
- **Burden:** Campaign 27 preserved the arity-three child-only, equal-weight, and arity-weighted failures, then returned four safe active-role bindings and stopped before submission 114. The policy stores child-only failure under the whole parent relation key, although `_child_isolation_plan` tests only the initially nonactive child. In the official trace that child had arity three; the structurally distinct arity-two sibling was never isolated at the parent target. The relation-wide rejection therefore suppressed an unfalsified bounded discriminator.
- **Failure evidence:** Campaign 27 frozen commit `db5c70ed93970ef16eab10349e634c203dc8a625`; evaluator status `FAILED_INFRASTRUCTURE`; official final state `NOT_FINISHED`, `levels_completed=4/6`, `win_levels=6`; 114 observations and 113 submissions; zero resets, `GAME_OVER`, or `WIN`. Actions 110-113 `(34,55)`, `(43,34)`, `(52,55)`, `(25,35)` changed only endpoint role glyphs plus the HUD and returned `NOT_FINISHED`; the final action restored the origin non-HUD raster. Step-113 candidates were durable, but the evaluator raised `PolicyError: readable affine hierarchy child-only sufficiency and its equal-weight and arity-weighted joint completion hypotheses were already falsified by official NOT_FINISHED consequences; readable affine hierarchy has no parser-safe target-preserving continuation` before selecting or submitting action 114.
- **Exact diagnosis:** `_child_isolation_plan` deliberately targets the initially nonactive child, but the terminal consequence adds only `_hierarchy_relation_key(...)` to `_failed_child_isolation_relation_keys`. Selection then suppresses every later child-isolation plan for that parent relation, regardless of which child structure would be selected. The official arity-three result does not manufacture evidence about the arity-two sibling. Exact recording/trace replay confirms no child-only arity-two terminal exists in Campaign 27.
- **Smallest repair:** Scope child-only rejection to the stable parent relation plus a normalized selected-child structural signature that ignores movable coordinates, object references, and active-role color. Retain the arity-three rejection across role exchanges and reset. Generalize child-isolation planning to test either child: keep the certified activation step when the selected child is inactive, but omit it when the selected child is already active. On the sealed action-113 frame, the active arity-two child has directly safe exact layouts, so no preliminary role exchange is necessary. Reuse the existing per-action certificates and inverse-recovery machinery, and fail closed on any raster, target, connector, count, role, or queued-precondition mismatch. Do not change the equal-weight or arity-weighted rejection keys.
- **Implemented repair:** Mechanics commit `894adbbc2a16a5043237a468d232af5805ac77c5` scopes failure to a stable child signature containing arity, normalized glyph and center-marker evidence, mediator palette, and connector color plus supported-leg degree while excluding movable placement, object identity, connector raster paths, and endpoint role color. Failed strata survive same-level reset and clear on level progress. Active-child plans omit activation and use an exactly aligned inverse; readable hierarchy exhaustion fails closed. Evaluator commit `a1f85dbb4607016f555358bb936343ee8ed99861` preserves new policy exhaustion as `FAILED_MECHANISM` and restores failure-state counts without retroactively changing Campaign 27.
- **Verification boundary:** Before Campaign 28, require focused tests for distinct child strata, same-stratum retention, reset retention, exact arity-two terminal and inverse recovery, corrupted consequence and queued-precondition fail-closed behavior, plus full lint/type/compile/integrity checks and a commit-bound replay matching Campaign 27 until the first policy-derived divergence. Production must contain no game ID, campaign label, frozen coordinate sequence, or replay hash.
- **Verification completed:** 38 child-isolation tests, 104 full visual-policy tests, and 37 evaluator completion/public-contract tests pass. Ruff lint/format, strict MyPy on both production modules, compilation, and diff checks pass. Independent review closed connector-identity and layout-invariance concerns with no remaining P0/P1. Production leakage scan found no target ID, campaign label, frozen recording hash, or audited coordinate literal. The live OneDrive integrity attempt remains `FAILED_INFRASTRUCTURE` after 36 passes and one changing-byte read; a clean detached commit passed all 37 integrity cases, the manifest-aware static receipt passed every check with zero findings, the exact-commit package-only Git-blob receipt returned `package_only_passed=true` with zero findings, and action-semantics scanning passed with zero findings. Receipt hashes and the immutable commit are bound in D-003-0020 and the run state.
- **Commit-bound replay:** `C:\a\arc3-b003-play\campaign27-a1f85dbb-2ce40400-child-stratum-divergence-replay-receipt.json`; file SHA-256 `1ba06cf96b3a4dde50de5bc75290f3ee9140bc5c7f48863cf66de1df8e0b5dc1`; payload SHA-256 `9a0694c84e7f413b2ba2de996df350509d23b8ef97d5967db674dbc02d9af80f`. Clean detached commit `a1f85dbb4607016f555358bb936343ee8ed99861` matches Campaign 27 submissions 1-73 and first diverges at submission 74 to direct arity-two `(50,22)` rather than recorded `(58,37)`. The three-action forward and inverse certificates restore the exact origin hash and active binding. No environment action, game-source inspection, holdout access, or completion claim occurred.
- **Campaign 28 official resolution evidence:** Frozen commit `4a9ebb4798003c5c1d3d6eedae352fc414a998fe`; root `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-4a9ebb4-campaign28`; final official state `NOT_FINISHED`, `levels_completed=4/6`, `win_levels=6`; 115 official `ACTION6` submissions; zero submitted resets, `GAME_OVER`, or `WIN`. Submissions 74-76 returned the direct arity-two discriminator and reached its exact target certificate; submission 76 remained `NOT_FINISHED`. Submissions 77-79 returned its exact inverse and restored the protected hierarchy and active binding. Later equal-weight and arity-weighted terminal consequences also remained `NOT_FINISHED` and were exactly recovered. Both child strata are now rejected only by their own exact official evidence.
- **Verification:** Receipt `C:\a\arc3-b003-play\campaign28-4a9ebb4-154f3e1f-integrity-replay-receipt.json`, file SHA-256 `45da1c1537f734f2ac9b34b131d01ed1df57eec742c3a35a98e09e73cf9a1222`, payload SHA-256 `2de56975166d4d88c5bce93218347e4ab6c519ef908230ef8702738963d93158`, verifies all 115 official selections and consequences and the 692-event sealed trace. Its `trace_manifest_object_hash_verified=false` discrepancy is preserved even though the authoritative public verifier and exact replay pass.
- **Next discriminating action:** None for this burden. Preserve both child-stratum failures and their exact recoveries. B-003-0021 owns the bounded visible-node hierarchy hypothesis.
- **Resolution condition:** Met by Campaign 28 officially reaching the distinct arity-two terminal, returning `NOT_FINISHED`, and exactly restoring the certified origin without target damage, reset, `GAME_OVER`, or lineage loss. `GameState.WIN` alone resolves game completion.

## B-003-0021 - Visible-node hierarchy composition is locally certified but officially untested

- **Status:** OPEN; CLEAN IMPLEMENTATION CHECKPOINT VERIFIED; CAMPAIGN 29 GATED
- **Stage:** 10
- **Opened:** 2026-08-25
- **Owner:** Codex
- **Burden:** Campaign 28 officially falsified both observed child-only strata plus equal-weight and endpoint-arity-weighted composition, then restored the exact protected origin. The smallest remaining readable hierarchy hypothesis counts each child mediator as one visible node in addition to its observed endpoints. This hypothesis has not been submitted to the official environment.
- **Evidence boundary:** Commit `ad3c604cbaa8ca97e451d58c8fa24dd15c73f611` implements generic arity-plus-visible-mediator weights, exact forward/inverse certificates, family-scoped rejection, same-level reset retention, and level-progress clearing. It passed 109 full visual-policy tests, 38 child-isolation tests, six post-format visible-node tests, Ruff, strict MyPy, compilation, and diff checks. Production contains no target ID, campaign label, audited coordinate literal, or frozen replay hash.
- **Noncanonical replay:** `C:\a\arc3-b003-play\campaign28-worktree-dd17a8b8-154f3e1f-visible-node-submission116-replay-receipt.json`; file SHA-256 `c63ee384e093a7bd12aa91ea79417a6b6ce4170c9309632e3d2eacbf32ec0b57`; payload SHA-256 `a18baf0096b2fcd36b4d6cc613e83eb18c4f2083b97d6182d522225d6f4c1c30`. It derives a nine-action candidate with observation-derived weights three and four, but it predates the final Stage 02-09 and production-reachability integration commit and therefore is not a launch receipt.
- **Canonical implementation-freeze checkpoint:** Frozen commit `83df5520478cc209c06d9ce4e658c90786914544`, tree `3d1a45995b004947900de3114449a7e42bd80a87`, was replayed from a clean detached checkout against the sealed Campaign 28 recording. Receipt `C:\a\arc3-b003-play\campaign28-83df552-154f3e1f-canonical-replay-receipt.json`, file SHA-256 `a31eccb229279a965c7a66dc93c80f40b061ee21401591577fe7f6dda2c42bb2`, payload SHA-256 `1ef595d61ec435680e105b6d91eceacf4debe807d146336de6444e5258d3e85b`, and receipt hash `78cb8c95703d6217c9c668fb49f6c433f9765f447f41e0214f86edf6117460fe` match all 115 recorded selections and accepted consequences. The learner derives the next visible-node candidate `ACTION6 (52,14)` with weights three and four, then cancels it without submission. The production action-semantics scan has zero findings, and the matched `MyAgent` profile passes route, determinism, terminal, time, and memory gates. These receipts used zero official interactions and do not resolve this burden.
- **Next discriminating action:** First complete the exact final matrix and independent audit in B-003-0022, then finish the current-head package, integrity, secret, and documentation gates. Only then start Campaign 29 from a fresh authorized public-development session and submit one official action at a time with reobservation. Treat exact `GAME_OVER` as family failure, issue only legal `RESET`, retain the implicated rejection, and continue; treat exact `NOT_FINISHED` as bounded family falsification and recover through the certified inverse.
- **Resolution condition:** An official returned consequence reaches or invalidates the visible-node terminal under exact certificates. Neither replay nor synthetic success resolves the game; only official `GameState.WIN` completes it.

## B-003-0022 - Final v0.2 paired learner failed mechanism and accurate re-audit

- **Status:** RESOLVED BY PRESERVED FAILED_MECHANISM AND CORRECTED AUDIT FAIL
- **Stage:** 02-09
- **Opened:** 2026-08-25
- **Owner:** Codex
- **Burden:** The first complete 30-seed by four-variant Stage 08 run produced deterministic receipts but did not validly exercise the required learner lifecycle or literal hypotheses. The adapter navigated with bespoke logic, immediately resolved residuals, represented only a subset of consequence channels and mechanic modes, could not generate erroneous global reopenings, and did not instantiate the production mechanical-policy route. Its retention field was self-reported rather than inferred from observed transitions.
- **Failure evidence:** Diagnostic root `C:\a\arc3-b003-stage08-dd17a8b8-20260825-01`; receipt `stage08-dd17a8b8-synthetic-matrix-audit-receipt.json`; file SHA-256 `d77d4b3cf4046ca1e4acd2bb1e465194bf04b10dbfaa75a7e4a009bc059f040a`; payload SHA-256 `f528472ce295d578522063292710fa13f1202266a03251d2dd6b7adafce728bc`. All 1,200 rows and 120 sequence receipts exist, but only eight sequences reached synthetic `WIN`; 101 exhausted reset budget and 11 exhausted action budget. H1 and H3 had median delta zero instead of a strict reduction. H2's reported retention was 80/150 and was not observation-grounded. No public, holdout, or official target environment was opened.
- **Protocol discrepancy:** The initial preregistration listed seeds 73000-73029 and budgets of 192 actions and 10 seconds; the later matrix used hash-derived seeds and 1,500 actions and 120 seconds without a ledgered supersession. It compared against Build 002 commit `753b0e007222a973a2c8a6d7ce14a395135d3c5f`, not the Stage 01 frozen baseline `5448c53f3b7e08f606cf292e6068f3f9c9db16d4` with tree `700718c09c2a1532cea16526b290f57be0120371`.
- **V0.2 freeze:** Commit `24c34dbd25a946446550de8f00529cabbf587026` preregisters the corrected protocol and fresh 30-case seed family before any v0.2 result or smoke. It restores the original action/time bounds, adds a 48-action level-attempt ceiling, binds the exact Stage 01 comparator commit and tree, keeps base movement resource cost and randomized action mapping stable across all ten levels in one sequence, and prohibits replacement rows. All seed and case derivations independently verify. This closes only the preregistration-identity sub-burden; it does not validate the implementation or hypotheses.
- **Integrated v0.2 checkpoint:** Protocol commit `a642a4c63aa1d90605646c948a44140234b48001`, production commit `6080f9937017c03db9995f912669a72746a524ba`, lifecycle commit `ca86e33a047e5e6b6689e509cea899f6c5bd07a6`, and gate commit `d68815fc421758b90ef805d2bdea706deceec2ed` now bind the exact baseline, observation-only learner lifecycle, packaged mechanical-policy route, literal H1-H3 decisions, action-by-action replay links, and independently reconciled environment-action/reset counters. A complete anti-result, missing H2 assessment, policy error, infrastructure failure, replay mismatch, receipt mismatch, or v0.1 selector can no longer earn matrix `PASS`. Thirty-six focused evaluation tests pass with the separately preserved legacy synthetic authoritative-WIN acceptance excluded; affected lint, format, strict typing, and compilation pass.
- **Development and adversarial resolution checkpoint:** Commits `276f8af`, `5296afe`, `0ea67e0`, `ec7b7d9`, `8c87244`, `493a3e6`, and `614d055` bring every declared v0.2 development curriculum to authoritative synthetic `WIN` under the frozen 192-action, 48-action-per-level-attempt, ten-reset, ten-second, and 1-GiB limits. A 5-seed by 4-variant development matrix shows 5/5 full BLA+CLEF wins, 3/5 BLA-only wins, and 0/5 per-level-reset or exact Build 002 wins; this is development selection evidence, not the final paired result. The named Stage 09 cases now cover provisional/distinct-context movement, passive strengthening, resource failure plus reset, localized restoration, additive-versus-override discrimination, transfer and visual remap, CLEF parking and relevance ordering, deeper reopening, bounded coordinate behavior, failed-root avoidance, cross-game ledger quarantine, evaluator isolation, and measured persistence/CLEF loss. The final resource repair installs exact-context override evidence, centralizes failed-root avoidance, and archives level-local repair state; 17 lifecycle checks plus all five development WIN checks passed. Explicit limits remain occurrence-only override support, bounded root/disposition ledgers, and fail-closed coordinate-only fallback.
- **Independent-audit checkpoint:** Commit `0bb6ddeb2c0febebff992a3d69ac1b0e93b05eda` binds the runner to a clean Build 003 commit/tree and source hashes and adds a separate fail-closed auditor for exact 30-by-4-by-10 coverage, Build 002 identity, canonical files, no replacements, row reconstruction, action/reset/link reconciliation, literal H1-H3 status, and before/after matrix-root integrity. The auditor cannot re-simulate transitions because full environment transcripts are not stored; it states that limit. Its fabricated tests passed after a known Windows pytest-temp setup failure was retried with an explicit fresh temp root. A broad development-time search may have read manifest bytes but exposed no seed values, cases, or results; no final case was derived or executed and the frozen set was not amended.
- **Implementation-freeze checkpoint:** Commit `83df5520478cc209c06d9ce4e658c90786914544`, tree `3d1a45995b004947900de3114449a7e42bd80a87`, is frozen after a canonical 115-action recorded-frame replay, a zero-finding production action-semantics scan, and a matched `MyAgent` profile that reached the production mechanical route in every cycle. The exact final matrix root is `C:\a\arc3-b003-stage08-v02-final-83df552-01`; the separate audit root is `C:\a\arc3-b003-stage08-v02-final-83df552-01-audit`. Both remain unexecuted at this checkpoint. Later commits are documentation or evidence only unless a specific failure explicitly reopens implementation.
- **Smallest repair:** Freeze v0.2 before looking at final results. Bind the exact Stage 01 comparator; independently freeze seeds, budgets, modes, and literal acceptance rules; keep resource cost stable across levels in a sequence; randomize action mapping; emit all ten consequence channels with honest `UNKNOWN`; retain complete prediction receipts; exercise passive confirmation, narrow repair, two-failure implicated reopening, transfer confirmation, conditional/additive/gating/override/delayed mechanics, composition, CLEF promote/park/stop, and a behaviorally real BLA-only divergence. Compute exact observation-grounded metrics and audit action-by-action receipt linkage. Prove `MyAgent` can reach the production policy without privileged game labels.
- **Known residuals:** Pixel-only inventory interpretation, topology inferred without behavioral evidence, one-off delayed consequences, exact-resource-context override evidence without magnitude confirmation, and full-transition replay outside the stored matrix format remain bounded limitations. A structurally complete receipt must not smooth these into measured success.
- **Final v0.2 result:** The exact one-shot root `C:\a\arc3-b003-stage08-v02-final-83df552-01` contains all 1,200 rows and 120 sequence receipts and is frozen at matrix-receipt file SHA-256 `a01c3dfca5c18d6282e978c07de035932dd28de21491e7a7ea04f8354a0fc8a6`. It returned `FAILED_MECHANISM`: H1 `FAIL`, H2 `NOT_MEASURED`, H3 `FAIL`, replay determinism 1.0, receipt completeness 0.615, zero infrastructure rows, and zero policy-error rows. Forty-seven sequences reached synthetic `WIN`; 43 reached the action budget and 30 reached the wall-clock budget. This is a bounded synthetic anti-result, not target progress.
- **First independent audit result:** Root `C:\a\arc3-b003-stage08-v02-final-83df552-01-audit` returned `FAIL`; audit-receipt file SHA-256 `3f694030d1c7e9a5e2e3e6770a16ec9c8fadba0ee896bbe2098d3b61a66a0214`, report file SHA-256 `f112a31e436ffb46a92aab26ce452971794cc0e3dff67d9ca15eea3a7f563172`, and sealed payload SHA-256 `7af34ca1e851bfd46a3e643e687d58b780c667de9a6965447219d69e71486bdc`. Thirteen of fourteen checks passed and the source matrix was unchanged. Its 1,692 findings split into 1,158 auditor defects and 534 genuine evidence-completeness failures. Both the matrix and first audit are immutable.
- **Corrected audit resolution:** Commit `d3d8eddb85549550ddc6629de4aa32e202ef897a` passed 11 focused tests and source-quality checks. The final source-projected audit root `C:\a\arc3-b003-stage08-v02-final-83df552-01-audit-v3-d3d8edd-source-projected` returned `FAIL` with exactly the 462 per-level action/receipt count mismatches and 72 aggregate incomplete-link attestations. All other checks passed; file SHA-256 `518cc61bdf981a2c6537bda321c27d46d66deb95974f0bedc922db040e0a0c89`; sealed payload SHA-256 `fda2cc01f12746abbb98b44d8d3513161366e0b8b3c4be8876d65eccb564dde3`; unchanged matrix manifest SHA-256 `747e4c35b13f68714548b8af196ced026e555f8c33491dff5d942b5baab88389`.
- **Next discriminating action:** None for this burden. Preserve the failed mechanism and missing links in final reporting; proceed to package and official-development gates without a synthetic success claim.
- **Resolution condition:** The repaired lifecycle, exact baseline, preregistered v0.2 matrix, adversarial suite, production reachability, and audit receipts meet their stated acceptance criteria or are preserved under the workflow's honest failure statuses. Official play may resume only after the achievable gates are complete; synthetic results never establish target `WIN`.

## B-003-0023 - Final auditor schema drift is preserved beside genuine link incompleteness

- **Status:** RESOLVED; CORRECTED AUDIT DISTINGUISHES 534 GENUINE FINDINGS; MATRIX IMMUTABLE
- **Stage:** 08 and 11
- **Opened:** 2026-08-25
- **Owner:** Codex
- **Burden:** The first independent audit rejected two level-summary fields emitted by the final Build 003 worker, `resource_discrimination_actions` and `restoration_ambiguities_resolved`, as unknown. That is assurance-schema drift. Separately, the final matrix itself attests only 0.615 action/receipt completeness, and the auditor independently found 462 per-level count mismatches and 72 incomplete sequence-link attestations. A post-result schema-only repair would reduce diagnostic noise but could not turn the matrix into `PASS` or supply missing receipts.
- **Preserved evidence:** The immutable audit root, hashes, 1,692-error grouping, twelve passing checks, one failing link/counter check, and unchanged pre/post matrix-root manifest are recorded in D-003-0027 and B-003-0022. No public, holdout, or official environment was opened by the matrix or audit.
- **Decision boundary:** Do not rerun or modify the final matrix. Repair only the auditor's two independently diagnosed defects, with regressions, then use only the existing immutable matrix as input and write to a new empty audit root. Preserve the first failed audit and make no stronger mechanism or completion claim; the 534 genuine findings must remain.
- **Resolution evidence:** Auditor commit `d3d8edd` recognizes both declared counters and reconstructs valid per-level rows independently of an aggregate incomplete-link flag. The corrected source-projected audit removed exactly 1,158 verifier-generated findings and retained exactly 534 genuine findings. Its thirteen passing checks bind exact identities, coverage, hashes, canonical reconstruction, literal decisions, absence of infrastructure/policy rows, and an unchanged source matrix; only sequence replay links and counters fail.
- **Resolution condition:** A later source-independent auditor recognizes every declared level-summary field and cleanly distinguishes schema compatibility from the already-sealed missing-link evidence, without changing the matrix root or its literal `FAILED_MECHANISM` result.

## B-003-0024 - Synthetic fallback-anchor collision generated an unsolvable locked-key case

- **Status:** RESOLVED AT `48bd18d`; CURRENT-HEAD CI REQUIRED; PRODUCTION FREEZE UNCHANGED
- **Stage:** 10
- **Opened:** 2026-08-26
- **Owner:** Codex
- **Burden:** The exact `83df552` package-safe recovery collected 1,022 tests and found one deterministic failure. Development root seed `262795363413301024` generated `synthetic-lab-v1-0005-3d5e218b550b3d1b` with `key == door == target`, so the key was placed on its own locked door and the exact evaluator could not reach `WIN` at any search depth tested.
- **Preserved evidence:** Integrated verifier `C:\a\o3f83\release-verification-receipt.json`, file SHA-256 `35e84860f5c167677cfaa1de45fa903e099fff895a73e0ff81de0e366fd72ddb`, remains `FAILED_INFRASTRUCTURE` at its 2,400-second ceiling. Standalone receipt `C:\a\o3f83\package-only-test-guard-recovery.json`, file SHA-256 `a2b21eeb5c4a7ac7448ecfd9abdaf77902cf8ceaba12d0dde462c68d23d31a1a`, remains `FAILED_BOUNDARY`; it records zero guard attempts, exact source projection, 1,022 collected tests, and the genuine property failure.
- **Package sub-gate evidence:** Two exact-source package builds passed and were byte-identical at candidate SHA-256 `d83efdd5c08b082ec8f1402120e75645b15e960fa954e42eb33fd116ee875ee8`; the first-party payload is `27bfe4fd15459d20fa1750c4e4bbe69cb1873ded13db26b43030f7866def1e16`. Cold start passed in 3.0803145 seconds with zero network/process attempts. Package-only integrity reports `package_only_passed=true` but top-level `passed=false` because public identifiers are intentionally `NOT_EVALUATED`; 22 persisted redacted logs had zero secret findings or redactions. The full static lock-only scan also has top-level `passed=false`: source, policy, archive, and secret sub-checks pass while supply chain is explicitly `NOT_EVALUATED` with 60 nonblocking warnings. These scoped passing sub-gates do not relabel either preserved verifier failure or claim every raw byte was secret-scanned.
- **Smallest repair:** Reject duplicate fallback anchors during deterministic spec generation and lock the exact root seed into a public evaluator-solvability regression. Do not special-case a family, game ID, coordinate, or target result.
- **Verification:** Exact regression and property file pass; the combined 17-test lab catalog/transition/integration/property suite passes; Ruff lint/format and strict MyPy pass. No official environment, holdout, game source, credential, or submission surface was touched.
- **Resolution condition:** Met for the implicated generator mechanism at `48bd18d`. Archive inspection confirms the lab is absent from all 114 executable payload members, so `83df552` remains the production/campaign freeze. The final release burden remains open until the preserved package boundary and current-head CI are reported honestly.

## B-003-0025 - Composite bridge sinks are observation-supported but officially untested

- **Status:** RESOLVED BY CAMPAIGN 30 FAILED_MECHANISM; EXACT NON-EDGE INVERSE RECOVERY OBSERVED
- **Stage:** 10
- **Opened:** 2026-08-26
- **Owner:** Codex
- **Burden:** Campaign 29 officially falsified the visible-node parent-composition family and exactly recovered it, but production then failed closed because every implemented hierarchy family bound both children to the single raw parent target. The returned frame instead contains two exact carrier-mask elimination examples and two exact composite sinks. Their source-pair proximity gives a unique metric-stable assignment of child groups to sinks, but no observed structural correspondence establishes that those child groups themselves implement the carrier-elimination operator. The repaired certificates can now represent each standalone composite ring, but the proximity-assigned paired sufficiency hypothesis remains officially untested.
- **Preserved official evidence:** Root `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-83df552-campaign29`; official recording SHA-256 `c9f7422dcb96a24a47ce734c83a32aa35d3d7816ef8b94c7750e81d6814f692e`; failure-receipt SHA-256 `6e4ec80f9556967ec3d438a48f5cc403491d3b4bec1526326dcd22d668ec9935`; 141 observations, 140 submissions, one reset, one `GAME_OVER`, zero `WIN`, final raw state `NOT_FINISHED`, and `levels_completed=4/6`. The public exposure ledger has 55 development events, zero holdout events, and tail hash `aa117fe9da9dc595c36cf86aa831cba1d4591426b71f62794e99b42e69b3b717`.
- **Smallest repair:** Detect exact two-color 21-cell disk pairs sharing one carrier color whose distinct residual colors exactly match an exact two-color 12-cell ring; require two independent examples and a unique assignment under Euclidean, Manhattan, and squared-Euclidean source-pair aggregate costs. Generate target-relative layouts without fixed coordinates or colors. Freeze each chosen ring surface separately, permit only that ring to become filled at its terminal, preserve the raw parent and every unrelated target exactly, retain all endpoint identities, and bind a complete inverse. Retain a bridge-pair-specific failure across same-level reset and clear it only on real level progress.
- **Projected route boundary:** Observation-only search found a complete paired forward and inverse. The active arity-two child reaches its assigned ring in three actions; while held there, the arity-three child reaches its assigned ring in six actions. The exact inverse restores first the arity-two terminal and then the original hierarchy and active binding. Recorded coordinates are test/replay evidence only and must not appear as production constants. An arity-two-only `NOT_FINISHED` is an intermediate observation, not paired-family falsification.
- **Implementation evidence:** The generic detector, factored sink certificate, family lifecycle, exact inverse, prerequisite gates, corrupt-raster fail-closed behavior, and reset/level-progress semantics are covered by the 111-test visual-causal policy file. The reset-aware replay contract has 13 passing tests and preserves Campaign 28 compatibility. Read-only replay matched all 140 Campaign 29 submissions, derived the paired candidate, cancelled it unsubmitted, and issued no environment action. Planner memoization reduced repeated projected parsing from 111 calls to 41 unique states without changing the route. The first live-worktree integrity run preserved one `FAILED_INFRASTRUCTURE` result after OneDrive changed candidate bytes during the bounded read; 36 other cases passed. A stable snapshot of HEAD plus all nine modified tracked files then passed integrated repository integrity, a direct 564-file secret scan returned zero findings, the action-semantics scan returned zero findings, and all ten production-reachability tests passed.
- **Official resolution:** Campaign 30 from clean exact commit `6c32d2a65e61a7b1160fd1c09e8f991078fc9477` officially reached the paired terminal at submission 149 and returned `NOT_FINISHED`, `levels_completed=4/6`. Both composite sink surfaces were occupied exactly, while the singleton raw target and unrelated examples were preserved. Submissions 150-158 executed the exact inverse; every gameplay-field cell at `x=1..63` matched the protected origin, while nineteen left-edge counter-band cells at `x=0` reflected consumed actions. The campaign observed no `WIN` and ended as sealed `FAILED_MECHANISM` evidence.
- **Next discriminating action:** Freeze a clean commit, run integrity and canonical replay gates, then start fresh authorized development Campaign 30 and submit one planned action per reobservation. Recover or fail closed on any mismatch. If the exact paired terminal remains `NOT_FINISHED`, preserve the rejection and exact inverse before selecting a new observation-grounded family.
- **Post-resolution follow-on:** Campaign 30's exact `NOT_FINISHED` terminal and inverse close this bridge-sufficiency burden. The next bounded family is B-003-0026's residual-linked mixed-support conjunction; it requires its own clean exact-commit gates and Campaign 31 official consequence. Do not repeat the rejected paired bridge family.
- **Resolution condition:** Met by the exact paired terminal returning official `NOT_FINISHED` and the exact non-edge inverse completing. This resolves only the bridge-sufficiency discriminator; target completion remains open and only official `GameState.WIN` resolves it.

## B-003-0026 - Residual-linked mixed support is structurally grounded but officially untested

- **Status:** RESOLVED BY CAMPAIGN 31 FAILED_MECHANISM; EXACT TERMINAL AND FULL INVERSE OBSERVED
- **Stage:** 10
- **Opened:** 2026-08-26
- **Owner:** Codex
- **Burden:** The rejected bridge family placed both children on their two proximity-assigned composite sinks. Earlier child-only observations placed each child at the singleton raw target while the sibling remained at origin. No preserved official observation combines one child on its nonmatching composite sink with the other child on the uniquely residual-linked singleton raw target.
- **Observation-grounded discriminator:** Require one unique singleton raw-target palette member and exactly one witnessed composite residual palette containing it. Reuse the bridge assignment only when it remains unique under Euclidean, Manhattan, and squared-Euclidean source-pair costs. Place the linked child at the raw target and each other child at its own nonmatching composite sink. In the Campaign 30 origin, level-3 evidence associates arity two with `{8,9}` and arity three with `{11,14}`; `{14}` uniquely links the raw target to the arity-three branch. Palette containment and translated geometry must derive the route; the recorded colors and coordinates are evidence, not production constants.
- **Exact gate:** Every projected intermediate must preserve unrelated static pixels, every raw/composite target surface except the specifically permitted occupancy, all endpoint identities, oriented connector raster, and child lineage. Only an exact mixed terminal followed by official `NOT_FINISHED` or `GAME_OVER` rejects the family. Intermediate mismatch or `GAME_OVER` rejects only that plan. `NOT_FINISHED` requires the complete certified inverse; terminal `GAME_OVER` requires legal reset with family rejection retained. Level progress clears the family-local rejection; `WIN` completes the target.
- **Resource limit:** The left-edge action band recovered with nineteen additional consumed cells after the bridge forward/inverse and previously filled at `GAME_OVER`. Its exact semantics are not privileged, but it bounds the value of a nine-action discriminator plus inverse over broad target or palette enumeration.
- **Implementation evidence:** The current generic policy derives a collision-safe nine-action mixed route and complete nine-action inverse from the Campaign 30 recovered observation. It fails closed on ambiguous residual links, duplicate raw-parent/bridge-sink centers, palette and translated-geometry counterexamples, corrupt target raster, and incomplete lineage. Exact-terminal and reset lifecycle tests pass. The full visual-policy suite passed 113 tests; the stable learner/replay/CLI suite passed 154 tests. A read-only replay over the complete sealed Campaign 30 trace matched all 158 selections and regenerated receipts, staged the mixed candidate, and cancelled it without an environment action. That diagnostic does not replace the incomplete SDK recording and did not emit a receipt file.
- **Prelaunch gate update:** The clean exact-commit replay receipt at `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-6c32d2a-campaign30-sealed-trace-replay-b7d10cd.json` matched all 950 sealed events, all 158 selections, and all 158 regenerated mechanics receipts, staged the mixed candidate, and cancelled it without an environment action. Exact-clone integrity passed 54 cases and action-semantics scanning had zero findings. These gates do not replace the incomplete SDK recording, and its recording/run/receipt completeness fields remain false.
- **Official resolution:** Campaign 31 ran from clean commit `0434b3c95b7c1e3bd98488e9277df5c82f8c5850`, tree `fb77bc0c752f9b1932f6aa6c7b46e069f1e216da`. Submission 167, `ACTION6 (59,25)`, reached the exact mixed terminal. Receipt `visual-receipt:31cb136cfbac9bc38025fd1f`, mechanics event SHA-256 `80a94331adc08ca4dcb29d1a05b46321732760b7e265e58f3a3e538befc58e8f`, records that the residual-linked child reached the raw parent and every other child its nonmatching composite sink; the official consequence remained `NOT_FINISHED`, `levels_completed=4/6`. Submissions 168-176 returned the complete inverse. Final receipt `visual-receipt:e3c1f8b38472c716bf4e312e`, event SHA-256 `d1307302b017c7f8ce22f529f40296fd2d30484bd26ea5b409d83acb7af6d937`, records exact pre-hypothesis hierarchy restoration. Independent recording comparison matched every `x=1..63` cell; only twenty consumed left-edge cells differed. The campaign observed zero `WIN` and ended `FAILED_MECHANISM`.
- **Resolution condition:** Met for this family by the clean exact terminal returning official `NOT_FINISHED` and the complete inverse returning. This resolves only residual-linked mixed-support sufficiency. Target completion remains open; offline, synthetic, structural, and exact-inverse evidence cannot substitute for official `GameState.WIN`.

## B-003-0027 - Campaign 30 official recording omitted seven sealed actions

- **Status:** RESOLVED PROSPECTIVELY BY COMPLETE CAMPAIGN 31 RECORDING; CAMPAIGN 30 REMAINS IMMUTABLE AND INCOMPLETE
- **Stage:** 10 and 11
- **Opened:** 2026-08-26
- **Owner:** Codex
- **Burden:** Campaign 30's one official SDK recording contains 152 rows: the initial observation plus 151 actions. Its sealed trace contains all 158 submissions and consequences. The recording maps exactly to global submissions 1-87 and 95-158, omitting submissions 88-94 without a reset, session change, new file, or declared segmentation.
- **Preserved evidence:** The seven omitted submissions are all hash-linked `ACTION6` events with official `NOT_FINISHED`, `levels_completed=4/6` consequences. The same recording GUID resumes at global submission 95. Pinned SDK behavior appends each response synchronously to the same recording and defines no segmentation; its recording routine catches/logs write exceptions, but no worker log survives to establish the exact root cause. Therefore `recording_verified=false`, `run_evidence_complete=false`, and `receipt_complete=false` remain authoritative even though the public artifact verifier passes 267 declared artifacts and the 950-event sealed trace is complete and valid.
- **Claim boundary:** Do not reconstruct or rewrite the immutable recording from trace blobs. Use the sealed trace for the complete action/consequence chain, label the SDK recording incomplete, and keep Campaign 30 `FAILED_MECHANISM` separate from this `FAILED_INFRASTRUCTURE` evidence surface.
- **Replay mitigation:** The new trace replay contract verifies sealed source bytes read-only, replays an immutable temporary copy, rejects aliases and escaping compressed copies, regenerates all policy receipts, and keeps `recording_verified=false`, `run_evidence_complete=false`, and `receipt_complete=false`. It matched Campaign 30's 950 events and 158 complete action cycles. This supplies complete trace-level policy evidence but does not repair or resolve the SDK recorder defect.
- **Prospective resolution evidence:** Campaign 31's official SDK recording at `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-0434b3c-campaign31\official-recordings\c2120ed7e4beb839e4d0\1e993a4e-dbe4-43dd-bf3e-69b5b2a084bd\r11l-495a7899-e354b8bc-0e2b-4e02-9fef-4958df2c6d3b.jsonl` is 6,828,250 bytes with SHA-256 `1e2e478ab3d039cd91023dc03bae1b479497bcc1e53b4c2d11ee82de9b609173`. Its 177 rows are exactly the initial observation plus all 176 submitted actions. Every action name and coordinate, consequence state, level count, and session GUID agrees one-to-one with the 1,058-event sealed trace; the sole legal reset aligns at submission 122. The run therefore reports `recording_verified=true` and `run_evidence_complete=true`. Its `receipt_complete=false` remains the non-completion boundary: no `WIN` was observed.
- **Resolution condition:** Met prospectively by Campaign 31's complete official recording and exact trace crosscheck. Do not backfill, reconstruct, or relabel Campaign 30: its seven missing rows and false completeness flags remain immutable `FAILED_INFRASTRUCTURE` evidence, and the recorder's exact historical cause remains unknown.

## B-003-0028 - One Windows package-only runner crossed the guarded suite timeout

- **Status:** RESOLVED BY EXACT 0434B3C CURRENT-HEAD CI; B7D10CD FAILURE PRESERVED AS FAILED_INFRASTRUCTURE
- **Stage:** 10 and 11
- **Opened:** 2026-08-26
- **Owner:** Codex
- **Burden:** The `b7d10cd` pull-request package-only Windows runner reached the exact 2,400-second guarded-suite ceiling near 95 percent, so the verifier correctly returned `FAILED_INFRASTRUCTURE` and the workflow correctly refused to relabel it as the expected sealed-surface `BLOCKED_EXTERNAL` result.
- **Paired diagnosis:** The duplicate push runner used the same commit, tree, plan hash, command, timeout, guarded collection, and package artifacts, then completed 1,053 passed and three skipped in 2,382.0408983 verifier seconds. The failed stdout is a byte-for-byte prefix of the passing stdout. This supports hosted-runner speed variance, not a source, plan, or test difference. The immutable receipts, hashes, run IDs, and durations are recorded in D-003-0033.
- **Smallest repair:** Raise only the package-only guarded-suite ceiling to 2,700 seconds and add an exact plan assertion. Do not alter the full-profile timeout, outer 60-minute cap, test scope, path/process guard, failure classification, or `BLOCKED_EXTERNAL` receipt contract.
- **Local verification:** A detached non-OneDrive worktree passes all seventeen focused verifier tests plus Ruff lint/format, strict MyPy, and diff checks. The live OneDrive attempt remains separate `FAILED_INFRASTRUCTURE` evidence because its shared pytest temp root was inaccessible and candidate bytes changed during a bounded scan.
- **Current-head resolution:** Exact repair commit `0434b3c95b7c1e3bd98488e9277df5c82f8c5850`, tree `fb77bc0c752f9b1932f6aa6c7b46e069f1e216da`, passed all five required workflow runs and all nine jobs: Build 002 PR `32958543305`, ARC3 CI PR `32958543329`, ARC3 CI push `32958539045`, package-only PR `32958543361`, and package-only push `32958538967`. The Windows package-only PR receipt `C:\a\arc3-ci-0434-package-win-pr\release-verification-receipt.json`, file SHA-256 `119f882c2039bebc8462a52b94341ee94419d2d79a87e10b5b3c1d3ffd9fa10e`, receipt hash `4e4f56e170a54bbf30fe94adb67a98798a595717e1224d8ba1de848603b2069b`, completed all twenty non-private checks and 1,053 passed/three skipped in 2,505.1840711 verifier seconds, with no timeout, before returning the expected private-only `BLOCKED_EXTERNAL`. The push receipt `C:\a\arc3-ci-0434-package-win-push\release-verification-receipt.json`, file SHA-256 `e9086d415d7a4107b278c27a732d52a190954c6490fafbe973644d2ac8da4bed`, receipt hash `58692f577bc5236ec710caebc52297867a860830ff78868a2ac4661a58648620`, returned the same boundary in 1,740.549541 verifier seconds.
- **Launch boundary:** Resolved for Campaign 31; the campaign launched from exact `0434b3c`. These passing current-head receipts do not erase or relabel the earlier `b7d10cd` timeout, the live OneDrive failure, or any private-surface blocker.
- **Resolution condition:** Met by the exact repair commit's unchanged Windows package-only scope completing within 2,700 seconds with the expected `BLOCKED_EXTERNAL` verdict, while every required current-head workflow and job also passed.

## B-003-0029 - Unique external residual-chain family requires bounded official discrimination

- **Status:** RESOLVED BY OFFICIAL FAILED_MECHANISM; EXACT TERMINAL, RETRY, RECORDING, AND INVERSE OBSERVED
- **Stage:** 10
- **Opened:** 2026-08-26
- **Owner:** Codex
- **Burden:** Campaign 31 officially falsified residual-linked mixed-support sufficiency and restored the protected hierarchy. The returned observation admits one narrower external chain. Its generic mechanism and prelaunch replay are now verified, but its sufficiency has not received an official consequence. Structural readability and replay certification are not progress, and no `WIN` was observed.
- **Observation-grounded discriminator:** Preserve the unique metric-stable child-to-bridge assignment. Require exactly one singleton raw target and exactly two carrier-elimination examples. The raw target color must occur in exactly one source disk; that source's carrier-offset mask must have exactly one counterpart in the other example; and the counterpart residual must occur in exactly one additional exact sparse target after excluding every bridge output. Choose distinct supports and use the raw target exactly once. Fail closed if any source disk, offset-mask counterpart, external residual target, support assignment, or exclusion is absent or ambiguous.
- **Safety and lifecycle gate:** Prior mixed-family rejection is mandatory. The planner certifies a protected target-, static-raster-, connector-, endpoint-, and lineage-safe complete forward and inverse before submission. Reject the family only when the exact external-chain terminal returns official `NOT_FINISHED` or `GAME_OVER`. Intermediate mismatch rejects the plan. Bridge, residual-linked, and external late hierarchy families may retain one same-level retry after a preterminal `GAME_OVER` and legal `RESET`; a repeated interruption rejects the plan. Equal-weight, weighted, and visible-node families retain immediate failure. Clear level-local rejection only on real progress; official `GameState.WIN` alone completes the target.
- **Identity boundary:** Campaign 31 observation-only evidence supports were `(15,15)` and `(53,28)`. Those coordinates, all observed colors, the game ID, campaign number, level number, and target identities are evidence only and must not appear as production decision constants. Detection must remain palette-equivariant, translation-equivariant, and identity-agnostic.
- **Resource burden:** The recovered left-edge band suggests that only six cells remain before the previously observed exhaustion boundary. That interpretation remains provisional. The implementation now has a bounded nine-action plan, exact inverse, and at most one same-level retry after a legal reset; it does not infer meter semantics or authorize exhaustive interaction enumeration.
- **Prelaunch evidence:** Commit `83dbe5211451f65e45c8a768b92ae7c5ed305b0a`, tree `71fd0ebdd5def55770e4dbfe3780c7e3241c53fb`, passed five focused compatibility/lifecycle tests, the complete 120-test visual-policy suite, Ruff, strict MyPy, and 54 detached exact-commit integrity/reachability cases. Its manifest-aware integrity receipt file SHA-256 is `b7bef298f269dec4029189c787889dd3a565356a2cef81f917abe3f8e3f30364`; package-only file SHA-256 is `2d47f7b2a5df65353e87e5607b5c1c23ac4af0b9f92ee7727e8a595a469cdc4c` with `package_only_passed=true`; action-semantics file SHA-256 is `4615ff438676d992f09e6cbbfdda78bf72443806939c1fe31bf6929d3a2c965d` with zero findings. The corrected sealed replay matched all 1,058 events, 176 historic submissions, and 176 mechanics receipts and staged then cancelled the unique external candidate with zero pending actions and zero environment actions. The `79c9f28` overbroad-retry replay failure, the `0d0cd86` one-test full-policy failure, and the rejected first `83dbe52` CLI invocation remain preserved prelaunch evidence.
- **Frozen-head CI:** Five of five workflows and nine of nine jobs passed at exact `83dbe52`: push/PR Windows and Ubuntu CI, push/PR Windows and Ubuntu package-only verification, and the native Linux Build 002 cold-start rehearsal. The four package receipts truthfully remain `BLOCKED_EXTERNAL` for their out-of-scope private surface while their package-only assertions passed; no public inventory or gameplay was attempted by CI.
- **Official resolution:** Campaign 32 ran from detached frozen commit `83dbe5211451f65e45c8a768b92ae7c5ed305b0a`. Submission 182 returned preterminal `GAME_OVER`; submission 183 legally reset; submission 192 reached the exact external residual-chain terminal and returned raw `NOT_FINISHED`, `levels_completed=4/6`; submissions 193-201 completed the certified inverse. The run ended `FAILED_MECHANISM` after 201 submissions (199 `ACTION6`, two legal `RESET`s), two `GAME_OVER` consequences, and zero `WIN`. The 202-row official recording and 1,208-event sealed trace agree one-to-one; the public verifier passed all 327 artifacts and independent hashing found zero discrepancy. Run receipt hash is `sha256:7f36c601284723715e8db17f5c1543da85c767744949c390896c1a9f363928db`; recording SHA-256 is `8a9b3bea1620ef4eb9807dfdaba3e7f24ce05d92bf52c3dcefdb35e09c4139d2`; trace chunk SHA-256 is `4d9b3ce76cfa096185c7a9801c60223e88b4ba44cd3f5b879dcc0bf8a8d65a2a`.
- **Resolution condition:** Met for this family by the exact certified terminal returning official `NOT_FINISHED` after its one bounded retry, followed by the complete inverse. This resolves only external residual-chain sufficiency. Target completion remains open; offline, synthetic, structural, and exact-inverse evidence cannot substitute for official `GameState.WIN`.

## B-003-0030 - Post-Campaign-32 hierarchy continuation and stale retry latch require bounded repair

- **Status:** OPEN; OBSERVATION-ONLY DIAGNOSIS IN PROGRESS
- **Stage:** 10
- **Opened:** 2026-08-26
- **Owner:** Codex
- **Burden:** Campaign 32 restored the exact level-4 origin after officially falsifying the external residual-chain terminal, but policy selection then failed because no further parser-safe family existed. The final snapshot also retained `hierarchy_preterminal_retry_count=1` after the retry had already reached terminal `NOT_FINISHED`, been rejected, and been fully inverted. No `WIN` was observed.
- **Smallest repair boundary:** Clear the stale retry reservation when a retry reaches an exact terminal `NOT_FINISHED` and when its certified recovery completes. Do not reopen any rejected relation, increase reset authority, or treat the repair as progress. Admit at most one next hierarchy family only if it composes already observed relations into a unique, collision-safe, target-preserving, fully invertible candidate; fail closed on ambiguity. Do not enumerate arbitrary target permutations or use a game ID, campaign number, level number, coordinate, palette value, or target identity constant.
- **Preserved evidence:** Root `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-83dbe52-campaign32`; logical run receipt `sha256:7f36c601284723715e8db17f5c1543da85c767744949c390896c1a9f363928db`; physical run JSON SHA-256 `3c5f15f68c8b9d7e08422f8ed640d706f0514611eff07a6f67931fa2069e9d35`; completion receipt `sha256:b87c019ab67e887bda6103d511578885baa368e8a9a5813c1318258db8402d2f`; trace tail `sha256:65a541dc22ee75c5a9d0111c3680624b6af1d809ecff31fc890b7cc77ee4214e`.
- **Resource boundary:** The two observed `GAME_OVER` returns remain failure/resource evidence. The next route must be shorter than exhaustive interaction enumeration, retain legal reset handling, and carry a complete inverse. Repeated testing of already rejected terminals is prohibited.
- **Resolution condition:** A generic repair clears the stale latch with focused regressions; one observation-grounded candidate, if any, passes the complete visual-policy suite, strict source-quality and integrity gates, sealed Campaign 32 replay with zero environment actions, exact frozen-head CI, and then receives an authoritative official consequence. Only official `GameState.WIN` resolves target completion.

## B-003-0031 - Campaigns 33 and 34 exhausted the remaining sparse-sink hierarchy families without WIN

- **Status:** OPEN; FAILED_MECHANISM PRESERVED
- **Stage:** 10 and target-game continuation
- **Opened:** 2026-08-26
- **Owner:** Codex
- **Burden:** Campaign 33 tested the exact raw-matching composite family and Campaign 34 tested the exact external-own composite recombination. Both official terminals remained `NOT_FINISHED`; both campaigns ended at `4/6` with score-boundary `GAME_OVER`, exact recovery evidence, and zero observed `WIN`. The complete sparse-sink family is therefore exhausted for the recovered observation, but that does not prove that the level has no winning relation.
- **Evidence:** Campaign 33 root `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-7df4046-campaign33`; Campaign 34 root `C:\a\arc3-b003-play\build003-r11l-mechanical-seed7-1315a60-campaign34`. Campaign 34 recording SHA-256 `607a4271a58d54fd44a793d4c477221b15b92b8118074ed51388126db12d06fb`; run receipt hash `012aee25314cf5ab8d965e140f47d44718633ab295a71fee88ff38590f88a801`; completion receipt hash `12f3bd06c18fc3333868967b06a115613e2007842c8e9e0fbbe477d75ea00e21`.
- **Impact:** Build 003 completion remains false. No synthetic, replay, score, or complete-looking mechanic map can close this burden.
- **Reopening handle:** An exact source-free relation outside the rejected sparse-sink family passes reversible parser certification and official discrimination, or the environment itself reports `GameState.WIN`.

## B-003-0032 - Carrier-source occlusion is uniquely identified but not yet sequentially certified

- **Status:** OPEN; IMPLEMENTATION EXPERIMENT
- **Stage:** 10 and target-game continuation
- **Opened:** 2026-08-26
- **Owner:** Codex
- **Burden:** The recovered observation uniquely links the singleton raw color to one carrier-sharing source disk and one exact carrier-offset counterpart in the other bridge example. Those source disks are occupied 21-cell evidence surfaces rather than hollow sinks. Final projected layouts exist when only those exact surfaces are assigned for occlusion, but every transient, preserved-cell boundary, recovery step, and official consequence remains unverified.
- **Impact:** No Campaign 35 action is earned yet. A final parser-readable projection cannot substitute for a complete sequential certificate or an official consequence.
- **Reopening handle:** A generic implementation with no game, campaign, level, palette, or coordinate decision constants proves the complete forward sequence and inverse against exact projected rasters, passes sealed replay and clean-source CI, and then receives an official `WIN` or a bounded falsifying consequence.

## B-003-0033 - Exact-head Windows coverage twice crossed the outer CI ceiling

- **Status:** OPEN; BOUNDED INFRASTRUCTURE REPAIR IN PROGRESS
- **Stage:** 10 and 11
- **Opened:** 2026-08-26
- **Owner:** Codex
- **Burden:** Exact commit `5fccb5751fdbbd30e42e473fb3a33a7e699c0931` passed the full pull-request ARC3 CI matrix, but the duplicate push workflow's Windows job was cancelled at the configured 60-minute outer ceiling. An unchanged rerun reproduced the cancellation. Both attempts passed setup, dependency sync, Ruff, formatting, and strict typing and reached only the long coverage step; neither reported a test failure. Linux passed in both attempts.
- **Preserved evidence:** Pull-request run `33015837950` passed Linux and Windows. Push run `33015834403` attempt one passed Linux and cancelled Windows at the 60-minute boundary; attempt two passed Linux and again cancelled Windows at the same boundary. The successful Windows suite reported 1,813 passed and 20 skipped in 3,493.53 seconds, only seconds inside the job cap. The prior `1315a603` Windows baseline was already 3,453.11 seconds, so the current 40.42-second increase does not support a mechanism or regression failure claim.
- **Smallest repair:** Raise only `.github/workflows/ci.yml` `timeout-minutes` from 60 to 75. Preserve every command, test, platform, coverage threshold, runtime doctor, and fail-fast setting. Do not treat the separate exact-head success as permission to erase either cancellation, and do not launch official play until the repaired head passes all required CI jobs.
- **Impact:** Campaign 35 remains gated. Clean-clone action semantics, competition integrity, package-only integrity, and the 237-action sealed replay already pass with zero environment actions, but they cannot substitute for the unresolved exact-head CI boundary.
- **Resolution condition:** A new exact head containing the bounded ceiling repair completes all five required workflow runs and all nine jobs with no cancellation or failure, while the carrier-source production bytes and sealed-replay result remain unchanged and exactly bound.
